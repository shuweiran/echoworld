> ⚠️ 本文件较大（约 44 KB），agent 请按需搜索读取，勿整体加载

# DECISION_LOG.md — 架构决策史（AI 必读②）

> ⚠️ **所有 agent 开工前必读**：这是"为什么这么设计"的答案。改代码前先查这里，避免推翻历史决策或重复踩坑。
> 格式：日期｜决策｜原因｜放弃了什么｜影响。

---

## 2026-07-31（Phase 1-4 Track 融合）

### D-001 Arbiter 输入脱敏是生死线，不是可选
- **决策**：RouterService 加 `sanitizeSummaryForArbiter()`，Arbiter 永远看不到全轨道全文，只看脱敏摘要
- **原因**：调研发现 Arbiter 若看到 Track 全文即泄露信息源——"谁说了什么"从摘要就能推断，信息隔离失效
- **放弃**：让 Arbiter 看完整轨道上下文的便利性
- **影响**：信息隔离的根基；后续任何接入 Arbiter 的路径（剧本杀审批/狼人杀判定）都必须过脱敏

### D-002 Phase 3 WorldDirector 纯规则，零 LLM 调用
- **决策**：WorldDirectorService 目标生成用规则（情绪异常→平静 / 对话→参与 / 闲置→探索），不用 LLM
- **原因**：成本控制。每组每 tick 调 LLM 的成本估算：组×10 轮×角色数，纯规则省掉大头
- **放弃**：LLM 驱动的更"聪明"目标生成（保留 `generateGoalWithLLM` 作为手动扩展点，失败回退规则）
- **影响**：2D 模拟运行成本可控；目标语义较粗糙（P1 可升级）

### D-003 Phase 3 只加外层 SimulationOrchestrator，不重构 RouterService
- **决策**：新编排层 `SimulationOrchestrator.tick()` 包裹现有对话流，RouterService 一行不动
- **原因**：需求文档第十四条要求；避免影响 SSE / Memory / 游戏模式（狼人杀/剧本杀）
- **放弃**：彻底重构 Router 的架构洁癖
- **影响**：现有 73 tests 零破坏；新功能全部在编排层增量

### D-004 TrackScore 阈值 40（需实测调参，勿 hardcode）
- **决策**：`InteractionDetector.TRACK_THRESHOLD = 40`（≥40 触发 Track 模式），因子：size 30 / bystander 20 / conflict 30 / secret 50 / emotion 25
- **原因**：Demo 实证（真实 LLM：无隔离泄露率 100% → Track WEAK 0% 泄露，token ↓14%）
- **放弃**：动态阈值（当前阶段无数据支撑）
- **影响**：已加 BoundaryTest 锁定 39/40 边界（问题清单 D18 建议配置化，待办）

### D-005 TrackStrategy 统一替换 GROUP_DISCUSSION + DEBATE
- **决策**：新 `TrackStrategy`（17.8KB）统一承担 GROUP_DISCUSSION 与 DEBATE；DYAD/Speech 不动
- **原因**：混合轨道上下文（MERGED 全文 / WEAK 摘要 / ISOLATED 独白 / 无 track 回退 GroupStrategy）
- **放弃**：给 DEBATE 单独写策略的重复工作
- **影响**：GroupStrategy/DebateStrategy 不删除（需求文档第十三条：降级为默认模式）

### D-006 MovementConstraint 纯规则，零 LLM
- **决策**：轨道→运动约束（MERGED 聚集质心±偏移 / WEAK 听觉带 [0.5h, h] / ISOLATED 避让≥60 格），优先级 ISOLATED > WEAK > MERGED；玩家手动目标不被覆盖
- **原因**：需求第八/九条——Track 不直接移动角色，而是生成空间约束；纯规则实时性
- **放弃**：GroupAnchor 完整队形（leader+follow slot，D2 降级项，P2 待补）
- **影响**：约束是边界不是牢笼——角色在带内自由漫游，越界才纠正

### D-007 前端角色详情走 body + 向后兼容 query
- **决策**：`SceneController.startScene` `@RequestBody(required=false)` 收 `{agents, me, characters[]}`，与 query 并存
- **原因**：前端未改时不崩（向后兼容）；characterDetails 需要结构化传递
- **影响**：7 项前端修复清单的一部分（loadState 扁平结构兼容等）

### D-008 测试基建：RANDOM_PORT + H2 mem + mock LLM
- **决策**：`src/test/resources/application-test.yml`：`server.port: 0`（避开 8000 生产）、`jdbc:h2:mem:testdb`（不污染 `./data/roleplay`）、`llm.api-base: http://localhost:9999/mock-llm`（零成本）
- **原因**：G2 禁止 spring-boot:run 直跑 + 数据隔离 + LLM 成本
- **放弃**：测试连生产 H2 文件库 / 真实 LLM
- **影响**：93 tests 可重复、可 CI；LONG-01 10 万字 5.09s 跑完

### D-009 剧本杀 Step 3 降级预案（未衡审查定案）
- **决策**：若 ConversationManager 适配 SimulationWorld 失败（剧本杀无 2D 世界），降级为"轮次发言"：按 assignments 顺序轮流每人 1 条消息，轮数可配置默认 2，结束自动进 VOTE；秘密隐藏退化为 prompt 禁令
- **原因**：`ConversationManager.init` 需要 `SimulationWorld + Function<String,Agent>`，剧本杀没有 world 实体
- **放弃**：降级版接 Track（P1 再补）
- **影响**：降级版验收 D-3-1~D-3-4 替代 A3-1~A3-4（蓝图第四章）

### D-010 剧本杀 Step 4 killer 与 truth 职责分离
- **决策**：判定用 `mostVoted.equals(killer)`（机器对比例，角色名精确匹配），不再 `truth.contains(mostVoted)`（字符串扫文案）；truth 保留为人类可读叙述文
- **原因**：contains 子串误匹配（投"张三"命中"管家张三在书房…"判中）；无凶手实体概念
- **放弃**：继续用 truth 文案做模糊判定
- **影响**：schema 需加 killer 字段；双生成器（ScriptService/initGame）schema 必须同步（风险 5）

### D-011 剧本杀 Step 1 Part A 已被并行工作流完成
- **决策**：secrets 字段/toMap your_secret/getSecretFor 已由另一主会话的 coder 实现（16:05），本会话仅做 Part B（Track ISOLATED 集成）
- **原因**：并行工作流撞车的事实（sessions_list 发现 4 个 coder 在改同一批文件）
- **影响**：**任何剧本杀派单前先 `git diff` 确认基线**，避免重复实现

---

## 2026-08-01（GAP-3 批次 A）

### D-012 剧本杀讨论引擎：主路径最小适配，未启用降级轮次发言
- **决策**：GAP-3 走主路径（spike 验证可行），ScriptGameService 自建轻量 `SimulationWorld`（仅作 Agent 容器，不起 tick）+ **独立 `ConversationManager` 实例**（非 Spring 单例），通过 ConversationManager 新增的 `createScriptDiscussionGroup`（显式成员+显式轨道）+ `runScriptDiscussionRounds`（同步驱动 maxRounds 轮，复用 TrackStrategy，WEAK=EavesdropSummarizer 摘要）接对话引擎；持秘密角色轨道=WEAK（只给摘要不给明文）、未持=MERGED（全文）；目标按秘密注入（隐藏秘密/查明真相，WorldDirectorService.setGoal）；讨论结束自动进 VOTE
- **原因**：① `new SimulationWorld()` 无参可建，角色仅需 registerAgent，无 2D 寻路/障碍依赖；② Spring 单例 ConversationManager 已被 2D 模拟 init 占用，重 init 会互相覆盖 → 必须独立实例；③ 2D 群组按空间听觉（ModeClassifier）自动生成，无界、无结束回调、不可指定成员 → 无法满足"讨论结束自动进 VOTE"；④ 现有 ScriptController 旧桥 `setSecretAgents(全员)` 走 TrackDirectorService 强制 ISOLATED（内心独白、不入群轮），而剧本杀人人有秘密 → 全员 ISOLATED = 无对话，且语义需要的是 WEAK 不是 ISOLATED
- **放弃**：① 降级轮次发言（D-009 预案，主路径已验证不需要）；② 复用 ScriptController 旧 2D 桥跑讨论（保留其 2D 可视化职责不动）；③ 讨论 persona 写入秘密明文（违反 A3-2）——讨论 persona 只含"你有一个不可告人的秘密"禁令式描述，秘密原文仍经 your_secret 仅发对应玩家
- **影响**：① A3-1~A3-4 全过（ScriptGameDiscussionTest 4 用例）；② 讨论在后台虚拟线程驱动，/api/script/start_discussion 不阻塞，前端轮询 status 见 DISCUSSION→VOTE；③ 轮数可配置 `roleplay.game.discussion.max-rounds`（默认 2）；④ 已知限制：讨论引擎为 service 实例级共享（多局并发讨论会互覆世界，与 2D 单世界同限制，P1 可改 per-game）；真实 LLM 下每轮耗时受最长 agent 调用限制，讨论总时长≈maxRounds×单轮最长，长局建议降轮数或改异步节流；⑤ 旧 2D 桥的"秘密 persona+全员 ISOLATED"旧语义保留未动（2D 可视化路径独立，P1 可对齐）

### D-013 剧本杀 ENDED 终态走显式确认（confirmEnded），落库双点 + script SSE 走既有广播机制（批次B）
- **决策**：① ENDED 不并入 resolveVote 自动流转——resolveVote 批准后仍停 REVEAL（保留 D7 既有验收与揭晓展示语义），新增 `confirmEnded`（REVEAL→ENDED，幂等；非 REVEAL 拒绝）并暴露 `POST /api/script/finish`，由前端“🏁 结束对局”按钮/调用方确认收尾；终态不越界由既有 phase 守卫天然承担（search/castVote/resolveVote/startDiscussion/startVoting 均按 phase 拒绝）；② 落库双点：initGame 落剧本（type=script）+ confirmEnded 落对局结果（type=result，含 players/assignments/killer/winner/votes/correct/truth/讨论记录摘要）——saveScript 从 0 调用变 2 个业务调用点；ScriptEntity 字段够用（contentJson 整包存 JSON），未扩展表结构；③ script SSE 复用 `SSEController.broadcast`（Jackson JSON 序列化）新增 3 个 typed 事件：script_phase/script_status/script_reveal，状态变更点全推；**调研发现后端此前并无 werewolf_* 实际推送（前端监听器是先行建的死监听）**，故无既有“werewolf 推送范例”可抄，按 RouterService→SSEController 广播模式新建；④ ScriptGameService 加 `@Autowired` 四参构造（LLMClient/ApprovalService/DatabaseService/SSEController），保留二参构造供既有测试，落库/推送均 null 守卫
- **原因**：① 直接改 resolveVote 自动 ENDED 会破坏 D7 既有验收（断言 phase==REVEAL）且前端会错过 REVEAL 展示；② 蓝图 A4-4/A4-3 分别要求“判定流程结束后 phase==ENDED”与“对局结束后 findAll 非空”，显式确认 + 双点落库同时满足且语义清晰；③ SSE 推送走既有广播通道零新依赖，前端参照 werewolf_* 分支写法接线
- **放弃**：① 审批通过后 resolveVote 内部自动 REVEAL→ENDED 一步到位（破坏既有测试/展示）；② 给 ScriptEntity 加专用字段（killer/votes 等列）——contentJson 已够用，避免表结构膨胀；③ 新建独立 script SSE 通道（与既有 emitter 管理重复）
- **影响**：① A4-4/A4-3 全过（ScriptGameEndedTest 3 用例 + ScriptPersistenceTest 1 用例，@SpringBootTest+H2 mem 真落库）；② 前端 ScriptStatePanel 由轮询改 store 驱动（SSE 优先 + 3s 轮询兜底），appStore 新增 scriptState/scriptPhase/scriptReveal；③ 全量 139 tests 全绿；④ 已知限制：SSE 推送为全局广播（无 session 过滤，多局并发时各局事件会串到同一连接，前端按 session_id 字段可区分，P1 可做 per-session emitter）；⑤ 剧本杀对局结果已可持久化，但暂无查询/回放入口（ScriptRepository 自定义查询 P2 后备）

### D-014 剧本数据模型 Schema v1：JSON 内嵌版本 + 双生成器统一（批次 C1，2026-08-01）
- **决策**：① 剧本 JSON 新增 `schema_version: 1`，结构化字段 metadata{title,player_min,player_max,tags} / roles[]{id,name,intro,is_hidden,secret} / clues[]{id,title,location,content,transferable,visible_to_owner_only} / killer_id / truth / background / locations / secrets——务实对齐通用剧本杀范式 Chronos Script Schema v2 核心子集（调研报告 §2.2），契约文档 `docs/剧本-schema-v1.md`；② **双生成器统一**：ScriptService.generateScript 为唯一生成路径（统一 prompt + 宽容 normalize + defaultScript 兜底），ScriptGameService.initGame 委托之（内部 new ScriptService(llmClient)，二/四参构造均生效，Spring 无需改线），两路径输出同一 schema；③ secrets 纳入 schema：raw.secrets ∪ roles[].secret，**仅当所有角色均无秘密时全量兜底**（A1-3 键集合==roles），部分秘密保持部分（不破坏 A3-2/A3-4 未持秘密角色走 MERGED 的讨论轨道语义）；④ ScriptEntity 不加 schema_version 列——版本内嵌 contentJson（schema 版本化即 JSON 版本化），避免 H2 file 库迁移；⑤ killer_id 为元数据/落库字段，**运行时判定仍走 D6 truth 精确解析**（killer_id 与判定解耦，判定路径零改动）
- **原因**：① 蓝图 P2 后备首项（双生成器 schema 漂移）+ D-010 风险 5（Schema 需加 killer 字段且双生成器必须同步）——ScriptService 骨架无 secrets/killer，initGame 内联生成含 secrets，同一剧本两条生成路径字段不一致；② 前端消费靠约定而非契约——schema 化后生成/落库/读取有统一契约；③ 宽容解析保证既有 LLM 输出与测试 mock（roles 字符串数组 / clues 带 public / 无 metadata / 无 killer_id）零破坏归一
- **放弃**：① Chronos 全量（timeline DAG 节点图 / visible_to 表达式 / ap_cost 行动点 / assets）——本项目为线性六态状态机 + 按地点搜证，DAG 化留 P2；② 给 ScriptEntity 加 schema_version 列——contentJson 已内嵌版本，且 D-013 已定“不扩展表结构”；③ 改造 ScriptGameService 改注入 Spring ScriptService bean——new 实例即可，避免构造链与测试改动
- **影响**：① 全量 153 tests / 0 failures（146 基线含并行 broadcast 7 用例 + 新增 ScriptSchemaV1Test 7 用例：旧格式归一/新格式透传/兜底符合 schema/双生成器一致性/兜底与 killer_id 解析）；② 落库 type=script 的 contentJson 升级为 v1（含 schema_version/metadata/roles[]/killer_id），既有 type=result 不变；③ toMap/SSE 状态新增 schema_version 键（附加，前端契约不破坏）；④ POST /api/session/script/generate 响应升级为 v1 schema（前端实际走 /api/script/init，无旧形状消费，零破坏）；⑤ 双生成器一致性由测试锁定（同输入 → generateScript 与 initGame.getScriptSchema 输出一致）

### D-015 演讲与广播合并地基：统一消息管线，不建两套系统（2026-08-01 coder demo 批次）
- **决策**：不分开建「演讲系统」和「广播系统」——统一一条消息管线：`BroadcastMessage`（level SYSTEM>EVENT>PLAYER>NPC + channel global/area/system + mode speech/announcement + 坐标/半径）+ `AnnouncementService`（PriorityQueue + 滑动窗口节流 + 同 key 合并 + 队列上限 + 断线补发环形缓冲，节流参数 `roleplay.broadcast.*` 可配，对齐 D-004「阈值勿 hardcode」）→ 每 100ms flush → 复用 WorldEventBus 进程内分发（新事件常量 TYPE_ANNOUNCEMENT）+ SSEController.broadcast("announcement") 推前端。演讲=带空间范围(radius)+听众模型的广播（channel=area），公告=无范围/全局广播（channel=global）；前端统一 AnnouncementBanner（打字机+队列）+ AnnouncementTicker
- **原因**：① 调研报告结论——项目已有 80% 地基（WorldEventBus/GameEvent/SSEController/SpeechStrategy/HearingSystem），缺统一领域类 + 优先级/节流机制、演讲产出不落广播通道、前端无横幅/公告栏；② 需求：AI 与玩家都可发起、形态自动选择
- **AI 自动选择**：AI 发言（PUBLIC_SPEAKING 轮次产出，经 ConversationManager 新增 speechBroadcastListener 回调，SpeechStrategy 本体零改动）→ SimulationService 复用 ModeClassifier.wouldOthersListen（原 private 改 public）判定听众（2.5×hearRange 内距离>50 未入群角色≥2）→ enqueueAutoSpeech：有听众→演讲(area+半径) / 无听众→全局公告。玩家侧 POST /api/announcements（默认 PLAYER/global，可显式 level/channel/mode）；AI 演示端点 POST /api/simulation/speech（自动选 NPC+默认文案，形态自动判定，同一条管线）
- **放弃**：① 引 Guava EventBus / Spring ApplicationEventPublisher（项目自研 WorldEventBus 已够用，Guava 官方已不推荐）；② 演讲单独建一套队列系统（与广播并行走两套）；③ 节流参数硬编码
- **影响**：① 新增 broadcast 独立包（4 类）+ 2 个 REST 端点 + 前端 2 组件/1 SSE 事件/1 demo 面板，未触碰 RouterService/ArbiterService/狼人杀/剧本杀主链路；② PUBLIC_SPEAKING 才触发回调，剧本杀讨论（GROUP_DISCUSSION）不受影响；③ 演示闭环：AI 自动演讲→有听众区域广播/无听众全局公告 + 玩家发广播→全员横幅；④ 遗留：断线补发 REST（GET /api/announcements/recent）已就绪但前端重连自动补拉未接（P3）；⑤ 全量 146 tests 全绿 + npm run build 通过，产物已同步 static/

### D-016 搜证机制增强：AP 行动点 + 线索转交（批次 C2，2026-08-01）
- **决策**：① **schema 扩展**——ScriptSchemaV1 的 `clues[]` 补 `ap_cost`（搜索该线索消耗的行动点，缺省 1，normalize 恒填默认）、`roles[]` 补 `ap_bonus`（角色行动点加成，侦探类角色给 1-2，缺省 0），宽容解析旧剧本（无字段→默认值）保持向后兼容；② **AP 池**——initGame 按玩家分配初始 AP = 基础值（`roleplay.game.ap.base`，默认 3）+ 角色 ap_bonus，存入 game 状态（playerAp/playerApMax）；③ **搜证扣 AP**——search 保持“一次搜索=探索一个地点，必得该地点全部未持有可搜线索”语义，消耗 AP = 各线索 ap_cost 之和；AP 不足 → 整次拒绝、不部分授予、提示“行动点不足”；公开线索（public=true）不耗 AP 始终可见；④ **线索转交**——新增 `transferClue` + `POST /api/script/transfer_clue`（body: player/target_player/clue_id）：仅搜证/讨论阶段可转交（讨论交换线索是剧本杀常规玩法，讨论引擎本体零改动）、转交方必须持有（归属校验：visible_to_owner_only 的线索持有即归属，只有所有者能转交）、目标玩家必须在本局且非自己、`transferable=false` 拒绝、成功后 ownership 变更（源移除/目标加入）；⑤ **toMap 暴露**——新增附加键 ap/ap_max/ap_pool（全员剩余 AP 一览）/my_clues（请求者持有的全部线索含转入的），均附加键不破坏前端契约；⑥ **前端**——搜证面板显示 AP 余额、搜证结果/行动点不足提示、我持有的线索列表、可转交线索的转交 UI（选目标玩家），client.ts 加 scriptTransferClue；构建产物已同步 static/（index-DkvIhlhJ.js）
- **原因**：① 蓝图 P2 后备“行动力限制；角色差异化搜证（侦探线索多）”正式落地，对齐通用剧本杀范式 Chronos CLUE_SEARCH 节点设计（ap_cost/transferable/default_owner）；② schema v1 已预留 transferable/visible_to_owner_only 字段但搜证流程未用（docs/剧本-schema-v1.md 明示），本批次把预留字段投入使用；③ AP 不足“整次拒绝不部分授予”保证行动力限制语义清晰、无部分发放的边界状态；④ 讨论阶段允许转交是因为线索交换是讨论的核心玩法，且转交是状态变更不经过对话引擎，不影响 D-012 讨论链路
- **放弃**：① 按 Chronos 做 timeline DAG / visible_to 表达式 / 每线索独立 default_owner 字段（线性六态状态机 + 持有即归属已满足需求，DAG 化仍留 P2）；② 搜证改为“每次只搜一条线索”的逐条消耗（破坏既有“一次搜一个地点得全部线索”语义且无既有测试依赖，保持必得语义只加资源限制）；③ AP 池做成每玩家独立秘密值（AP 余额透明可见，棋盘游戏惯例，且可观察性更好）
- **影响**：① 新增 ScriptGameApTransferTest 9 用例（AP 初始化含 ap_bonus/旧剧本默认、搜证扣减/公开线索免费/同地点不重复得、AP 不足拒绝、转交成功 ownership 变更/status 可见、转交拒绝 5 类、schema 兼容默认值、端到端）；② 全量 162 tests / 0 failures（153 基线含并行批次 C1 零破坏；LONG-01 3.5s 全绿）；③ 旧剧本（无 ap_cost/ap_bonus）与旧测试 mock 零破坏（默认 1/0）；④ 转交后接收方 status/my_clues 立即可见、原持有者不可见（ownership 变更即信息变更，无 SSE 专门事件——script_status 轮询/SSE 已有状态推送覆盖）；⑤ AP 基础值可配 `roleplay.game.ap.base`（D-004 阈值勿 hardcode 纪律）

### D-017 剧本杀断线重连与会话恢复：roleKey 认证 + 全量快照恢复（批次 C3，2026-08-01）
- **决策**：① **roleKey 令牌**——initGame 为每玩家生成唯一 UUID 存 playerKeys；玩家级端点（status/search/transfer_clue/vote）支持可选 `player_key` 认证（有 key 校验匹配→错误 403；无 key 向后兼容按玩家名）；toMap 仅向本人暴露 `role_key`（匿名/广播视图不含）；**顶号语义落地为“key 校验替代玩家名可被任意冒充”**（本项目无 WebSocket，无“新连接顶旧连接”概念，防冒充与重连一体化由 key 承担）；② **全量快照恢复**——ScriptEntity 落 `type=snapshot` 行（name 前缀「对局快照:<sessionId>」作鉴别器，避免 JSON 内容查询；不扩展表结构，遵循 D-013/D-014 纪律），每次状态变更全量快照（对齐 Chronos“内存 Room 仅缓存，每次状态变更写持久化，崩溃后 restore 重建”）；新增 `POST /api/script/resume`（game_id/room_code/player_key 三选一定位 + player_key 认证）：内存对局存在直接返回视图（restored=false）、不存在从快照重建（restored=true，重启后可用）、ENDED 返回终态（terminal/murderer/correct/truth/votes/winner）；③ **房间绑定（轻量）**——init 可选 `room_code` 存 ScriptController 内存映射（roomCode→sessionId），resume 可按房间码定位；RoomController 本体未动；④ **DM 分发**——新增 `GET /api/script/keys`（全员令牌一览，DM 面板批次用）
- **原因**：① 调研报告 §2.4 结论——本项目断线重连与防冒充双缺失，roleKey 顶号是 Chronos 的鉴权+重连一体范式；② 批次 B 已落 ScriptEntity（type=script/type=result），快照复用同一实体零新表；③ 适配 SSE+轮询现实——无 socket 可顶，改为“凭 key 恢复个人视图”
- **放弃**：① 新表存快照（ScriptEntity contentJson 已够用，避免表结构膨胀）；② 增量快照/事件溯源（全量 JSON 每次写入已满足规模，diff 逻辑过度设计）；③ 双向房间绑定（/api/rooms 为纯内存大厅、无 gameId 字段，双向绑定需改 RoomController 结构——跳过并在报告说明）
- **影响**：① 全量 170 tests / 0 failures（162 基线 + ScriptGameResumeTest 8 用例）；② 玩家级端点契约向后兼容（无 key 行为不变）；③ 已知限制——DISCUSSION 阶段的后台讨论线程随重启丢失（恢复后 phase 停 DISCUSSION，DM 可调 /api/script/start_voting 手动推进）；player_key 反查对局仅限内存对局（重启后需 game_id/room_code）；SSE 推送仍全局广播（D-013 既有限制）；④ 前端本轮未动（重连 UI 归 DM 面板批次），API 契约已在报告写明 |

### D-018 剧本杀 DM（主持人）面板：全量仪表盘 + 状态机推进端点，前端面板与重连 UI 落地（批次 C4，2026-08-01）
- **决策**：① **DM 全量视图**——新增 `GET /api/script/dm/status`（service 层 dmStatus）：对齐 Chronos state:dm_dashboard，DM 可见全部玩家角色/秘密/AP/线索数/投票状态/roleKey + 对局元数据（truth/killer_id/判定/winner/审批门状态）；与玩家级 toMap（脱敏）分道——DM 视图不脱敏，越权由 controller 层 DM key 门承担；② **DM 手动推进**——新增 `POST /api/script/advance`（service 层 advancePhase）：对齐 Chronos dm:advance，状态机逐级推进 INVESTIGATION→DISCUSSION（复用 startDiscussion 接讨论引擎）→VOTE（复用 startVoting，即 C3 已知限制“恢复后 DM 手动推进”的入口）→REVEAL（复用 resolveVote，天然继承 D6 精确判定/平票回滚 + D7 审批门）→ENDED（复用 confirmEnded 落库）→ENDED 幂等终态不越界；advance 响应补 phase 键（resolveVote 响应无 phase 键，属既有 /resolve 契约，advance 统一补全）；③ **DM key 越权保护**——新增配置 `roleplay.game.dm.key`（@Value 读，空=放开，非空时 DM 端点要求 X-DM-Key 请求头匹配否则 403），放开默认与审批门 D7 同模式（项目无强鉴权传统），前端 DM 面板提供密码输入框（存 localStorage）；④ **前端**——ChatPage 顶部「🎛 主持人」按钮（仅剧本杀模式）开 DM 抽屉：对局概览/玩家表（秘密可切换显示）/roleKey 分发复制（GET /api/script/keys 数据源）/「推进→下一阶段」/审批门 pending 时「✅批准/驳回重投」/ENDED 终态；「🔄 恢复对局（重连）」折叠面板：对局ID 或 房间码 + roleKey → POST /api/script/resume → 恢复玩家视图写入 store，ENDED 显示终态结果卡；⑤ 本轮零改动狼人杀/中断/对话引擎/AP 转交/重连后端逻辑
- **原因**：① 调研报告 §2.4 结论——通用范式 DM 拥有 dmToken 可推进流程（dm:advance）、强制查线索、改 AP、禁言，DM 看全量仪表盘（state:dm_dashboard），本项目此前**无 DM 专用控制台**（审批门 D7 仅手动审批单点，剧本杀流程控制散落多个端点）；② C3 已提供 keys/resume 但无面板 UI，重连入口 UI 缺失（蓝图 P2 最后一项）；③ VOTE 步复用 resolveVote 而非另写判定——避免双套判定路径（D6/D7 语义单一事实源），advance 只是编排器
- **放弃**：① 单独实现 dm:override_ap / dm:force_view / dm:mute_player（Chronos 其余 DM 能力）——本项目 AP 透明可见、无私聊私密视图推送、无 WebSocket 顶号概念，三项在本架构下无明确消费场景，留 P2；② 给 DM 端点做完整登录/角色体系鉴权（项目全局无此体系，审批门亦开放；用可配置 key 门 + 放开默认保持开发体验一致）；③ advance VOTE 步改为“不阻塞直接返回 pending”（既有 /resolve 与审批门 submitForApproval 就是阻塞语义，改为异步需动审批门机制，超范围）
- **影响**：① 全量 176 tests / 0 failures（170 基线 + ScriptGameDmTest 6 用例：DM 全量视图/未知对局/advance 全状态机/审批门挂起批准/controller DM key 403/200/未知对局报错）；② 新增 2 端点 + 1 配置键（roleplay.game.dm.key），前端新增 ScriptDmPanel 组件 + ChatPage 主持人抽屉 + 恢复对局入口，构建产物同步 static/（index-DFAHiAkk.js）；③ advance 是纯编排不新增状态（无新 phase/无表结构变更/无快照格式变更——各步内部已有快照点）；④ 已知限制——advance VOTE 步阻塞等待审批（最长 roleplay.game.approval.timeout-seconds=60s，超时自动驳回回滚，与 /resolve 同语义）；SSE 仍全局广播（D-013 既有限制）；DM 面板 3s 轮询 dm/status（无专属 SSE 事件，面板自己拉）
- **Virtual Threads** 并行 Agent 执行（LLM 大量 IO 等待）；放弃 Reactive WebFlux（代码复杂度）
- Python → Java 全量迁移（v4 前端保留 React，后端重写）
- H2 双模式：开发 mem / 持久 file（README 声明，注意 D14 缺陷：角色/场景实际未落库）


### D-019 方案B（分步/内联接线）演讲广播落地 + 方案A/B 共存开关（2026-08-01 coder 方案B 批次）
- **决策**：按调研报告 §4 落地计划 Step 2-3 实现第二套 demo，**保留方案A demo 不动**：① Step 2 演讲接广播走**内联路径**——`SpeechStrategy.processResults`（L76）内 `speakerState.setCurrentMessage` 后追加 `broadcastSpeechInline`（L124），split 模式下直接 `AnnouncementService.enqueue(NPC/area/speech+坐标+半径)`，演讲即刻变区域广播；远近判定复用 `HearingSystem`（`countHearingListeners` L147：computeAudibility+canHear）；② Step 3 剧本杀阶段广播——`ScriptGameService.broadcastPhase` 统一漏斗处追加 `broadcastSystemAnnouncement`（L1048），五处阶段切换（initGame/startDiscussion/startVoting/resolveVote/confirmEnded）发 SYSTEM 级 announcement（channel=system）到全局横幅通道，与 script_phase SSE（会话面板通道，台账 #35）并存不冲突，前端轮询保留兜底；③ **共存开关** `roleplay.broadcast.speech-mode`（AppConfig.BroadcastConfig.speechMode，默认 **auto=方案A**；split=方案B），`AnnouncementService` 为单事实源（get/set），两路径各自 gate（ConversationManager.executeRound 回调块判 auto；SpeechStrategy 判 split）互斥不重复推送，同一运行实例可经 POST /api/announcements/mode 或前端 ScenePage 面板运行时切换
- **原因**：① 主人要求"保留方案A demo 不动，按调研报告实现第二套 demo 后对比两方案稳定性/灵活性/真实游戏性"；② 报告建议 SpeechStrategy 经构造参数注入 AnnouncementService（非 Spring 类，与 agentLookup/narrationSupplier 同模式）；③ Step 3 的 announcement SYSTEM 广播与台账 #35 script_* SSE 是**两个通道**（会话面板 vs 全局横幅），需并存而非替换；④ 两路径都触发会重复推送 → 必须配置化互斥开关，且要支持同一实例演示切换
- **放弃**：① 改方案A 既有接线（onSpeechBroadcast 回调路径原样保留，SpeechStrategy 旧三参构造/ConversationManager 旧四参 init 均保留委托）；② 给阶段广播用默认 coalesceKey（speaker|channel=system|system）——investigation+discussion 同窗快速连发会被合并成 ×N，阶段切换是离散横幅事件，改 phase 级 key（script_phase|<phase>）；③ Step 3 前端新组件——announcement 全局横幅（AnnouncementBanner/Ticker，台账 #37）已渲染 SYSTEM 金色横幅，剧本杀阶段横幅自动出现，仅补 demo 面板方案切换 chips
- **影响**：① 全量 183 tests / 0 failures（176 基线 + SpeechStrategySplitModeTest 5 用例 + ScriptGamePhaseAnnouncementTest 2 用例）；② 方案A 行为逐字节不变（auto 默认，SpeechStrategy gate 直接 return，ConversationManager 回调照旧）；③ 新增 2 端点（GET/POST /api/announcements/mode）+ 1 配置键（roleplay.broadcast.speech-mode）+ SpeechStrategy/ConversationManager 构造重载（旧构造保留）；④ ScriptGameService 5 参 @Autowired 构造（4 参/2 参保留委托，既有测试零改动）；⑤ 前端 ScenePage demo 面板方案A/B 切换 chips + client.ts 2 个 API 封装，构建产物 index-CcvzWufL.js 已同步 static/；⑥ 对比报告《speech-broadcast-方案对比.md》：稳定性 A 胜（广播外挂于对话链路）、灵活性各有所长、真实游戏性 B 胜（空间声学+AI 自主+玩家空间感知），正式版建议合并两方案优点（A 的管线 + B 的 HearingSystem 声学判定），无听众兜底做成可配置；⑦ 遗留：wouldOthersListen 阈值硬编码未配置化（D-004 纪律欠账）、前端断线补发未接线（P3）、8000 实例未重启新前端产物未生效

---

## 2026-08-01（Phaser 3.90 2D 渲染层迁移决策）

### D-020 接入 Phaser 3.90（锁 v3 稳定线）作为 2D 渲染层，渐进式替换自研 Canvas 渲染
- **决策**：前端接入 Phaser 3.90（锁定 v3 稳定线，不追 v4 重构线）作为 React 内的 2D 渲染层，**渐进式迁移**替换自研 Canvas 渲染：阶段 0 验证 demo（瓦片渲染+碰撞 / BSP 分区 / Zone 热点 / Aseprite 动画 / 地图 JSON 契约草案）→ 阶段 1 ScenePage 渲染层换 Phaser（数据流不变）→ 阶段 2 LLM 生成地图 JSON 接入、搜证线索绑定热点、schema 版本化；完整迁移计划见 `docs/Phaser迁移计划.md`
- **原因**：工作量对比——自研 2D 渲染能力 10–16 人日 vs Phaser 引擎 2.5–4.5 人日（引擎已含瓦片/碰撞/动画/输入/摄像机/粒子等），回本点 3–5 个 2D 渲染功能点，当前路线已确定 5 个（瓦片地图+碰撞、BSP 分区、Zone 热点、Aseprite 动画、地图 JSON 契约）→ 越过回本点后引擎路线净收益
- **结构性前提**：后端 Java 权威模拟 + 前端纯渲染（数据流：后端 SSE/REST 推送状态 → 前端渲染），引擎只换渲染层，**后端零改动**；Track 数据管线 / 演讲广播 / 剧本杀链路均不感知渲染层替换
- **资产保值**：自研资产约 70% 保值——vision_core.js（视线/迷雾，纯函数逻辑）可移植或算法参照、Track 数据管线（SpatialTrackResolver→TrackStrategy→MovementConstraint 的产出即渲染数据源）、演讲广播地基（AnnouncementService/WorldEventBus 驱动横幅与提示）、React 组件（ScenePage/ChatPage/各面板）全复用；仅 simulation.html 手绘渲染（约 450 行）作废
- **放弃**：① 继续在自研 Canvas 上堆渲染能力（10–16 人日且后续功能点边际成本不降）；② 一次性整体重写渲染层替换（破坏渐进迁移「每阶段可验收」原则）；③ Phaser v4（重构线，生态与资料成熟度不及 v3 稳定线）
- **影响**：① 后端零改动、数据流契约不变；② 前端渲染层从自研 Canvas 过渡到引擎，瓦片地图/碰撞/BSP/热点/动画由引擎能力承载；③ 阶段 0 验证 demo 先行，若验证失败则回滚保持自研渲染（风险预案见迁移计划 §3）；④ 5 个已确定功能点对应迁移路线与验收标准见迁移计划 §2

---

## 2026-08-01（演讲广播合并方案正式版落地）

### D-021 merged 合并方案转正式：A 的管线 + B 的声学判定 + 可配置兜底（主人拍板，台账 #46）
- **决策**：按对比报告 4.2 建议，`roleplay.broadcast.speech-mode` 新增 **merged** 并设为默认（正式版），auto/split 保留供回退对比：①**保留方案A 管线架构**——ConversationManager 回调（SpeechTurn）→ SimulationService 判定 → AnnouncementService.enqueueAutoSpeech，SpeechStrategy 不加内联广播（内联仅 split 启用）；②**听众判定升级为 B 的 HearingSystem 声学判定并集中回管线层**——SimulationService 不再用 ModeClassifier.wouldOthersListen（2.5×/50/≥2 硬编码，仅留 auto 回退路径），改用新增 `HearingSystem.countHearingListeners(speaker, allStates)`（computeAudibility/canHear 距离衰减、半径内可听听众计数）作**判定单事实源**，split 的 SpeechStrategy 内联判定也委托同一声学方法（删除原各自实现，杜绝双份漂移）；③**「无听众→全局公告」配置化**——新增 `roleplay.broadcast.fallback-to-global`（默认 true=自动升级全局公告保持现行为；false=不升级仅区域演讲/纯空间语义），AppConfig.BroadcastConfig 补字段+setter、yml 双键、AnnouncementService 构造真实读取；④**剧本杀阶段 SYSTEM 广播转正式**——#44 的 broadcastPhase→broadcastSystemAnnouncement 五处阶段切换（initGame/startDiscussion/startVoting/resolveVote/confirmEnded）无条件启用（不再依赖 split 模式），总开关 `roleplay.broadcast.script-phase-broadcast`（默认 true）
- **原因**：①对比报告结论——A 稳定性胜（广播外挂对话链路）、B 真实游戏性胜（空间声学+AI 自主+玩家空间感知）、灵活性各有所长，正式版合并两方案优点；②wouldOthersListen 阈值硬编码是 D-004 纪律欠账，声学判定集中后正式路径不再有硬编码阈值；③「无听众→全局公告」是游戏性便利（信息可达）与真实性（空间感）之争，用配置吸收，不再需要双路径共存做形态取舍；④剧本杀阶段广播已是统一管线红利（SYSTEM 级横幅通道），无理由继续留在 demo 开关后
- **放弃**：①继续双路径共存默认 auto（正式版形态决策含糊）；②把判定留在策略层（B 模式，判定分散易漂移）；③无听众恒全局公告（方案A 语义，牺牲空间感）或无听众恒区域（方案B 语义，信息易哑火）二选一
- **影响**：①默认行为=merged：AI 演讲产出经回调，HearingSystem 判定半径内可听听众≥1→区域演讲，否则按 fallback-to-global 决定升级全局公告或保持区域；②auto/split 行为逐字节不变（回退对比可用，POST /api/announcements/mode 运行时切换，三值合法）；③剧本杀五处阶段切换无条件发 SYSTEM 横幅（总开关可关）；④全量 190 tests / 0 failures（183 基线 + MergedSpeechModeTest 7 用例：声学判定正确性三分支/剧本杀阶段广播 merged 触发+开关/merged 防双发回归）；npm run build 通过并同步 static/（index-OlgI0-rr.js）；⑤遗留：前端断线补发接线（P3，台账 #37）、8000 实例重启后新产物生效

---

## 2026-08-01（批次 D：发言门控 SpeechGate）

### D-022 SpeechGate 发言门控：每轮先判“是否发言”，规则触发必发言 + 阈值打分静默（P0-1，台账 #52）
- **决策**：讨论引擎（D-012 剧本杀 DISCUSSION 链路）每轮 LLM 生成前加**发言门控** `simulation/conversation/SpeechGate.java`（纯确定性组件，无 LLM 无随机，可单测）：①**规则触发 → 必发言**——被点名（MENTION）/被提问（QUESTION，点名+问句标记）/新线索公开（CLUE）/人类公开线索相关（HUMAN_CLUE，动机分≥50）/情绪超阈值（EMOTION，ANGRY/SAD/CONFUSED/SURPRISED）/轮次首句（ROUND_FIRST，开局自我介绍）/冷场破冰（COLD_BREAK）七类事件，命中即发言**不受 talkativeness 概率限制**；②**阈值打分 → 静默**——否则 P = motiveScore(动机优先级) × 人格化 talkativeness（roles[].talkativeness，缺省 0.5），人类发言中且未被点名再 × wait_bias；P < silence_floor → 静默（跳过 LLM 生成省成本，以占位符 `SILENCE_MARKER=“……（沉默）”` 入发言记录，冷场检测亦以该标记识别全员静默）；③**接线**——ScriptGameService.buildRoundGate 每轮编排：排空人类发言事件（人类发言权豁免：直接注入对话流不过门控，该角色 AI 不代声）→ 扫描新增发言生成点名/提问触发（被质疑者注入辩解临时目标 pri=100，N 轮衰减回落，对齐 Bates 情绪→目标再评价）→ 人类线索/情绪/轮次首句/冷场破冰 → 逐成员 decide；④**阈值可配**（对齐 D-004 勿 hardcode 纪律）：`roleplay.game.discussion.silence-floor`（默认 **0.15**）/`wait-bias`（默认 **0.5**）/`cold-break`（默认 **true**）三键 yml 双份（主/test）；⑤**schema 扩展**——ScriptSchemaV1 roles[] 补 `talkativeness`（缺省 0.5，兼容 `personality.talkativeness` 嵌套），initGame 按角色名装载 playerTalkativeness
- **原因**：①调研结论（tmp/AI动机与静默机制调研.md P0-1 + speech-demo 对比报告，demo 实测参数直接采用）——真实 LLM 下全员每轮必发言产生“话痨冷场失衡”与“全员抢话信息密度低”双问题，凶手因话多反而获益；②“是否发言”与“说什么”解耦——门控只输出决策，发言内容仍由 TrackStrategy+LLM 生成，改动面最小且可单测；③规则触发优先是防冷场第一道闸（事件驱动确定性），打分静默只作用于无事件的低意愿轮次
- **放弃**：①把静默做成概率随机（门控确定性可测，随机留给 LLM 内容层）；②静默轮不产出任何记录（占位符保证发言记录/轮次结构完整，前端/冷场检测可消费）；③阈值写死（D-004 纪律，已配置化）
- **已知限制**：①阈值区间 silence_floor ∈ [0.10, 0.20] 为 demo 实测安全区间，0.25 会冷场失衡、凶手获益——仍为单值配置非动态自适应（P1 可做按对局情绪/冷场统计自适应）；②门控实例为 service 实例级（与 D-012 讨论引擎同限制，多局并发共享）；③被点名判定 isMentioning 为规则近似（@名/句首/标点后），复杂句式可能漏判（P2 可接入 LLM 判定或语义解析）；④前端 SILENCE_MARKER 渲染与 @AI 输入提示未做（批次 D 收尾报告已注明，P-0801-B 占用前端文件未动，建议后续批次处理）
- **影响**：①新增 SpeechGateTest 24 用例（触发必发言七类/低分静默含占位/阈值边界 0.15 附近/ wait_bias 打折/动机分映射/COLD_BREAK/静态工具 isMentioning·isQuestioning·scanTurns·reasonOf），全量 214 tests / 0 failures（190 基线 + 24）；②人类发言权豁免：人类 @某 AI → 该 AI 强制发言且注入辩解目标；③静默成员跳过 LLM 生成（成本控制）；④schema 契约文档 docs/剧本-schema-v1.md 同步 talkativeness 字段；⑤注意：任务书原指定登记 D-019，但 D-019 已被演讲广播方案B 占用，按序顺延登记为 **D-022**；⑥未 git commit（未获授权）

---

## 2026-08-01（P1 缺陷修复批次）

### D-023 LLM 剧本生成 maxTokens 修复：600→4000 根治 JSON 截断，全局调用点按输出复杂度分级设值（P1，台账 #57）
- **决策**：①**主修复**——ScriptService.generateScript 的 llmClient.callJson(prompt, 600) → **4000**（真机验证 3/3 生成失败根因：4 角色剧本 schema v1 JSON（metadata+roles[]×4+clues[]+secrets+killer_id+truth+background）真实输出需 2000-4000 tokens，600 被硬截断 → LLMClient 日志 Unexpected end-of-input: expected close marker for Array → 全员走 defaultScript 兜底，且 SpeechGate 静默分支不可观测（兜底剧本全员 talkativeness=0.5 恒过阈值））；4000 远低于 DeepSeek 单次输出建议上限 8192，仅剧本生成类大 JSON 使用；②**全局分级设值**（grep 全部 14 个 callJson/callSimple 调用点评估）——「多角色/多条目结构化 JSON 类」6 处同步提升：ArbiterService 轨道配置 400→600、主持整合 800→1000、TrackRequestService 需求评估 300→600、审批 200→400、Compressor 压缩摘要 150→300、SimulationService 主控轮次 600→1000；「短回答/单字段类」5 处保留（分类 20 / 旁白 120 / 窃听摘要 120 / 单目标 300 / 单场景 300 / 单角色 400——输出量级与 maxTokens 匹配）
- **原因**：①真机实证——修复前 3/3 init 走兜底（角色名全是「嫌疑人_X」），修复后 4/4 独立生成完整剧本（角色名具体化、killer_id/truth/clues/locations/secret 齐全）；②600 tokens ≈ 400-600 中文字，仅够 metadata+2 角色 intro，硬截断必然破坏 JSON 闭合 → callJson 三连重试后返回空 map → normalize 落入 defaultScript；③同类风险调用点（多角色 decisions/tracks/审批列表 JSON）在真实 LLM 下同样有截断风险，一并按复杂度修正，避免逐个爆雷
- **放弃**：①把 max_tokens 做成配置项（各调用点输出量级差异大，per-call 常量更直白，D-004「阈值勿 hardcode」纪律对行为阈值适用，此处为容量预算）；②全部调用点统一放大到 4000（短回答类放大无收益且增加截断容忍/延迟）；③改 LLMClient 做 max_tokens 自动估算（过度设计，量级差异已知且固定）
- **影响**：①全量 217 tests / 0 failures（214 基线 + ScriptServiceMaxTokensTest 3 用例：2000+ token 长剧本 JSON 解析字段齐全不截断 / verify maxTokens=4000 防回归 / 空输出兜底 A1-3 不回归）；②真机复验：8000 实例重启（java -jar，pid 21676）后 4/4 次真实 LLM 完整生成剧本，后端日志无 end-of-input/callJson failed；讨论流程正常（真实多样发言 8 条，自动进 VOTE）；静默占位本次 2 轮内未触发（概率性，门控路径已由 SpeechGateTest 24 用例锁定）；③运行成本：仅剧本生成/主控轮次等大 JSON 调用点 token 预算上升，短回答类不变；④未 git commit（未获授权）；⑤并行批次 P-0801-C（Phaser 阶段2 地图生成）在作业期间写入未完成代码（MapValidator/ScriptMapService 编译错误中间态 + ScriptMapServiceTest 构造不匹配），本批次打包经 -Dmaven.test.skip=true 规避其未完成测试编译完成，已在其登记行注明


---

## 2026-08-02（C-2 批次 A 部分：一般模式串行调度实施）

### D-024 一般模式串行调度：配置开关 roleplay.round.serial（默认 false），runRound 并行改串行循环 + 同轮即时入史（主人拍板）
- **决策**：①**配置开关**——新增 `roleplay.round.serial`（默认 **false**=保持既有并行行为零破坏；true=串行调度），接线双轨：AppConfig.RoundConfig 补 serial 字段（@ConfigurationProperties 绑定）+ RouterService `@Value` 读取 + yml 双份（application.yml / application-test.yml），对齐 roleplay.game.* 先例（D-018/D-027：服务层 @Value 与 AppConfig 同源同值）；②**runRound 串行分支**——serial=true 时 Step3-4 重排为串行循环 `executeRoundSerial`：按轨道顺序 × 轨道内 agent 顺序（PLAYER>DM>NPC，computeSerialPriority 同 AgentExecutor 排序规则）逐个生成，每个 agent 输出完成**立即** memory.addMessage + SSE broadcastAgentOutput + 收集到 agentOutputs，后发言者 buildAgentContext 读 memory.getAgentContext（30 条可见消息）自然包含前面角色本轮已完成的发言=同轮上下文共享；③**上下文真正传入 LLM**——串行路径用 `agent.generateWithContext(context, token)` 把 buildAgentContext 全量上下文（身份/场景/轨道/摘要/对话历史含本轮前者发言）显式传给 LLM；调研发现并行路径 AgentExecutor.buildTasks 里 ctxBuilder 构建的 context 变量实际未传给 generateSync（传的是 persona 轻量 prompt + 空 history），属既有行为不在此批修复，串行路径按方案文档显式传入；④**D1 中断语义保留**——串行循环每步注册 AgentTask + CancellationToken（InterruptManager.register/toRunning/toDone/unregister），检查 running 标志，TaskCancelledException 中断循环返回 cancelled 走既有分支（broadcastStopped + error），LLM 失败输出占位「走神了」不入史（isSuccess=false 与并行一致），软停止 partial 保存到任务；⑤**测试**——RouterServiceSerialRoundTest 4 用例：serial=true 同轮上下文共享（后发言者 B 上下文含 A 本轮发言+内存/输出顺序 A→B）、serial=false 默认并行行为不变（两角色均有输出入史）、serial=false 不共享同轮上下文（并行路径 LLM 消息不含本轮发言）、配置默认 false+setSerialRound 切换
- **原因**：①C-2 调研结论——「同轮上下文不共享」断点=AgentExecutor 全并行执行 + RouterService Step3→Step4 顺序（各角色输出在并行完成后的 Step 4 才批量入史，生成期间无人可见），对照 2D 世界 ConversationManager 同构问题；②主人拍板方案明确「本轮按顺序发言，后发言者上下文包含前者发言（解决同轮次上下文不共享的通病）」「不限速直接显示」——串行化性能代价（n agents = n× LLM delay）为预期；③默认 false 保证 272 基线零破坏，串行仅按需开启（2D/剧本杀讨论 ConversationManager 并行路径零触碰，范围只限 RouterService.runRound 一般模式）
- **放弃**：①AgentExecutor 内新增串行方法 + RouterService 调新方法（方案 B）——最小改动直接落在 runRound 串行循环即可，AgentExecutor 非禁动但零改动更安全；②前端排序/延迟展示（方案 C）——只改展示顺序不改生成依据，治标不治本；③把同轮上下文共享同时应用到 2D 世界群组（ConversationManager.executeRound 同构问题）——本轮范围只要求一般模式，2D 打字机已由 B 部分解决展示层串行，生成层串行另议（方案文档第四节明确）
- **影响**：①新增 1 配置键（roleplay.round.serial）+ RouterService @Value + setSerialRound/isSerialRound（测试/运行时切换）；②serial=true 时 SSE agent_output 逐个即时推送，前端 addAgentMsg 顺序天然正确（更符合「顺序发言」观感）；③Track 隔离语义保留——memory.getAgentContext 走 getMessagesVisibleTo 过滤（visibleTo 空=全员可见，与并行路径一致），串行化不改变可见性过滤；④全量 285 用例中 284 通过（272 基线 + 本批 4 + 并发未登记批次 WerewolfAiPlannerTest 8），唯一失败属并发未登记批次（狼人杀 AI 行动器 G0-2，与本批无关，禁动零介入）；⑤未 git commit（未获授权）

---

## 2026-08-02（狼人杀后端授权批次 P-0802-H）

### D-025 狼人杀最小可玩闭环：autoPlay 自动推进 + AI 行动器 + werewolf_* SSE + 白天讨论接对话引擎（主人授权后端解禁）
- **决策**：按调研报告《狼人杀重构调研-20260802.md》阶段 0 落地最小可玩版：①**autoPlay 自动推进闭环（核心）**——init 后整局自动流转：夜间 AI 行动→（真人行动后）自动结算→白天讨论自动驱动→自动进投票→ AI+真人投票完毕自动结算（D7 审批门保留，配置 auto-approve-ms 默认 3000ms 自动批准，0=等 DM 手动）→回夜，直到胜负判定；②**AI 夜间行动器**——WerewolfAiPlanner 纯规则零 LLM（对齐 D-002）：狼共刀不刀狼、预言家验随机存活、女巫首夜救被刀者/后续夜按概率毒随机、AI 猎人死亡自动反杀、白天 AI 投票（村民随机非己/狼队共投非狼）；③**werewolf_* SSE**——服务注入 SseBroadcaster 接口（测试可注入录制假实现），十类事件全链路推送，前端死监听变真消费；④**白天讨论接对话引擎**——复用 D-012/D-022 资产（独立 ConversationManager + SimulationWorld + TrackStrategy 全员 MERGED + SpeechGate 门控 + 人类发言权豁免），讨论结束自动进投票；⑤**同时修复四项既有缺陷**：G0-1 init 返回 session_id+注册 RouterService、D-014 宽容解析 customRoles（小写/别名 wolf《狼人》→WEREWOLF 等）、G1-3 toMap 补 visible 键（狼人互认）、G1-1 猎人夜间死亡保留一次开枪机会
- **原因**：①调研报告结论——「狼人杀完全没进游戏」三重脱节：前端 init 只传真人（AI 未进 GameState，单机退化 1 人村民死局）+后端状态机纯手工驱动无自动调度/无 SSE（前端零感知）+前端「自动玩」走普通对话管线（「仍按一般模式交流」根因）；②「无自动推进」是 P0 反馈的直接原因——夜晚永远等不到人齐、讨论死地，必须有闭环才能玩通一局；③自动推进要保留 D7 审批门语义（关键决策点人工审查），单人 solo 场景下无 DM，用可配置自动批准吸收（对齐 D-004 阈值可配纪律）；④人类猎人不自动反杀（人工保留手动开枪权），避免多人局被 AI 抢走高光瞬间
- **放弃**：①给狼人杀用前端自动点击驱动流程（多玩家同步后端状态机复杂度高，单人+AI MVP 以后端自动推进为主）；②女巫 AI “透视”狼刀目标做成真正的“获知被刀者”机制（引入夜间信息传递事件，机制复杂度超出阶段 0，首夜救被刀者为简化近似）；③本批改 SSEController 添加 typed 方法（werewolf_* 经 SseBroadcaster 接口直接复用既有 broadcast 管线，测试可控性更强，SSEController 主链路零改动）；④引入 AnnouncementService SYSTEM 阶段横幅（横幅仅 2D 视图展示，狼人杀聊天 UI 不消费，SSE 为正确渠道，留 P1）
- **影响**：①全量 293/0 BUILD SUCCESS（272 基线 + 本批 WerewolfAiPlannerTest 9 + WerewolfGameFixTest 8 + 并行 C-2-A RouterServiceSerialRoundTest 4）；②新增 4 配置键（roleplay.game.werewolf.*）+ 1 新端点（POST /api/werewolf/discussion_say）+ 前端行动面板；③旧行为保留：autoPlay=false 时纯手工 admin 驱动（resolve_night/start_voting/resolve_vote 旧路径不变），既有测试无需改；④已知限制——讨论引擎为 service 实例级共享（D-012 既有限制，同时只开一局讨论）；SSE 仍全局广播（D-013 既有限制，死者身份不进全局广播，角色按玩家视角经 status API 获取）；夜间人类女巫的“被刀者提示”未实现（G1-2 留 P1）；无对局落库/重连/联机房绑定（G1-4~G1-7 留阶段 1）；⑤未 git commit（统一 gate 未获授权）

---

## 2026-08-02（P-0802-G 批次：串行调度开启 + 2D 视图遮盖修复）

### D-027 一般模式串行调度正式开启（roleplay.round.serial: true）+ 2D 视图占主体修复（主人反馈）
- **决策**：①**串行调度正式开启**——src/main/resources/application.yml 
oleplay.round.serial: false → true（D-024 开关保留，可随时切回 false；application-test.yml 不动，测试走显式 setSerialRound 与主配置解耦）；②**2D 视图占主体**——ScenePage showPhaserSim=true 时整页折叠场景设置区（角色/场景面板全部隐藏），2D 视图（PhaserSimulationView height=640）独占主体区域，顶部提供「✕ 退出 2D（返回场景设置）」按钮恢复原布局；③**用户在场判定自查结论：链路正确不强改**（详见影响④）
- **原因**：①主人反馈「前端好像还是并联输出」——根因即 D-024 开关默认 false（当时零破坏），串行实现早已就位只是未开启；②主人反馈「进入 2D 游戏上方有场景设置的 UI，导致 2D 游戏下方 UI 被遮盖」——2D 内嵌视图在场景设置页底部被角色/场景选择面板挤压，聊天/输入被推出版心；③「用户进入对话判断还是很晕」——主因即串行未开（并行输出乱序），2D 在场判定链路本身正确
- **放弃**：①2D 视图做真正的全屏/新窗口模式（页面内主体展示已满足「不遮盖」诉求，改动面最小且保留上下文）；②改 ConversationManager 2D 世界生成层串行（D-024 已明确一般模式先行，2D 打字机展示层已串行，生成层另议）；③在场判定逻辑调整（玩家在 2D 世界被标 playerControlled 后本就不进自动邻近对话组，仅主动发言进 DYAD 组——语义正确，非 bug）
- **影响**：①一般模式（free/director 等非 2D）正式走串行调度：同轮按序发言、后发言者上下文含前者本轮发言（同轮上下文共享），SSE agent_output 逐个即时推送，前端顺序天然正确；性能代价 n× LLM delay 为预期（主人方案「不限速」）；②2D 视图进入后场景设置区整体隐藏、2D 占主体（顶部仅小标题行+角色数+退出按钮），退出恢复原布局；与 C-1 可折叠右聊天面板、P1-8 公告栏（均在 PhaserSimulationView 内部）天然兼容；③用户在场判定链路确认：前端 4s 轮询 conversation-status → groups[].participants 含玩家名 → 在场单轨气泡；后端 ConversationManager.getStatus 提供 participants（AgentState.getAgentName），字段完整无缺失；玩家主动发言才进 DYAD 组=「在场」真实生效；「晕」主因=串行未开，问题 ① 修复后应改善；④全量 mvn test **302/0 BUILD SUCCESS**（serial=true 下 RouterServiceSerialRoundTest 4/4 仍全绿——测试走 application-test.yml 显式配置+setSerialRound，不依赖主 yml）；npm run build 通过 + static 同步（index-4rQ391AJ.js SHA256 dist↔static 一致，最终 active 产物为并行批次 P-0802-I 基于含本批改动的完整源码重构建 index-Dn8o7YSC.js，bundle grep 命中「退出 2D/返回场景设置」）；⑤未 git commit（统一 gate 未获授权）


## 2026-08-02（狼人杀阶段1遗留项批次）

### D-028 狼人杀阶段1遗留项：女巫获知被刀者机制 + 讨论引擎 per-game 隔离 + werewolf_* SSE 会话定向 + 快照落库/重连/联机房绑定（主人批准阶段1遗留项，P-0802-I）
- **决策**：①**G1-2 女巫获知被刀者机制**——狼刀决策完成即置 GameState.witchInformed=true 并定向推送 werewolf_witch_info（victim=狼刀目标，仅女巫存活时），人类女巫先获知再操作（save 仅限被刀者本人，新增「不使用解药」nosave 与「不用毒」nopoison 决策放行夜间完成判定），AI 女巫获知后按可配概率 roleplay.game.werewolf.witch-save-probability（默认 1.0=经典首夜必救）决策救/不救，toMap 女巫视角暴露 witch_victim（SSE 丢失/轮询/重连兜底）；②**讨论引擎 per-game 隔离**——WerewolfService 用三张 Map（SimulationWorld/ConversationManager/WorldDirectorService 均按 sessionId）替代原 service 实例级共享字段，ensureDiscussionEngine(sessionId) 懒创建，多局并发讨论互不覆盖/互不串状态（修复 D-012 已知限制）；③**werewolf_* SSE 会话定向**——SseBroadcaster 新增 broadcastToSession（默认实现回退全局广播=既有实现/测试零破坏），SSEController stream 支持 ?session_id= 注册过滤，broadcastToSession 只送达匹配会话的连接（无匹配静默丢弃），WerewolfService 全部 werewolf_* 事件改走定向（修复 D-013 已知限制「SSE 全局广播多局互串」）；④**对局快照落库/断线重连/联机房绑定**——快照复用 ScriptEntity（type=snapshot，name 前缀「对局快照:ww:<sessionId>」，零新表零 DatabaseService 改动），7 个状态变更点全量快照；POST /api/werewolf/resume（session_id/room_code+player 定位：内存命中直接返回/快照重建/终局终态）；init 可选 room_code 绑定 roomGames→sessionId 映射并回显，resume 可按房间码定位（对齐 C3 剧本杀轻量绑定先例，RoomController 本体零改动）
- **原因**：①调研报告阶段1清单 G1-2——AI 首夜救被刀者原为简化近似（planner 直接救），人类女巫夜间无任何被刀者信息（面板盲选目标），「获知→决策」是经典狼人杀规则核心；②D-012 明确「讨论引擎为 service 实例级共享（多局并发互覆世界）P1 可改 per-game」——本批即该 P1 落地；③D-013 明确「SSE 推送仍全局广播（多局事件串到同一连接）P1 可做 per-session emitter」——本批即该 P1 落地；④D-017 剧本杀 C3 先例（roleKey+快照+room_code 绑定）已验证可行，狼人杀照搬轻量版（按 session_id 定位，不引入 roleKey——任务要求「按 session_id 拉取当前对局状态」）
- **放弃**：①给狼人杀做 roleKey 令牌认证（任务只要求按 session_id 重连，剧本杀 C3 的 roleKey 防冒充体系属另一个阶段；狼人杀 resume 以 player 名定位，后续可对齐 C3 补 roleKey）；②script_* 剧本杀 SSE 一并改定向（任务范围限定 werewolf_*，剧本杀多局并发串扰为既有已知限制，前端按 session_id 字段可区分，留后续批次）；③快照做增量/事件溯源（全量 JSON 每次写入已满足规模，对齐 D-017 决策）；④改 RoomController 做双向绑定（与 D-017 相同结论：/api/rooms 为纯内存大厅无 gameId 字段，双向绑定需改其结构，controller 层轻量映射已满足）；⑤SSE 定向做成前端过滤（后端定向才是隔离本原，前端过滤只是展示层掩盖）
- **影响**：①全量 mvn test **302/0 BUILD SUCCESS**（41 类；293 基线 + WerewolfStage1Test 7 + SSEControllerSessionTest 2；首轮 LongTextStabilityTest 堆增长 49.4% 为既有堆测量脆性 #37/#40/#41 同款，单跑 1/0 通过后全量复跑全绿）；②后端新增 1 配置键（witch-save-probability）+ 2 端点（resume）+ SseBroadcaster 1 默认方法 + SSEController 定向方法（全局 broadcast 向后兼容）；③前端女巫面板改为「先获知被刀者再决策」时序 + 狼人杀恢复对局入口 + useSSE 会话定向连接；④已知限制——resume 不校验 player 身份（无 roleKey，按名定位）；恢复后的 DAY_DISCUSS 讨论线程随重启丢失（与 D-017 剧本杀同限制，可经 /api/werewolf/start_voting 手动推进）；ScriptGameService 讨论引擎仍为实例级共享（本批仅隔离狼人杀侧，剧本杀侧留后续）；⑤与并行批次 C-2-A（RouterService 串行区）/P-0802-G（ScenePage/yml serial:true）共存零冲突：本批零改动 RouterService/ArbiterService/审批，SSEController 仅新增方法；⑥未 git commit（统一 gate 未获授权）


## 2026-08-02（狼人杀范围外遗留项批次 P-0802-J）

### D-029 狼人杀范围外遗留项三项：剧本杀讨论引擎 per-game 隔离 + script_* SSE 会话定向 + 狼人杀 resume roleKey 防冒充（主人批准范围外遗留项）
- **决策**：①**剧本杀讨论引擎 per-game 隔离**——ScriptGameService 用三张 Map（SimulationWorld/ConversationManager/WorldDirectorService 均按 sessionId）替代原 service 实例级共享字段（D-012 已知限制「讨论引擎为 service 实例级共享（多局并发讨论会互覆世界）」的剧本杀侧落地，对齐狼人杀侧 P-0802-I 同款三 Map），ensureDiscussionEngine(sessionId) computeIfAbsent 懒创建，runDiscussionEngine/buildRoundGate/pickIceBreaker/getDiscussionGoal 全部改取本局实例；②**script_* SSE 会话定向**——SSEController 三个 script helper（broadcastScriptPhase/Status/Reveal）内部改走 P-0802-I 已就绪的 broadcastToSession 定向通道（sessionId 为空回退全局零破坏，全局 broadcast 不变），前端 App.tsx SSE 连接按当前模式选会话（script→scriptSessionId / werewolf→werewolfSessionId / 其他→无过滤），剧本杀 session_id 由 init/resume/轮询回写 store（+scriptSessionId）；③**狼人杀 resume roleKey 防冒充**——对齐剧本杀 C3 roleKey 体系：initGame 每玩家发放唯一 UUID roleKey（toMap 仅向本人暴露 role_key，匿名/他人视图不含防泄露），resumeGame 改三参 (sessionId, player, playerKey) 强制 key 校验（缺 key/错 key/他人 key 冒充/玩家名与 key 不匹配全部拒绝，仅凭 key 可反查玩家恢复），roleKey 随快照持久化（跨实例重启后仍校验），新增 GET /api/werewolf/keys 主持人分发端点，前端恢复对局入口 roleKey 必填 + 「我的 roleKey」展示
- **原因**：①P-0802-I 汇报未决疑问明确「ScriptGameService 讨论引擎仍为实例级共享（本批仅隔离狼人杀侧，剧本杀侧留后续）」——本批即该后续落地；②D-013 已知限制「SSE 推送仍全局广播（多局事件串到同一连接，前端按 session_id 字段可区分）P1 可做 per-session emitter」的剧本杀侧（script_*）落地——werewolf_* 已在 P-0802-I 定向，script_* 是同一隔离本原的剩余半边；③D-028 明确「狼人杀 resume 不校验 player 身份（无 roleKey，按名定位）……后续可对齐 C3 补 roleKey」——任何客户端拿到 session_id 即可恢复任意角色的跨角色冒充漏洞，roleKey 校验是防冒充本原（对齐 C3：开房生成每角色 roleKey、断线重连/顶号认证一体）
- **放弃**：①剧本杀侧另起一套讨论引擎（与狼人杀 P-0802-I 三 Map 完全同构，直接照搬最小风险）；②script_* 定向做成前端过滤（后端定向才是隔离本原，前端过滤只是展示层掩盖——与 D-028 ⑤同一结论）；③resume 保持向后兼容无 key 路径（防冒充语义要求 key 必填，剧本杀 C3 resume 同样强制 key，前端恢复入口同步改为必填，无既有前端依赖旧路径——P-0802-I 前端恢复入口本批同步升级）；④给狼人杀玩家级端点（night_action/vote/discussion_say）也加 key 校验（任务范围限定 resume，玩家级端点防冒充可作后续批次，与剧本杀 C3 checkPlayerAccess 全端点体系对齐留后续）
- **影响**：①全量 mvn test **309/0 BUILD SUCCESS**（43 类；302 基线 + ScriptGamePerGameIsolationTest 2 + WerewolfRoleKeyTest 4 + SSEControllerSessionTest +1；无堆测量脆性波动）；②后端：ScriptGameService 讨论引擎按对局隔离（多局并发剧本杀讨论互不覆盖/互不串状态）+ SSEController script_* 定向 + WerewolfService roleKey 体系（resume 三参强制校验/快照持久化）+ WerewolfController +GET /api/werewolf/keys；③前端：SSE 按当前模式选会话连接（script/werewolf/其他），剧本杀与狼人杀对局事件互不串扰；狼人杀恢复对局 roleKey 必填；npm run build 通过 + static 同步（index-Bp4ixzCe.js，SHA256 一致）；④已知限制——狼人杀玩家级端点（night_action/vote/discussion_say）暂无 key 校验（按玩家名定位，防冒充留后续批次对齐 C3 checkPlayerAccess）；script_* 定向后无过滤 SSE 连接（如未带 session_id 的旧客户端）不再收到 script_* 事件（全局广播仍全量，前端 3s 轮询兜底）；⑤与并行批次共存：C-2/C-2-A（RouterService 串行区）零触碰，P-0802-G/I 前端改动全保留（本批构建基于含其改动的完整源码）；⑥未 git commit（统一 gate 未获授权）


## 2026-08-03（剧本选择与角色卡功能改造批次 P-0803-H）

### D-030 剧本绑定三字段（category/default_roles/default_map）落 SceneEntity 持久化 + BSP 默认地图端点；前端「场景选择」整体改为「剧本选择」（主人需求 1-8）
- **决策**：①**剧本绑定三字段落库**——SceneEntity 增 category（general=一般模式/werewolf=狼人杀模式）、defaultRoles（JSON 数组串）、defaultMap（地图 JSON 契约 v1 串）三列（ddl-auto=update 自动加列，旧行默认 general/空组/无地图零迁移）；DatabaseService saveScene 8 参重载（旧 5 参委托默认值，既有调用方零破坏）+ entityToMap 宽容解析新键（parseRoleList/parseJsonMap，非 JSON 逗号兜底对齐 D-014 纪律）；SceneController create/update/persistScene 全链路透传，**default_map 空串=清除语义**（null=不修改保留旧值，空串=清空，JSON=写入——前端「清除地图」依赖）；②**默认地图生成端点**——新 POST /api/scenes/map 直接调 BspMapGenerator.generate（契约 v1，确定性同 seed 同输出，零 LLM 零成本），前端剧本编辑弹窗「生成默认地图」绑定到 default_map；③**前端整体改造（主人需求 1-8）**——「场景选择」入口全部改为「剧本选择」；剧本卡按分类（一般/狼人杀）chips 展示；点开剧本卡显示角色卡栏（默认角色组，用户角色置顶不默认勾选）+ 地图预览（default_map → PhaserScriptMapView，无则占位）；角色库按所属剧本（default_roles 派生，零角色表改动）分组页签 + 自由角色卡页；角色卡 hover 编辑/删除按钮（DELETE /api/characters/{name} 后端已就绪）；用户角色卡每栏置顶（withMeFirst）+ 自动带上默认角色/狼人杀默认角色均排除用户角色卡（需求 7）；2D 规则——狼人杀「默认 2D」checkbox 默认勾选（开局自动开 2D 视图 + 2D 内「游戏面板（聊天页）」直达），一般模式「是否 2D」checkbox 用户自选（需求 8）
- **原因**：①主人需求原文 8 项——角色卡增删/场景选择改剧本选择/剧本绑默认角色与地图/场景分类/点开卡显示角色栏+地图预览/角色卡按场景分类+自由页/用户角色卡置顶且不默认勾选/狼人杀默认 2D 而一般模式给选择；②「绑定」需持久化才能跨重启生效——既有 SceneEntity 无字段，最小必要后端改动即三列+端点；③角色卡按场景分类用剧本 default_roles 派生而非角色表加 scene_id——零角色表改动、语义一致（绑定关系单源），避免双写漂移；④默认地图用 BSP 确定性生成而非 LLM——剧本卡绑地图要即点即有零成本，LLM 生成留给对局内（POST /api/script/map）
- **放弃**：①给角色表加 scene_id 字段做显式归属（default_roles 派生已满足「按所属场景分类+自由角色卡」，显式归属会造成绑定双源漂移，留后续若需角色多归属再说）；②默认地图走 LLM（成本与延迟不可接受，BSP 确定性已够预览语义）；③改 RoomController/狼人杀/剧本杀主链路（需求 8 的狼人杀 2D 只动前端入口，后端游戏逻辑零改动）
- **影响**：①全量 mvn test **378/0 BUILD SUCCESS**（54 类；373 基线 + SceneBindingTest 5 用例，LONG-01 PASS）；②后端 3 文件 + 1 端点（POST /api/scenes/map），禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE）零改动；③前端 4 文件（types/client/ScenePage/global.css）+ static 同步（index-BvHFWNFM.js SHA256 dist↔static 一致）；④已知限制——用户角色卡不自动勾选后，狼人杀剧本卡默认角色不足 5 人时需手动勾选（UI 有提示）；角色卡分组为派生关系（角色可在多剧本 default_roles 中出现，各栏均展示）；「剧本」与「场景」为同一数据实体（scene 表）的展示语义（需求原文如此，未拆分表）；⑤未 git commit（统一 gate 未获授权）；⚠️ 本批标记 P-0803-H 与并行「scripts.name 修复批次」（18:12）撞标，改动区域不同零冲突，提请主会话协调

## 2026-08-03（剧本杀地图容量扩展批次 P-0803-J）

### D-031 地图尺寸可配置化 + 大图走 BSP：尺寸参数贯穿 API（默认 24×16 零破坏）+ 热点数按面积缩放（P-0803-J）
- **决策**：①**尺寸参数贯穿 API**——POST /api/script/map body 新增可选 width/height → ScriptGameService.generateMap 六参重载（显式 → 对局已定尺寸 mapWidth/mapHeight → 配置默认三级解析，regenerate 无显式尺寸时保持原尺寸）→ ScriptMapService.generateMap 五参重载 → BspMapGenerator.Options.of；旧签名/旧调用方（缺省或 0）恒走默认 24×16，逐字节零破坏；②**尺寸进配置**——新增 `roleplay.game.map.default-width`（默认 24）/ `default-height`（默认 16）yml 双份，ScriptGameService @Value 注入（对齐 D-018/D-027 先例）；③**热点数按面积缩放**——BspMapGenerator 新增 `scaledZonesCount`：基准 24×16=384 格 ↔ DEFAULT_ZONES_COUNT=3，√面积缩放（64×64≈4096 格 → ≈10 热点），下限恒 3、生成时受房间数 min 封顶；`Options.of` 的 zonesCount<0 语义由「默认 3」改为「按面积自动缩放」——默认尺寸下结果仍=3，旧调用方（ScriptMapService/SceneController 传 -1）行为不变；④**大图与 LLM token 预算**——LLM 路径上限约 40×24（ground+collision ≈1920 数字 ≈2500-3000 tokens，逼近 callJson 4000 上限）：显式尺寸超上限跳过 LLM 直接 BSP 确定性生成（fallback 原因含预算说明，LLM 零调用）；预算内显式尺寸仍走 LLM 且 prompt 内嵌本次要求尺寸；⑤**尺寸随快照持久化**——saveSnapshot/restoreFromSnapshot 增 map_width/map_height，重启恢复后 regenerate 保持原尺寸
- **原因**：①任务要求「地图尺寸可配置、支持 64×64+ 大图」——调研已确认 BspMapGenerator.Options 本就支持任意尺寸，缺的是链路透传（降级调用点写死 0,0→24×16）；②大图若仍走 LLM：4000 tokens 装不下 64×64 双层数组（截断风险与 D-023 剧本 JSON 同款），且 LLM 输出尺寸不可控——确定性 BSP 才是大图正路；③热点数写死 3 在大图会空旷（64×64 只有 3 个搜证点）、在小图会过密——按面积√缩放对齐「房间数随叶子数增长」的自然规律
- **放弃**：①大图也走 LLM（token 预算与尺寸不可控双缺陷）；②给 ScriptMapService 的 LLM 路径做「输出尺寸强制校验重试」（LLM 尺寸不可控且重试成本高，预算闸已从源头分流大图，预算内尺寸不强制）；③SceneController POST /api/scenes/map（剧本卡默认地图预览端点）同步透传尺寸（预览语义 24×16 已够，前端地图预览无大图诉求，留后续按需）；④改前端（任务范围纯后端+测试，前端已有 PhaserScriptMapView 消费 map 契约 width/height，无需改动即可渲染大图）
- **影响**：①全量 mvn test **390/0 BUILD SUCCESS**（55 类；378 基线零破坏 + ScriptMapSizeExpansionTest 11 参数化用例（64×64/48×48/100×60/128×64 生成+校验+缩放+可通行）+ ScriptMapPersistenceTest M9 快照尺寸恢复，LONG-01 PASS 2.337s）；②后端 4 文件 + yml 双份，禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static/前端）零改动；③默认行为逐字节不变（缺省尺寸→24×16、zonesCount=-1→3、旧断言未改）；④已知限制——LLM 路径不强制输出=请求尺寸（预算内显式尺寸仅 prompt 建议，LLM 输出以校验通过为准；大图尺寸由 BSP 保证精确）；前端「生成地图」入口尚未传 width/height（默认尺寸渲染，调用方可按需加 body 参数）；⑤未 git commit（统一 gate 未获授权）


## 2026-08-03（剧本杀模式多地图切换批次 P-0803-K）

### D-032 剧本杀多地图切换：对局层多图注册表 + door zone 触发切图 + 足迹按图隔离（P-0803-K）
- **决策**：①**对局层多图注册表**——ScriptGame +maps（mapId→契约 v1 数据）+mapFallbacks（每图溯源）+searchedByMap（每图足迹）+currentMapId；init 自动图注册为 map_1 并设为当前；generateMap 生成即注册并设为当前（map_id 显式/自动 map_<n>，LLM/BSP 输出 map_id 统一归一为注册表键保证唯一）；②**door zone 触发切换**——POST /api/script/map/switch：door_zone_id 必须命中当前图 type=door zone（+可选 x/y 靠近校验，radius+DOOR_PROXIMITY_SLACK=2，缺坐标跳过）+ 目标解析（body target_map_id → door zone target/to/target_map_id → 未注册自动生成 BSP 兜底，尺寸取 door 可选 width/height 联动 P-0803-J 链路）+ 非法 door 目标全容错（不存在/非 door 型/无目标/目标=当前图/阶段不符/非本局玩家/远离/双缺）；POST /api/script/map/door 布门端点（x/y 缺失或不可通行自动吸附最近可通行格）；③**足迹按图隔离**——searchedLocations 恒为当前图足迹视图（前端绿点数据源不变），切图时当前图足迹暂存 searchedByMap、目标图足迹载入；角色/线索/AP/秘密/票型为对局级状态天然保留；④**尺寸联动**——切图后 mapWidth/mapHeight 随目标图更新；⑤**持久化**——注册表/当前图/每图足迹随快照落库（旧快照无 maps 键 → 兼容回退：仅当前图注册），重启恢复后切图链路完整（K8）；toMap 暴露 current_map_id/map_ids 附加键（不破坏既有契约）；⑥**同步**——切图复用现有 script_status SSE 全量推送 + announcement SYSTEM 横幅（coalesceKey=script_map|<to> 防同窗合并成 ×N），SSE 主链路零改动；LLM prompt 预留 door 可选输出
- **原因**：①任务要求剧本杀模式支持多地图探索——地图从「对局单张」升级为「对局注册表多张 + 当前图指针」（P-0803-J 地图容量扩展的自然延伸，64×64 大图已就绪，多图场景按需组合）；②door zone 是空间切图的最小语义载体——前端 Phaser 已渲染 zones（类型/坐标/半径），复用 zone 契约零新前端协议，服务端只加 type=door 判别；③足迹按图隔离是空间语义正确性根基——「在某图搜过」必须随所在图暂存/恢复，否则跨图切回后绿点与搜证状态串图（K5/K8 验收语义）；④显式布门而非生成即随机 door——多图连通由编排/DM 按需控制（生成的地图默认无 door，K7 验证布门→自动生成目标图全链路）
- **放弃**：①切图做成前端仅展示切换（足迹/当前图/尺寸必须后端权威迁移，前端只消费，对齐「后端 Java 权威模拟 + 前端纯渲染」结构性前提）；②切图绑定服务端移动碰撞实时检测（服务端不持有玩家权威位置——坐标由客户端上报、靠近校验尽力而为，缺坐标跳过，与 2D 模拟权威分离）；③door 做成独立实体表（复用 zone 契约内嵌，零表结构变更，对齐 D-013/D-014 不扩表纪律）；④per-player 独立当前图视角（本轮为全员同步切换——任一玩家触发全员同步，多玩家同时分处不同图留 P1）；⑤switchMap 阶段守卫放参数校验之后（校验链=阶段→玩家→door 解析，阶段守卫先行使投票后禁切语义不可绕过，K4 双缺用例随之调整）
- **影响**：①全量 mvn test **403/0 BUILD SUCCESS**（58 类；390 基线零破坏 + ScriptMapSwitchTest 7（K1-K7）+ ScriptMapSwitchPersistenceTest 1（K8）+ 并行未登记批次 ScriptChatModeTest 5 共存；LONG-01 首轮堆测量 32.5%>30% 为既有脆性（#37/#40/#41 同款），单跑 PASS 后全量复跑全绿）；②修复 4 失败——**generateMap 缺足迹迁移**（实现 bug：切图后 searchedLocations 残留旧图足迹 → 切回时把污染足迹存进旧图，K5 与 K8 连锁根因；补与 switchMap 同规则迁移后同图 regenerate 保持足迹、首个地图无迁移）、**K7 缺 session_id 用例与 currentSessionId 兜底设计不符**（测试 bug：P-0803-H2 先例 body 缺省回退当前对局，改显式空串触发守卫分支）、**K4 h)双缺用例置于投票后不可达**（测试 bug：阶段守卫先于参数校验，移至搜证阶段）、**K8 恢复后 Bob 误用 Alice roleKey**（测试 bug：身份校验失败 switched=null，改用 Bob 本人 key）；③后端 3 文件改动（ScriptGameService/ScriptController/ScriptMapService buildPrompt），禁动文件零改动；④已知限制——切图为全员同步（非 per-player 独立视角，P1）；靠近校验依赖客户端上报坐标（尽力而为）；script_status SSE 沿用全量通道（D-013 既有限制，前端按 session_id 区分）；⑤未 git commit（统一 gate 未获授权）

## 2026-08-03（剧本选择净化与剧本杀双版本批次 P-0803-K 剧本杀双版本）
### D-033 剧本杀双版本（B 方案）：简单对话版 mode=chat（无取证无地图，直达讨论）+ 剧本选择页净化（P-0803-K 剧本杀双版本）
- **决策**：①**剧本杀模式 = 双版本（主人选 B）**——版本一**真剧本杀**（mode=full 默认：剧本生成 + 地图搜证 + 讨论 + 投票 + 揭晓，现有 ScriptGameService/scriptMap 流程零改动）；版本二**简单对话版**（mode=chat：无取证、无地图，只有多人对话——init 直达 DISCUSSION 并自动启动讨论引擎，结束自动进 VOTE）。实现：ScriptGame +mode 字段（volatile，快照落库/恢复），initGame 四参重载（mode 显式/缺省 full 零破坏），chat 模式跳过 generateMap 自动串联（省一次 LLM 地图调用，init 更快）、phase 直达 DISCUSSION、自动启动讨论引擎（复用 GAP-3/D-012 链路）；搜证/转交被阶段守卫天然拦截；toMap 暴露 mode 键（前端据此隐藏搜证区/2D 讨论区/地图）。设计出处：蓝图《剧本杀差距分析-待办.md》v3 Step3v「降级路径（轮次发言：按 assignments 轮流每人 1 条消息，轮数可配置，结束自动进 VOTE）」+ raw 报告 N-1「聊天页对话通道」——复用剧本生成/角色/秘密/讨论引擎全链路，仅跳过搜证与地图。②**剧本选择页净化（主人需求 1-2）**——剧本选择页不再加载角色卡（原共享角色库 section 收敛为仅规则模式显示），页面只分两类页签「一般模式 / 剧本杀模式」；一般模式页签 = 一般+狼人杀剧本卡（带 chip）；选中剧本 → **独立设置页（非弹窗内嵌）**：角色选择（跟剧本 default_roles 走，charTab 默认切到该剧本；用户角色卡置顶不默认勾选；增删/编辑/新建保留）+ 2D 设置（一般「是否 2D」/ 狼人杀「默认 2D」）+ 启动按钮 + 地图预览。③**启动分流**——一般模式 start/openPhaserSim（2D 自选）/ 真剧本杀 genScript('full') / 简单对话版 genScript('chat')；client.ts scriptInit 加可选 mode 参数（缺省 full 零破坏）。④**ChatPage 适配**——ScriptStatePanel + 2D 面板按 scriptState.mode==='chat' 隐藏搜证区/2D 空间讨论区/地图（phase 守卫 + mode 守卫双重），phase banner 文案区分
- **原因**：①主人需求「剧本杀模式 = 双版本，B 方案：正常的不需要取证、只有多人对话的简单版本」——简单对话版是完整剧本杀的子集而非新玩法：复用同一剧本生成/角色/秘密体系，仅裁剪搜证与地图阶段，工程量最小且与真剧本杀共享全部后端资产；②净化——剧本选择页角色卡栏与设置页角色选择功能重复且页面臃肿，角色选择应发生在「选中剧本后」的上下文（跟剧本走），而非前置在列表页
- **放弃**：①简单对话版做成独立游戏模式/独立 Controller（复用 initGame 一个端点 + mode 参数即达，零新端点）；②chat 模式保留地图但默认不渲染（无取证无地图是需求本意，地图区 full 专属展示）；③设置页做成弹窗内嵌（主人明确「不是弹窗内嵌」，独立视图切换 + 返回按钮）
- **影响**：①后端 2 文件（ScriptGameService/initGame 四参重载 + mode + toMap.mode + 快照 mode；ScriptController/init 读 mode）+ 测试 ScriptChatModeTest 5 用例（C1-C5：chat init 直达 DISCUSSION/搜证拦截/讨论自动收束进 VOTE/full 零变化/快照恢复 mode）；②前端 4 文件（client.ts scriptInit +mode / ScenePage 净化+设置页+双版本+启动分流 / ChatPage chat 模式隐藏 / global.css 版本卡样式）+ static 同步（index-NSMdVek6.js，SHA256 dist↔static 一致）；③全量 mvn test **403/0 BUILD SUCCESS**（58 类，378 基线零破坏 + 并行批次用例共存）；④P-0803-H 全部能力保留（角色卡增删/置顶/不默认勾选、剧本绑定默认角色/地图、狼人杀默认 2D、rules 模式角色库零改动）；⑤已知限制——rules 模式（狼人杀入口）内剧本杀 tab 保留旧式 genScript（默认 full，与新剧本杀模式页签并存）；⚠️ 批标 P-0803-K 与多地图批次 1c283f43 撞标（两者均用 K）；⑥未 git commit（统一 gate 未获授权）

## 2026-08-03（简单对话版可选配置地图批次 P-0803-M）

### D-034 简单对话版（chat）可选配置地图：开关默认关 + 前端方案零后端改动 + 只读氛围展示（主人需求「简单对话可以选择配置地图」）
- **决策**：①**chat 版地图做成可选配置**——剧本杀设置页 chat 版新增「🗺️ 配置地图」checkbox（默认**关**=保持 D-033 chat「无取证无地图」基线零回归；开启后设置页显示地图生成/预览区，启动时自动生成，游戏中可查看）；②**地图来源复用既有链路**——POST /api/script/map（LLM 统一路径 → 契约 v1 校验 → BSP 降级兜底 + 缓存命中），chat 模式无默认地图、不引入新来源、**后端零改动**（核实 ScriptController.map 与 generateMap 均无 phase/mode 守卫：仅 session_id 存在性 + 缓存检查，chat 对局 DISCUSSION 阶段可直接生成并注册进多图注册表，toMap 自动附加 map 键 → 前端轮询 scriptStatus 即得地图）；③**启动链路**——genScript('chat') 开启地图时 init 成功后自动调 api.scriptMap（失败不阻塞进入对局，可手动重试）；④**只读展示**——PhaserScriptMapView/ScriptMapScene 新增 readOnly 模式（禁点击/E 键搜证交互、提示文案区分「氛围展示」；WASD 漫游/滚轮缩放/小地图/全屏保留），chat 版地图只读无搜证；⑤**ChatPage 适配**——chat 有地图：ScriptStatePanel 增地图查看入口 + chat-main 面板放行渲染（readOnly）；chat 无地图维持纯对话现状；**2D 空间讨论区 chat 模式不启用**（保持 D-033 隐藏语义，地图走独立只读通道）
- **原因**：①主人需求「简单对话可以选择配置地图」——chat 版从「无地图」升级为「可选项」，保留纯对话路径（默认关）同时给想要氛围地图的玩家入口；②优先前端方案（任务书）：先核实后端端点可用性——map 端点无守卫 chat 模式可用 → 零后端改动最小风险（不触碰 3 个并行批次改过的 ScriptGameService/ScriptController）；③地图与搜证解耦——chat 版无取证语义不变，地图退化为纯氛围展示（只读），不破坏「简单对话」定位；④readOnly 走组件级 prop 而非新组件——改动面最小且 full 版缺省行为逐字节不变
- **放弃**：①chat 版地图默认开（破坏 D-033「简单对话版无地图」基线语义，且多一次 LLM 等待）；②后端 initGame 加 chatMap 参数自动串联（需改 ScriptGameService/initGame 契约 + 快照格式，前端方案已达成同样效果零后端风险）；③chat 版开启 2D 空间讨论区（超需求且与「简单对话」定位冲突，地图只读展示已满足氛围诉求）；④新写独立只读地图组件（现有组件加 prop 更小）
- **影响**：①全量 mvn test **403/0 BUILD SUCCESS**（58 类，后端零改动为验证性全量，LONG-01 PASS 2.520s）；②前端 4 文件（ScriptMapScene/PhaserScriptMapView +readOnly、ScenePage +chatMapEnabled 开关+启动自动生成+chat 地图区、ChatPage chat 地图查看入口+面板放行）+ static 同步（index-ChF7_wXr.js，SHA256 dist↔static 一致，删旧产物 index-NSMdVek6.js）；③bundle grep 命中（配置地图/氛围展示/只读浏览/readOnly 等）；④兼容性——full 版零改动（readOnly 缺省 false 行为不变）、chat 无地图纯对话现状不变、resume 后 chat 地图随快照恢复前端轮询可见；⑤已知限制——chat 地图生成与后台讨论引擎并发，saveSnapshot 对 discussionTranscript 的 ArrayList 拷贝与讨论线程 append 存在极小概率 CME（失败仅地图请求 500 不损坏对局，前端可重试；full 模式 switchMap 同款快照路径亦然，P2 可改 CopyOnWriteArrayList）；地图生成为启动串行等待（init+地图两次 LLM 调用，用户开启即知情）；⑥未 git commit（统一 gate 未获授权）

## 2026-08-03（两条地图链路 LLM 全量生成批次 P-0803-O）

### D-035 剧本卡默认地图双模式（BSP 确定性 / LLM 全量生成）+ 剧本杀对局地图 theme 参数前端暴露（主人需求「都加上 llm全量生成」）
- **决策**：①**链路 1（剧本卡默认地图 POST /api/scenes/map）双模式**——body 可选 theme：非空 → 走 ScriptMapService LLM 全量生成统一路径（LLM 完整输出 ground+collision 双层数组 + rooms/zones/spawns 全量元素 → MapContract.normalize → MapValidator 契约 v1 校验 → 校验失败/LLM 失败/超预算自动 BSP 降级兜底，防御性保留不删），响应附加 mode（llm / bsp-fallback）+ generator + validation{ok,errors,warnings} + fallback 溯源键；空/缺省 → BSP 确定性生成（P-0803-H 既有行为逐字节零回归，同 seed 同输出，零 LLM）；②**注入可行性核实**——ScriptMapService 为 @Service 仅依赖 LLMClient，依赖链 SceneController→ScriptMapService→LLMClient 无环（ScriptGameService 内部 new ScriptMapService(llmClient) 与 Spring bean 并存互不影响），5 参 @Autowired 构造安全；4 参旧构造委托 mapService=null（Spring 不选它，既有测试/调用点零破坏），theme 请求时防御回落 BSP 不崩；③**前端剧本编辑弹窗**——「生成方式」选择（✨ LLM 全量生成需主题 / BSP 默认无主题）+ LLM 主题输入（占位示例「民国宅邸凶案」），生成后绑定 formDefaultMap → default_map 逻辑不变；既有 BSP「🗺️ 生成默认地图」按钮保留为 BSP 模式入口；④**链路 2（剧本杀对局地图）**——后端 POST /api/script/map 的 LLM 全量生成路径（theme 驱动 → 契约 v1 校验 → BSP 兜底 + 缓存命中）本已就绪零改动，前端把 theme 暴露为可编辑输入：剧本杀设置页地图区（gameSetup 双版本 + rules script tab 两处）新增主题输入（mapTheme 状态，init/恢复对局后默认剧本名、可改），genScriptMap / genScript('chat') 自动氛围地图均消费 mapTheme（空回退剧本名）；seed/尺寸参数后端已支持（P-0803-J/K）但 UI 空间不允许未暴露（保持现状）
- **原因**：①主人需求「都加上 llm全量生成」——两条地图链路都要支持 LLM 全量生成：链路 1 从纯 BSP 升级为带主题的 LLM 全量生成（保留 BSP 确定性作为无主题/降级选项），链路 2 确认强化并前端暴露 LLM 参数；②复用 ScriptMapService 统一路径而非另写生成器——契约 v1 校验 + BSP 兜底 + 缓存逻辑单点复用（对齐 D-014 双生成器统一纪律），SceneController 注入的是同一个 @Service bean 的独立消费者，ScriptGameService 自建实例不受影响；③「无 theme 恒 BSP」保证零回归——P-0803-H 既有前端调用（api.sceneMap() 无参）与 SceneBindingTest 5 用例逐字节不变；④前端暴露 theme 而不暴露 seed/尺寸——UI 空间有限，theme 是 LLM 生成语义的核心参数（任务要求「至少 theme」），seed/尺寸保持后端支持前端不暴露（P-0803-J/K 已留调用方按需传 body 参数）
- **放弃**：①链路 1 直接调 LLMClient 不经过 ScriptMapService（绕过契约 v1 校验与 BSP 兜底防线，与 D-020「校验器防线」冲突）；②给 ScriptGameService 加「默认地图 LLM 生成」代理方法让 SceneController 依赖 ScriptGameService（引入无必要的 service 间耦合，ScriptMapService 直注更小）；③seed/尺寸在剧本杀设置页 UI 一并暴露（UI 空间不允许，且尺寸大图预算闸/seed 仅为降级控制，普通用户无消费场景）；④删除/改造 BSP 兜底路径（防御性保留是任务明确要求「校验失败/超预算仍走 BSP 降级兜底（防御性保留，不删）」）
- **影响**：①全量 mvn test **407/0 BUILD SUCCESS**（58 类；403 基线零破坏 + SceneMapLlmModeTest 4 用例：O1 LLM 成功路径（mode=llm/kind=llm/契约 v1 全量元素/校验通过/无兜底）、O2 无 theme BSP 零回归（null/空串/空白 + 同 seed 同输出）、O3 LLM 空输出 bsp-fallback（fallback 含「输出为空」+ 兜底地图自洽）、O4 4 参构造防御回落；单跑 SceneMapLlmModeTest 4/0 + SceneBindingTest 5/0）；②前端 npm run build 通过 + static 同步（index-StVXWN_F.js，SHA256 dist?static 一致，删旧产物 index-Cx-YssBi.js，CSS 未变）+ bundle grep 命中（生成方式：/LLM 全量生成/BSP 默认/民国宅邸凶案/默认剧本名可改 等）；③兼容性——BSP 模式零回归（SceneBindingTest 全绿）、链路 2 后端零改动（仅前端暴露 theme）、ChatPage 零改动、seed 双模式透传；④已知限制——LLM 模式为真实 LLM 调用（成本/延迟与对局地图同档：预算内默认尺寸 24×16，LLM 输出失败自动 BSP 兜底）；剧本杀设置页 seed/尺寸 UI 未暴露（后端已支持）；⑤未 git commit（统一 gate 未获授权）

## 2026-08-04（P-0804-A 授权批次：GroupAnchor 完整实现 + 零冲突项 + 编码损坏事故修复）

### D-036 GroupAnchor 完整实现（leader+follow slot，D2 关闭）+ BSP seed 配置化 + LONG-04 测试 + 前端断线补发/联机房/地图参数/SILENCE_MARKER（主人授权批次）
- **决策**：①**GroupAnchor 完整实现（D2/D-13 关闭）**——需求文档「群体移动系统」：MERGED 组内 leader=字典序最小名（确定性锚点，跨 tick 稳定），leader 收敛到组质心保持凝聚，其余成员按名称序占 follow slot（k=1,2,…）排列在 leader 后方直线队形（SLOT_SPACING=16，方向=formationAngle(leader)=hash 角度确定性），leader 移动后槽位随新位置重算（跟随语义）；替代原「质心±散点」（GroupAnchor 雏形）；WEAK/ISOLATED 零改动。②**BSP seed 配置化（Phaser P2① 关闭）**——ScriptMapService 新增 `@Value("${roleplay.game.map.bsp-seed:0}")` 注入 + application.yml/application-test.yml 双份 bsp-seed=20260801 键；缺省 0 回退 DEFAULT_BSP_SEED 零行为变化（对齐 D-004 勿 hardcode 纪律）。③**LONG-04 测试类（D-基建1 收尾）**——新增 `stability/LongSimulationConcurrentTest` 3 用例：2D 世界定时 tick + GroupAnchor 约束并发运行坐标有界；8 线程并发 compute/apply 1000 迭代无异常/死锁/越界；并发迭代计数核验。④**前端零冲突项**——a) 断线补发（P3 关闭）：useSSE 重连成功（onopen 且曾重连）后调 api.announcementRecent(lastTs) 补拉断线期间公告/演讲逐条重放，lastTs 游标随事件推进；b) 联机房剧本杀接线（缺口关闭）：scriptInit +roomCode 可选传 room_code（后端 C3 已支持），ScenePage genScript 透传；c) 地图尺寸/seed UI（D-031/D-035 关闭）：剧本杀设置页地图区暴露 宽/高/seed 输入，genScriptMap 透传 scriptMap body；d) SILENCE_MARKER 渲染（D-022 关闭）：新增 utils/silenceMarker.tsx（SILENCE_MARKER 常量 + isSilenceText + SilenceTurn 组件），狼人杀讨论面板与 2D 侧聊天列表静默占位灰色斜体渲染；e) 删除 ChatPage.tsx.bak 残留。⑤**⚠️ 事故修复（前端两文件编码损坏重建）**——本批误用 PowerShell `Set-Content -NoNewline` 处理 ChatPage.tsx / PhaserSimulationView.tsx，触发 UTF-8→GBK 双编码有损转换（非法 GBK 对→单个 `?`，丢失中文字符尾字节 + 后续 ASCII 引号/`$`/`<`/换行），中文内容损坏且不可自动逆转；修复方案：从 static bundle（index-StVXWN_F.js，含全部原始 UI 串的 \u 转义）逆向 unescape + 逐处上下文匹配恢复全部中文字符串，再逐行修复结构（缺失引号/`}`/`</`/`$`/换行 40+ 处 + 注释吞代码 60+ 处）→ tsc 全绿
- **原因**：①主人授权「完整实现 GroupAnchor」——原质心散点为设计降级（D2），需按需求文档实现 leader+follow slot 队形；leader 用字典序最小名保证跨 tick 确定性、可单测；直线队形最贴近「RPG 队伍跟随」语义。②bsp-seed 是终审遗留 P2①，@Value 注入 + yml 键落地即关闭，默认值不变零回归。③D-基建1 原记录 LONG-01/03/04 缺测试类，实际 LONG-01/02/03 已有，仅 LONG-04（并发模拟稳定性）缺失，补上即收尾。④前端五项零冲突（非禁动/纯前端/纯新增），按主人「全部实施」授权推进；SILENCE_MARKER 渲染与 @AI 输入提示是 D-022 已登记未做项，@AI 提示早已就位，补齐占位渲染。⑤事故为本次操作失误（PS 编码），需透明记录并完成重建，教训写入本条目
- **放弃**：①GroupAnchor 用「leader 锚定 + 成员围绕 leader 圆周」的简化（仍属散点，不满足「队形」语义）；②bsp-seed 改到 AppConfig 统一配置类（ScriptMapService 单点 @Value 已够，对齐 ScriptGameService @Value 先例）；③断线补发用 SSE replay buffer（REST recentSince 已就绪，拉取重放最简）；④SILENCE_MARKER 在 2D 世界气泡内特殊样式（气泡在 Phaser 场景内渲染，改动面大收益低，侧聊天列表已覆盖可见处）
- **影响**：①全量 mvn test **412/0 BUILD SUCCESS**（58 类；407 基线零破坏 + MovementConstraintTest GroupAnchor 新增 2 用例 + LongSimulationConcurrentTest 3 用例；首轮 LONG-01 堆 32.3%>30% 为既有脆性 #37/#40/#41 同款，单跑 PASS 后复跑全绿）；②前端 npm run build 通过（66 modules）+ static 同步（index-Bdi8t8bk.js + index-CpI7G5E6.css，SHA256 dist↔static 一致，删除旧产物 index-StVXWN_F.js/index-BC-6X7pW.css）；③⚠️ 事故影响——ChatPage.tsx / PhaserSimulationView.tsx 曾因编码损坏 tsc 全红，已重建恢复（中文字符串从 bundle 逐处还原 + 结构修复），重建后 **tsc -b exit 0**；重建文件与 bundle 原文对齐（抽查 PHASE_EMOJI/ROLE_EMOJI/getPhaseGuide/getLoadingText 等区段中文与 bundle 一致）；④禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路）零改动；⑤未 git commit（统一 gate 待授权）


## 2026-08-04（剧本杀 P1 可玩性修复清单批次 P-0804-B）

### D-037 剧本杀 P1 可玩性修复三项：投票超时+quorum / 玩家退出托管+ENDED 重开 / LLM 降级提示（配置可开关，向后兼容）
- **决策**：①**投票超时（惰性判定 + 托管联动）**——VOTE 阶段进入时记录 `voteStartedAt`（enterVotePhase 统一入口：startVoting/讨论引擎结束/启动失败降级路径同一计时基准），`resolveVote` 被调用时检查 `System.currentTimeMillis() - voteStartedAt >= roleplay.game.script.vote-timeout-ms`（默认 60000，开关 `vote-timeout-enabled` 默认 true）：超时后未投票玩家按弃票处理（响应 abstained 名单 + vote_timeout + 提示文案）并转为托管（任务 2a「超时无操作→托管」在此落地）；**不做后台定时器**——避免并发/测试 flaky，且 D7 审批门阻塞语义不受影响。②**quorum 门槛（独立开关 `quorum-enabled` 默认 true）**——有效票 < ceil(在线玩家数/2)（在线=本局玩家−托管玩家）不判定：首次不足清票重投一轮（quorumFailCount=1），重投仍不足按已投计并标记 `lowParticipation=true`（低参与度判定，随快照/对局结果落库）；**平票（D6 清票重投）前置子 quorum 判定零破坏**；超时/平票/重投均重置投票计时。③**玩家退出/重开**——新增 POST /api/script/leave（服务层 leaveGame）：角色标记托管（trustees 集合，toMap 暴露，前端 🤖 标记），已投票作废、castVote 拒绝、不计入 quorum 在线数、随快照落库、ENDED 拒绝；新增 POST /api/script/restart（服务层 restartGame）：仅 ENDED 可重开，同剧本主题同玩家（theme 字段留档随快照），**复用 sessionId**（playerSessions/roomGames/currentSessionId 不变，前端轮询/SSE 定位自动进入新局），新局全新剧本/roleKey/票型、托管与降级标记重置；前端 ChatPage 结束面板「🔄 再来一局（同剧本）」「📋 回到剧本选择」。④**LLM 降级提示**——ScriptService 新增 `generateScriptChecked`（与 generateScript 同一调用路径，额外返回 degraded 标记），initGame 改走 checked 变体注入 `gameState.llmDegraded=true`（toMap 暴露 llm_degraded 键），前端 ScriptStatePanel 显示「⚠️ 当前为离线模板模式，内容为占位剧本」提示条；既有 generateScript 调用方/测试零变化。
- **原因**：①评估报告 §五 P1-1~P1-3——「投票无超时/无 quorum（1 票即可定局、无人投票卡 VOTE）」「无玩家中途退出/重开对局」「无 LLM key 时内容降级无提示」三个影响可玩性的机制缺口；②投票超时与 quorum 均做成配置开关（默认开启）——关闭即回到旧行为（可无限等/少数票即定局），对齐 D-004「阈值勿 hardcode」纪律与项目「向后兼容优先」传统；③惰性判定替代定时器——resolveVote 是唯一判定入口（玩家/DM advance 均经此），无后台线程即无并发竞态与测试 flaky，且「超时后未投票玩家按弃票处理并提示」在 resolve 响应中即可满足（提示随揭晓/重投文案展示）；④托管语义=「AI 代管但标记清楚」——剧本杀讨论引擎本就全员 AI 代声（人类发言权豁免），托管的实际差异在投票权（作废）与 quorum 在线数（排除），前端 🤖 标记 + 提示条让状态可感知；⑤restart 复用 sessionId 而非新 id——前端零改造成本（轮询/SSE 自动进入新局），旧局快照被新局覆盖（name 前缀相同、getLatestScriptSnapshot 取最新，语义正确）。
- **放弃**：①投票超时做后台定时自动 resolve（并发/测试 flaky 风险，且与 D7 审批门阻塞语义叠加复杂化）；②「无人投票且全员托管」的自动收束（极端角落：与现状同样依赖 DM 人工，未使情况变差，消息中已注明等待 DM 处理）；③restart 生成新 sessionId（需前端重写定位链路，复用 sessionId 零成本）；④给 ScriptGame 加独立「退出时间戳」做挂机检测（投票超时已覆盖局内无操作的唯一硬性等待点，其他阶段挂机不影响流程推进）。
- **影响**：①全量 mvn test **426/0 BUILD SUCCESS**（62 类 surefire 汇总：412 基线含 P-0804-A GroupAnchor 批次零破坏 + 本批 ScriptGameVoteTimeoutQuorumTest 7 + ScriptGameLeaveRestartTest 7；Script 专项 117/0；LONG-01 PASS）；②新增 3 配置键（roleplay.game.script.vote-timeout-ms=60000 / vote-timeout-enabled=true / quorum-enabled=true，yml 双份）+ 2 新端点（POST /api/script/leave、POST /api/script/restart）+ 服务层测试 setter（setVoteTimeoutMs/setVoteTimeoutEnabled/setQuorumEnabled，对齐 setSerialRound 先例）；③toMap 新增 llm_degraded/trustees 键（附加键不破坏既有契约）；快照新增 theme/llm_degraded/vote_started_at/quorum_fail_count/low_participation/trustees 键（旧快照无此键 → 恢复默认值零行为变化）；④已知限制——投票超时为惰性判定（需有人调用 resolveVote 才触发弃票处理，无人 resolve 时仍等待，DM 面板/玩家「揭晓真相」按钮即触发入口）；托管玩家恢复（resume）后角色保持托管状态（不自动解除）；「再来一局」为同主题新剧本（LLM 重新生成，非复用旧剧本 JSON）；⑤前端 npm run build 通过（66 modules，index-4xjAceRg.js 1,868.01 kB）+ static 同步（SHA256 dist↔static 一致，index.html 更新，删旧产物 index-Bdi8t8bk.js）+ bundle grep 8 项命中；⑥未 git commit（统一 gate 未获授权）。


## 2026-08-04（角色与场景素材库批次 P-0804-C）

### D-038 素材库独立新表（assets）+ 登记式导入 + Phaser 双消费（角色 Aseprite 动画 / 场景瓦片图集）
- **决策**：①**独立新表 AssetEntity**（assets 表：id / name 唯一 / assetType（CHARACTER_ANIMATION / SCENE_TILESET 字符串枚举）/ characterName 可空 / sceneId 可空 / filePath / metaJson @Lob / createdAt / updatedAt）——对齐 SceneEntity 先例「不扩既有表结构、新表独立」；素材关联复用现有 characters.name / scenes.id（**不设外键约束**防级联麻烦，不新建角色/场景概念），校验由 Controller 层 CharacterRepository/SceneRepository 存在性查询承担；②**导入=登记元数据+文件路径引用**（不做 multipart 上传）——素材文件由调用方/前端预置 `src/main/resources/static/assets/` 下，filePath 为相对路径；Aseprite JSON 全文经 meta_json 字段登记（前端消费时经 Blob URL 提供，无需二次落盘）；取舍说明：multipart 需处理二进制落盘与路径穿越/大小限制，登记式对「素材文件实体由人工/脚本预置」的场景最简可控；③**关联校验=严格拒绝**（二选一）：character_name / scene_id 提供时必须存在 → 400，两者皆空=未关联素材允许；④**Phaser 双消费（核心价值点）**——角色动画：SimulationScene 新增可选 agentAnims（角色名→{pngUrl,jsonUrl}），preload `load.aseprite` + create `anims.createFromAseprite`（Phaser 3.90 原生 Aseprite 支持，阶段 0 demo 已验证）→ 有素材的角色用动画 sprite 渲染、无素材回退圆点渲染；场景瓦片：ScriptMapScene 新增可选 tilesetUrl，preload 加载外部图集 → create 用 addTilesetImage 外部图集切片、无素材回退运行时 5 色块纹理；两条消费链路都保证**无素材时行为逐字节不变**（前端先行拉取素材列表，空表/失败→不传参→原路径零改动）；⑤**示例素材内置**——static/assets/CHARACTER_ANIMATION/demo_player/player.png+player.json（Aseprite 走行动画 16 帧 4 frameTags，复用阶段 0 验证资产）+ SCENE_TILESET/demo_tiles/tiles.png（160×32=5 格 32px，对齐契约 tile_count=5）
- **原因**：①主人需求「对角色与场景建立素材库，支持导入角色像素动画（Aseprite PNG+JSON）与场景瓦片图集（tileset）」——素材是既有角色/场景的渲染增强层，不是新实体，故只在 characters/scenes 之上加素材登记层；②任务指引「对齐 SceneEntity 先例：不扩既有表结构、新表独立」+「优先简单方案：导入=登记元数据+文件路径」；③Phaser 3.90 的 Aseprite 加载与 tileset 渲染是阶段 0 已验证能力，消费端最小接入（素材优先/无素材回退）满足「零破坏」硬性要求；④示例素材复用 phaser_validate 既有资产（player.png+json 已验证可播、tiles.png 恰为 5 格契约）零新造资产
- **放弃**：①multipart 文件上传（二进制落盘+安全边界复杂，登记式已满足「导入」语义，上传留 P2）；②素材表加外键约束（防级联删除麻烦，关联存在性由 Controller 校验，对齐「轻量绑定」传统）；③角色动画直接改 SimulationScene 圆点渲染主路径（保持可选参数注入，无素材零破坏）；④按场景精确选择瓦片图集（PhaserScriptMapView 取首个 SCENE_TILESET，按 scene_id 绑定消费留 P2——数据模型已支持 sceneId 字段）
- **影响**：①全量 mvn **434/0 BUILD SUCCESS**（63 类 surefire 汇总；426 基线零破坏 + AssetLibraryTest 8 用例：导入登记+列表透传/类型过滤/角色·场景过滤/未知关联 400 拒绝/未关联允许/删除+404/枚举与缺 name 400/关联名解析；LONG-01 PASS——前 2 轮 LONG-01 堆 47.3%/30.5%>30% 为既有脆性 #37/#40/#41 同款，单跑 PASS 后复跑全绿）；②后端新增 3 文件 + DatabaseService 7 参 @Autowired 构造（旧 6 参委托 null，WerewolfRoleKeyTest/WerewolfStage1Test 直构零变化）；③前端 client.ts 4 API + ScenePage 🧰 素材库面板（AssetLibraryPanel）+ MaterialPage assets Tab（死代码未入包）+ 两个 Phaser 场景消费接入；npm run build 通过（tsc 0 错误 + vite 67 modules，index--3UfVeic.js 1,891,327B）+ static 同步 SHA256 dist↔static 一致 + 删旧产物 index-4xjAceRg.js/index-B4VS6nUI.js；④示例素材已内置可直接登记演示；⑤已知限制——角色动画按角色名精确命中（2D 世界 agentName 与素材 character_name 相同才应用）；瓦片图集取首个 SCENE_TILESET 全局应用（按场景关联消费留 P2）；外部瓦片图集需覆盖契约 5 格（格数不足的索引渲染为空块）；Aseprite JSON 建议贴入 meta_json（同名 .json 文件约定为回退路径）；⑥未 git commit（统一 gate 未获授权）


## 2026-08-04（删除前端素材库管理页批次 P-0804-E）

### D-039 删除前端素材库管理页（主人需求：素材库不用放前端，2D 游戏内能调用即可）
- **决策**：删除前端素材库**管理**入口，保留 2D 游戏内**消费**链与后端资产服务。①删除 `components/AssetLibraryPanel.tsx` 整文件（D-038 建的 🧰 素材库面板，ScenePage 已集成的唯一管理入口）与 ScenePage 顶部「素材库」Tab 按钮/面板渲染/import（含仅被该按钮使用的 goToView 解构）；②client.ts 只删管理 API（assetImport/assetGet/assetDelete，均无其他消费者）；③**必须保留** assetList/assetFileUrl/asepriteJsonUrl——PhaserScriptMapView（SCENE_TILESET 瓦片图集）与 PhaserSimulationView（CHARACTER_ANIMATION 角色动画）依赖其加载 2D 游戏内素材，删除即断游戏内渲染链；④types 无 asset 类型定义（面板全 any[]）无需清理；MaterialPage 无残留（P-0804-C 整改已回退）不动。
- **原因**：①主人明确「素材库不用放前端，2D 游戏内能调用即可」——管理 UI 是面向人工登记的入口，但素材的实际价值在 Phaser 渲染消费（角色 Aseprite 动画 / 场景瓦片图集），该链路必须完整保留；②素材文件的预置/登记仍可通过后端 API（POST /api/assets/import 等）或直接落 static/assets/ 完成，删除前端面板不损失能力，只移除用户可见的管理页。
- **放弃**：①保留 AssetLibraryPanel 但隐藏入口（死代码风险，D-038 已实证 MaterialPage 死代码教训）；②一并删除后端 AssetController/AssetEntity/AssetRepository（**保留**——2D 游戏内 assetList 调用链与外部登记均依赖后端，前端删除后后端仍是唯一登记通道）；③删除 assetList/assetFileUrl（PhaserScriptMapView 瓦片加载链会断，违反「2D 游戏内能调用」）。
- **影响**：①前端删除 1 文件 + 3 处改动，npm run build 通过（tsc 0 错误，vite 66 modules，67→66）；bundle grep 证实管理页代码全移除（素材库 0/assetImport 0/assetDelete 0/assetGet 0/AssetLibraryPanel 0）、2D 消费链保留（assetList 3/map-tileset-asset 3/SCENE_TILESET 1/CHARACTER_ANIMATION 1）；②全量 mvn **434/0 BUILD SUCCESS**（63 类，426 基线零破坏 + AssetLibraryTest 8，后端零改动）；③static 同步新产物（index-rtqdhfbk.js，SHA256 dist↔static 一致 C6FEABC9...），mvn clean package 重新打包 + 8000 重启（PID 31324）；④真机验证（Edge headless+CDP）：素材库 Tab 消失（只剩 一般模式/剧本杀模式 chips）+ 默认场景卡地图预览真实瓦片纹理渲染（像素 285 色/主色纯黑 92%≈图集 87-96%，console 0 tile 警告 0 异常）+ 素材文件 GET 200；⑤后端素材数据与文件（static/assets/）不受影响，AssetController 四端点继续可用；⑥未 git commit（统一 gate 待授权）。

## 2026-08-05（主人需求「全部做完」批次一 P-0805-A）

### D-040 架构影响项先 demo 验证 + B1/B2/B4/B5 + 记忆检索 + 生图后端接入
- **决策**：主人需求「对现有架构有影响的先做 demo，全部做完」。①**先 demo 验证架构影响项**——生图管线（`static/simulation/imagegen/`：image_spec 契约 v1 → provider 三层降级（openai_image / sd_webui / offline SVG 占位）→ 渲染 → 接入映射，15/15 自测）与剧本杀双通道合并（`static/simulation/discussion_demo/`：人类发言并入 discussion_say 单一讨论组 + 点名强回应 + 秘密守卫 + SpeechGate 静默，6/6 自测），零后端零禁动，验证行为契约后整合；②**B1 双通道合并**——前端封装 `scriptDiscussionSay`，ChatPage composer 在 script 模式 phase=DISCUSSION 时路由 `POST /api/script/discussion_say`（后端端点 D-012 既有，人类发言权豁免不过门控），失败降级 /api/send；消除「人类走 /api/send、AI 走讨论引擎」两条互不互通线程；③**B2 killer_id 结构化判定**——ScriptGameService 新增 roleNamesById（schema id→角色名，快照持久化），resolveKillerByRoleId 三级容错（id 直查→killerId 即角色名→assignments 反查）优先于 D6 文本解析，killerId 空回退文本解析零破坏；修复「LLM truth 用别名/不含凶手名→恒判冤枉好人」；④**B4 重投上限**——平票清票与审批驳回回滚共用 revoteCount（快照持久化），达 `roleplay.game.script.max-revotes`（默认 3）按「无人被定罪」终止（revote=false+tie_limit），防无限循环；⑤**B5 对局 TTL**——games 内存表 lastAccess + 惰性清扫（每 256 次访问触发 + sweepExpired 测试钩子），`roleplay.game.script.game-ttl-ms` 默认 12h 0=禁用，防长期运行内存泄漏（快照持久化不受影响，清理后 resume 可重建）；⑥**记忆检索接入讨论**——buildDiscussionPersona 注入该玩家已搜证持有的线索「你当前掌握的证据」（信息收集≠推理闭环，PLAYER* 论文结论：检索记忆让 AI 记得证据才能推理引用，对齐 MemoryRetrieval additive 注入思想）；⑦**生图后端接入**——新增 ImageSpecService（剧本 schema v1→image_spec 契约 v1 四类生图单元 character/scene/clue/tile_style + 全局风格锚点 styleForTheme）+ ImageController（POST /api/image/spec），纯文本合成不阻塞主循环；图片本身由外部 provider 消费（扩散模型/程序化占位），本服务只产出结构化描述（文本模型擅长的部分）
- **原因**：①主人明确要求「全部做完」且「架构影响先 demo」——生图是全新能力（架构影响），双通道合并动聊天主链路（架构影响），均先以独立静态 demo 验证行为契约，避免直接整合引入回归；②B1 是体验提升最大项（讨论对不上是当前最明显的剧本杀短板，调研 B1）；③B2 消除文本解析不稳（D-014 明确 killer_id 为元数据未接入判定，属既有待办）；④B4 是平票/审批驳回死循环的工程兜底；⑤B5 是长期运行内存泄漏防护（playerSessions/games 无 TTL，调研 B5）；⑥记忆检索解决「搜证了但不记得」——一般模式的 MemoryRetrieval 已实现未接线，剧本杀侧用最贴合语义的形态（持有线索即检索记忆）接入
- **放弃**：①私聊前端本轮不做（后端 PrivateChatService 纯内存无 AI 应答/无历史查询端点，做前端需先补后端能力，属架构影响项，另起批次）；②生图 provider 接入本轮不做（需用户配置真实文生图 API key/provider，demo 已演示三层降级与接入口，整合 provider 适配留后续）；③WebSearch 搜证 / 时间推进（纯增量，留后续批次）；④记忆检索做成通用 MemoryRetrieval 全量接线（剧本杀侧以「持有线索注入 persona」最小闭环达成语义目标，通用检索接入 RouterService 是另一项架构改动）
- **影响**：①全量 mvn **447/0 BUILD SUCCESS**（65 类 surefire 汇总；434 基线零破坏 + ScriptGameKillerIdRevoteTest 6 + ScriptGameDiscussionTest +1 + ImageSpecServiceTest 5；LONG-01 PASS）；②新增 2 配置键（roleplay.game.script.max-revotes/game-ttl-ms，yml 双份）+ 2 新文件（ImageSpecService/ImageController）+ 1 新端点（POST /api/image/spec）；③前端 3 文件（client/ChatPage/appStore）+ npm run build 通过（tsc 0 错误 + vite 71 modules，index-BKIx7IAX.js）+ static 同步 SHA256 dist↔static 一致 + 删 5 旧产物；④新增 2 个独立 demo（imagegen / discussion_demo）；⑤兼容性——B2/B4/B5 全部向后兼容（旧剧本无 killerId→文本解析；旧快照无 revote_count/role_names_by_id→默认值；TTL 默认 12h 不影响正常对局）；双通道合并只在 DISCUSSION 阶段生效，其余阶段 composer 行为不变；⑥禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；未 git commit（统一 gate 待授权）

## 2026-08-05（主人需求「全部做完」批次二 P-0805-B）

### D-041 私聊完整闭环 + WebSearch 搜证 + 时间推进
- **决策**：①**私聊完整闭环（剧本杀核心玩法）**——此前后端只有空壳 PrivateChatService（纯内存 requestChat/reply/sendMessage，无 AI 应答、无历史查询端点、前端零 UI），剧本杀"秘密结盟/套话/传递情报"无法实现；本批在 ScriptGameService 落地完整私聊：ScriptGame +privateChats 字段（会话键 "A|B" 字典序，双向可查）+ 快照持久化/恢复；+privateSay（玩家发消息→目标 AI 角色以本人 persona+秘密+已持线索经 LLM 生成应答，guardPrivateSecret 拦截「我是凶手/我杀了/凶手是我/我下的毒/我捅的」认罪自曝句改写为遮掩——修订机器人思想，LLM 失败降级占位不中断）；ScriptController +POST /api/script/private +GET /api/script/private/history（player_key 鉴权复用 checkPlayerAccess）；前端 client.ts 封装 + ChatPage 💬 私聊按钮与抽屉（目标 chips/历史加载/气泡渲染）；②**WebSearch 搜证**——`roleplay.game.script.web-search-enabled`（默认 false，避免每局都打 DuckDuckGo）开启时 search 对首条线索自动联网检索背景（复用既有 WebSearchService 懒创建），附加 web_results，失败静默降级不阻塞搜证（对齐搜证零破坏纪律）；③**时间推进**——ScriptGame 对局时钟 startedAt（initGame 置位）+ 快照持久化 + toMap 下发 started_at/elapsed_ms（前端可显示对局已进行时长/阶段倒计时基准）
- **原因**：①主人需求「全部做完」剩余缺口中"私聊后端有前端无"是剧本杀最大未闭环玩法（PROJECT_CONTEXT 明示）；②PrivateChatService 空壳无法满足剧本杀私聊语义（AI 角色需知道本人秘密/角色身份/已持线索，且不能直接认罪）——故私聊实现放 ScriptGameService 而非扩 PrivateChatService（语义归属对局，快照随对局持久化）；③WebSearchService 一直未接线（调研 C5），搜证是天然增点子；④时间推进基于既有 round/voteStartedAt 机制补全局时钟
- **放弃**：①扩 PrivateChatService 做剧本杀私聊（它是通用纯内存服务，无对局/角色/秘密概念，改造面反而更大）；②私聊做 SSE 实时推送（走轮询+history 已满足，实时推送留 P2）；③WebSearch 做成必须开启（默认关控成本，开启为增强）；④对局时钟做倒计时强制推进（阶段自动推进有风险，时钟只做展示基准，倒计时留 P2）
- **影响**：①全量 mvn **452/0 BUILD SUCCESS**（65 类 surefire 汇总；447 基线零破坏 + ScriptGamePrivateChatTest 5 用例（私聊 AI 应答+历史/守卫拦截认罪/校验拒绝（不在局/和自己/空消息/游戏不存在）/对局时钟 toMap/WebSearch 默认关开启不阻塞）；LONG-01 PASS）；②新增 1 配置键（roleplay.game.script.web-search-enabled，yml 双份）+ 2 新端点（POST /api/script/private + GET /api/script/private/history）；③前端 2 文件（client/ChatPage）+ npm run build 通过（tsc 0 错误 + vite 74 modules，index-CqdAlTZ2.js 1,865.81 kB）+ static 同步 SHA256 dist↔static 一致（97D544C9...）+ 删旧产物 index-BKIx7IAX.js；④兼容性——web-search-enabled 默认关旧行为零变化；私聊为新端点不影响既有；对局时钟 toMap 附加键不破坏契约；旧快照无 private_chats/started_at → 恢复默认值；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；未 git commit（统一 gate 待授权）

## 2026-08-05（主人需求「剩下的全部做完」批次三 P-0805-C）

### D-042 私聊 SSE + 阶段倒计时强制推进 + 生图真实 provider 适配
- **决策**：①**私聊 SSE 实时推送**——ScriptGameService.privateSay 落快照后经 SSEController.broadcastScriptPrivate 会话定向推送（script_private 事件，broadcastToSession；前端按本人 player 过滤）；前端 useSSE 注册事件 + 私聊抽屉打开时 3s 轮询历史兜底（主动态 app=demo2 走轮询而非 SSE——SSE script_* 通道由 AppLegacy 消费，故双通道并存保证实时性）；②**阶段倒计时强制推进**——ScriptGame +phaseStartedAt/phaseTimeoutMs（快照持久化/恢复），`roleplay.game.script.phase-timeout-ms`（默认 0=禁用）开启时任一玩家轮询（touchGame→maybeAdvanceOnTimeout）惰性推进：INVESTIGATION→DISCUSSION（自动启讨论引擎）→VOTE（enterVotePhase 统一计时/quorum 重置）；**VOTE 不在此推进**（复用投票超时/弃票惰性判定，避免未经审批直接揭晓）；toMap 下发 phase_started_at/phase_elapsed_ms/phase_timeout_ms，前端阶段 banner 显示「⏱ 本阶段 Ns 后自动推进」倒计时；③**生图真实 provider 适配**——ImageSpecService.generateImage 三层：OpenAI 兼容 /images/generations（response_format=b64_json，`roleplay.image.provider-url/key/model` 可配）→ 失败/空配置离线 SVG 占位（不抛异常，对齐 demo 三层降级）；ImageController 重写 +POST /api/image/generate：生成→Base64 解码写 static/assets/IMAGES/<uuid>.<ext>→databaseService.saveAsset 登记（AssetController ASSET_TYPES +ROLE_PORTRAIT/SCENE_BACKGROUND/CLUE_IMAGE）→响应 url/asset/fallback；前端 🎨 美术抽屉：image_spec 合成→逐个生成→展示真实图/占位图/错误
- **原因**：①主人需求「剩下的全部做完」——私聊实时性（上批只做轮询）、阶段无人操作卡死（无 DM 时剧本杀可无限停留搜证/讨论）、生图只到描述未到图片（上批只有 spec 端点）；②阶段倒计时用惰性判定（轮询触发）而非定时器——对齐投票超时惰性判定先例，零线程开销，重启后由快照 phaseStartedAt 自然续算；③生图 provider 做成可配 + 离线占位——真实文生图 API 需用户自配 key（对齐 demo 三层降级），无 key 时管线仍完整可演示；图片落 static/assets/ 并登记 assets 表 = 复用既有 Phaser 消费链（assetFileUrl）
- **放弃**：①私聊 SSE 做 per-participant 定向（broadcastToSession 已够，前端按 player 过滤，per-emitter 定向需改造 SSEController 连接注册表，收益小）；②阶段倒计时用 Spring @Scheduled 定时器（惰性判定已满足且零开销，定时器需管理生命周期/多局并发）；③生图做成异步任务队列（单张同步已够；批量异步留 P2）；④VOTE 阶段倒计时自动揭晓（需走审批门语义，风险高，明确不做）
- **影响**：①全量 mvn **458/0 BUILD SUCCESS**（65 类 surefire 汇总；452 基线零破坏 + ImageGenerationTest 3（离线 SVG/空配置回退/provider 失败降级）+ ScriptGamePrivateChatTest +3（阶段倒计时 INVESTIGATION→DISCUSSION→VOTE/VOTE 不推进/phase 时钟 toMap）；LONG-01 首轮堆 31.1%>30% 为既有脆性 #37/#40/#41 同款，单跑 7.75s PASS 后复跑全绿）；②新增配置 roleplay.game.script.phase-timeout-ms:0 + roleplay.image.{provider-url,provider-key,model}（yml 双份）+ 1 新端点 POST /api/image/generate + AssetController ASSET_TYPES 扩展 3 类型；③前端 4 文件（useSSE/client/ChatPage）+ npm run build 通过（tsc 0 错误 + vite 74 modules，index-C03zxnb0.js 1,869.63 kB，清 .tsbuildinfo 后干净全量）+ static 同步 SHA256 dist↔static 一致（88825345...）+ 删旧产物 index-CqdAlTZ2.js/index-k6prgkhJ.css；④兼容性——phase-timeout-ms 默认 0 旧行为零变化；image provider 空默认离线占位；新素材类型向后兼容（AssetLibraryTest 8 断言枚举仍过）；旧快照无 phase_* 键 → 恢复默认；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；未 git commit（统一 gate 待授权）

**P-0805-C 收尾修复（2026-08-05 13:5x，台账 #118）**：打包重启 8000 真机验证发现——生图图片写 `src/main/resources/static/assets/IMAGES/`（classpath）运行时 jar 读不到新文件 → `GET /assets/IMAGES/x.svg` 被 FrontendController SPA 兜底返回 `text/html`。修复：ImageController 改写入运行时目录 `./data/generated/images/`（jar 外持久化，与 H2 `./data/roleplay` 同目录族）+ 新增 `GET /api/image/file/{name}` 专用文件端点（显式 content-type image/svg+xml/png + 路径穿越防护 `/ \ ..` 拒绝 + 404 + no-cache）。真机验证：generate 200 → file GET **image/svg+xml**（修复前 text/html）+ `<svg` 内容 → `/api/assets?type=ROLE_PORTRAIT` 登记成功；首页/bundle/两 demo/config 全 200；ImageGenerationTest/ImageSpecServiceTest 8/0 PASS（生图逻辑不变仅落盘与端点路径变化）。

---

## 2026-08-09（阶段② API 可视化逻辑链批次）

### D-043 后端调用链追踪 + 前端追踪面板（方案 C 正式落地，默认关闭零影响）
- **决策**：按调研报告 docs/ui-api-survey.md §5 方案 C 落地后端 API 可视化逻辑链：①请求打点用 **OncePerRequestFilter（TraceFilter）而非 HandlerInterceptor**——需要 finally 收尾 + 响应头 X-Request-Id 回写 + ContentCachingRequestWrapper 读请求体（Filter 层才能可靠 wrap，Interceptor 拿不到未消费的 body）；仅拦 /api/**，/api/events（SSE 长连接）跳过条目（异步生命周期与普通请求不同，其事件由 broadcast 钩子承载）；②**X-Request-Id 关联**——客户端带则回显并标来源=frontend，无则 UUID 标 auto；SSE 事件经 SSEController.broadcast/broadcastToSession 各 1 行钩子（TraceContext.recordSse）记入触发请求链路；③**LLM 耗时采集**——LLMClient 统一漏斗 callSyncInternal 与 callStream 各加打点包装器（墙钟含重试 + 模型，finally 语义失败也记录），经 TraceContext（ThreadLocal 请求上下文 + static registry 门面，零构造侵入）关联；④查询端点 GET /api/debug/trace（默认 50 上限 200）+ /{requestId} 详情；⑤存储=内存环形缓冲（ConcurrentLinkedQueue 容量 1000 硬上限 2000）不进 DB；⑥**开关 roleplay.debug.trace-enabled 默认 false**——关闭时 Filter 直通零打点、端点 404，既有行为逐字节不变；⑦前端 ChatTopbar「🧭 逻辑链」入口激活 → 新增 TraceDrawer.tsx（3s 轮询 + 展开详情：LLM 子调用时间线/请求体摘要/SSE 事件标记），开关关闭显示「未开启（roleplay.debug.trace-enabled=false）」提示不报错；client.ts request() 每请求自动带 X-Request-Id（UUID）- **原因**：主人诉求字面即「后端 API 在前端的可视化逻辑链」；方案 A（纯前端）看不到后端内部编排（LLM/SSE），方案 B（静态图）无真实运行数据；C 数据最真实完整且与禁动约束兼容（全部为新增横切 + 纯增量钩子，不触碰 RouterService/ArbiterService/审批/狼人杀/剧本杀业务逻辑）- **放弃**：①HandlerInterceptor（body 读取不可靠 + 无 finally 收尾语义）；②前端本地链路记录（方案 A，刷新即丢且无后端编排深度）；③SSE 事件做独立缓冲（不关联请求，展示价值低）；④把 LLM 打点做成 LLMClient 构造注入（侵入构造链，static registry 门面零侵入，测试可 register/unregister）
- **影响**：①全量 mvn **471/0 BUILD SUCCESS**（69 类；458 基线零破坏 + TraceHooksUnitTest 7 + TraceIntegrationTest 6）；②新增 2 配置键（roleplay.debug.trace-enabled/trace-size，yml 双份）+ 2 查询端点 + 6 新文件 + 2 文件纯增量钩子；③前端 npm run build 通过（vite 92 modules）+ static 同步 SHA256 一致；④已知限制——后台虚拟线程（讨论引擎/autoPlay）触发的 SSE 事件与 LLM 调用无请求上下文，无法关联到具体 REST 请求（ThreadLocal 语义的固有边界，已注释说明）；SSE GET /api/events 连接本身不产生条目；trace 端点自身请求也会被记录（调试面板轮询可见，属预期）；⑤前端「逻辑链」抽屉在开关关闭时显示未开启提示（404 静默处理），不报错不打扰；⑥未 git commit（统一 gate 待授权）


---

## 2026-08-10（立绘自动抠背景批次 P-0810-04）

### D-045 生成立绘自动抠背景：RMBG-1.4 集成 ImageGenService 固定流程（原图保留 + 透明版并存）
- **决策**：①**onnxruntime CPU 推理**——pom 引入 com.microsoft.onnxruntime:onnxruntime 1.20.0（CPU 版，本机无 CUDA provider，实测 1.5-2s/张），模型文件外置 models/rmbg/rmbg-1.4.onnx（168MB 不打包进 jar，路径配置化 
oleplay.ai-image.rmbg-model，默认绝对路径 D:\\roleplay-java\\models\\rmbg\\rmbg-1.4.onnx，以工作目录为基准的约定与 P-0810-01 output-dir 同族）；②**新类 aiimage/RmbgRemover**——懒加载 OrtSession（首次 removeBackground 才初始化；模型不存在/加载失败/图片不可读/推理失败一律返回 false 不抛异常 = 降级保留原图，主流程零阻塞）；流水线对齐已实测的 python 推理脚本：读图→双线性缩放 1024×1024→RGB float32/255→NCHW（1×3×1024×1024）→session.run（输入/输出名从 session 动态取，适配任意 ONNX 导出命名）→min-max 归一化（官方 RMBG-1.4 后处理惯例，防原始 sigmoid 背景灰值产生半透明薄雾）→mask 双线性缩回原尺寸→1px 高斯羽化（3×3 σ=1.0）→RGBA 透明 PNG（颜色原样保留）；③**ImageGenService 固定流程**——genOne 每张生成图保存后自动抠图（同基础名 {frame}_t.png，原图保留+透明版并存），失败仅 log.warn 不影响生成任务（setRmbgRemover/setRmbgEnabled 测试钩子，yml 
mbg-enabled 总开关默认 true）；④**URL 暴露**——imagesOf 磁盘扫描把 {frame}_t 一并收录（frame→URL 映射新增 avatar_t/happy_t 等条目），characterStatus/imagesResponse 的 images 映射随之携带，前端立绘可优先用 _t 版；avatar/expressions 键仍指向原图，既有契约零破坏
- **原因**：①Gal 立绘叠加需要透明背景 PNG，Pony V6 XL 出图固定带背景 → 必须在生成链路内自动抠图（每张生成后立即处理，用户零操作）；②RMBG-1.4 是 BRIA 开源最成熟的通用抠图模型（1024 输入、单 mask 输出、ONNX 部署简单），且本机已实测通过（python_embeded 1.5s/张）；③降级纪律对齐项目「生图失败不阻塞主流程」传统（P-0810-01 任务 FAILED 标记 + P-0805-C 占位降级先例）——模型缺失时系统照常出图，只是不产透明版；④透明版命名 {frame}_t 后缀而非覆盖原图——原图保留供无透明需求的场景（聊天头像），透明版供立绘叠加，并存零破坏
- **放弃**：①用 Pillow/OpenCV 做传统抠图（绿幕/色度键对 AI 生成图无效，必须语义分割模型）；②引入 python 子进程调 python_embeded（Java 直连 onnxruntime 无进程管理/编码/部署依赖）；③mask 做硬阈值二值化（官方脚本为软 mask+羽化，硬阈值会切掉头发丝等软边缘；实测 alpha 过渡带仅 3.3%，软 mask 视觉等效实心且边缘更自然）；④透明版覆盖原图（见决策④）；⑤默认关 rmbg-enabled（需求明确固定流程自动抠，默认开，关是逃生舱）
- **影响**：①全量 mvn **491 run / 3 failures / 0 errors（两轮复跑一致）**——新增 RmbgRemoverTest 7 + ImageGenServiceTest +2 全绿（定向 13 用例）；3 失败均非本批：ComfyUIClientTest×2 = 并行批次 P-0810-01 在途重构（13:12 模板改 CheckpointLoaderSimple、ComfyUIClient 同步改接，其测试 mtime 11:17 未同步，登记表仍作业中——按并行纪律未代改其文件，提请主会话协调）；ScriptGamePrivateChatTest.privateSayRepliesAndPersists = 既有 flaky（单跑 8/0 复证，P-0810-01 打包 flaky 记录在案）；②新增 2 配置键（roleplay.ai-image.rmbg-model/rmbg-enabled，yml 双份）+ pom 1 依赖 + 1 新文件 + 2 文件增量；③**真实冒烟**：heroine/avatar.png → src/main/resources/static/ai-images/heroine/avatar_t.png（1,949,247B 含 alpha；直方图 alpha 0-25=36.3% 背景透明（四角=0）/231-255=60.4% 前景近不透明/26-230 过渡带仅 3.3% = 干净抠图；耗时 1.9s）——8000 重启后新生成图自动产 _t 透明版；④已知限制——CPU 推理每张约 1.5-2s（角色 7 张 = 多 10-14s 生成时长，可接受）；OrtSession 懒加载首次调用含模型加载（约 2s）；未做 CUDA 优化（本机无 GPU provider，留后续）；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路/static/前端）零改动；不打包不重启 8000 不 git commit（统一 gate 待授权）


---

## 2026-08-10（表情集生成升级批次 P-0810-05）

### D-046 表情集生成从纯文生图升级为 avatar 文生图 + 表情 img2img（同角色脸型漂移根治）
- **决策**：①**新增 img2img 工作流模板** `pony-v6-img2img-workflow.json`——与文生图模板同构（CheckpointLoaderSimple(1) + LoraLoader(5,0.8) + CLIPSetLastLayer(13,-2) + CLIPTextEncode(7/8) + KSampler(10) + VAEDecode(11) + SaveImage(12)），差异=去掉 EmptyLatentImage(9)，改 LoadImage(D,__REF_IMAGE__) + VAEEncode(F,pixels=[D,0],vae=[1,2])，KSampler latent_image=[F,0]、denoise=__DENOISE__ 占位符；JSON 不支持注释，节点说明独立成 `pony-v6-img2img-README.md`；②**ComfyUIClient 新增 img2img 能力**——`uploadImage`（JDK HttpClient multipart/form-data POST /upload/image，字段 image，Content-Type image/png，取响应 name 作 __REF_IMAGE__）+ `buildImg2ImgWorkflow`（复用占位符替换 + applyLoraRewiring，__REF_IMAGE__/__DENOISE__ 经 replacePlaceholders 新增 extra 映射重载注入，__DENOISE__ 数值化 Double）+ `generateImg2Img`（上传→构建→提交→轮询→下载，后半段抽取为 finishGeneration 与 generateOnce 共用）；③**ImageGenService 生成顺序改造**——triggerGenerate：先 avatar（现有文生图 portrait），avatar 成功后才逐个生成 6 表情（img2img，底图=dir/avatar.png 原图非透明版——不用 _t 透明版，构图信息完整），denoise 可配 `roleplay.ai-image.img2img-denoise`（默认 0.5，yml 双份）；单帧失败跳过继续（log.warn，try/catch 圈在单帧内），avatar 失败则任务 FAILED（无底图后续 img2img 无意义，终态按现有逻辑）；进度事件不变（avatar→happy→...→done）；img2img 产物同样走 RMBG 抠图（P-0810-04 _t 透明版链路零破坏）；④**测试**——ComfyUIClientTest +4（模板结构/占位符+rewiring/端到端 mock multipart/错误态）+ ImageGenServiceTest +2（顺序与底图断言/单帧失败跳过与 avatar 失败终态）
- **原因**：①P-0810-01/04 实测：同角色 7 张纯文生图（固定外貌词 + 固定 seed）仍存在脸型/发型/服装漂移——文生图每次采样构图从噪声起步，外貌词只能约束风格不能锁定几何；img2img 以 avatar 为起点、低 denoise（0.45）下构图由底图主导，方案已在 ComfyUI 实测验证（脸型/发型/服装 100% 保持，表情变化正常）→ 从「纯文生图」升级为「avatar 文生图 + 表情 img2img」；②denoise 做成配置而非硬编码（对齐 D-004「阈值勿 hardcode」纪律），0.5 为默认、0.45 为实测安全值，前端/调用方可按需调；③单帧失败跳过继续——表情集是展示资源，单帧失败不阻断整角色出图（任务终态仍 DONE），与「生图失败不阻塞主流程」传统（P-0810-01 FAILED 标记 / P-0810-04 降级保留原图）一致
- **放弃**：①把 img2img 参数塞进 WorkflowSpec 记录（7 组件 record 的既有构造调用点多，新增组件会破坏全部既有测试构造；独立 extra 映射 + 独立方法签名改动面最小）；②表情帧继续纯文生图（P-0810-01 行为，漂移问题未解决）；③denoise 固定 0.45（配置化保留调参空间）；④img2img 底图用 _t 透明版（透明通道对 VAEEncode 是额外输入，原图构图信息完整；透明版仅前端展示用）
- **影响**：①定向 aiimage 27/0（ComfyUIClientTest 11 + ImageGenServiceTest 8 + RmbgRemoverTest 8）；②全量 mvn **498 run** 两轮各 1 failure 均为既有 flaky（ScriptGamePrivateChatTest / LongTextStabilityTest，单跑均 PASS，Round 50/v51 同款）；③真机冒烟 PASS：真实 ComfyUI 0.31.0 img2img 全链路（multipart 上传→工作流→出图 49.9s 1,069,251B；MAE 整体 6.2/脸区 11.3/255、RGB 均值几乎一致=脸型/发型/服装保持）；④新增 1 配置键（roleplay.ai-image.img2img-denoise，yml 双份）+ 1 模板 + 1 文档；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路/static/前端）零改动；⑥未 git commit（统一 gate 待授权）；8000 实例（PID 22096）未触碰，img2img 生效需主会话统一重启

## 2026-08-10（一般模式「场景与场景目标」机制批次 P-0810-09）

### D-047 一般模式「场景与场景目标」：LLM 目标集生成 + 隐藏目标驱动注入 + 每轮异步判定 + scene_target_update SSE（用户已确认设计）
- **决策**：① **场景目标集**——Scene 加 goals 字段（结构化 JSON：`global_goal` 全局隐藏主线 1 条 / `role_goals{roleName:{desc,status}}` 每 AI 角色 1 条隐藏目标 / `player_goal` 玩家目标可选），落库 SceneEntity.goals JSON 列（ddl-auto=update 自动加列，旧场景无 goals 零迁移）；状态机 NOT_STARTED/IN_PROGRESS/COMPLETED/FAILED（机器值英文，前端映射中文）。② **生成（init 时）**——一般模式 init（含 scene）且场景无目标集 → 新 SceneGoalService LLM 生成（prompt 全英文，风格同 GeneratorService/ScriptService；init body 可选 scene_id 命中 DB 场景 → 装载已有 goals / 生成后回写持久化，下次 init 免 LLM；可选 player_goal 自定义玩家目标，缺省 LLM 生成）；LLM 失败/缺角色/空输出 → 规则兜底 + 归一化补全，恒返回结构完整目标集零崩溃。③ **目标驱动**——Agent +hiddenGoal，RouterService.setSceneGoals 时按角色注入，buildContext / generateWithContext(Stream) 系统提示追加「你的目标：xxx（不要主动暴露给玩家，用行为引导）」（并行 AgentExecutor 路径经 generateSync→buildContext、串行路径经 generateWithContextStream 全覆盖）。④ **进展判定（每轮 send 后）**——runRound 末尾异步（CompletableFuture，不阻塞主流程）轻量 LLM 判定最近 3 轮对话对各目标状态；失败静默降级（log.warn 不广播）；状态有变化 → `sse.broadcastToSession(sessionId, "scene_target_update", payload)` 会话定向广播；完成/失败目标揭示全文且仅揭示一次（goalRevealed 集合防重复广播），进行中保持隐藏。⑤ **init 响应**——新增 goals 键：玩家目标明文、全局/角色目标仅「??」占位 + ai_goal_count 数量（隐藏不泄露）。⑥ **SSE 契约**——event=scene_target_update，data={session_id, role_goal_status:{roleName:status}, global_goal_status, player_goal_status, revealed:[完成/失败时揭示的目标全文]}；无变化不广播。判定频率先做每轮一次（任务注明性能优化留后续：可 N 轮一次或仅发言涉目标时判定）。
- **原因**：① 用户确认的设计要求——一般模式场景需要「隐藏事件/氛围主线 + 角色隐藏目标 + 玩家可见目标」三层的目标驱动结构，此前 Scene 无目标字段、Agent 无目标注入、无进展判定链路；② 目标对玩家隐藏是戏剧张力来源（AI 目标用行为引导而非明示），玩家目标明文可展示；③ 判定每轮同步会拖慢对话主流程（额外 LLM 调用），异步 + 失败静默对齐「轻量、不阻塞、可降级」要求；④ 生成/判定都走既有 LLMClient.callJson 统一路径（含 fuzzy JSON 提取与三连重试），零新基础设施；⑤ 落库复用 SceneEntity（对齐 D-013/D-014「不扩表优先」纪律——本次是加列而非新表，goals 为业务字段随场景走）。
- **放弃**：① 目标集做成独立表/独立实体（goals 随场景单点归属，JSON 列已够；快照/结果不消费该结构）；② 判定结果经 LLM 回传 desc（揭示文本以目标集内存原文为准，防 LLM 编造）；③ 判定同步阻塞主流程（性能与可用性双输）；④ 全局主线也注入所有 Agent（任务只要求角色目标注入，全局主线仅判定/广播消费，避免过度泄露隐藏主线）；⑤ 每轮必判的节流优化本轮不做（任务明确「先做每轮判定+轻量 prompt，性能问题后续优化」）。
- **影响**：① 定向 mvn test **9/9 全绿**（SceneGoalServiceTest 6：结构字段齐全/自定义玩家目标优先/LLM 失败兜底/缺角色归一化/判定解析/判定失败静默；SceneGoalStatusTest 3：状态机未开始→完成落回目标集 + scene_target_update 广播 revealed 揭示全文/状态无变化不广播/Agent 隐藏目标注入）；全量 mvn test 基线 498 不降（2 既有 flaky 除外，见 TEST_STATUS Round 53/v53）；② 新增 1 service（SceneGoalService）+ 1 列（SceneEntity.goals）+ 1 SSE 事件（scene_target_update，走既有 broadcastToSession 定向通道零新机制）；③ 行为兼容——旧场景无 goals 字段零破坏（宽容解析 null）、无目标会话不触发生成/判定、werewolf/script 模式 isGeneralMode 守卫零触碰、Agent 无 hiddenGoal 时系统提示逐字节不变；④ 一般模式 init 新增一次 LLM 调用（目标生成，约与剧本生成同量级；DB 已有 goals 或失败兜底时零成本）；⑤ 已登记 docs/修改记录.md #134 + TEST_STATUS Round 53/v53 + 并行作业登记 P-0810-09；⚠️ 解阻塞修正 CharacterController.java 的 P-0810-10 变量名笔误（name→oldName，其编译失败阻断全量测试，最小修正已注明，提请主会话与该批次协调）；⑥ 未 git commit（统一 gate 待授权）

## 2026-08-10（persona 五层模板接入后端，批次 P-0810-10，主人拍板）

### D-047 persona 五层模板：Persona 扩展五层结构，LLM 上下文完整可见、对外 API 只回表层
- **决策**：①**Persona.java 五层扩展**——新增 `layers` 内部字段（layer0 行为规则 / layer1 身份 / layer2 表达风格 / layer3 情感模式 / layer4 冲突链与雷区 + contrast 反差设定 + humanDetails 人味细节），`buildSystemPrompt()` 有五层数据时输出五层模板（【反差设定】→【Layer 0 行为规则】→【Layer 1 身份】→【Layer 2 表达风格】→【Layer 3 情感模式（含人味细节 + 允许情绪波动·不完美回应）】→【Layer 4 冲突链与雷区】→【行为总原则】，身份锁定/表演规则原样保留），LLM 上下文完整可见；无 layer 数据完全回退旧 4 字段（personaDesc/voice/background）零破坏；②**对外 API 不透出五层（硬性）**——层数据存独立内存库（CharacterController.personaCards，不入角色表/H2）+ `Persona.toMap()` 不序列化 layers，GET /api/characters、GET /api/state characters、init 响应只回表层 name/appearance/voice/background/summary，绝无 layer0/layer3/layer4/contrast/humanDetails 键；③**反差 + 人味（硬性，写进模板结构）**——contrast（表面 X ↔ 实际 Y，例：小铃 温柔体贴↔胜负欲强泡茶执念 / 凯尔 爽朗热心↔怕被看轻爱面子）+ humanDetails（小缺点/小习惯/口头禅/情绪化表达），Layer 3 明确「允许情绪波动，不完美回应」；④**默认卡**——resources/persona/ 三张五层卡（xiaoling 小铃 / kyle 凯尔 / luna 露娜），PersonaCardLoader 静态懒加载（name/id 双索引），四处 Persona 构建点（SessionController.init / SceneController.startScene / SimulationController.loadCharacters / HistoryController.buildAgents）经 `attachPersonaCard` 一行挂载（导入卡优先→默认卡→无卡 no-op）；⑤**导入 API**——POST /api/characters/{name}/persona（body 含 layer0~4/contrast/humanDetails 至少一键否则 400；响应只回表层 + 层键名列表）；⑥**轻量提示**——buildLightweightPrompt 五层版只带 Layer2 风格 + Layer0 前 3 条摘要 + 反差（不含完整冲突链，省 token）
- **原因**：① 主人拍板三条硬性需求（五层进上下文 / 对外不透出 / 反差+人味进模板）；② 旧 4 字段模型（personaDesc/voice/background）是压缩摘要，LLM 拿到的是「标签」而非「可执行行为规则」——五层模板把形容词翻译成「当 [场景] 时你 [行为]」的表演指令，Layer 0 放最前利用 LLM 长上下文前部权重防人设漂移；③ 五层是内部表演指令，透出给前端等于公开「雷区/情感模式」底牌，破坏角色扮演沉浸感与对局博弈（剧本杀秘密机制同理）；④ 向后兼容：旧数据（无 layer 字段）不炸、旧格式逐字节不变
- **放弃**：① 把五层卡并入角色表/H2 存储（characters 表无 layer 列，加列需 H2 file 库迁移——沿用 D-013/D-014「不扩展表结构」纪律；导入卡为内存态，重启重导或用默认卡，已文档化）；② 改 Agent.java（P-0810-09 并行在改，五层输出完全封装在 Persona 内，Agent.buildContext 调用点零改动）；③ 让 buildSystemPrompt 每次 IO 读卡（纯 POJO 保持确定性，卡挂载在 Persona 构建时一次性完成）；④ 把五层卡写进 /api/characters 角色 map（list() 会整体序列化 → 必须独立存储才能保证不透出）
- **影响**：① 全量 mvn **518 tests / 76 类 / 0 failures / 0 errors**（基线 507 + P-0810-10 新增 PersonaFiveLayerTest 10 用例 + 1 并行未登记用例；一次通过，未复现 v52 flaky）；② 新增 1 静态加载器（PersonaCardLoader）+ 1 导入端点（POST /api/characters/{name}/persona）+ 3 张默认卡 resources + 1 转换工具（tmp/persona-builder/md2json.py）；③ 行为兼容——无 layer 数据的旧角色/剧本杀/狼人杀 persona（personaDesc-only 构建路径）零变化；attach 规则「已有 layer 不覆盖、用户显式 personaDesc/voice/background 绝不覆盖」保证用户数据优先；④ 一般模式 Gal 快速起局（小铃/凯尔）与 2D 模拟（loadCharacters）自动获得五层 persona（默认卡按名挂载），LLM 扮演质量提升；⑤ 已知限制——导入卡为内存态（重启丢失，需重导或用默认资源卡）；script/werewolf 游戏路径（ScriptGameService/WerewolfService 自建 personaDesc-only Persona）未接五层卡（任务边界：游戏内角色由剧本 intro 驱动，P1 可按 A2 行为锚方案增强）；⑥ ⚠️ 协调注记——renameCharacterInMemory 一处变量名笔误（name→oldName）由并行 P-0810-09 批次解阻塞修正，语义一致；禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路/static/前端）零改动；不打包不重启 8000 不 git commit

## 2026-08-10（地图方案阶段 1 后端批次 P-0810-17）

### D-048 剧本杀两阶段生成：init 概略先行（<10s）+ generate_full 后台异步完整生成 + B1-B5 前置 bug 批次
- **决策**：①**init 概略先行**——POST /api/script/init 默认只生成概略 outline（outline_only 缺省 true）：轻量 LLM prompt（ScriptService.generateOutline，maxTokens 1200）产 locations/roles（名+一句话人设）/clues（标题+地点）/storyline/killer_hint → game.phase=Phase.SETUP（枚举现成从未被赋值，天然「概略已生成、完整待生成」中间态，零新增枚举）→ 概略态也发放 roleKey + 快照落库（outline 随快照存取）+ broadcastPhase("setup")+broadcastStatus → toMap 附加 outline/generating 键；3/4 参旧 initGame 重载委托 outlineOnly=false 保持既有同步完整生成路径（~100 既有调用点/测试零变化）。②**新端点 POST /api/script/generate_full**——ScriptController.generateFull → ScriptGameService.generateFull：discussionExecutor（虚拟线程）异步执行，不阻塞 init；守卫仅 SETUP 可调；①完整剧本 generateScriptChecked(theme, players, outline)（outline 作 prompt 约束防两阶段矛盾）→ applyScript（从 initGame 完整路径抽取共用私有方法，双路径零漂移）→ persistScript+saveSnapshot → 阶段推进 full→INVESTIGATION / chat→DISCUSSION → broadcastScriptReady(map_ready=false)+broadcastPhase+broadcastStatus → full 模式异步 generateMap（LLM→7 项校验→BSP 降级，多图注册表 maps/currentMapId/searchedByMap 复用，失败不阻塞）→ 完成再 broadcastScriptReady(map_ready=true)+broadcastStatus。③**SSE 进度事件**——决策点 6 选「新增 script_ready 事件」（结构化 {session_id, ready, phase, name, map_ready, generated?}，会话定向 broadcastToSession），与 script_phase（阶段切换）/script_status（全量状态）并存；另新增 script_speech（讨论发言实时回显）。④**B1 讨论发言实时回显**——复用 ConversationManager 既有 SpeechTurn/setSpeechBroadcastListener 资产族：新增独立 scriptSpeechListener（setScriptSpeechListener），runScriptDiscussionRounds 每轮结束对新增发言逐条回调（emittedTurns 游标；null 监听器零开销、行为逐字节不变；per-game 实例隔离——2D 世界/狼人杀各自 CM 不设此监听器零影响）；ScriptGameService.runDiscussionEngine 订阅 → script_speech SSE；discussionSay 入口对 human 发言立即回显（human=true）+ speechEmitted 去重集（speaker|message，ConcurrentHashMap.newKeySet）防讨论线程逐轮回调双发。⑤**B2 chat 单人局 phase 快速翻转**——initGame chat 单人局与 runDiscussionEngine 单人局路径 enterVotePhase 改 discussionExecutor.submit 异步提交（同一请求内 script_phase discussion→vote 连发会致前端 setScriptPhase 竞态只见 vote；异步让 discussion 广播先落 SSE 通道）。⑥**B3 role_key 错位**——ScriptController.getStatus/getKeys sessionId 解析优先 scriptGameService.findSessionByPlayerKey(player_key)（key 与对局一一对应，反查是唯一可靠归属；修复 playerSessions 回退 currentSessionId 在并发对局下拿到全局最后对局 → 玩家 1..N 误用玩家 0 的 key → 403），反查失败回退旧行为兜底。⑦**B4 WerewolfController.discussionSay 500**——WerewolfService.discussionSay 返回 map 改 LinkedHashMap（根因 Map.of 不可变 put 崩溃，一行修复）+ WerewolfController 防御性拷贝 new LinkedHashMap 兜底。⑧**B5 discussionTranscript ArrayList→CopyOnWriteArrayList**（D-034 登记项：讨论线程逐轮 append 与读取并发安全）。
- **原因**：①用户 21:46 实测 init 超时——现 initGame 同步双 LLM（剧本 4000 tokens + 地图 8000 tokens/45s）不可接受，用户明确要求拆分成「概略快返 + 后台完整生成」；②Phase.SETUP 枚举调研确认存在但从未赋值，是现成的中间态（零枚举新增）；③B1-B5 为方案文档 §4.4 前置 bug 批次（discussionSay 无 SSE 实时回显 / chat 单人局快速翻转 / role_key 并发错位 403 / Werewolf discussionSay 500 / transcript 并发安全），与两阶段生成同文件区域（讨论链路），同批修复避免二次触碰禁动文件。
- **放弃**：①init 继续同步完整生成（用户明确否决，超时根因）；②复用 script_phase/script_status/announcement 承载「完整剧本就绪」语义（这些事件职责是阶段切换/状态快照/公告，无结构化就绪载荷；新增 script_ready 语义最清晰，前端一次注册即可）；③B1 用轮询 transcript 补回显（实时性差、重复轮询开销；ConversationManager 已有 SpeechTurn 逐轮资产，接线成本最低）；④B2 用「跳过 discussion 广播直接 vote」（丢失 discussion 阶段可见性；异步提交既保广播顺序又保最终态）；⑤B3 直接改 playerSessions 索引（其语义是「玩家→最近对局」缓存，改语义影响面大；key 反查是服务层已具备的精确归属）。
- **影响**：①定向 16/0 全绿（ScriptOutlineFullTest 7 + ScriptSpeechSseTest 4 + ScriptControllerBugBatchTest 4 + WerewolfDiscussionSayMutableTest 1）；全量 mvn **553 run / 1 failure / 0 errors**（551 基线 + 本批 16 用例；2 既有 flaky 单跑复证通过：ScriptGamePrivateChatTest 8/0（Round 50/v51/v52 登记 mock 角色名随机）、LongTextStabilityTest 1/0（堆测量脆性 #37/#40/#41 同款））；唯一失败 RouterServiceAutoFirstRoundTest.startScene_wiresAutoFirstRound = 并行 P-0810-16 在途 SceneController 改动（sessionRegistry==null 时 sessionRouter==router 双调 initSession）与 P-0810-14 该测试 verify(times(1)) 冲突，非本批引入（本批文件不含该调用链），按并行纪律未代改。②新端点契约：POST /api/script/generate_full body{session_id?} → 200 {ok, generating, session_id, phase:"setup", message}；重复调用/非 SETUP → {error, phase}。③SSE 事件序列（full 模式）：script_phase(setup) → script_ready(map_ready=false)+script_phase(investigation)+script_status → script_ready(map_ready=true)+script_status；chat 多人：script_phase(setup) → script_ready(map_ready=false)+script_phase(discussion)+script_status（地图不生成）；chat 单人：直接 vote。④既有测试适配 2 处：ScriptRenameTest（initGame 五参 stub）、ScriptGameResumeTest（C3-8 controller init 显式 outline_only=false 保完整局语义 + getKeys 双参）。⑤已登记 docs/修改记录.md #140 + 并行作业登记 P-0810-17；不打包不重启 8000 不 git commit（主会话统一部署 gate）。

## 2026-08-10（场景卡目标双层缺口修复批次 P-0810-16）

### D-049 startScene 起局会话走 SessionRegistry 独立实例（对齐 /api/init），场景目标机制在 startScene 路径生效
- **决策**：① `SceneController.startScene` 不再直接在默认单例 router 上 initSession，改走 `SessionRegistry.getOrCreate(sessionId)` 独立实例执行 initSession/ensureSceneGoals/triggerAutoFirstRound（响应 state/goals 均取独立实例），默认单例仅做镜像 initSession（向后兼容无 session_id 旧客户端）；② `SessionController.getState`（GET /api/state）响应附加 `scene_goals` 键（= r.getSceneGoalsView()，无目标时 enabled=false 零影响），供前端场景卡进入/刷新轮询拉取；③ 前端 Gal 场景卡目标三通道接入（起局响应 goals 即时 / /api/state scene_goals 轮询兜底 / scene_target_update SSE 增量合并），AI 目标恒 ?? 占位、terminal 状态经 revealed 揭示全文一次（P-0810-09 契约原样），无目标会话场景卡优雅降级（只显示场景名+描述，删除「目标系统开发中」占位）
- **原因**：① P-0810-12 走查实证——startScene 走默认单例 router 时其 sceneGoalService 恒 null（P-0810-09 仅在 SessionRegistry.createRouter 注入，默认单例 Spring bean 从未注入）→ ensureSceneGoals 直接 return → 响应 goals.enabled=false；② D-011/D-007 已记录同型建议「SceneController.startScene 改 getOrCreate 独立实例」，本批即落地（顺带彻底解决单例 roundHistory 跨会话残留类问题，P-0810-11 为同一根因的缓解面）；③ 前端 GalGeneralView 硬编码 targets={[]} + GalStore 无 scene_target_update 分支是走查 FAIL 的第二层缺口，三通道接入保证起局即时可见 + 刷新/重连可恢复 + 对话轮后状态实时更新
- **放弃**：① 给默认单例 router 显式 setSceneGoalService（单例跨会话共享目标状态，多个 startScene 会话互相覆盖，隔离语义劣于 getOrCreate）；② 前端新增专用 GET goals 端点（/api/state scene_goals 键已够，零新端点）；③ startScene 响应改为 /api/init 形状（保持既有契约，仅新增 goals 键——P-0810-09 已定契约）
- **影响**：① startScene 响应 goals.enabled=true（player_goal 明文 / role·global ?? 占位 / ai_goal_count），场景目标正常生成 + 对话轮后 scene_target_update 定向广播（P-0810-09 机制原样生效）；② startScene 会话从共享单例升级为隔离实例（MemoryStore/roundHistory/sceneGoals 全隔离，多会话互不串扰——与 /api/init 同构），默认单例镜像保旧客户端兼容；③ /api/state 附加 scene_goals 键（旧消费方忽略未知键零破坏）；④ 前端场景卡真实渲染 + 优雅降级；⑤ 定向测试 20/0（SceneStartSceneGoalsTest 2 新用例锁定「startScene goals.enabled=true + scene_target_update 广播」）；前端 build 通过（dist index-C1sYPVr9.js，未同步 static）；⑥ 已知限制——旧会话（修复前创建、未注册 SessionRegistry）经 /api/state?session_id= 会落到默认单例，scene_goals 可能反映共享单例目标集（重启后旧会话不存续，影响面为零）；4174 实机验证待主会话打包重启 8000


## 2026-08-10（地图方案统一版 P0 批次 P-0810-22（原 21 撞标让位））

### D-050 地图生成质量三护栏：剧本上下文注入 prompt + rooms 覆盖 warning + 尺寸下限 clamp（32×20，B 方案；用户裁决后实施）
- **决策**：①**P0-1 剧本上下文注入**——`ScriptMapService.buildPrompt` 新增 background 段（剧本 background/storyline + truth 拼接，明确「仅用于氛围与场景布置参考，rooms/zones 地点仍以剧本地点为准」防 LLM 把背景场景名词当房间）；`generateMap` 7 参重载透传，4/5 参旧签名委托 null 零破坏；`ScriptGameService.doGenerateAndRegister` 拼 `game.background`（概略态=outline.storyline）+ `game.truth` 传入。②**P0-2 rooms 覆盖校验**——生成后处理挂 ScriptMapService（MapValidator 零改动，契约 v1 校验 7 项语义不变）：`missingRoomLocations` 复用 `matchLocation`（精确/trim/子串双向/同义词表宽容匹配）计算 `rooms[].name ∪ zones[].clue_location` 对 locations 覆盖率，缺失记 `MapResult.warnings` 新组件（独立于 fallbackReasons，不驱动 usedBsp → 不降级 BSP、不误杀），LLM 与 BSP 双路径接入。③**P0-3 尺寸下限 clamp（B 方案）**——`roleplay.game.map.min-width/min-height`（32×20）yml 双份（测试 yml 同步 + 断言同步更新，用户裁决 B），ScriptGameService.generateMap/doGenerateAndRegister 尺寸解析处 clamp（显式/对局已定/配置默认统一，低于下限 log warning）；显式超 LLM 预算（40×24）仍走 BSP 既有行为不变。④**随地点数自适应放大本批不做**（会改变生产默认尺寸 40×24 并牵连大量断言/前端观感，留 P1）。⑤**S2 风险（map 端点无 phase 守卫 / SETUP 期空 locations）核实不改**，修复路径（map 端点 locations 空保护）留 P1-3。
- **原因**：①统一版方案 T1——地图生成时 LLM 只能看到 theme/locations/clueLocations/尺寸 4 类信息，剧本背景/真相/人设零注入，贴合度不足根因；②T2——MapValidator 7 项校验无 locations 概念，LLM 漏输出地点房间校验照过（实测跨剧本 rooms 匹配率 20/24≈83%）；③R1——全链路无尺寸下限，8×8 也能出图（BSP 8×8 在 minLeaf=7 下无法分裂只剩 1 个「房间 A」）；④用户裁决：P1-1（LLM 尺寸强校验）放弃维持 D-031；P0-3 选 B 方案（测试 yml 同步 min + 断言同步更新）。
- **放弃**：①P1-1 LLM 输出尺寸强校验/补边/重试（维持 D-031，用户裁决）；②rooms 覆盖缺失走 BSP 降级（房间数≠地点数不是校验错误，是质量提示——warning 不误杀）；③把覆盖 warning 塞进 fallbackReasons（会驱动 usedBsp() 把 LLM 地图误标为「BSP（降级）」，故独立 warnings 组件，语义更正确）；④随地点数自适应放大（本轮不做，见决策④）；⑤MapValidator 加重载做 warning（保持校验器 7 项契约语义零变化，生成后处理同层已有 ensureClueZoneCoverage 先例）。
- **影响**：①全量 mvn **572 run / 88 类 / 0 failures / 0 errors**（566 基线 + 本批 ScriptMapP0QualityTest 8 用例 + 并行批次 6 用例；LONG-01 PASS）；定向 3 批 162/0；②新增 2 配置键（roleplay.game.map.min-width/min-height，yml 双份）+ MapResult 记录 +1 组件（warnings()，既有访问器零破坏）+ generateMap/buildPrompt 各 +1 重载（旧签名委托零破坏）；③行为变化——显式小尺寸（<32×20）被提升（前端 D-036 已暴露宽/高输入，出图尺寸更合理）；测试 yml 默认 24×16 经 clamp 实际生效 32×20（K8 断言同步更新）；生产默认 40×24 不受影响（≥下限）；④prompt 输入 +250-400 tokens（输出零增量，8000 预算余量极大）；⑤已知限制——BSP 兜底地图 rooms 为占位名必然产生覆盖 warning（属实且不阻塞，zones 侧覆盖兜底保证搜证可用）；概略态（SETUP）无 truth（scriptSchema 空），注入仅 background=storyline；S2（SETUP 期手动 map 端点空 locations）仍开放（前端现行流程不触发）；⑥禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static/前端）零改动；不打包不重启 8000 不 git commit（统一 gate 待授权）。


## 2026-08-10（P-0810-23 三个新功能批次）

### D-051 AI 发言超长自动提醒（仅下一轮生效）+ 主控候选话术双机制融合（前端兜底）+ 空角色预设移除
- **决策**：①**D2 发言超长自动提醒（用户最终规则）**——RouterService 在 AI 角色单次发言（agent_output 落盘前）检测中文字符数（UnicodeScript.HAN 计数）> 阈值 `roleplay.llm.remind-threshold`（默认 150，yml 双份，<=0 关闭）→ 记录到该 agent 的 `pendingReminder`；下一轮该角色构建系统提示时经 `Agent.appendReminder` 注入「【系统提醒】你上一轮发言超过 N 字，本轮请精简输出」并**注入即消费清除**（仅下一轮生效，不持续、不广播旁白、无前端 UI、玩家无感知）；三处 system 构建点统一接入（buildContext=并行路径 / generateWithContext / generateWithContextStream=串行路径，流式降级回退不重复注入）。②**D1 主控候选话术（类 demo 选择支）双机制融合**——并行 v60修订3 已落地后端方案（POST /api/round/suggest：LLM 结构化生成 + fallbackSuggestions 规则兜底 + 前端 refreshSuggestions/liveSuggestions），本批按任务书「推荐先做②前端候选」补纯前端零成本候选（galChoices.ts：继续聊聊/换个话题吧/追问刚才提到的「最近 AI 发言前 10 字」…/说说你自己，每条 ≤40 字硬约束）；GalChoiceBar **互斥融合**：后端候选优先（按用户硬规则 ≤40 字过滤）→ 空时回退前端候选（isPlayerTurn 门控：liveMode + general + SSE open + 空闲未发送）——后端方案质量更高（场景感知、真实 LLM），前端方案零成本恒可用（旧 jar/后端失败时兜底），两机制互斥显示防重复。③**任务 C 空角色兜底修复收尾**——后端 400 由并行 v60修订2 落地（SessionController 空 characters → 400「至少需要一个角色」移除「助手」兜底），本批前端把 GalLivePanel「空角色 · 单人快速局」预设由 disabled 方案改为**移除**（更清晰，守卫保留）。
- **原因**：①用户反馈「AI 发言太长老是刷屏」→ 要求自动提醒且明确「仅下一轮生效、不持续、不广播、无 UI、玩家无感知」；提醒注入位置选系统提示（两路径最终都经 Agent 构建 SYSTEM 消息），检测位置选落盘前（串行 executeRoundSerial 与并行 runRound Step4 两处对称，不漏检）；阈值配置化对齐 D-004「阈值勿 hardcode」纪律。②任务书要求评估①后端生成/②前端生成——并行批次已按①实现且质量更高（带场景与最近对话上下文），本批按推荐做②并与其融合为「后端优先+前端兜底」分层，兼顾质量与零成本可用性；「每条 ≤40 字」为用户硬规则 → 后端 LLM 候选也在渲染层按 ≤40 过滤，全过滤回退前端。③用户实测「没选助手但对局里出现助手」根因即空 characters 兜底 new Persona("助手")；与 startScene 一致返回 400，前端移除可触发路径。
- **放弃**：①D2 用全局旁白/广播提示（用户明确「不广播旁白、玩家无感知」）；提醒做成持久限制（用户明确「仅下一轮生效」）；前端做超长 UI 提示（无 UI）。②D1 删除并行批次后端方案只留前端（后端方案质量更高且已接线，融合而非二选一）；候选在 GalChoiceBar 双机制同时渲染（互斥防重复）。③任务 C 保留空角色预设仅 disabled（移除更清晰，且该预设本无价值）。
- **影响**：①后端 2 文件 + yml 双份 + 测试 RouterServiceReminderTest 4 用例（串行注入→再下轮清除+短发言不受影响 / 正常不触发 / 汉字计数 / 并行双角色），串行+并行两路径锁定；全量 591 run / 91 类 / 1 failure（既有 flaky ScriptGamePrivateChatTest 单跑 8/0 复证）；②前端 galChoices.ts 新增 + GalChoiceBar 融合渲染 + GalLivePanel 预设移除，npm build 115 modules（dist/index-Det9X7SH.js 与并行 v60修订3 同哈希，未同步 static）；冒烟 12/12 + CDP 真机走查 21/21 PASS；③D2 仅注入一般模式 runRound 链路（剧本杀/狼人杀讨论引擎走 ConversationManager 独立路径未接入，属设计范围外，P1 可按需对齐）；8000 旧 jar 无本批与并行 v60修订3 后端改动，待主会话统一打包重启生效；④未 git commit（统一 gate 未获授权）。
---

## 2026-08-11（P-0811-B：角色生成五层拟合 + 场景上下文注入）

### D-052 五层 persona 生成进 LLM 角色生成链路 + 场景上下文注入（主人拍板方案 A）
- **决策**：①**场景上下文注入**——GeneratorService.generateCharacter 新增三参重载（keywords, sceneName, sceneDescription），旧单参签名保留委托零破坏；有场景时 prompt 注入「当前场景：{name}（{description}）。生成的角色必须与场景背景契合、人物身份与场景自洽」，无场景时通用生成（向后兼容）；prompt 全中文（旧默认英文 Generate an interesting character 移除）。②**五层 persona 拟合进 LLM 角色生成链路**——生成输出升级为五层 persona JSON（严格按 docs/persona-五层卡-格式.md 契约：表层 name/appearance/summary/personaDesc/voice/background + layer0[]（行为规则 5-8 条）/layer1{}/layer2{}/layer3{}/layer4{}/contrast{}/humanDetails[]），maxTokens 400→4000（D-023 纪律：结构化大 JSON 用 400 必截断；4000 为 LLMClient 上限内，与剧本生成同档）。③**生成卡自动挂载**——POST /api/characters/generate 结果含五层键（LAYER_KEYS 任一命中）→ 按 P-0810-10 既有机制存入 CharacterController.personaCards 内存库（ConcurrentHashMap<角色名, card>，键名对齐导入卡契约），之后任何 Persona 构建点经 attachPersonaCard 自动挂载五层（Layer0-4 进 LLM 系统提示）；对外严格不透出五层——响应只回表层（name + appearance/summary + layers 键名列表，参考 POST /{name}/persona 写法），GET /api/characters 的 getAll 只附加表层 appearance/summary。④**宽容解析**——LLM 输出缺层/缺字段不崩（layer0/humanDetails 字符串归一单元素列表，其余类型不符原样保留，Persona 渲染端宽容跳过），完全失败回退旧 4 字段默认行为（与改造前一致）。⑤前端 client.ts generateCharacter +可选 sceneName/sceneDescription，ScenePage（activeScene）/MaterialPage（editingScene）传当前场景，SettingsPage 无场景仅关键词。
- **原因**：①用户实测根因（已实证）——generateCharacter prompt 完全不含场景上下文（默认英文提示），generateScene 只出 {name,description} 不生成角色，两链路脱钩，生成的角色与场景毫无逻辑关联；②既有 P-0810-10 五层 persona 机制（PersonaCardLoader/personaCards/attachPersonaCard/buildSystemPrompt 五层模板）已就绪，角色生成链路是最后一个未接入五层的构建点——接入后 LLM 生成的角色天然获得完整表演指令（行为规则/身份/表达风格/情感模式/冲突链），并与手工导入卡同一渲染路径；③D-023 教训：结构化大 JSON 用 400 必截断（全员走兜底、输出不可观测），五层卡输出量级与剧本 JSON 相当，按同档 4000 处理；④对外不透出纪律（P-0810-10 硬性）：五层为内部表演指令，只进 LLM 系统提示，绝不进任何对外 API。
- **放弃**：①只修 prompt 加场景描述不改输出结构（治标不治本，生成角色仍无表演指令）；②生成角色落库进 characters 表/角色库（generate 端点既往不写角色表，H2 四字段机制零改动，落库由前端保存流程负责——保持既有行为，避免重复生成撞名 409 连锁）；③maxTokens 用 2000（五层卡+表层输出量级接近完整剧本 JSON，4000 更稳且为既有剧本生成同档先例）；④把五层卡写进 H2/静态资源（personaCards 内存库独立机制 P-0810-10 已定，重启后默认资源卡兜底，保持一致性）。
- **影响**：①GeneratorService 返回类型 Map<String,String>→Map<String,Object>（表层键名不变 name/persona/voice/background，唯一调用方 CharacterController.generate 同步改造，前端 SettingsPage 响应消费修正 result.character→result.name）；②生成角色 name 命中 personaCards 后，getAll 自动附加 appearance/summary 表层（与 P-0810-10 导入卡同机制）；③测试：新增 GeneratorCharacterFiveLayerTest 6 用例（五层解析挂载/场景注入 prompt 断言/对外不透出/向后兼容/maxTokens≥2000 防回归），全量 602 run / 92 类 / 1 failure（唯一失败=既有 flaky ScriptGamePrivateChatTest 单跑复证 PASS）；④前端 dist/index-D_6i8NVh.js（SHA256 2519B4B8...）未同步 static（惯例：dist 构建+记录哈希，static 零改动）；⑤运行成本：仅角色生成调用点 token 预算上升（400→4000），对话主链路不受影响；⑥未 git commit（统一 gate 未获授权）。

---

## 2026-08-11（P-0811-D 生成角色自动落库 + 五层卡持久化 + 旧角色批量升级）

### D-053 五层卡外部目录持久化（data/persona/*.json）+ 生成角色自动落库 + 旧角色批量升级端点
- **决策**：①**生成角色自动落库**——POST /api/characters/generate 生成成功后自动保存到 H2（复用 databaseService.saveCharacter 四参重载，对齐 create 端点逻辑）+ 同步内存列表（getAll 立即可见）；撞名（库内已有同 name）→ 409 复用 conflict()，不落库不挂卡；响应补 saved=true（已有返回结构上增量不破坏既存消费）。②**五层卡外部目录持久化**——卡写入 data/persona/{角色名}.json（UTF-8 无 BOM，目录自动创建；文件名经 sanitizeFileName 安全化防 LLM 生成非法字符）；三个挂载点（generate 自动落库 / POST /{name}/persona 导入 / 批量升级）均写盘；加载侧 PersonaCardLoader 新增 EXTERNAL_DIR 静态字段 + setExternalCardsDir + ensureLoaded 合并 classpath 默认卡 资源 + 外部目录（同名外部优先覆盖），目录不存在/不可读静默跳过零破坏；角色改名/删除跟随文件操作（renameFile/deleteFile best-effort）。③**路径可配**——yml 双份 
oleplay.game.persona-cards-dir（主 yml 默认 ./data/persona，test yml 指向 target/persona-test-cards），CharacterController @Value 注入 + setPersonaCardsDir 测试钩子，对齐 D-004「阈值勿 hardcode」纪律。④**旧角色批量升级**——新端点 POST /api/characters/upgrade（body 可选 max_roles 限制缺省全部）：异步虚拟线程遍历所有无五层卡角色 → 对每个调 GeneratorService.generateCharacterForUpgrade（以角色现有 persona/voice/background 拼接为上下文，不做 100 字截断保持身份一致；scene 上下文不传）→ 生成成功：五层卡写盘 + personaCards 挂载 + 表层替换（persona/voice/background 用生成结果覆盖，显式「替换」动作仅升级路径，与 D-047/P-0810-10「不覆盖用户显式内容」的常规挂载规则区分）+ H2 更新表层 4 字段；有卡跳过（幂等）；单角色失败跳过继续 log.warn；汇总 {upgraded, skipped, failed, names[]} 经 GET /api/characters/upgrade/status 查询。响应立即返回 {started:true}。⑤**前端适配**——SettingsPage.generateCharacter 从「回填表单手动保存（create）」改为「生成后打开表单为编辑态」（editTarget=新角色名，保存走 PUT updateCharacter），避免再次 create 撞名 409；表单展示生成的角色名/persona/voice（从 refreshData 列表取）；ScenePage/MaterialPage 仅加流程验证注释（loadState 后新角色自动出现无需手动 create）。
- **原因**：①主人反馈生成角色不自动写角色表（已实证：generate 只调 LLM 生成不落库；ScenePage/MaterialPage 生成后直接 loadState+closeModal 结果丢弃；SettingsPage 是生成后回填表单手动保存）→ 生成即落库消除空窗。②实证角色表 13 角色全部旧式（appearance/summary 空、无五层卡）；personaCards 纯内存（ConcurrentHashMap）重启即失，五层卡无持久化→ 外部目录持久化是五层卡「重启不丢」的最小方案（与 H2 ./data/roleplay 同 data/ 目录族，零新依赖）。③外部目录合并 classpath 默认卡且外部优先，既保持已有 resources/persona/ 三张默认卡逻辑不动（向后兼容），又允许生成/导入/升级的卡在重启后自动恢复（attachPersonaCard 回退外部目录卡）。④旧角色表所有角色无五层卡，需一次性批量升级为五层 persona 格式，异步+幂等+失败单跳进保证可反复执行
- **放弃**：①把五层卡并入 H2 角色表（characters 表无 layer 列，加列需迁移 characters 表，与 D-013/D-014「不扩展表结构」纪律冲突——外部目录/json 零表结构变更）；②静态加载器改 Spring bean 注入（保持静态工具类设计，CharacterController @PostConstruct 调用 setExternalCardsDir 注册配置值即可，测试经 resetForTests 隔离零风险）；③generateCharacterForUpgrade 走公共的 100 字截断（角色 persona 最多 2000 字，截断到 100 字不可能保持身份一致——升级专用重载取消截断用 1000 字上限）；④前端新增升级 UI（部署后由主会话调端点触发 13 角色真实 LLM 批量，status 端点可查进度）
- **影响**：①全量 mvn test **623/97/0**（596 基线 + 本批 14 用例 + 并行批次新增零破坏；LongTextStabilityTest PASS）；②新增 1 配置键（persona-cards-dir，yml 双份）+ 2 新端点（POST /api/characters/upgrade + GET /api/characters/upgrade/status）+ 3 新测试类（CharacterGeneratePersistTest 7 / CharacterUpgradeTest 5 / PersonaCardsDirConfigTest 2）+ GeneratorCharacterFiveLayerTest 适配 auto-persist 行为变更；③前端 SettingsPage 改编辑态流程 + ScenePage/MaterialPage 流程注释，npm build 产物 index-Db5LNOEs.js（SHA256 40040FD0...，dist 未同步 static）；④行为变化——generate 成功后角色已入库（getAll/列表可见 + H2 持久化），不再需要前端手动 create；SettingsPage 生成后走 PUT 更新而非 POST create；五层卡重启后自动恢复（外部目录）；⑤兼容性——无 @Value 绑定时落盘关闭零破坏（测试直构 / 旧 jar）；旧角色表无卡不影响正常 CRUD；rollback 操作不牵涉五层卡（只动内存 personaCards）；⑥禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路/static）零改动；⑦部署后主会话调 POST /api/characters/upgrade 触发 13 角色真实 LLM 批量升级（轮询 GET /api/characters/upgrade/status 等待完成）；⑧未 git commit（统一 gate 待授权）


## 2026-08-11（P-0811-E：前端所有生成接口统一走真实 LLM）

### D-054 前端所有「AI 生成」mock 接口统一替换为真实后端 LLM 调用，mock 降级为失败兜底（主人拍板）
- **决策**：①前端 demo2 与 demo 两套 demo 的 9 处「AI 生成」mock 调用点（角色生成×2 / 剧本杀剧本生成×2 / 一般场景生成×3 / 地图生成×3）全部接对应真实后端端点——角色生成 POST /api/characters/generate（带剧本场景上下文 title+背景，P-0811-B/D 能力，后端自动落库、撞名 409）、剧本杀剧本生成 POST /api/session/script/generate（Schema v1，characters 数量决定角色数，不建局）、一般场景生成 POST /api/scenes/generate（{name,description}）、地图 POST /api/scenes/map（带 theme=LLM 全量生成 / 无 theme=BSP 确定性）；②**mock 保留为失败兜底**——LLM 失败/超时回退原 mock 函数 + 用户可见提示「LLM 生成失败，已用本地模板兜底」（不中断用户流程），demo2 兜底提示经 store genNotice 在生成后跳转的角色选择页横幅展示；③**撞名 409** 不兜底 mock（mock 角色名同样可能撞名），弹窗保持打开提示「角色名已存在，请换个描述」；④**映射函数**——后端 Schema v1 → demo2 RoleCard/MurderScript/GeneralScript（demo2/mappers.ts：v1RoleToRoleCard/v1ScriptToMurder/assembleGeneralScript）、demo → DemoScript（v1ToDemoScript 内联）；⑤**异步与 loading**——生成中按钮 loading+「AI 正在构思…」文案防重复点击（AiGenBox 既有 busy 门控 + 按钮 disabled + 地图生成中提示条）
- **原因**：①主人明确要求「前端所有 AI 生成 mock 接口全部换成对应的真实 LLM 接口」——demo 是能力展示层，mock 生成的内容不可玩、与后端真实生成能力（五层 persona/剧本 schema v1/LLM 地图）脱钩；②mock 全删则 LLM 不可用时 demo 完全不可用（无 key/网络失败/超时），降级保留是「演示可玩性」底线（对齐 G1 无 key 降级传统）；③映射层独立成文件（mappers.ts）而非散落页面——多调用点复用 + 单测可验证（esbuild 冒烟 20/20）
- **放弃**：①直接删 mock 函数（降级兜底需要）；②撞名 409 自动加序号重试（提示换描述更简单直接，简单优先）；③demo2 一般模式场景的角色/地图也走 LLM 生成（任务范围只要求场景描述接 LLM，角色/地图为本地结构装配；地图 LLM 生成由 demo/ 与对局侧场景地图承担）；④改后端（任务明确不改后端——端点全部既有，零改动基线验证）
- **影响**：①前端 9 处全部接上真实 LLM，mock 降级兜底 + 可见提示；②client.ts +scriptGen 接口 + generateCharacter/generateScene 超时放宽（240s/180s，真实 LLM 耗时）；③新增 demo2/mappers.ts（3 个映射函数）+ demo/ScriptGenPage 内联 v1ToDemoScript；demo2 store +genNotice（兜底提示跨页可见）；④npm run build 通过（116 modules，dist/index-32JSLmnM.js SHA256 740340E5...，dist 未同步 static）；esbuild 冒烟 30/30（mappers 20 + client 10）；⑤后端零改动，全量 mvn 616/95/1（唯一失败=既有 flaky LongTextStabilityTest 堆测量脆性单跑复证 PASS）；⑥⚠️ src/demo/* 为孤儿组件（App.tsx 只接 demo2/App2，旧 bundle 亦 0 命中 demo/ 串）——demo/ 改动编译通过但不在当前 bundle（既有状态非本批引入）；⑦未 git commit（统一 gate 待授权）

- **【追加（主人实测反馈）场景+配套角色一次生成】**：generateScene 从「只出 {name,description}」升级为「场景 + 4~6 个配套角色一次生成」（每角色与 generateCharacter 同结构五层 persona；maxTokens 300→4000；roles 缺失→空数组零破坏），配套角色经 CharacterController.persistGeneratedRole 自动落库（撞名自动加序号后缀「名字_2」重试避免 409 中断整批；落库失败跳过但保留响应 saved=false；响应只回表层不泄五层 P-0810-10 硬性）；前端 demo2 一般模式与 demo/GeneralGenPage 把 LLM 角色作为新场景角色列表展示（demo2 替代预置三占位角色，点开场景即见配套角色）。原因：主人实测「生成场景后角色还是原来的三个」——场景与角色两链路仍脱钩；决策：单次 LLM 输出场景+角色（一次调用省两次延迟且角色天然契合场景）而非前端二次调 generateCharacter。影响：全量 mvn 622/96/1（唯一失败=既有 flaky 单跑复证 PASS）；前端 dist/index-BauzW6pf.js（SHA256 25879234...）。

---

## 2026-08-11（P-0811-F 批次）

### D-055 生成场景联动生成配套角色并自动落库（主人拍板，P-0811-F 收尾）
- **决策**：①**generateScene 单次 LLM 输出「场景 + 4~6 个配套角色」**（{name, description, roles[]}，每角色结构与 generateCharacter 一致：表层 + 五层 layer0-4/contrast/humanDetails；prompt 中文要求「角色必须与场景背景契合、人物关系自洽」；maxTokens 300→4000 根治多角色大 JSON 截断——D-023 教训同款）；②**配套角色自动落库**——POST /api/scenes/generate 每角色经 CharacterController.persistGeneratedRole 落库（内存列表 + H2 四字段 + 五层卡 persistCardToDisk + personaCards 挂载，与 P-0811-D generate 端点同机制，SceneController 复用既有 CharacterController 依赖）；③**撞名不 409**——角色名已存在自动加序号后缀「名字_2」「名字_3」重试（最多 10 次），落库失败跳过但保留在响应（saved=false + 表层），前端仍可用；④**响应只回表层**——roles 条目 {saved,name,appearance?,summary?,layers 键名列表}，layer 内容绝不回（P-0810-10 不透出纪律）；⑤**前端联动**——demo2 ScriptGenPage 一般「AI 生成场景」把返回 roles 经 v1RoleToRoleCard 映射为 RoleCard 作为新场景角色列表（替代预置「引路人/同行者/守望者」三占位，用户进该场景即见配套角色），demo/GeneralGenPage 同款展示
- **原因**：①主人实测「生成场景后，场景角色还是预置的三个」——根因 generateScene 只出 {name,description} 不产角色，前端接了 api.generateScene 却拿不到 roles；②P-0811-E 会话中途结束，其追加需求代码写入工作树但未编译通过（GeneratorService 缺 ArrayList import），本批收尾修复并补全测试/文档；③场景与角色一次 LLM 生成而非前端二次调 generateCharacter——单次调用省延迟且角色天然契合场景（LLM 在同一上下文中编排，避免两链路脱钩的既有缺陷）
- **放弃**：①generateScene 保持旧 {name,description} 由前端逐个调 generateCharacter 补角色（两链路脱钩正是本次缺陷根因，二次调用角色与场景契合度差）；②撞名直接 409 中断整批（生成场景是批量写角色场景，一个撞名应加序号而非失败）；③响应回五层内容（不透出纪律是 P-0810-10 硬性，前端只需表层展示）
- **影响**：①全量 mvn **633 run / 99 类 / 0 failures / 0 errors**（623 基线 + SceneGenerateRolesTest 4 + GeneratorSceneRolesTest 6（P-0811-E 遗留测试类本批编译修复后通过））；②前端 npm build tsc 0 + vite 116 modules → dist/index-BGfj1BV2.js（SHA256 C1C30C8F...，dist 未同步 static，主会话统一打包）；③兼容性——roles 缺失/空数组 → 空列表，旧调用方只取 name/description 行为与旧版一致（零破坏）；④已知限制——场景生成是真实 LLM 调用（4000 tokens，4~6 角色耗时可达数十秒，前端超时 180s 已放宽）；demo/ 模块为孤儿组件（App.tsx 不 import，编译通过不参与 bundle，与 P-0811-E 已注明一致）；⑤未 git commit（统一 gate 待授权）


## 2026-08-14（P-0813-A 批次补登记）

### D-056 非 2D 一般模式自动续轮：roleplay.round.auto-continue-ms（默认 3000ms）后台自续轮 + 首轮自动触发修复（P-0813-A 补登记）
- **决策**：①**新配置 roleplay.round.auto-continue-ms**——默认 3000ms，0=禁用（yml 双份：application.yml + application-test.yml，注释标 0=禁用语义）；AppConfig.RoundConfig +autoContinueMs=3000 绑定（对齐 D-004「阈值勿 hardcode」纪律）。②**后台自续轮机制**——RouterService.runRound 末尾调 scheduleAutoContinue（L965，调用时持有方法锁）延时调度下一轮：复用 runRound(null,null) 全链路（入史/SSE/轨道分配全保留）；静态共享 2 daemon 线程 ScheduledExecutorService（L109 AUTO_CONTINUE_SCHEDULER），按 session 隔离 pending（L117 pendingAutoContinue + L119 autoContinueScheduledRound，每会话至多一个 pending 替换式调度）；fireAutoContinue（L991，方法级 synchronized 与 runRound 天然串行互斥）触发前复查：pending 仍在、会话活跃（running + agents 非空）、仍一般模式、autoContinueScheduledRound == roundCount（轮次未被外部推进，防重复轮）。③**玩家发言可打断**——runRound 入口 userInput 非空即取消 pending（L206）；stop()（L572）/initSession()（L242）/loadSession()（L518）清理 pending 防泄漏；续轮完成后 runRound 末尾再次 scheduleAutoContinue 自续（无限循环直到 stop/终局）。④**仅一般模式生效**——isGeneralMode 守卫（free/protagonist/multi_track/director），狼人杀/剧本杀走各自状态机零影响；runTurns（L1744）/runAutoRounds（L1796）手动批量置 manualRoundBatch（L121）不触发续轮（防批量后多跑一轮）；goalsAchieved 剧情终局停止续轮。⑤**SessionRegistry.createRouter setAutoContinueMs 显式透传**（@Value 仅对 Spring bean 生效；SessionRegistry @Value autoContinueMs + createRouter setAutoContinueMs 注入，RouterService 直构实例零影响，同 serialRound 模式）。⑥**测试**——新增 RouterServiceAutoContinueTest 8 用例（含手动批量接管场景）。
- **原因**：修复「一般模式（导演模式/无玩家）进对局后只跑第一轮就永久卡住」——根因（tmp/调查-一般模式卡住-20260813.md 实证）：triggerAutoFirstRound 仅起局自动一轮，之后无自动驱动源；导演模式前端隐藏输入框/候选区，玩家也无法手动驱动——否则只跑第一轮就卡死。auto-continue 即补上「首轮之后每轮完成自动调度下一轮」的后端驱动源。
- **放弃**：（补登记资料有限，台账/代码注释未见明确「放弃」记录；从实现推断）①不用前端轮询/手动驱动（导演模式前端已隐藏输入框/候选区，玩家无驱动入口，且违背后端自洽）；②续轮不另建独立执行路径（复用 runRound(null,null) 全链路保证入史/SSE/轨道语义与手动轮完全一致）；③不做全局调度器跨会话排队（按 session 隔离 pending + 替换式调度，会话间互不干扰，fire 前复查轮次防重复）。
- **影响**：①涉及文件——src/main/java/com/roleplay/engine/service/RouterService.java（+autoContinueMs @Value L92 +AUTO_CONTINUE_SCHEDULER L109 +pendingAutoContinue L117/autoContinueScheduledRound L119 +manualRoundBatch L121 +scheduleAutoContinue L965 +fireAutoContinue L991 +cancelPendingAutoContinue L1007/hasPendingAutoContinue L1014/cancelPendingAutoContinueLocked L1019 +setAutoContinueMs L1332；runRound 入口取消 L206/末尾调度 L837；runTurns L1744-1753、runAutoRounds L1796-1806 manualRoundBatch；stop L572/initSession L242/loadSession L518 清理）、service/SessionRegistry.java（+@Value autoContinueMs +createRouter setAutoContinueMs 透传）、config/AppConfig.java（RoundConfig +autoContinueMs=3000）、application.yml + application-test.yml（+auto-continue-ms: 3000 注释 0=禁用）；测试新增 src/test/java/com/roleplay/engine/service/RouterServiceAutoContinueTest.java（8 用例）。②测试情况——定向 RouterService 相关 11 类 55/0 全绿（含新测试）；全量 mvn **647 run / 100 类 / 1 failure / 0 errors**（三轮 646/2 → 646/1 → 647/1；唯一失败=既有 stale ObstacleCollisionGridTest.horizontalMerge，P-0811-G Round 79 瓦片铺满改动后测试未同步，与本批无关未代改；LongTextStabilityTest 首轮 flaky 失败复跑通过=既有堆测量脆性）；mvn package 成功（fat jar 245,373,436B 验证含新代码；主名 jar 被运行中 PID 8484 文件锁 → 临时 pom finalName 构建 + copy 恢复主名 jar，pom 已还原零改动）。③与 D-024/D-027 串行调度关系——D-024 建立 roleplay.round.serial 开关、D-027 正式开启（serial: true，runRound 内同轮按序发言、后发言者上下文含前者本轮发言），auto-continue 在其之上是**轮间驱动层**：起局 triggerAutoFirstRound 自动开首轮（既有机制）→ 串行调度决定轮内发言顺序 → auto-continue 决定轮间推进节奏（延时 autoContinueMs 自动跑下一轮），三层叠加构成一般模式无人值守连续运转；不改变 runRound 单轮内串行/并行执行语义（与 D-024/D-027 正交），玩家发言取消 pending 与串行轮次天然互斥（fire 前 roundCount 复查防与手动轮重复）。④部署状态——8000 未重启（PID 8484，重启由主会话统一处理）；未 git commit（统一 gate 待授权）。台账 docs/修改记录.md #173 + TEST_STATUS Round 80/v80（2026-08-13）。

## 2026-08-14（P-0814-A：点击驱动对话模式改造）

### D-057 点击驱动对话模式：roleplay.round.playback-driven（默认 true）——一轮生成完即停，等「播出完毕」信号再下一轮（主人拍板，D-056 auto-continue 的后续演进）
- **决策**：①**新配置 roleplay.round.playback-driven**——默认 **true**（主人拍板新语义）；false=回退旧行为（D-056 auto-continue-ms 定时自续 + 2D pacing 间隔自动连跑）。yml 双份：生产 true / **测试 false**（Spring 上下文测试保持旧定时语义零破坏，新行为由测试显式 setPlaybackDriven(true) 验证，与 D-024 serial 的测试解耦先例同款）；AppConfig.RoundConfig 绑定；**auto-continue-ms 在 playback-driven=true 时被忽略**（不再定时自续，语义文档化于 yml/AppConfig/RouterService 注释）。②**一般模式轮间门（RouterService）**——runRound 末尾 scheduleAutoContinue 的 playback 分支不再设定时器，改置 `awaitingPlayback` 标志（守卫同旧：一般模式/活跃会话/未达终局/非手动批量）；新 `onPlaybackDone()`（POST /api/simulation/playback_done 无 group_id 路径）消费标志并 runRound(null,null) 驱动下一轮（synchronized 与玩家发言串行互斥；幂等：非等待态重复信号 no-op）。玩家发言（runRound 入口）即时驱动轮次（输入即推进，无需播出完毕信号——protagonist/free 模式不受等待态阻塞），轮完重新武装等待；stop/initSession/loadSession/runTurns/runAutoRounds 清等待防串场。③**2D 组轮间门（ConversationManager）**——startGroup 后台循环 executeRound 后 playbackDriven → `group.awaitPlayback()`（阻塞等信号；ConversationGroup 新增 monitor 闸门：awaitPlayback/signalPlaybackDone/wakePlaybackWaiters/isAwaitingPlayback，active=false 退出等待）；`notifyPlaybackDone(groupId)` 信号唤醒该组生成下一轮（一轮=组内成员按发言顺序各一句，executeRound 既有语义不变）；tick 对 awaiting 组跳过 idle 超时解散（导演思考/播放期间组不拆，防点击推进时组已散）；stopAll/dissolveGroup 唤醒等待者（防虚拟线程悬挂）。**仅 2D 世界实例注入**（SimulationService 10 参构造读 cfg.getRound().isPlaybackDriven()）——剧本杀/狼人杀各局 CM 不注入=旧行为零影响（模式守卫，与 P-0813-F pacing 同款接线）。④**统一信号端点**——POST /api/simulation/playback_done（body{session_id?, group_id?}）：group_id 非空→2D 组推进（SimulationService→CM）；否则→一般模式 RouterService.onPlaybackDone（SimulationController 新增 4 参 @Autowired 构造注入 SessionRegistry，3 参旧构造委托 null 测试零破坏）。**选此端点而非 SSE**：信号是单向、低频、请求-确认式（要幂等结果），REST 天然契合且可重试；SSE 是后端→前端推送通道，反向复用会引入连接生命周期/重连补偿复杂度（前端重连后 SSE 信号易丢）。⑤**前端推进入口三处（导演聊天框）**——一般模式 GalGeneralView（round_complete SSE 置位 → 队列排空显示「▶ 推进下一轮」；导演模式无输入框，此即唯一推进入口）、2D SimGalChatPanel（组内新消息入队武装 → 队列排空显示推进按钮，group_id 路径）、经典视图 ChatMessageFlow（appStore.playbackArmed + AppLegacy round_complete 一般模式武装，防旧视图卡死）。⑥**玩家输入影响下一轮剧情=复用既有机制**（查证结论，零新机制）：一般模式玩家发言经 arbiter 主控旁白/角色直述入史（RouterService L651-679 memory.addMessage）→ 下一轮 buildAgentContext 读 memory.getAgentContext（D-024/D-027 串行同轮共享+跨轮历史）→ 影响下一轮；2D 玩家发言 setCurrentMessage → executeRound 玩家分支单次消费（P-0813-K）→ 组 messageHistory → 下一轮 DyadStrategy/TrackStrategy buildContext 读取。本批用测试锁定（RouterServicePlaybackDrivenTest ⑦ + ConversationPlaybackDrivenTest ⑥：round 2 上下文断言含 round 1 玩家输入）。
- **原因**：①主人拍板「对话不再后台无限自动跑」——D-056 的 auto-continue-ms 定时自续（3000ms 后台无限跑）与 2D pacing 间隔自动连跑（拉入轨道即自动对话）正是「后台无限自动跑」的两个源头；②2D 与一般模式统一为同一语义（点击驱动）+ 同一开关（roleplay.round.playback-driven）——避免两套模式两套开关的维护分裂；③「播出完毕」由前端判定最准确（打字机展示完成是前端可观测事实，后端不知道播放进度）——REST 信号 + 后端幂等门是「前端告知、后端权威」的最小契约；④无玩家（导演模式）播完即停是本需求明确项——导演模式前端的「▶ 推进下一轮」即推进入口（requirement 4），替代旧 auto-continue 无人值守语义；⑤玩家输入影响剧情是既有能力（D-024 入史机制），点击驱动改造只改轮间推进节奏、不动入史/上下文链路——零新机制即可满足，测试锁定防回归。
- **放弃**：①继续 auto-continue-ms 定时自续做默认（主人明确否定后台无限自动跑；保留为 playback-driven=false 的回退路径）；②播放完成信号走 SSE 通道（REST 更契合单向低频幂等语义，见决策④）；③2D 用 per-group 独立开关（统一开关+模式守卫已隔离剧本杀/狼人杀，无需第三套配置）；④等待态加超时自动推进（「播完即停」语义要求严格停；前端失败可重试，后端幂等防重复轮）；⑤导演模式做「一键推进全部组」（2D 各组独立等待、导演经加入组逐个推进——与「点击驱动」逐轮交互语义一致，全组推进留 P1 按需）。
- **影响**：①全量 mvn **741 run / 1 failure / 0 errors**——唯一失败=既有 stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改）；既有 flaky ScriptGamePrivateChatTest 复跑 8/0 PASS 复证；定向 3 类 19/0 + 回归 30/0（AutoContinue 8/Pacing 7/TrackPacing 9/JoinGroup 6）全绿。②新增 1 配置键（roleplay.round.playback-driven，yml 双份）+ 1 端点（POST /api/simulation/playback_done）+ 3 测试类 19 用例 + 前端三处推进入口 + static 同步（index-R_qwgkHY.js/index-DQoW5Uya.css，SHA256 dist↔static 一致，清旧产物）。③兼容性——playback-driven=false 逐字节回退旧行为（RouterServiceAutoContinueTest 8/0 原样通过）；剧本杀/狼人杀 CM 实例不注入零影响；RouterService 直构默认 false（@Value 仅 Spring bean 生效，测试 yml false 与直构同语义）。④已知限制——等待态下旧前端（未发 playback_done）会停轮（新语义预期，部署需前后端同步）；2D 非当前组在无玩家时不推进（需求「无玩家时也停」的预期行为）；经典视图推进按钮依赖 AppLegacy SSE（Gal 视图为主入口，经典为回退）；SimGalChatPanel 重挂载会重放 worldMsgs 并重新武装（后端幂等防重复轮）。⑤与并行批次共存——P-0813-A/I/F/H/K 全部保留（RouterService auto-continue 仅语义分支化、CM 仅新增闸门分支，未触碰其 pacing/日程/串行队列逻辑）；禁动文件（ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路）零改动；8000 未重启；未 git commit（统一 gate 待授权）。台账 docs/修改记录.md #184 + TEST_STATUS Round 90/v90 + 并行作业登记 P-0814-A。

## 2026-08-14（P-0814-B：P-0814-A 验收问题修复批次）

### D-058 P-0814-A 验收问题 6 项修复：executeRound 调序 + 卡死全覆盖 + 2D 输入=点击 + 前端归并排序 + UI 收尾（P-0814-B）
- **决策**：①**executeRound 调序（玩家消息先入史再构建上下文）**——ConversationManager.executeRound 在 prepareContext 前对玩家控制成员做预过账：currentMessage 非空且非「(主控」前缀 → `group.recordTurn` 入组 messageHistory + 清空 currentMessage；prepareContext 构建的上下文（MERGED 对话记录/Dyad 记录+「刚才说」回退）当轮即含玩家发言，修复「玩家发言被消费那一轮 AI 看不到、下一轮才看到」的错位一轮；入史后玩家分支 natural 跳过（currentMessage 已空）→ 不进 responses → processResults 不重复记录（ISOLATED 语义零变化）；玩家消息不再进 convEntry/recentConversations（前端本地回声已覆盖展示，无消费者依赖该路径，测试核实）。剧本杀/狼人杀讨论（pendingHumanEvents 经 buildRoundGate 入史）不受影响（成员非 playerControlled）。②**卡死全覆盖**——a) 无玩家组等待超时自动解散：新配置 `roleplay.round.group-await-timeout-ms`（默认 30000，yml 双份，AppConfig.RoundConfig 绑定，SimulationService 构造注入 ConversationManager）；tick 解散判定三分支：非活跃→解散 / awaiting 且无玩家成员且 idleMs>超时→解散（AI-AI/单 AI 组防 awaitPlayback 永久阻塞角色冻结）/ awaiting 且有玩家→保留（玩家输入/点击唤醒）/ 非 awaiting idle 超时→原语义；b) 等待信号改**计数器持续置位**（ConversationGroup：signalPlaybackDone 计数+1、awaitPlayback 等待期间每信号唤醒一轮、消费在同步块内完成——消除「布尔置位+进入清位/唤醒后清位」双重丢失竞态，连点 N 信号=N 轮不丢、每信号至多推进一轮防堆积）；c) 玩家输入唤醒本组等待：SimulationService.sendUserMessage 追加 `conversationManager.wakeGroupForAgent(agentName)`（AI-user 组解卡；组在等待态→信号唤醒生成回复轮，不在任何组→false 由 tick 建 DYAD 原路径）；d) 单 AI 组（玩家离开后组内仅 1 AI）走同一等待/超时路径不悬挂（leaveGroup 成员非空不解散、组继续运行 → 无玩家 → 超时自动解散）。③**2D 输入=点击**——后端 sendUserMessage 唤醒所在组（②c）+ 前端 SimGalChatPanel liveSayOverride 发送成功后自动 `simPlaybackDone({group_id})`（组在等待态时后端已唤醒生成回复轮，此信号再推进一轮 AI 续接；信号计数幂等）。④**前端消息归并排序**——PhaserSimulationView worldMsgs 时间戳从 tick 数改为**首见 epoch ms**（worldTsRef 签名→首见时刻），allMsgs = 世界消息∪玩家回声按 ts 升序稳定排序（玩家回声不再永远拼在 worldMsgs 之后）。⑤**UI 收尾**——三处推进按钮统一为新组件 `PlaybackNextButton`（src/gal/，统一 galg-playback-next 样式 + 固定高度插槽 .playback-next-slot 常驻渲染，显隐不引起布局跳动/闪动）；导演模式武装加轮询兜底（GalGeneralView refreshMeta 5s 轮询比对 /api/state round：round>挂载基准轮即武装——round_complete SSE 错过也能出按钮；ChatMessageFlow 同款轮询兜底）；收紧武装条件（挂载基准轮机制：重挂载回放旧历史不武装、仅新轮次武装；SimGalChatPanel 首批 worldMsgs（挂载回放）不武装、后续新消息才武装）。
- **原因**：①P-0814-A 真机验收 6 项问题（玩家消息不进上下文/UI 多余异常/卡死全覆盖/AI-user 组卡死主因/玩家输入位置乱序/2D 输入未触发自动下一句）根因已取证（任务书根因摘要，实施时逐项核对行号）：executeRound 顺序错位一轮；awaitPlayback 永久阻塞无人唤醒（sendUserMessage 不唤醒 + tick idle 守卫跳过等待组）；信号布尔置位进入清位连点丢一次；localMsgs 恒拼 worldMsgs 之后；simSend 只存消息不发 playback_done。②「输入=点击」是需求语义（一般模式已有：玩家发言直接驱动 runRound），2D 侧补齐后端唤醒+前端信号双保险。③超时解散只作用于无玩家组：有玩家组靠输入/点击唤醒（玩家可能正在读对话，不能擅自解散）；对齐 D-004「阈值勿 hardcode」纪律配置化。
- **放弃**：①有玩家组也做等待超时解散（玩家在场交互中，解散破坏对话连续性；输入唤醒已解卡）；②信号改 per-signal 队列/令牌（计数器已满足「不丢轮+每信号至多一轮」，队列过度设计）；③玩家消息继续进 responses 同时预入史（processResults 会重复记录，需改五策略；预入史+跳过 responses 改动面最小且语义等价）；④2D 输入后不补 playback_done（「输入=点击」要求输入后 AI 续接无需再点；补信号=回复轮+续接轮，与一般模式输入驱动一轮语义对齐后自然延续）；⑤轮询兜底做 SSE 事件补偿重放（轮询比对轮次更简单且不依赖 SSE 通道）。
- **影响**：①全量 mvn **739 run / 2 failures / 0 errors**（741 基线 + 本批 ConversationPlaybackDrivenTest +3（⑦ AI-AI 超时解散/⑧ 玩家输入唤醒+同轮入史/⑨ 单 AI 组不悬挂）+ SimulationServicePlaybackWakeTest 1（sendUserMessage 唤醒全链路）；两失败均既有：stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改）+ LongTextStabilityTest 堆测量脆性 44.3%>30%（#37/#40/#41 同款，单跑 1/0 PASS 复证）；ConversationPlaybackDrivenTest ③改断言（布尔置位→计数器：连点 2 信号=2 轮不丢）、⑥改断言（round 1 消费当轮上下文即含输入——原错位断言替换）；定向 5 类 29/0 全绿。②新增 1 配置键（roleplay.round.group-await-timeout-ms=30000，yml 双份）+ ConversationGroup 信号计数器 + ConversationManager wakeGroupForAgent/setGroupAwaitTimeoutMs/executeRound 预过账 + SimulationService sendUserMessage 唤醒；③前端：新组件 PlaybackNextButton + PhaserSimulationView 归并排序 + SimGalChatPanel 输入自动推进/首批不武装 + GalGeneralView/ChatMessageFlow 轮询兜底；npm build tsc 0 + vite 118 modules → dist/index-BM5idMXO.js + index-EAvwCigB.css 已同步 static（SHA256 dist↔static 一致，清旧产物 index-R_qwgkHY.js/index-DQoW5Uya.css）；esbuild 冒烟 smoke_p0814b 3/3 PASS。④兼容性——有玩家组行为不变（输入/点击唤醒）；无玩家组从「永久等待」变「超时解散」（AI-AI 组 30s 无推进自动散，角色解冻）；playback-driven=false 路径零触碰（超时解散/唤醒仅 playback 分支消费）；剧本杀/狼人杀 CM 实例不注入零影响。⑤mvn package 成功：主名 jar roleplay-engine-1.0.0-SNAPSHOT.jar **249,914,291B**（mtime 2026-08-14 19:04:11，fat 验证 BOOT-INF 新类 + 内嵌 index-BM5idMXO.js + application.yml 含 group-await-timeout-ms）；8000 未重启（PID 11800 跑旧版，重启由主会话统一处理）；未 git commit（统一 gate 待授权）。台账 docs/修改记录.md #185 + TEST_STATUS Round 91/v91 + 并行作业登记 P-0814-B。

## 2026-08-14（P-0814-C：UI 修正批次——删推进按钮改自动推进 + 删 TTS 预留 + 修历史记录面板）

### D-059 自动推进取代「▶ 推进下一轮」按钮：播放完成事件驱动（非定时器）+ awaiting_playback 精确武装（主人拍板，D-057 点击驱动的后续演进）
- **决策**：①**删三处「▶ 推进下一轮」按钮**（GalGeneralView/SimGalChatPanel/ChatMessageFlow + PlaybackNextButton 组件/样式全清）——主人明确不要按钮（"谁让你加的……UI 下面难看死了"），推进入口全部改为自动。②**自动推进语义**：前端「播放完成」事件（打字机/消息队列排空）→ 自动 POST /api/simulation/playback_done（一般模式 session_id / 2D 组 group_id）；玩家输入自动触发（输入=点击，D-057/B 既有机制保留）；导演模式播完自动推进。**触发点是播放完成事件不是定时器**——保持 playback-driven 语义（生成完停、播完才下一轮），禁止退回 D-056 auto-continue 定时无限跑；settleMs 300 稳定窗（吸收轮次完成与消息入队的到达顺序竞态）+ minGapMs 800 安全地板（防空轮连发）仅作稳定性保护不驱动节奏。③**实测锁定后端端点阻塞式**（playback_done 同步生成完下一轮才返回，10-40s）→ 自动推进 hook 请求完成（.then）后自动补发 in-flight 期间到达的新一轮武装（防卡轮），失败由 onAdvanceFailed 延迟重新武装（不自动无限重试刷后端）；修复经典视图「effect 内 setTimeout + 同步清 armed → cleanup 取消未执行 timer → 永久停摆」的 React 陷阱（改为即时发信号）。④**/api/state 暴露 awaiting_playback**（RouterService.getState +1 行）——status 常驻 running（roundCount 在 runRound 开头递增）不可作「轮次完成」信号，前端轮询兜底改为 `awaiting_playback=true && round>lastFiredRound` 精确武装，替代 D-058 挂载基准轮 diff；自愈覆盖 SSE round_complete 错过/断线重连/挂载前轮次已播完三类漏武装（含首次进对局第一轮即停的场景）。⑤**经典视图武装补位到 useGameSse.ts**——D-057 的经典视图武装加在 AppLegacy（旧版入口，不参与构建的死代码）上从未生效，useGameSse（ChatPage 实际 SSE 桥）round_complete 才置 playbackArmed。⑥**历史抽屉删 TTS 预留块 + ttsReserve.ts 删除**——「历史记录出错」与「把下面的 tts 预留删掉」同一对象：历史抽屉底部 241px 高 TTS 预留占位（禁用控件+「后端未实现」徽标）被主人视为错误 UI；消息列表/回滚功能零改动（CDP 实测 rows=8 渲染正常）。⑦**验证**：CDP 端到端（临时 finalName 构建 roleplay-engine-p0814c.jar 起 8001 测试实例，验证后已杀）Gal 视图 7/7 + 经典视图 3/3 全 PASS（自动推进多轮递增无需点击、无按钮、历史抽屉无 TTS、无 console 错误）；全量 mvn 739/2（两失败均既有）。
- **原因**：①按钮是 P-0814-A「点击驱动」的产物，主人验收明确否定（要自动推进、一切无需点击按钮）——D-057 的「播完即停等导演点击」演进为「播完自动推进」；②「播放完成」由前端判定最准确（打字机队列排空是前端可观测事实），REST 信号 + 后端幂等门契约不变（沿用 D-057 选型）；③阻塞式端点决定前端必须「请求完成后补发在途武装」，否则一轮后永久卡死（CDP 实测复现）；④awaiting_playback 是「是否该发信号」的唯一权威状态，轮询兜底必须基于它才能精确（挂载基准轮 diff 在挂载前轮次已播完时漏武装——CDP 实测复现）；⑤删除死代码上的无效武装是修复经典视图自动推进的前提。
- **放弃**：①任何定时器驱动的自动推进（auto-continue 定时无限跑被主人明确禁止；minGap/settle 仅是稳定窗与安全地板，不驱动节奏）；②前端「播放完成」用 SSE 事件替代队列排空检测（队列排空已是权威且与按钮时代同源）；③无 awaiting 状态时用「round diff + 轮询重试 no-op 探测」推进（awaiting_playback 一行后端字段更精确、零请求噪声）；④TTS 预留保留为接口（主人明确要删，删除 ttsReserve.ts 占位桥，真实 TTS 链路 ttsPlayer/tts_* SSE 不动）。
- **影响**：①新增 1 个后端字段（awaiting_playback，加性无破坏）+ 前端新 hook（useAutoPlaybackDone）+ 删除 2 文件（PlaybackNextButton.tsx/ttsReserve.ts）+ 三视图删按钮改自动推进 + 经典视图武装补位 useGameSse + 历史抽屉删 TTS 块；②npm build tsc 0 + vite 117 modules → static 同步 index-B9zdRZnt.js/index-CF8r03BC.css（SHA256 一致）；③全量 mvn **739 run / 2 failures / 0 errors**（两失败均既有：stale ObstacleCollisionGridTest + flaky ScriptGamePrivateChatTest 复跑 8/0 复证），零新增；④兼容性——playback-driven=false 回退路径零触碰；剧本杀/狼人杀 CM 实例不注入零影响；旧前端（不自动发信号）会停轮（新语义预期，部署需前后端同步）；2D 未入组消息仅展示不推进（组无玩家由等待超时解散兜底，D-058 语义保持）；⑤后端小改需随主会话重启 8000 生效（前端 static 同步 + RouterService 字段同时上线）；未 git commit（统一 gate 待授权）。台账 docs/修改记录.md #186 + TEST_STATUS Round 92/v92 + 并行作业登记 P-0814-C。

## 2026-08-14（P-0814-E：一般模式一问一答修正）

### D-060 一般模式一问一答：有玩家播完停等输入（不自动发信号），无玩家（导演模式）才自动推进（主人 21:51 拍板，D-059 自动推进的语义修正）
- **决策**：①**玩家在场门控（前端）**——一般模式自动推进 hook（useAutoPlaybackDone）加 enabled=!hasPlayer：**有玩家**（livePlayerName 或 playerName prop 非空）时 AI 轮播完**不自动发 playback_done**（AI 绝不自动连说），候选选项自动出现等玩家输入；**无玩家**（导演模式）维持播完自动推进（无输入者，播完自动下一轮防卡死）。②**门控语义基于「玩家角色在场」而非「轮次驱动模式」**——D-057/D-059 的 awaiting_playback 轮间门保持不动（后端零改动），前端只是「有玩家时不消费这个门」：播完停 = 不发信号 = 后端自然停在 awaiting 态，玩家输入（liveSay→api.send→runRound 玩家分支 userInput 非空清 awaiting 直接驱动一轮）即下一轮，回应播完再停。③**首渲染竞态修复**——livePlayerName 由 enterLiveMode（effect 内）写入，首渲染前为空 → 门控误判「无玩家」→ 挂载时若自动首轮已播完（早于 SSE/挂载完成）会漏发一次信号（CDP 实测复现：有玩家会话多出第 2 轮）→ hasPlayer 同时读稳定 prop playerName（GameBridge 起局即传）封死该窗口（CDP 复验：严格停 1 轮）。④**经典视图同款门控**——ChatMessageFlow 自动推进 effect 加 store.agents.includes(store.currentPlayer) 守卫（有玩家角色=currentPlayer 在 agents 中→不自动发信号；导演/无玩家维持自动推进）+ 轮询兜底武装同步同款守卫。⑤**候选自动出现（保持 D-057/0811-G 语义零改动）**——GalChoicesArea isPlayerTurn 就绪条件（liveQueue 排空 && !current && !typing && liveStatus=open && !liveSending）即「停止等待」状态，AI 播完自动显示。⑥**2D 保持自动推进不动**（主人拍板：2D 群聊氛围保留，SimGalChatPanel enabled=!!groupInfo 不变）。⑦**验证**——esbuild 冒烟 8/8（React 桩对齐真实语义）+ CDP 端到端 10/10（真实后端 8000 + 真实 LLM：A 有玩家 7/7——round1 播完 pbCount=0 不连说、轮次停 1、候选自动出现、输入后 round2 回应一轮再停；B 导演 3/3——播完自动发 pbCount>=1、轮次自动推进到 >=2 无需输入）。
- **原因**：①P-0814-C 播完自动推进上线后 **AI+玩家场景 AI 连续说话**（玩家只发 2 条消息 AI 连说 6 句：R1/R2 连发、R4/R5 连发）——播完自动触发下一轮与「玩家在场」冲突：玩家不说话 AI 就继续说；②主人 21:51 明确「AI+用户场景要正常一问一答」——AI 说一轮→停→等玩家输入→输入后回应一轮→再停；导演模式（无输入者）才需要自动推进防卡死；③「玩家在场」是前端可观测事实（GameBridge 传 playerName / store livePlayerName），门控放前端最简（后端 awaiting 门复用，零后端改动）。
- **放弃**：①有玩家也自动推进（正是 AI 连说缺陷本身）；②后端加「有玩家不置 awaiting」开关（信号控制在前端语义下已足够，后端加开关反而引入双端状态漂移；前端不发信号后端自然停）；③2D 组内玩家也改输入驱动（主人拍板 2D 群聊氛围保留自动推进）；④仅用 livePlayerName 判定玩家在场（首渲染竞态实测漏发一次信号，需叠加稳定 prop）。
- **影响**：①前端 3 文件改动（GalGeneralView +enabled 门控与竞态修复 / ChatMessageFlow +currentPlayer∈agents 守卫×2 / useAutoPlaybackDone 注释）+ 后端零改动；②npm build tsc 0 + vite 117 modules → static 同步 index-BdhOWN08.js/index-CF8r03BC.css（SHA256 一致，清旧 index-BZdblCqV.js）；③esbuild 冒烟 8/8 + CDP 端到端 10/10（A 有玩家 7 + B 导演 3，真实后端/真实 LLM 轮次）；④兼容性——导演模式行为逐字节不变（无玩家 enabled=true 同 D-059）；有玩家场景从「播完自动推进」变「播完停等输入」；2D/剧本杀/狼人杀零触碰；playback-driven=false 回退路径零触碰；⑤static 已同步、后端零改动——8000 重启仅影响页面资源加载（旧运行实例仍服务旧内存 bundle，主会话决定是否重启）；未 git commit（统一 gate 待授权）。台账 docs/修改记录.md #187 + TEST_STATUS Round 93/v93 + 并行作业登记 P-0814-E。

---

## 2026-08-15（P-0815-A 分组逻辑修复批次，原会话中断验证+补登记）

### D-061 分组逻辑修复 6 项（调研报告-移动与分组问题.md 2.4 节，只做问题 2）：空间聚类 + 组类型 + 距离门槛 + 按听觉组分分配 + 距离单位修正 + 移动距离守卫
- **决策**：按调研报告 2.4 节 6 项修复「很远的地方都能一组 / AI 组·AI·用户组混为一谈」：①**ModeClassifier 空间聚类**——findConnectedComponents 后加 withinDiameter 校验（组内任意成员对距离 < MAX_GROUP_DIAMETER=300px），禁止听力链式传递（A↔B↔C 每跳 80-130px）把相距 400px+ 成员拉进同组（原未接线 CLOSE_THRESHOLD=150 改造接线并调至报告建议区间 250-300）；②**组类型 GroupKind 枚举**（AI_AUTO/USER_JOINED/SCRIPT_DISCUSSION/WEREWOLF_DISCUSSION，默认 AI_AUTO）——ConversationGroup +kind，joinGroup→USER_JOINED、createScriptDiscussionGroup→SCRIPT_DISCUSSION（4 参重载可显式指定），getStatus 下发 kind + participantInfo[{name,isPlayer}]（participants string[] 旧契约不动）；③**距离门槛**——joinGroup 距最近成员 < JOIN_GROUP_MAX_DISTANCE=120px 否则拒绝（后端校验是本原，前端 findApproachableGroups 只是展示层）+ 玩家自动 DYAD MAX_AUTO_DYAD_DISTANCE=200px；④**按听觉组分分配**——SimulationOrchestrator.tick 不再全场景 allMerged，改「每个活动对话组 applyToGroup + 组外 agent 按听力连通分量分别 assign」，单人分量 ISOLATED（无听力接触不得两两互见）；⑤**距离单位修正 + 配置化**——SpatialTrackResolver.DEFAULT_CONVERSATION_DISTANCE 5.0「格」错位→70px（px 语义，非法值回退默认）+ 配置键 roleplay.track.conversation-distance（AppConfig.TrackConfig + yml 双份 + TrackDirectorService.setConversationDistance 重建 resolver + SimulationService 接线 + ConversationManager legacy 常量 5→70 同步）；⑥**MovementConstraint.computeMerged 距离守卫**——成员距 leader > MERGED_MAX_FOLLOW_DISTANCE=300px 不强制归队（保持原位防瞬移式聚拢，等自然靠近再归队；leader 收敛质心行为不变）
- **原因**：①调研报告问题 2 三重缺失实证——听力传递闭包无组直径上限（B1/B2）、无组类型字段（B13）、joinGroup/自动 DYAD/全场景 MERGED 三处无距离校验（B5/B6/B10/B11），配合 MovementConstraint 物理聚拢（B12）造成「人在千里外也能一组」；②R6「格 vs px」混用实证——5.0 按 px 用 ≈ 贴脸，近距离对话 MERGED 几乎永不触发，≥5 人局又必然触发空间分配 → 全员 WEAK/ISOLATED；③对齐 D-004「阈值勿 hardcode」纪律（conversation-distance 配置化）；④问题 1（移动控制）已由 P-0814-I 独立交付，本批只做问题 2 避免跨批次耦合
- **放弃**：①把超距链式分量拆成多个子簇成组（实现为整组拒绝——报告原文语义即「组内任意成员对距离 < 阈值 → 允许成组，否则不成组」，子簇拆分留后续；②kind 用字符串自由字段（枚举更安全可测）；③joinGroup 距离校验放在展示层（前端限制只是掩盖，后端校验才是本原）；④对话组与分量两套分配逻辑合并（组内走既有 applyToGroup 含 TrackChangeEvent 发布，组外走分量——分而治之改动面最小）
- **影响**：①全量 mvn **828 run / 126 类 / 2 failures / 0 errors**（两失败均既有：stale ObstacleCollisionGridTest P-0811-G 遗留 + flaky ScriptGamePrivateChatTest 单跑 8/0 复证 PASS；LongTextStabilityTest 本轮通过）；定向 10 类 63/0 全绿；本批新增 6 测试类 27 用例（空间聚类 5 / 组类型 5 / join 距离 5 / orchestrator 分量 4 / 距离配置 3 / 移动守卫 5）+ SimulationOrchestratorTest C 50→100px 适配（70px 默认下 50px 落 MERGED 带）；②本批修复 1 项测试几何缺陷（TrackDistanceConfigTest E 位置与断言矛盾——E(150,0) 距 D(80,0) 仅 70px < 100px 合法 MERGED，改 (190,0) 使距两成员均 ∈[100,200) 听觉带 → WEAK；生产行为正确非实现缺陷）；③与并行批次零冲突：P-0814-H（ScriptGameService/map/interact）零文件重叠、P-0814-I（玩家分速）不同键增量共存；④禁动文件（ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路）零改动；改动文件 UTF-8 无 BOM；不重启 8000 不 git commit（统一 gate 待授权）；已登记 docs/修改记录.md #194 + TEST_STATUS.md Round 99/v99

---

## 2026-08-17（MiMo TTS 接入批次 P-0817-A）

### D-062 小米 MiMo TTS 接入：三模式合成服务 + 角色声线配置（方案1 落库）+ 独立 TTS 端点（对话 TTS 集成走独立端点方案，SSE 主链路零改动）
- **决策**：①**服务层**——新建 service/MimoTtsService.java（@Service，构造仅依赖 AppConfig）：三种模式（BASIC 内置音色 audio.voice=音色名 / CLONE 声音克隆 audio.voice=参考音频 data URL（本地 wav 路径自动 base64 转 data URL）/ DESIGN 声线设计无 voice 字段、user 消息即音色描述），Java HttpClient（虚拟线程执行器）POST MiMo OpenAI 兼容 /v1/chat/completions（请求/响应形态真机复验 3/3：200 + RIFF/WAVE 字节），响应 choices[0].message.audio.data base64 → WAV 字节 + transcript 透传；synthesize 同步 + synthesizeAsync（CompletableFuture + 虚拟线程，对话生成后可异步合成不阻塞主流程）；②**角色声线配置（方案1，与现有架构一致）**——CharacterEntity +voice_mode（basic/clone/design）+/voice_data（design=音色描述 / clone=参考音频路径或 data URL / basic=内置音色名）两列（ddl-auto=update 自动加列，旧行 null 零迁移），DatabaseService.saveCharacter 7 参重载（旧 4/5 参委托 null 零破坏；voiceMode/voiceData 非空写入、空保留既有值，对齐 playerId 惯例）+ entityToMap 暴露键，CharacterController create/update 透传（空白→null）；③**配置管理**——
oleplay.tts.mimo.* yml 双份 + AppConfig.TtsConfig.MimoConfig（enabled/api-key/base-url/三模型名/default-voice/default-tone/builtin-voices/timeout-seconds/openclaw-config），api-key 解析链 yml → 环境变量 ROLEPLAY_MIMO_TTS_KEY → openclaw.json models.providers.xiaomimimo-tp.apiKey（路径可配，空=不读；实测 key len=51 可读）；④**对话 TTS 集成（任务书二选一）**——选「独立 TTS 端点供前端调用」：MimoTtsController POST /api/tts/mimo/synthesize（默认返原始 WAV 字节 + X-Tts-Mode 头，?json=true 返 {audio_base64,transcript,model,elapsed_ms,mode}；body 支持 character 按角色名从角色库解析声线）+ POST /synthesize/async 任务式（job_id → GET /result/{jobId} 轮询）+ GET /voices + GET /status（不含 key）+ GET /voice-config/{name}；不改 SSE 主链路/RouterService/对话引擎（禁动），前端在收到 AI 消息后自行调本端点合成即达成「回复后语音输出」
- **原因**：①主人需求「每个角色/AI 对话都有独立语音输出」——三种模式分别覆盖：内置音色零成本起步 / 声音克隆让角色说「自己的声音」/ 声线设计用文字描述定制音色；②方案1（角色配置加 TTS 参数）与现有 characters 表天然契合（表已有 voice 字段先例，加两列零迁移），避免新建 TTS 配置表的双源漂移；③api-key 解析链覆盖三种部署方式（yml 明文 / 环境变量 / 读 openclaw.json 与主人密钥管理一致），openclaw.json 路径做成配置防硬编码；④独立端点方案符合任务书允许的第二路径且严格遵守「不动 SSE 主链路」硬约束——在对话 SSE 消息里塞 audio 需改广播主链路，成本与风险不成比例；⑤异步任务式端点给前端轮询拿音频的选项，避免长文本合成阻塞 HTTP 线程
- **放弃**：①对话 SSE 消息附带 audio 字段（需动 ConversationManager/RouterService 广播路径——禁动清单，且音频数据（base64 WAV 数万字节）塞 SSE 事件会撑爆事件体积，独立端点按需拉取更优）；②新建 TTS 配置表（方案2——角色表两列已够，零表结构膨胀，对齐 D-013/D-014 不扩表纪律）；③复用 LLMClient 调 MiMo（LLMClient 面向 DeepSeek 对话/JSON 生成，无 audio 参数形态，独立 HttpClient 更直白）；④RestTemplate（项目无既有 RestTemplate 依赖，JDK HttpClient + 虚拟线程更轻）；⑤voicedesign 模式下也传 voice 字段（真机验证 design 需无 voice 字段，传了会改变语义）
- **影响**：①全量 mvn test **933 run / 1 failure / 0 errors**（唯一失败=既有 stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改，历次全量同款）；新增 MimoTtsServiceTest 17/0 + MimoTtsEndpointTest 11/0，定向 28/0 BUILD SUCCESS）；②新增 6 端点 + 2 配置块（roleplay.tts.mimo.* yml 双份）+ 2 表列（voice_mode/voice_data）+ 1 服务；③真机验证（Python UTF-8）：MiMo API 三模式复验 3/3（basic 92204B / design 230444B / clone 99884B 均 RIFF/WAVE），与 Java 服务构建的请求 JSON 形态逐字段一致；④已知限制——basic 内置音色清单仅配置驱动（默认 mimo_default，真实可用音色名以 MiMo 文档为准，接口透传不做本地校验）；合成延迟 = API 往返时间（异步端点可避免阻塞）；前端未接线（本次纯后端，前端消费按需后续批次）；api-key 明文 yml 占位 + openclaw.json 兜底（不落库不落日志）；⑤未 git commit（统一 gate 未获授权）

### D-063 前端接入 MiMo TTS：消息播放按钮 + 角色声线管理（P-0817-A 前端子任务，UI 风格一致）
- **决策**：①**播放链路**——新建 services/mimoTts.ts（全局单例：同一时间只播一条，后点击顶替先播放者，杜绝多消息串音；Web Audio API 播放 base64 WAV（对齐既有 ttsPlayer.ts 先例，浏览器解码）；useSyncExternalStore 式状态订阅 → useMimoTts(key) hook，按钮状态机 idle→loading→playing→error（error 3s 自动回 idle））；②**播放按钮**——新建 components/TtsPlayButton.tsx（🎙 播放 / ⏳ 合成中（spin）/ ⏹ 播放中可停止 / ⚠️ 失败 tooltip 原因），两处接入：经典视图 MessageView agent 消息 meta 行（.message-meta 内，margin-left:auto 右对齐）+ Gal 对话框（GalDialogBox AI 消息完成态显示于提示行右侧，flex space-between）——Gal 是 2D/剧本杀/狼人杀/一般模式共用对话框，单点接入全覆盖；③**声线解析优先级**——显式参数（mode/voice_data）> 本地角色卡（demo2 store freeRoles/extraRoles/genRoles/生成剧本 roles 按名匹配，localStorage 角色不依赖后端角色库）> 后端角色库 character 名解析（P-0817-A /voice-config 链路，404 时错误提示「角色声线未配置」）；④**角色声线管理**——RoleForm（角色库添加/编辑共用表单）新增「🎙️ MiMo 声线」模块：启用 checkbox + voice_mode select（basic/clone/design）+ voice_data 输入（placeholder 按模式联动）+ 试听按钮（固定试听文本合成播放，角色名空时禁用）；RoleCard +voice_mode/voice_data 字段，formToRole 勾选才写、未勾选清除防残留；⑤**API 封装**——client.ts +4（synthesize?json=true 超时 120s 异步友好 / voices / status / voice-config）
- **原因**：①需求「每条 AI 对话消息旁 🎙 播放按钮」——经典视图消息流（MessageView）+ Gal 对话框（GalDialogBox）是全部消息呈现面，两处接入即全覆盖；②合成可能数秒（真实 LLM 音色）——loading 态 + 120s 超时 + 错误提示（503 未启用/404 角色缺失/超时 中文化）满足「异步友好」；③本地角色卡优先——demo2 角色是 localStorage 结构，未必在后端角色库，显式传参才保证「角色面板配了声线就能生效」；④单例播放——多消息同时播放会灾难性串音，单例 + 同 key 再点停止是直觉交互；⑤Gal 按钮只在消息完成态显示（打字中内容未定不可播）
- **放弃**：①每条消息独立 AudioContext/独立播放器（资源浪费 + 串音）；②后端角色库作为唯一声线源（demo2 localStorage 角色会 404）；③改造 SSE 主链路推送音频（禁动清单，D-062 同理由）；④播放完成自动播下一条消息（需求是手动点播，不做自动连播）
- **影响**：①npm run build 通过（tsc 0 + vite 142 modules → dist/index-BTsy_FND.js + index-CPmXZN7K.css，未同步 static 待部署）；②CDP 走查 17/17 全过——角色表单声线模块 10/10（模块渲染/模式 select/placeholder 联动/试听 loading→播放→回 idle/保存落卡含声线字段/编辑回填/console 零错误）+ 消息流按钮 7/7（经典视图 MessageView meta 行按钮 + Gal 对话框按钮；点击→loading→合成请求发出→播放→停止→idle 全状态流转，SSE agent_output→store→渲染真实链路，mock proxy 提供一般模式对局 + TTS 端点（8000 未运行，见下））；③**环境说明**：验证期间 8000 后端无 java 进程（外部状态，非本批），真机 API 复验不可用——mock proxy 按 MimoTtsController 源码契约实现（synthesize json=true/voices/status/voice-config/async+result），前端链路（fetch→解析 audio_base64→decodeAudioData→播放）与 P-0817-A 后端契约逐字段对齐；④新增 2 前端文件 + 6 文件改动 + 3 验证工具（tools/static_proxy_p0817a.mjs / cdp_p0817a.mjs / cdp2_p0817a.mjs + mock_tts_wav.b64 留档）；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/后端 Java）零改动；⑥未 git commit（统一 gate 未获授权）

### D-064 大图支持：地图尺寸可配上限 256×256 + 前端尺寸接线（P-0817-D，延续 D-031/D-035 尺寸链路）
- **决策**：①**新增可配尺寸上限**——`roleplay.game.map.max-width/max-height: 256`（yml 双份）；ScriptGameService 的 `clampToMin` 升级为 `clampToBounds`（下限 32×20 + 上限 256×256 统一收敛，显式传参/对局已定尺寸/配置默认全生效）；ScriptMapService 同加 max 字段并在 `generateMap` 显式尺寸超限时 clamp（scene 预览与 script 双路径单点统一，fallback 原因记录 clamp 说明）；②**前端尺寸接线（此前写死 32×20、设置页宽高是死控件）**——RoleSelectPage/GameBridge 的 sceneMap 调用改读 settings.mapGen 宽高，store 默认调为 32×20（与既有生成尺寸一致，接线后行为零变化），SettingsPage 宽高上限放宽到 256 并提示「超 40×24 自动走 BSP 确定性生成」；③**大图策略维持**——LLM 语义路径保持 40×24 token 预算闸（P-0803-J/D-031 决策不动），显式超限自动走 BSP 确定性路径（精确尺寸、零 LLM 成本、同 seed 同输出）；测试扩展 128×128/256×128 参数化 + 上限 clamp 2 用例
- **原因**：①主人需求「支持生成很大的地图」（传统 RPG 小镇/大地图场景）——BSP 路径本就无硬上限（早测 64×64/100×60/128×64），缺的是「前端能请求大尺寸」+「防超大图的兜底上限」；②上限 256×256=65K 格（ground+collision ≈130K 数字 ≈1MB 级 JSON）——Phaser 瓦片层相机裁剪可承载，再大 JSON/渲染收益递减且有风险，256 作为生产上限足够传统 RPG 小镇规模；③上限做成配置（对齐 D-004 勿 hardcode 纪律），默认值不影响既有默认 40×24 行为；④前端接线是「支持大图」的用户可见关键——不接线则设置页 128×96 形同虚设
- **放弃**：①提高 LLM 路径上限（声明式输出后 40×24 上限偏保守，但多房间 LLM 可靠性下降、语义路径仍应小图优先，大图确定性生成更稳——维持 D-031 预算闸，后续可单独评估）；②前端默认改大（保持 32×20 零回归，用户按需调大）；③渲染层 chunk 加载（≥100×100 才需要，H4 已规划，本批不引入）
- **影响**：①定向 mvn 4 类 **53/0**（ScriptMapSizeExpansionTest 15 含新 2 用例 + SceneMapLlmModeTest 4 + MapValidatorTest 24 + BspMapGeneratorTest 10）；全量 mvn **937 run / 1 failure**（唯一失败=既有 stale ObstacleCollisionGridTest.horizontalMerge，P-0817-A 同款登记，零相关）；②前端 tsc 0 + npm build 0（vite 139 modules → dist/index-BLCKrd30.js + index-C7fFMz6S.css 未同步 static）；③真机 128×64 大图 200（mode=bsp-fallback / 94 rooms / 14 zones / 校验 ok）；④新增 2 配置键（yml 双份）+ ScriptGameService/ScriptMapService 尺寸 clamp + 2 测试用例 + 3 前端文件接线；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static）零改动；⑥未 git commit（统一 gate 未获授权）
### D-065 人物固定比例尺：玩家/AI 屏显尺寸恒定，地图随人物缩放（P-0817-F）
- **决策**：①**人物屏显尺寸恒定**——新增常量 PLAYER_SCREEN_R=16 / AI_SCREEN_R=12（固定屏幕半径 px），玩家与 AI 圆点每帧按相机 zoom 反向 setScale（playerBaseScale=16/(ts×0.45)、aiBaseScale=12/(ts×0.28)），屏显半径=世界半径×scale/zoom×zoom=常量；Arcade body 保持世界尺寸（ts×0.3）不随视觉缩放，碰撞语义不变；②**地图跟随人物缩放**——相机基准缩放改按视口跨 ~18 格自适应（Clamp(800/(18×ts), 0.3, 1.2)），替代原固定 1:1：大图（128×64/256×256）自动缩小视野更广、小图不放大；滚轮缩放范围改绝对 [0.3, 1.5]，只变世界比例、人物恒定
- **原因**：①主人需求「将人物设为固定比例尺，让地图跟着人物来缩放」——大图（P-0817-D 支持后）下人物若随相机缩小会失去存在感，传统 RPG 人物应恒定屏显尺寸；②固定 1:1 缩放（P-0804-H）在大图上视野太窄（128×64 只能看 12×8 格），自适应基准 + 可拉远到 0.3 后视口跨 41 格，兼顾探索感；③人物视觉缩放与物理 body 解耦——Arcade body 独立于 sprite setScale，碰撞/寻路语义零变化，避免每帧改 body 的抖动
- **放弃**：①人物随相机同比例缩放（需求明确否定）；②用 ScrollFactor=0 固定人物在屏幕（人物需在世界中移动/被墙遮挡，ScrollFactor 0 只适合 HUD）；③整图适配相机（大地图全图缩小后人物过小、房间不可读——保持「局部视野 + 可拉远」而非整图适配）
- **影响**：①前端 tsc 0 + npm build 0（vite 139 modules → dist/index-DQavf2km.js + index-ByMGxCaU.css 未同步 static）；②后端零改动、测试零回归面；③屏显尺寸数学核验通过（玩家恒 16px / AI 恒 12px 屏幕半径）；④已知限制——高倍拉远（zoom=0.3）时物理 body 屏显仅 ~6px，视觉略大于碰撞体（可接受，碰撞以世界格为准）；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static）零改动；⑥未 git commit（统一 gate 未获授权）
### D-066 房间模式：契约 v0.2 exits[] 出口表 + 出口确定性推导 + 前端一屏一房间（P-0817-G）
- **决策**：①**契约扩展**——v0.2 新增可选 `exits[]`：`{id, from, to, side?, door:[x,y]}`（from/to 房间 id、door 墙环门洞格、side 方位），缺失兜底为空（整图模式零影响）；MapContract 宽容解析 + MapValidator 检查项 12（from/to 已知房间、door 可通行且在 from 墙环上、缺反向出口/门洞不对齐警告）；②**出口确定性推导**——新 `MapExits.deriveExits`：每个房间墙环（rect 外 1 圈）可通行格 → 沿可通行区 BFS（不穿回本房间内部）→ 到达的第一个其他房间即出口，双向天然互补、每对去重；BspMapGenerator 输出自带，ScriptMapService 三条路径（LLM 主/anchor 修复/BSP 兜底）`ensureExits` 双保险——**不让 LLM 手出坐标**，几何由程序保证；③**前端一屏一房间**——ScriptMapScene `roomMode`：相机 bounds=房间墙环、只显示本房间标签/AI 角色/出生点/装饰（装饰层按房间重建销毁防泄漏）、金门+箭头标记，踩门/E/点击三路走门切换（相机 fadeOut→定位门洞→fadeIn），onRoomChange 回调 React 层；PhaserScriptMapView 默认房间模式 + 🚪/🗺️ 切换按钮；MiniMap 当前房间金色高亮；④**人物固定比例尺延续**——房间内仍按 P-0817-F 玩家 16px/AI 12px 屏显恒定
- **原因**：①主人需求「进入单个房间只显示分房间内容，像传统 RPG」——小镇原型（town-explore）已验证体验闭环，真实项目落点即「房间视图 + 走门切换」，出口表是数据地基；②出口必须程序化推导而非 LLM 输出——LLM 出坐标历史上有热点埋墙/尺寸收缩等修补负担（P-0804-G），几何推导天然合法且 BSP/LLM 双路径统一；③渲染与生成解耦（D-020 纪律延续）——契约只是加可选键，旧前端按 v1 忽略零破坏；④房间模式默认开启但保留整图切换——大地图（P-0817-D）与房间模式并存，玩家可随时拉远看全局
- **放弃**：①LLM prompt 输出 exits（坐标类输出不可靠，几何推导更稳）；②房间模式另存独立 map 副本（同一契约 map 内 roomId 切换即可，零数据复制）；③后端权威房间状态（移动仍前端本地渲染，房间切换纯视图语义，与 P-0814-I 本地漫游一致）
- **影响**：①定向 mvn 6 类 **72/0**（新增 MapExitsTest 3 + 回归 5 类全绿）；②前端 tsc 0 + npm build 0（vite 142 modules → dist/index-UMaXEvLs.js + index-ByMGxCaU.css 未同步 static）；③契约文档 §7.1/§7.2 更新；④新增 1 Java 类 + 3 文件改动 + 4 前端文件改动；⑤已知限制——房间内 decor/标签按房间重建，切换瞬间有轻微重建开销（可接受）；多房间建筑（如咖啡馆 2 区）后续由多个 room rect + exits 表达；⑥禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static）零改动；⑦未 git commit（统一 gate 未获授权）

---

## 2026-08-17（结构树契约 + 生成 API 落地批次 P-0817-L）

### D-067 结构树契约 + 生成 API：structure 可选键 + POST /api/structure/generate（四模板，单图优先超限拆多图 + warps）
- **决策**：①**契约层**——地图 JSON 新增可选 `structure`（version/kind/name/seed/root 节点树/relations），MapContract 宽容透传（缺失 = 普通地图零破坏，D-014/D-020 版本纪律延续）；新 simulation/structure/ 包：StructureContract（归一 + childSeed hash64 确定性种子 + 模板默认尺寸表）+ StructureValidator 6 项校验（id 唯一/父子无环（身份去重防自引用栈溢出）/叶子 building 必须含 template/relations from·to 存在且 kind 已知/叶子 → rooms[] 映射警告/exits 落在结构叶子）+ validateWarps（多图目标存在、缺反向警告）；②**L0 模板库**——resources/structure/{castle,mansion,city_block,dungeon}.json 四套结构树（只含语义：节点/关系/模板键，几何全程序化——P-0804-H 教训延续「LLM 只做语义、几何程序化」，本批 L0 用模板零 LLM 成本）；③**L1 布局器**——StructureLayoutGenerator 确定性生成（打包 → 走廊先落 → 房间地板 → 墙环开门 → 外边界墙 → zones/spawns/decor），复用既有范式：模板原型 make_layout 墙环开门、MapExits 出口推导、ScriptMapService 线索绑定/覆盖补齐；**单图优先**（总面积 ≤ 预算 → 1 图 + exits 连接表）；**超限/map_mode=multi → 按 zone 节点切图**（建筑按 relations 归属 zone、无关联轮转、无 zone 按预算/交替切图）+ **warps 双向链式连接**（契约 v0.2 warps[]：{from:[x,y], to:[mapId,x,y]}，复用 P-0803-K 多图语义）；④**服务层**——StructureMapService：配置 roleplay.structure.*（enabled/l0-source/max-single-map-width·height 128/template-dir，yml 双份）；失败策略=降级不 500（开关关闭 / kind=custom（LLM 蓝图 L0 属 P4 未落地）/ 模板缺失 / 结构非法 / 布局失败 → BSP 常规地图兜底 + fallback 记录原因，对齐 ScriptMapService 语义）；⑤**API**——StructureController POST /api/structure/generate：400（缺 theme/未知 kind/负尺寸），200 {structure, maps{map_id→契约v1}, current_map_id, connections[](exit 图内/warp 跨图), generator{l0,kind,seed,map_mode,validation}, fallback[]}；响应 seed = 实际使用种子（缺省当前毫秒，回传供复现）
- **原因**：①主人需求「真实项目落地结构树契约 + 生成 API」（城堡/庄园/街区/地牢各一套模板，超单图上限自动拆多图 + warps 连接）——P-0817-H 设计定稿后进入代码落地；②大型结构（城堡/城市街区）必须分层：语义层（结构树）与几何层（房间矩形/走廊/墙/门）解耦，模板 L0 零成本、确定性可复现（同 seed 同输出），几何程序化保证「房屋模板不会出错」（P-0804-H 教训：LLM 手出坐标需大量修补）；③单图优先 + 超限拆图——128×128 预算内的小结构（城堡/庄园/地牢）一张图直接玩，超限（街区/自定义大结构）按 zone 切图 + warps 跨图漫游，与传统 RPG 多区域地图一致；④与既有端点共存——scenes/map、script/map 保留不动，structure 为大型结构专用新入口，可后续内嵌为脚本地图新 L0 来源
- **放弃**：①kind=custom 的 LLM 蓝图 L0（属 P4，本批降级 BSP 兜底，接口语义诚实）；②L2 模板配方 Java 化（模板原型 templates-proto.json 10 模板/28 家具的完整实例化留后续——本批 L1 已产 decor 基础装饰，房间内容深化独立批次）；③前端接线（结构树选择/生成入口 UI 属 P4，本批后端 API 先行）；④拆图接入 ScriptGameService 多图注册表（P-0803-K 的 maps/switchMap 面向对局态，structure API 无对局上下文，先返回 maps+connections 由消费方接入；P4 前端切图时再桥接）
- **影响**：①全量 mvn test **962 run / 1 failure / 0 errors**（唯一失败 = 既有 stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改，历次全量同款）；本批新增 4 测试类 22 用例全绿：StructureContractTest 7 / StructureLayoutTest 4 / StructureMapServiceTest 6 / StructureEndpointTest 5）；②契约文档 §7.1 +structure 字段；③新增 4 Java 类 + 1 服务 + 1 控制器 + 4 JSON 模板 + 2 yml 配置块 + MapContract 1 处透传；④已知限制——自动布局走廊可能穿房（几何合法、连通性 BFS 保证，美观优化留后续）；warp 落点=各图首房间中心（可通行）；多图地图尺寸可能略超预算（forceMulti 交替切图场景）；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static/前端）零改动；⑥未 git commit（统一 gate 未获授权）、未 spring-boot:run、未打包未重启 8000

### D-068 P4 收尾：kind=custom LLM 语义蓝图 L0 + 前端「大型结构」生成入口（P-0817-M）
- **决策**：①**LLM 语义蓝图 L0**——新 StructureLlmBlueprint：prompt 只要求语义结构树 JSON（root.children 8-14 个节点、叶子带 template 或 type=zone、relations 8-14 条且全树连通），**明确禁止坐标/网格/瓦片**（几何全程序化——P-0804-H 教训延续：LLM 手出坐标需大量修补，语义层输出更稳）；llm.callJson(prompt, 3000 tokens, 45s) → StructureContract.normalize（kind=custom、seed/name 写入）；空输出/超时/异常 → null（服务层 BSP 兜底不 500）；②**服务接线**——StructureMapService 注入 LLMClient（@Autowired 5 参构造 + 保留 4 参测试直构）：kind=custom 且 l0-source=llm/auto → LLM 蓝图 → StructureValidator → L1 布局（复用 P-0817-L 全部管线）；custom 且 l0-source=template → BSP 兜底（既有行为不变）；已知 kind 仍走模板库（零成本，模板比 LLM 更稳定）；generator.l0 溯源 llm/template/bsp；③**前端接线（P4 验收）**——store View +'structure'、App2 NAV「🏰 大型结构」+ 路由渲染、client.ts +structureGenerate、新 StructureGenPage：类型 chip（城堡/庄园/街区/地牢/自定义 LLM）+ 主题/种子/风格/单图预算/单图优先·强制多图 → 生成 → 结果卡（kind/l0/模式/seed + fallback 与校验错误提示）+ 每图 chip 切换 + PhaserScriptMapView 只读预览（房间模式自动开启）+ 跨图 warp 连接表 + 出口/传送计数
- **原因**：①主人「继续」推进 P4——P-0817-H 设计分期 P1-P4 全部落地即完整交付「结构树契约 + 生成 API」能力；②custom 长尾主题需要 LLM 语义层（模板库只覆盖 4 类，无法穷举飞船/研究所/小镇等自定义主题），但几何必须程序化（LLM 只出「有什么房间、怎么连」，不出坐标）；③前端入口是用户可见的最后一公里——没有页面，API 再完整用户也看不见；④前端复用既有 PhaserScriptMapView 只读预览（RoleSelectPage 同款弹窗模式），零新渲染代码
- **放弃**：①LLM 直接输出地图几何（本批 prompt 硬性禁坐标——P-0804-H 教训，几何由 L1 保证合法）；②已知 kind 也走 LLM（模板零成本且确定性更强，LLM 留给长尾）；③StructureGenPage 内嵌对局/保存功能（本批只生成 + 预览，对局桥接（进入 2D 探索复用地图）留后续批次）；④l0-source=auto 的模板未命中自动降级 LLM（本批 custom 直连 LLM，auto 语义留待扩展）
- **影响**：①全量 mvn test **969 run / 2 failures / 0 errors**——两失败均既有：stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改）+ flaky ScriptGamePrivateChatTest.privateSayRepliesAndPersists（Round 50-102 历次同款，**整类单跑 8/0 EXIT=0 复证 PASS**）；LongTextStabilityTest 本跑 PASS；②本批新增 7 用例（StructureLlmBlueprintTest 4 + StructureMapServiceTest 3）全绿，P1-P3 22 用例回归全绿（定向 29/0）；③前端 tsc 0 + npm build 0（vite 145 modules → dist/assets/index-kZImbtVK.js 2,096.97kB，CSS 未变，未同步 static）；④新增 1 Java 类 + 1 服务改动 + 4 前端文件（1 新增 3 改动）；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static）零改动；⑥未 git commit（统一 gate 未获授权）、未 spring-boot:run、未打包未重启 8000

### D-069 L2 房间内容深化：模板配方库 Java 化 + 家具/地面图案/搜证锚点 + 前端家具色块（P-0817-N）
- **决策**：①**模板配方库**——新 StructureRoomTemplates：35 种家具占地表 + 25 套房间配方（base_floor + pattern（top/bottom/border/center）+ 锚定/固定/随机家具 + note 搜证锚点），覆盖四套结构模板全部房间模板键（城堡 9 / 通用 kitchen·storage·garden / 庄园 5 / 街区 5 / 地牢 5）+ 未知键默认配方（桌+椅+灯+桌上便条）；②**布局器 L2 放置链**——房间地板改按配方（替代旧序号轮换，厨房石板/古风地毯边/花园草地）；placeRoomFurniture：锚定/固定家具原位放置、冲突螺旋就近兜底、随机家具（rock 等）Rng 种子确定性、家具避走廊格与已占格；occupy 全部落 **decor[]（渲染层）**——P-0814-F「decor 无碰撞语义」决策延续，不修改 collision/tileProps，防破坏 MapExits 出口推导/移动/zone 可通行校验；note 搜证锚点放指定家具旁 → zones 优先落 note 格（前端「线索点」视觉 = 白便条）；③**前端家具色块**——decorData.ts +31 种家具类型映射 + 类型化细节（床枕头/桌腿/书架层板/灶眼/窗框/车轮/树干/喷泉池沿），大型结构页 Phaser 预览直接可见家具；④**测试**——StructureRoomTemplatesTest（全部模板键有专属配方且含搜证锚点/家具尺寸合法 + 未知键默认）+ StructureLayoutTest（L2 房间风格差异断言 + 每房 ≥1 家具 + 全图含 note）
- **原因**：①主人「继续深化」——L1 布局只放 1-2 个通用装饰，房间内容单薄；用户早期关切「房间瓦片怎么上，每个剧本都要有不同风格不同线索，怎么保证房屋模板不会出错」——配方库就是答案：每种房间类型一套确定性配方（风格不同、搜证锚点不同），几何/摆放全程序化不会出错；②移植模板原型（templates-proto.json / gen_castle.py 已验证 10 模板 + 家具绘制与校验）而非从零设计——复用已验证资产；③家具走 decor[] 渲染层而非碰撞层——契约语义（P-0814-F）与移动/出口体系解耦，零破坏；真正的碰撞家具（桌/柜挡路）留后续独立批次（需要移动系统/寻路联动评估）；④前端色块映射让「深化」肉眼可见——结构页预览即可看到厨房灶台/卧室床/花园喷泉
- **放弃**：①家具 blocked 写 collision/tileProps（原型 blocked=True 的家具会挡路——会破坏 zones/spawn 可通行校验、MapExits BFS 与移动语义，且当前移动系统不消费 tileProps.blocked；渲染层先行，碰撞家具留后续）；②L2 配方 JSON 外置 resources（Java 常量库先落地，JSON 外置可后续拆——模板原型已留档）；③房间内多格家具逐格 decor（色块渲染单格图标足够辨识，逐格装饰后续细化）
- **影响**：①全量 mvn test **972 run / 2 failures / 0 errors**——两失败均既有：stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改）+ flaky ScriptGamePrivateChatTest.privateSayRepliesAndPersists（**整类单跑 8/0 EXIT=0 复证 PASS**）；LongTextStabilityTest 本跑 PASS；②本批新增 3 用例全绿（定向 32/0），L2 断言（每房 ≥1 家具 + 风格差异 + note 锚点）进布局测试；③前端 tsc 0 + npm build 0（vite 145 modules → dist/assets/index-CRQfopEy.js 2,099.47kB，未同步 static）；④新增 1 Java 类 + 布局器放置链重构 + 1 前端文件；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static）零改动；⑥未 git commit（统一 gate 未获授权）、未 spring-boot:run、未打包未重启 8000

### D-070 真正挡路的家具：家具占用格写碰撞层 + blocked 属性标记（P-0817-O）
- **决策**：①**BLOCKED_FURNITURE**——29 种挡路家具集合（counter/counter_4/stool/table_round/table_rect/chair/bookshelf/sofa/bed/desk/stove/sink/cabinet/shelf/chest/lamp/plant/pillar/tree/bench/fountain/rock/wood_stack/screen/tea_table/wardrobe/dressing_table/hay/cart，对齐模板原型 blocked=True）；不挡路：note（搜证便条）/flower_bed/rug/window/incense/scroll；②**碰撞落地**——occupy 对挡路家具：占用格写 **collision=1**（Phaser Arcade 移动碰撞 + A* 寻路天然绕开；MapExits 门洞推导自动避开家具）+ **tileProps{blocked:true}**（语义标记）；③**放置安全约束**——挡路家具避开房间中心 2×2 安全区（出生点/搜证点可通行兜底）+ 既有避走廊/门洞格规则（门洞永不堵死）；zone/spawn 自由格兜底优先安全区；④**校验器检查项 13**——MapValidator：tileProps 声明 blocked=true 的格必须 collision=1（一致性防线；旧地图/LLM 输出无 tileProps 或非 blocked 键零影响）；⑤前端零改动——碰撞语义由契约 collision 层驱动，Phaser 直接消费，家具视觉仍 decor 层
- **原因**：①主人「真正挡路的家具」——L2 家具此前纯渲染（P-0814-F「decor 无碰撞」），玩家/AI 穿桌过柜没有真实感；传统 RPG 家具应挡住移动、寻路绕行；②写 collision 层而非前端特判——移动系统（MovementSystem/A*）、MapExits 推导、碰撞校验全部消费同一 collision 事实源，单点生效零散改；③安全区 + 避走廊规则保证「家具挡路但不堵死」——门洞/走廊永不堵（freeCell 避 corridorCells）、出生点/搜证点永不压（安全区禁区 + firstFreeCell 安全区优先）；④检查项 13 防「声明 blocked 却不碰撞」的生成 bug（LLM/未来生成器），契约一致性纪律（D-014/D-020）
- **放弃**：①家具占格写 objects 层二维数组（decor 列表已是权威，双写易漂移）；②家具移动/可推动语义（本批静态挡路，交互推动留后续独立批次）；③按家具类型细分碰撞形状（瓦片级整格碰撞已够，精细碰撞留素材阶段）
- **影响**：①全量 mvn test **974 run / 1 failure / 0 errors**——唯一失败 = 既有 stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改，历次全量同款）；flaky ScriptGamePrivateChatTest 本跑通过；LongTextStabilityTest 本跑 PASS；②本批新增 2 用例 + MapValidatorTest 适配（定向 7 类 60/0）；③契约/校验闭环：挡路家具 collision=1 + tileProps.blocked 一致、note/出生点/搜证点/门洞全部可通行、四 kind 单图出口 BFS 全连通断言仍过；④ⓘ 撞车说明：首轮定向测试恰逢并行批次 mvn clean package 清 target 致 NoClassDefFound 假失败，等待并行完成重跑全绿（非代码问题）；⑤前端零改动；禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/static/前端）零改动；⑥未 git commit（统一 gate 未获授权）、未 spring-boot:run、未打包未重启 8000

### D-071 外部/内部分离 + 家具跨格绘制：进屋切换室内地图（P-0817-Q）
- **决策**：①**后端 exterior 模式**——结构生成 API 新增 `map_mode=exterior`：外部地图 = 城镇开放地面（全图可通行）+ 每栋建筑 = 外观块（石板底 + roof decor 铺满 + collision=1 + 底边门格）；每栋建筑一张内部地图（单房间布局 + 强制南墙入口门格）；外部门格 ↔ 内部入口 **双向 warp**；响应新增 `exteriors[]` 元数据（building_id/name/door/interior_map_id/interior_door）；②**前端传送漫游**——ScriptMapScene：渲染 warp（青色光柱 + 名字）、hitTestWarp/triggerWarp（淡出 → 回调 React）、踩中/点击/E 三路触发、构造 spawnTile 覆盖出生点；PhaserScriptMapView：多图注册表 maps + onMapChange，warp 回调找目标图 → currentMap state → 内容 key 变化重建 scene + 落点定位；StructureGenPage：🏠外部/内部 模式 chip + 预览多图漫游；③**家具跨格绘制（修复像素点）**——decorData 新增 FURNITURE_DRAW：35 种家具原型级完整图形（左上对齐、可跨格：桌面+桌腿/床+枕被/书柜+层板书/灶台+灶眼…），decorStyle 优先返回；新增 roof（屋瓦）/door（木门）外部装饰；④**测试**——exteriorSeparation（布局 + 服务）：外部屋顶碰撞 + 门格可通行 + 内部单房间 + 双向 warp 无警告 + exteriors 元数据
- **原因**：①主人「将房间或屋子用贴图隐藏，进去后再用新的地图显示内部」——传统 RPG 核心体验：大地图只显示建筑外观，进屋切换独立室内地图；②「家具像像素点」——上一版每件家具画成单格小色块、多格家具只显示左上角，改为原型级跨格完整图形（template_proto FURNITURE.draw 移植）；③传送点复用契约 v0.2 warps——不做新概念，前端打通 warp 后街区拆多图漫游也一并可用；④外部地图无 rooms（房间模式退化为整图漫游）、内部地图单房间（房间模式一屏一室）——两种视图语义自然
- **放弃**：①外部地图建筑内部也生成房间（外观=实体块，不进入；内部由独立地图承载）；②屋顶点进/淡出遮罩动画（色块屋顶 + 淡出切图已够，遮罩动画留素材阶段）；③自动「进门」判定用 door zone 交互（复用既有 warp 三路触发：踩中/点击/E，行为一致）
- **影响**：①全量 mvn test **976 run / 1 failure / 0 errors**——唯一失败 = 既有 stale ObstacleCollisionGridTest.horizontalMerge（P-0811-G 遗留未代改）；LONG-01/02 PASS；②本批新增 2 用例全绿（定向 7 类 60/0）；③前端 tsc 0 + npm build 0（vite 145 modules → dist/index-Dh9wYWBr.js）；④**部署上线**：新 jar 147,916,677B @12:18:55 + PID 6820；真机验证：街区 exterior 10 图（1 外部 + 9 内部）/ 9 exteriors / 18 warp / 外部 727 roof + 9 door；城堡 exterior 9 图 / 8 内部 / 16 warp；旧端点无回归；stderr 0B stdout 0 ERROR；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；⑥未 git commit（统一 gate 未获授权）

### D-072 页面导航接入浏览器历史 + 大型结构页缓存（P-0817-R）
- **决策**：①**URL hash 路由**——新 demo2/navHistory.ts 纯函数（viewToHash/hashToView + NAV_VIEWS 白名单）；store：初始视图从 location.hash 恢复（深链直达）、go/enterRoles/startGame 同步 pushState、back 优先 history.back()（popstate 恢复视图）、goHome replaceState、模块加载注册 popstate 监听——**浏览器后退/前进可回到上一个/下一个局内页面**（structure/game/settings…）；②**大型结构页缓存**——store +structureForm/structureResult/structurePreview + setter，sessionStorage（roleplay_demo2_structure_v1）持久化（会话内切页/刷新不丢）；StructureGenPage 表单/结果/预览改走 store；③**验证**——smoke_p0817r（hash 往返/宽容/非法/白名单 19/19）+ cdp_p0817r 真机 12/12（CDP：导航→生成→设置→history.back() 回结构页且结果卡/表单缓存保留→history.forward() 回设置→连续后退历史栈正确）
- **原因**：①主人「前端结构要有页面缓存，前进后退都要回到上一个，下一个局内页面」——此前导航只有应用内 back 按钮，浏览器前进/后退无效，且页面组件卸载状态即丢；②hash 路由兼容静态托管（无需服务端 rewrite），深链直达（#/structure 打开即结构页）；③大型结构页生成结果/表单最重，先做该页缓存（对局/设置等状态本就在 store/localStorage）
- **放弃**：①history API path 路由（hash 已满足且零部署成本）；②全页面 keep-alive 组件缓存（React 无内置，侵入大；store 级状态缓存覆盖重点页面即可，其余页面状态本就在 store）；③把 werewolf 加入 hash 白名单（roles 变体承载，避免双写）
- **影响**：①前端 tsc 0 + npm build 0（vite 147 modules → dist/index-CCW0g0v2.js 2,108,160B）；②smoke 19/19 + CDP 真机 12/12；③**部署上线**：新 jar 147,917,415B @12:29:21 + PID 26080；/ 200 引用新 bundle + 结构 API exterior 回归正常 + 日志 0 ERROR；④后端零改动（全量基线 976/1 不变）；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；⑥未 git commit（统一 gate 未获授权）

### D-073 大型结构打通一般模式 2D 地图（P-0817-S）
- **决策**：①**通路复用**——后端 P-0811-G 已支持 POST /api/simulation body.map → Obstacle.fromCollisionGrid 把地图 collision 转障碍注入 2D 模拟世界；一般模式 2D 探索（GameBridge general+explore → PhaserSimulationView scene=custom map=exploreMap）读 store.generalMaps[scriptId]——本批只补前端接线，后端零改动；②**接线**——StructureGenPage 生成后新增「🗺️ 进入一般模式 2D 探索」按钮：把当前预览地图写入 generalMaps[STRUCT_SCRIPT_ID='__structure__']（navHistory.ts 导出常量）→ 取 freeRoles/genRoles 前 2 角色 → setState selectCtx{kind:general,scriptId:__structure__}+runMode=explore → startGame('general') 直接进 2D 世界；GameBridge 缓存命中自动用结构地图（零改动）；③**验证**——cdp_p0817s 真机 8/8（大型结构→外部/内部→生成→进入一般模式 2D→#/game+canvas+2D 世界运行 running=true agentCount=2）；后端日志铁证「LLM map obstacles injected: 28（label=晨曦城堡 · 城镇外观）」——结构地图碰撞（建筑块/家具）已注入 2D 模拟世界，角色在城镇走动被建筑/家具真实挡住
- **原因**：①主人「大型结构先打通一般模式的 2d 地图」——让生成的结构城镇能直接进入一般模式对局玩（角色在城镇里活动、碰撞生效）；②复用既有 P-0811-G 地图注入障碍能力（SimulationController 已把 map.collision 转障碍），避免后端模拟层改动（禁动风险）；③exterior 外部图作为 2D 探索地图最合理——城镇街巷 + 建筑块障碍，玩家在街巷探索
- **放弃**：①外部/内部模式的多图 + 屋内切图接进 SimulationScene（SimulationScene 是后端权威模拟，屋内切图需要后端场景/障碍切换支持——属后续批次，本次「先打通」单图 2D）；②自定义角色流程（直接取自由角色库前 2 名进入，用户可后续在角色库调整）
- **影响**：①前端 tsc 0 + npm build 0（vite 147 modules → dist/index-BzOIcHs8.js 2,110,180B + index-B15NZqdV.css）；②CDP 真机 8/8 + 后端日志障碍注入 28 实证；③**部署上线**：新 jar 148,463,112B @12:34:32 + PID 25844；/ 200 引用新 bundle；stdout ERROR 408 条经核验全部为 SSE 心跳断连 benign（CDP 验证关闭浏览器触发，历史部署批次同款）；④后端零改动；⑤禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；⑥未 git commit（统一 gate 未获授权）

### D-075 地图生成接入小米 MiMo（mimo-v2.5 多模态）专用 LLM 通道（P-0818-B）

### D-076 代码审查后的输入边界与运行时降级策略（P-0818-I）
- **决策**：clone 声线只接受 wav/mp3 data URL，服务端统一校验扩展、10MB、10秒；异步 TTS 使用完整 UUID 与 FIFO 淘汰；结构生成严格拒绝非法模式/尺寸，视觉审核超时配置化；一般模式运行时仍是单地图，因此多地图结果只允许预览，不允许直接进入探索。
- **原因**：此前服务端信任任意本地路径，任务 ID 短且淘汰非 FIFO，结构参数可能静默降级，多地图会被压成单图运行，造成安全、可预测性与状态错误。
- **放弃**：本轮不引入新的异步视觉审核队列，也不伪造多地图切换能力；待运行时具备地图注册表/传送机制后再开放 multi/exterior 进入探索。
- **影响**：前后端双重音频约束；视觉审核默认 180 秒且前端 360 秒；结构视觉重排后重新执行 Map/Structure/Warp 校验；相关定向测试与前端构建通过。
- **决策**：①**地图专用 LLM 通道**——新 MapLlmClient extends LLMClient（mapProvider=true），端点/模型/key 读 roleplay.map-llm.*（token-plan-cn / mimo-v2.5 / 环境变量 ROLEPLAY_MAP_LLM_API_KEY）；LLMClient 加 @Primary（主链路默认 DeepSeek）+ mapProvider 分流（apiBase/defaultModel/authKey 三处）；AppConfig +MapLlmConfig；ScriptMapService 注入 @Qualifier(mapLlmClient)、StructureMapService 注入 MapLlmClient（custom 结构蓝图）、ScriptGameService 七参 @Autowired +MapLlmClient（mapService 地图专用，scriptService 主链路）；key 存本机 openclaw.json providers.xiaomimimo-map（不落仓库/不落 jar 明文）；②**mimo-v2.5 推理模型适配**——探测实证：复杂 JSON 任务先消耗大量 reasoning_tokens（结构树 3000 token 被推理吃光、正文空、100s+）；适配 LLM_MAX_TOKENS 3000→8000、超时 45→180s（结构蓝图）/120s（地图）、prompt 追加「请直接输出 JSON，不要任何推理过程/前言」；③验证——/api/scenes/map LLM 地图走 MiMo 成功（7 rooms 零降级）；/api/structure/generate custom 结构蓝图走 MiMo 成功（l0=llm ok=true fallback=0，雾隐研究所 10 节点结构树→88×83 地图 10 房间，实测 148s）；主链路 DeepSeek 无回归
- **原因**：①主人「只接入地图生成llm」「不要用pro要用多模态的2.5」——给地图/结构生成配独立 LLM（小米 MiMo mimo-v2.5 多模态），对话/剧本等主链路保持 DeepSeek；②推理模型特性（reasoning_tokens 吞预算）必须调参——不改则结构树正文为空、必失败；③key 安全——只进本机 openclaw.json，不写 application.yml 明文、不进 git/jar
- **放弃**：①用 mimo-v2.5-pro（主人明确不要 pro）；②让多模态直接出坐标/网格（P-0804-H 教训：几何仍程序化，多模态只做语义/结构树）；③地图生成与主链路共用 LLM 客户端（双 provider 解耦，互不影响）
- **影响**：①定向 mvn 4 类全绿（含 ScriptGameServiceTest/StructureEndpointTest Spring 上下文）；②真机：地图 LLM + custom 结构蓝图均走 MiMo 成功（148s），主链路无回归；③**部署上线**：新 jar 147,921,076B @13:14:59 + PID 8440（双 key 注入）；stderr 0B + ERROR 全 SSE 断连 benign；④ⓘ 并行批次曾覆盖打包/重启，已重新部署；⑤未 git commit（统一 gate 未获授权）

### D-077 剧本杀 SETUP 期复用 Gal 交流并把消息交给两轮收尾（P-0819-A）
- **决策**：概略先行的 `SETUP` 阶段开放与一般模式 Gal 相同的输入区；`discussion_say` 在 SETUP 只做身份/内容校验、实时 `script_speech` 回显并写入 `pendingHumanEvents`，不提前启动讨论引擎；完整剧本就绪后沿用既有 `discussionMaxRounds=2` 讨论链路消费这些消息。full 模式仍按原路径进入 INVESTIGATION，chat 模式仍在就绪后进入 DISCUSSION。
- **原因**：后台完整剧本生成是异步的，准备页不应空等；直接复用讨论事件队列可保留玩家输入且不复制一套临时对话引擎，现有两轮收尾与阶段守卫保持单一事实源。
- **放弃**：①准备期直接启动完整讨论引擎（角色/秘密尚未落位，存在上下文不完整）；②准备期另建临时 LLM 对话端点（重复上下文与消息管线，后台就绪时还需合并）；③full 模式跳过搜证直接讨论（改变真剧本杀既有流程）。
- **影响**：SETUP 可交流且消息不丢；chat 模式生成完成后仍执行两轮并自动进入 VOTE；full 模式行为零改变；新增 O-1b 回归测试。

### D-078 剧本杀身份和权限收敛为 roleKey 单事实源（P-0819-D）
- **决策**：开局必须选择真人角色并将其置于 `players[0]` 以获得本人 roleKey；局内换绑仅允许携带 roleKey 调用 resume 后回写 `currentPlayer` 与玩家视图。玩家推进阶段需提供有效 roleKey，讨论前至少完成一次搜证；投票状态永远返回全体在线候选。DM 真相及全员 roleKey 接口改为安全默认拒绝，必须配置并携带 `X-DM-Key`。
- **原因**：此前角色选择、左栏绑定、状态轮询和投票各自持有身份，造成“已扮演但未分配”；阶段接口可由匿名请求推进；首票前空候选令投票死锁；未鉴权 keys 端点可直接绕过身份体系。
- **放弃**：保留“空令牌按角色名绑定”和“未配置 DM key 即公开”兼容行为；这两条会使权限模型无法成立。
- **影响**：新局玩家身份、AP、搜证、讨论和投票共用同一 roleKey；部署环境须显式配置 `roleplay.game.dm.key` 才能使用 DM 分发/真相功能。

### D-079 剧本杀本地默认可自行揭晓（P-0819-G）
- **决策**：`roleplay.game.approval.enabled` 默认改为 `false`；显式设为 `true` 时仍保留 DM 审批门与超时回滚语义。
- **原因**：默认环境未配置 DM 口令且 DM 面板已安全拒绝空口令访问，若默认仍等待审批，单人前端局在“揭晓”必定等待 60 秒后回滚，无法完整收官。
- **放弃**：默认保留强制审批，或以空 DM key 绕过安全校验；前者破坏可玩性，后者破坏 D-078 的权限边界。
- **影响**：本地/单人局点击揭晓立即进入 REVEAL；需要主持人审核的部署只需显式将该配置设回 `true`。

### D-080 剧本杀 2D 地图复用一般模式角色与状态刷新链路（P-0819-O）
- **决策**：剧本杀地图直接复用当前对局角色库渲染其他角色的 AI 巡逻；地图搜证/交互成功后复用现有 `scriptStatus` 刷新链路，同步 Gal、线索和 AP，不新增地图位置后端协议。
- **原因**：一般模式已有成熟的角色巡逻与状态轮询能力，地图层只缺接线；剧本杀地图位置仍是本地表现，搜证结果才是后端权威状态，复用刷新链路可避免两套状态源漂移。
- **放弃**：本轮不把本地坐标上报后端、不新建地图聊天端点、不复制一套 Gal 消息管线；这些会扩大联机同步和权限范围。
- **影响**：剧本杀 2D 地图进入后可看到同局其他角色巡逻；搜证/地图交互结果完成后，主区 Gal 与右侧线索/AP 状态会立即按权威状态刷新。

### D-081 一般模式普通 AI 对话采用节奏化自然离场（P-0820-B）
- **决策**：普通 `AI_AUTO` 对话每轮结束后，由轮次、情绪、参与度和对话模式共同计算离场概率；离场角色从 ConversationGroup 移除、解除冻结、清理对话目标并恢复自主漫游；群组剩余成员继续对话并重新计算轨道。玩家控制角色不参与自动离场。策略最大轮数和空闲时间提高为异常兜底，不再作为主要结束机制。
- **原因**：固定最大轮数会让 AI 像脚本一样机械说满，缺少“突然想走、话题无聊、情绪不适、去做别的事”的社会节奏；离场后继续移动才能形成可观察的自然行为。
- **放弃**：不在 LLM 输出中解析“我要走了”作为唯一控制信号（不稳定且难以测试）；不对剧本杀/狼人杀讨论组启用该行为；不删除安全上限和空闲兜底。
- **影响**：普通 2D 双人对话安全上限 20 轮、群聊 30 轮；离场事件进入世界最近对话记录和 SocialState 事件；暂不改变玩家主动离组接口。

### D-082 一般模式 AI 社会实验首张地图采用确定性程序化地图（P-0820-H）
- **决策**：先用 Java 生成符合既有地图 JSON 契约的“晨雾镇”48×32 地图，通过 `GET /api/simulation/social-map` 提供给一般模式 2D 前端；正式瓦片美术后置。
- **原因**：当前阶段优先验证 AI 的移动、相遇、对话、离场、关系变化和事件传播；确定性几何更容易复现行为，也能快速增删建筑、区域和互动点。
- **放弃**：本轮不绑定具体 Phaser tileset、不生成最终 PNG/SVG 瓦片、不把地图硬编码进 RouterService 或 SSE 主链路。
- **影响**：地图已具备道路、桥梁、河流阻挡、建筑、林地、社交热点、互动装饰和出生点；后续可直接把同一 JSON 喂给前端渲染和 `load-characters`，再替换 tileset 资源。

### D-086 一般模式普通地图与大型地图统一使用结构地图配置（P-0820-M）
- **决策**：前端不再区分“普通地图生成”和“大型地图生成”两个入口；统一由设置页配置尺寸、结构模板、地图组织、风格、种子和视觉审核，统一调用结构地图生成 API。
- **原因**：两套入口此前分别调用 `/api/scenes/map` 与 `/api/structure/generate`，导致用户设置无法完整作用于大型地图，也容易产生缓存和行为差异。
- **放弃**：不删除既有后端接口，避免剧本杀和旧场景编辑链路回归；不在本轮把多图结果强行压成单图。
- **影响**：普通模式进入 2D 探索时也会使用统一结构配置；多图/外部-内部可以生成和预览，当前一般模式运行时优先选取当前地图，后续再完善跨图运行时切换。

### D-087 一般模式地图生成结果必须显式保存并按场景复用（P-0820-M）
- **决策**：统一地图弹窗提供“保存地图”和“保存并进入 2D 探索”；保存结果按 `scriptId` 写入现有 `generalMaps` 本地持久化缓存，进入 2D 时优先命中缓存。
- **原因**：仅生成并预览不等于保存；用户关闭预览后再次进入场景会重复调用地图生成接口，浪费时间和 LLM 成本。
- **影响**：同一一般模式场景保存一张当前地图后，刷新页面、返回角色选择再进入 2D，均可复用该地图；多图结果当前保存选中的活动地图。

### D-083 晨雾镇演示采用独立的大地图/室内视图壳（P-0820-I）
- **决策**：一般模式新增“晨雾镇 · AI 社会实验”演示入口；外部使用 96×64 不规则 SVG 地图与小地图视口，房屋节点点击后切换独立室内 SVG 地图；角色、情绪、对话和运行状态继续轮询后端 `/api/simulation/state`。
- **原因**：现有 Phaser `SimulationScene` 仍以单张契约地图为主，直接把 48×32 后端地图塞进旧 `park` 入口会再次出现“看见的不是目标地图”；独立 Demo 能先验证大地图/房间层级/观察体验，同时不破坏既有 Phaser 与剧本杀主链路。
- **放弃**：本批不把房间切图硬塞进 `RouterService`、SSE 或旧 Phaser 单图状态机；不伪装缺失的后端 Demo 角色，缺失角色只在前端以明确的演示位置补齐。
- **影响**：用户现在可从一般模式直接进入晨雾镇，观察 8 个角色、河流/林地绕路、房屋进入/返回和关系对话；后续可把独立室内地图替换成契约 `warps` 多图数据，并将同一视图迁回 Phaser。

### D-084 晨雾镇先按三层瓦片观察模型实现入屋与消息（P-0820-J）
- **决策**：独立 Demo 外部地图扩至 160×104，使用程序化逐格瓦片渲染地表层（草地/道路/水域/林地）、建筑与物件层（房屋/树木/路灯/前景树冠）、信息覆盖层（AI、消息气泡、房屋人数、事件流）；AI 以本地可观察状态自动进出房屋，进入后的角色在对应室内地图显示。
- **原因**：社会实验首先需要让观察者看清“谁在哪里、谁进入了哪间屋、刚刚发生什么”，抽象形状地图和独立静态室内页不足以表达连续空间行为；既有后端尚未给出多地图人物权威位置，先保留前端演示状态作为可视化兜底。
- **放弃**：本批不扩张后端多地图注册表、不把本地入屋坐标写回 SimulationService、不改 SSE 主链路；不引入最终 PNG tileset，继续使用代码原生瓦片以保持可编辑和确定性。
- **影响**：用户可以拖拽观察大地图、从房屋人数发现入屋、进入室内确认人物，并从地图气泡和事件流理解行为；后续接入后端 `warps`/多图位置后只需替换信息层状态源。

### D-085 晨雾镇入口回归一般模式 Phaser 2D 主链路（P-0820-K）
- **决策**：晨雾镇不再默认渲染 `SocialExperimentDemo` 本地状态壳，直接挂载 `PhaserSimulationView`；角色加载、地图 collision 注入、运行、位置、移动和对话统一由一般模式 `SimulationService` 驱动。Vite preview 同样代理 `/api` 到 8000，并移除转发的 Origin。
- **原因**：本地随机游走、入屋事件和系统气泡无法代表真实 AI 行为，且 preview 未代理 API 时 POST 被拒绝，用户看到的只是断开的视觉演示；复用已验证的 2D 主链路才能让画面、消息和行为对应同一后端事实源。
- **放弃**：不在本轮继续修补 SVG 壳的本地 AI；也不把尚未实现的多地图 warps 强塞进 SimulationService。房屋跨图进入留给后端多图位置协议后再接入。
- **影响**：晨雾镇进入时会实际重置并加载 8 名角色；Phaser 地图内移动/接近/对话/聊天使用真实一般模式能力。预览和生产静态站均可指向同一 API；当前预览验证 `running=true/agentCount=8`。

### D-086 晨雾镇 Phaser 使用专用室外地图，collision 只渲染一次（P-0820-L）
- **决策**：晨雾镇不再调用固定房间坐标的 `buildMap(96,64)`，改用专用室外地图：默认草地可通行，河流/建筑边界才是 collision，主街、支路、7 栋建筑和出生点覆盖全图。`SimulationScene` 在存在 `tileMap` 时不再额外绘制后端同源 obstacles。
- **原因**：旧 BSP 生成器只适合 24×16 室内，扩大尺寸却未等比例扩展房间，造成 80% 以上格子是墙、AI 挤在左上；同时 collision 已由瓦片层渲染，二次障碍层会遮住地图并重复显示地图名。
- **放弃**：不改变后端世界 1000×600 物理坐标及 collision 投影算法；不把普通 `buildMap` 改成室外生成器，避免影响其他一般模式预设。
- **影响**：晨雾镇在真实 2D 主链路中拥有完整可走户外区域，角色会分散移动；地图视觉和后端碰撞仍共享同一契约数据，且不会再出现灰蓝空区或重复障碍标签。

### D-087 一般模式对话只能由会话组进入（P-0820-N）
- **决策**：地图点选角色是观察/聚焦操作，不能自动发言或开新轨道；右侧对话与发言区只在玩家点击并加入既有会话组后出现，世界内聊天气泡默认关闭。
- **原因**：点击一个人触发自动问候会把观察动作变成主控介入，容易误将无关角色纳入同一对话轨道；地图上的多气泡也会掩盖群组边界。
- **影响**：AI 的轨道继续由后端自主调度，用户只通过明确的“加入会话组”动作参与某一组；右侧内容有明确归属，不再显示全世界混杂消息。

### D-088 导演模式的会话组采用经典旁听（P-0820-N）
- **决策**：没有用户角色时，点击会话组只打开一般模式经典消息列表，并关闭输入；不调用加入接口、不会生成虚拟 `me`。
- **原因**：导演是观察者，不应改变角色关系或对话成员。Gal 交互框会暗示用户参与，和旁听语义相冲突。
- **影响**：点击组仍能立即看到自动进行的 AI 会话，但轨道、成员和发言全部保持后端的自然状态。

### D-089 导演旁听必须有地图入口与群级消息过滤（P-0820-O）
- **决策**：没有用户角色时，活跃会话组仍渲染“旁听对话”按钮；点击后右侧仅显示该 group id 的消息，不显示世界全量消息。
- **原因**：原实现只在玩家角色在场时绘制加入按钮，导演看到的群组框不能点击；经典视图又未消费消息的 group 字段，导致即使面板打开也无法辨认组内内容。
- **影响**：旁听是可见、无副作用的操作；没有新消息时也会显示当前成员和自动调度状态，避免被误判为无响应。
