# 03 — 决策历史（为什么这么设计）
> 同步时间戳：2026-07-31 16:57（本文件为快照，冲突以项目内活文档为准）


> 改代码前必查。推翻历史决策前先想清楚。完整版见项目根 `DECISION_LOG.md`。

## Phase 1-4 Track 融合（2026-07-31）

| # | 决策 | 原因 | 放弃/影响 |
|---|---|---|---|
| D-001 | **Arbiter 输入脱敏是生死线** | Arbiter 若看到轨道全文即泄露信息源，"谁说了什么"可从摘要推断 | 放弃 Arbiter 看全文的便利；后续所有 Arbiter 路径（剧本杀审批/狼人杀）必须过脱敏 |
| D-002 | Phase 3 WorldDirector **纯规则零 LLM** | 成本控制（组×10 轮×角色数）；纯规则省大头 | 放弃 LLM 驱动目标；保留 `generateGoalWithLLM` 手动扩展点 |
| D-003 | Phase 3 **只加外层 SimulationOrchestrator，不重构 RouterService** | 需求第十四条；避免影响 SSE/Memory/游戏模式 | 放弃架构洁癖；现有 73 tests 零破坏 |
| D-004 | **TrackScore 阈值 40**（hardcode） | Demo 实证：无隔离泄露 100% → WEAK 0% 泄露，token ↓14% | 已加边界测试锁死；建议配置化（D18 待办） |
| D-005 | **TrackStrategy 统一替换 GROUP_DISCUSSION+DEBATE** | 混合轨道上下文（MERGED 全文/WEAK 摘要/ISOLATED 独白） | GroupStrategy/DebateStrategy 不删除（需求十三条：降级默认模式）；DYAD/Speech 不动 |
| D-006 | MovementConstraint **纯规则零 LLM** | 需求八/九条：Track 不直接移动角色，而是生成空间约束 | 约束是边界不是牢笼；GroupAnchor 完整队形降级 P2 |
| D-007 | 前端角色详情走 **body + 向后兼容 query** | 前端未改时不崩 | `@RequestBody(required=false)` 与 query 并存 |
| D-008 | 测试基建：**RANDOM_PORT + H2 mem + mock LLM** | G2 禁 spring-boot:run + 数据隔离 + 零成本 | 93 tests 可重复可 CI；LONG-01 10 万字 5.09s |
| D-009 | 剧本杀 Step 3 降级预案：**轮次发言**（顺序轮流 1 条/人，默认 2 轮→VOTE） | ConversationManager 依赖 SimulationWorld，剧本杀无 2D 世界 | 降级版秘密隐藏退化为 prompt 禁令；全量 Track P1 补 |
| D-010 | 剧本杀判定：**killer 实体比对**（`mostVoted.equals(killer)`） | `truth.contains` 子串误匹配（投"张三"命中"管家张三…"判中） | killer=机器对比例，truth=人类叙述文，职责分离；双生成器 schema 同步 |
| D-011 | 剧本杀 Step 1 Part A 已被并行工作流完成 | 另一会话 coder 已实现 secrets 字段/toMap/getSecretFor | **任何剧本杀派单前先 git diff 确认基线** |

## 迁移期（更早）
- **Virtual Threads** 并行 Agent（LLM 大量 IO 等待）；放弃 Reactive WebFlux（复杂度）
- Python → Java 全量迁移（前端保留 React）
- H2 双模式：开发 mem / 持久 file（注意 D14 缺陷：角色/场景实际未落库）
