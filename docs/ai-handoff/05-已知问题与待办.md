# 05 — 已知问题与待办（动手前必读）

> 活文档：项目根 `docs/问题清单-20260731.md`（含 H 章节：问题→文档对照表）。新缺陷发现必须登记（D 编号/优先级/证据行号）。

## 🔴 P0（必须修）
| # | 问题 | 现状 |
|---|---|---|
| D1 | 中断系统缺失（InterruptManager/AgentTaskManager/CancellationToken/WorldEventBus） | 整个包不存在，Agent 生成无法主动终止（需求第八条 P0） |
| D4 | 剧本杀前端占位 | ScenePage genScript/startScript 纯占位"敬请期待"，8 API 零封装 |
| D5 | 剧本杀 secrets | **Part A 已修**（字段/toMap/发放）；Part B（Track ISOLATED 隔离）待做 |
| D9 | Whisper 无端点 | `/api/voice/transcribe` 404（**可能已被并行会话修复，git diff 确认**） |
| D12 | 历史加载假成功 | `/api/history/load/{id}` 只返回 loaded 未恢复 router（**可能已修**） |
| D14 | 角色/场景不落库 | 内存 List，重启即丢（README 声称 H2 持久化） |
| D19 | admin 无鉴权 | `/admin/generate|list|deactivate` 裸奔（**可能已修**：adminKey + ROLEPLAY_ADMIN_KEY） |

## 🟠 P1（应修）
| # | 问题 |
|---|---|
| D6 | 剧本杀判定脆弱：`truth.contains(mostVoted)` 字符串匹配，无平票重投（蓝图 Step 4 有方案） |
| D7 | 审批门未接入剧本杀/狼人杀 |
| D8 | SSE 主对话事件流死代码（broadcast 无调用方） |
| D11 | 多会话不隔离（sessions 只 put 从不读，单例 router） |
| D13 | `/api/round/start` turns 参数无效（恒跑 1 轮） |
| D17 | G1 LLM 401（环境阻塞） |
| D20 | ConfigController 空操作（setVoiceConfig no-op） |

## 🟡 P2（建议）
| # | 问题 |
|---|---|
| D2 | GroupAnchor 群体移动锚点降级（MovementConstraint 替代） |
| D3 | ContextVisibility 命名差异（TrackStrategy+visibleAgents 承担） |
| D10 | MemoryStore 检索打分缺失（仅窗口+压缩链） |
| D15 | stress 脚本空目录 |
| D18 | TrackScore 阈值 hardcode（建议配置化） |

## ✅ 已解决
- A1 测试文件静默修改（台账登记+复跑验证，git 提交待主人确认）
- D16 LONG-01 已补跑通过
- C1-C9 文档过时（README 未更新，无需改代码）

## 前后端契约断点（5 项）
transcribe 404 / room 无包装 / assign 字段不匹配 / 剧本杀占位 / SSE 静默

## 优先级建议
1. 修环境 G1（LLM 401）→ 2. P0 缺陷（鉴权/剧本杀/历史/中断）→ 3. A1 git 留痕 → 4. 测试基建
