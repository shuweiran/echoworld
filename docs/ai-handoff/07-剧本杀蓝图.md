# 07 — 剧本杀 P0 开发蓝图（Step 1-5）
> 同步时间戳：2026-07-31 16:57（本文件为快照，冲突以项目内活文档为准）


> 活文档：项目 `docs/剧本杀差距分析-待办.md`（v2.1，含完整行号证据）。做剧本杀任务前必读此文件 + 调研报告 `docs/剧本杀调研报告-raw.md`。

## 现状一句话
后端六阶段状态机"名义齐全实质空壳"：SETUP 被跳过、ENDED 永不触达、DISCUSSION 只改 phase 不接对话；**secrets 发放已通（Part A），前端零接入**。

## 核心差距
| GAP | 优先级 | 说明 |
|---|---|---|
| GAP-1 前端 5 环节全空 | 🔴P0 (D4) | 发起/状态/API/事件/界面全无，ScenePage 占位+死代码 |
| GAP-2 secrets 未接 Track | 🔴P0 (D5) | Part A 已修（存储/发放），Part B（ISOLATED 隔离）待做 |
| GAP-3 DISCUSSION 空壳 | 🔴P0 | 讨论不接任何对话系统 |
| GAP-4 判定脆弱+平票+不落库 | 🔴P0 (D6) | truth.contains / 平票无条件设 winner / saveScript 零调用 |
| GAP-5 审批门未接 | 🟠P1 (D7) | ApprovalService 存在但未接入 |

## 开发顺序（Step 1-5，3-6 天）
| Step | 内容 | 工期 | 关键资产 |
|---|---|---|---|
| **1** | 秘密机制 **Part B**（Track ISOLATED 集成；Part A 已就位勿重复） | 0.5 天 | `TrackAssignment.isolated()` / `InteractionDetector.evaluate(agents, secretTaskAgents)`（SECRET_TASK=50） |
| **2** | 前端主链路：8 API 封装 + appStore 状态 + 占位替换 + 游戏页 | 1-2 天 | 照抄狼人杀链路（ScenePage.startWWGame / WerewolfStatePanel / SSE 分支） |
| **3** | 讨论接对话引擎：startDiscussion 接 ConversationManager + TrackStrategy（WEAK 分支藏秘密） | 1-2 天（±1 天） | ConversationManager.init（**需 spike：剧本杀无 SimulationWorld**）/ TrackStrategy / WorldDirectorService.setGoal；**降级：轮次发言 2 轮→VOTE** |
| **4** | 判定加固 + 平票重投 + 落库 + ENDED 触达 | 0.5-1 天 | killer 实体（与 truth 叙述文分离）/ DatabaseService.saveScript（已就位零调用） |
| **5** | 审批门接入（RoundResult 适配） | 0.5-1 天（P1 可选） | ApprovalService.submitForApproval |

## 验收要点（断言级，完整见蓝图第四章）
- A1-4：持秘密角色集合 → InteractionDetector SECRET_TASK +50 贡献
- A2-2：点「AI 生成剧本」→ scriptJson 非空（死代码区激活）
- A3-2：持秘密角色讨论上下文**不含秘密明文**（WEAK 摘要）
- A4-1：投票对象==killer 才 correct=true（近似名 false）
- A4-3：对局结束 scripts 表有记录

## 关键风险
1. **G1 LLM 401**：验收一律 mock LLM
2. **ConversationManager 适配**：剧本杀无 2D 世界，需 spike 或降级轮次发言
3. **双生成器 schema 不一致**：`/api/script/generate`（ScriptService）无 secrets/killer，Step 4 加 killer 时两处同步
4. **并行工作流**：ScriptGameService 已被另一会话改过（D5 Part A）→ **git diff 确认基线**
