# 08 — 硬性约束（红线，违反即失败）

> 完整版见项目 `AGENTS.md` / `CLAUDE.md`。所有 AI 必须遵守。

## 🔴 绝对禁止
1. **禁止 `spring-boot:run`**（8000 端口有运行中后端 pid 19848）——只准 `mvn compile/test`；测试用 RANDOM_PORT 隔离
2. **禁止 `git commit`**（等主人确认）
3. **禁止删除文件**（除非主人明确要求——roleplay-v4 曾从回收站恢复差点删没）
4. **禁止动主链路**：RouterService / ArbiterService / 审批 / 狼人杀 / SSE / static/（除非任务明确要求）
5. **禁止删除 GroupStrategy/DebateStrategy**（需求十三条：降级为默认模式）

## ✅ 必须做
1. **改代码后登记** `docs/修改记录.md`（修改人/时间/文件/摘要/核查状态）
2. **测试后更新** `TEST_STATUS.md`（通过写入，失败也写原因）
3. **新决策追加** `DECISION_LOG.md`（日期/决策/原因/放弃/影响）
4. **新缺陷登记** `docs/问题清单-20260731.md`（D 编号/优先级/证据行号）
5. **文档过时标废弃**（顶部 `> ⚠️ 已废弃：被 X 替代，勿读`）
6. **动 src/main/java 前先 `git diff` 确认基线**（防并行工作流撞车）

## 🛠 环境
- 系统 mvn：`C:\Users\shuweiran\AppData\Local\maven\apache-maven-3.9.8\bin\mvn.cmd`
- 前端：`roleplay-v4/frontend`（React+Vite+Zustand），构建后同步 `static/`
- 后端日志：`boot2-out.log`（当前运行实例）

## ⚠️ 技术陷阱（已踩过）
- **PowerShell 发中文 JSON → GBK 乱码**（"小明"变乱码导致测试误判）→ 用 Python（UTF-8）发请求
- **LLM key 重启后丢失**（application.yml 无 api-key，运行时注入不持久）→ 修复方案：key 写死 application.yml
- **测试必须串行**（并发互相干扰）
- 中文交流

## 📥 接手第一件事
1. 读本包 01 → 02 → 03 → 08（必读四件）
2. `git diff` 看未提交改动（可能含并行会话成果）
3. 跑一次 `mvn test` 确认基线（93 tests 应全绿）
4. 对照 05 问题清单选任务，对照 07 蓝图做剧本杀
