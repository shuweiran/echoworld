# 📋 并行作业登记表 — Roleplay-Java

> **用途**：防止并行 agent（主会话 + 多路子 agent 并发作业）互不认识、互相覆盖/删除文件。
> **启用**：2026-08-01（主人指示）。
> **本表只保留当天作业**：跨天一律视为失效，须重新登记；历史永久保留在 `docs/修改记录.md` 台账。

## 🔒 规则（所有 agent 强制）

1. **开工必读**：任何 agent 在 `D:\roleplay-java` 修改文件前，必须先读本表；若目标文件已被登记为他人占用 → **先协调**（等待完成 / 分工避让），不得直接覆盖或删除
2. **批次登记**：每个并行批次开工时必须登记一行：唯一识别标记 `P-<MMDD>-<序号>`（如 `P-0801-A`）+ agent/任务 + 时间窗 + 涉及文件 + 状态；主 agent 派单时在任务描述中注明批次标记
3. **识别标记只保留当天**：当日作业用当日标记；跨天后昨日标记作废、区块清空（新的一天开工时清理昨日区块，历史见台账）
4. **完成即标记**：批次完成并核查后，状态列改 ✅（含核查人/时间）；git commit 时 commit message 带批次标记
5. **发现未登记改动**：git status 发现无主改动（如未登记的文件变更）→ 登记为「未知批次」占位，通知主 agent 溯源

## 当日作业（2026-08-05）

> 按规则：2026-08-04 及更早登记跨天作废（历史见 `docs/修改记录.md`），新批次用当日标记。

| 标记 | agent/任务 | 时间窗 | 涉及文件 | 状态 |
|---|---|---|---|---|
| P-0805-B（本批） | coder（主人需求「全部做完」批次二：私聊完整闭环（剧本杀核心玩法）+ WebSearch 搜证 + 时间推进。私聊为新增能力非既有架构改动，直接整合不先 demo（后端 PrivateChatService 纯内存无 AI 应答/历史端点，本批补齐：AI 应答走 LLMClient 独立生成 + GET history 端点 + 前端 ChatPage 私聊抽屉）；WebSearch 搜证（ScriptGameService.search 可选联网增强）；时间推进（对局时钟/阶段倒计时提示）。禁动文件 RouterService/ArbiterService/审批/狼人杀/SSE 主链路零改动） | 13:0x-14:3x | 后端 service/ScriptGameService.java（+privateChats 字段+快照持久化/恢复 +privateSay 私聊 AI 应答（persona+秘密+历史→LLM 生成+guardPrivateSecret 认罪守卫）+getPrivateChatHistory 双向键 +startedAt 对局时钟+快照/toMap started_at·elapsed_ms +web-search-enabled 配置+search 可选联网+懒创建 WebSearchService）、controller/ScriptController.java（+POST /api/script/private +GET /api/script/private/history）、application.yml + application-test.yml（+web-search-enabled:false）；测试新增 service/ScriptGamePrivateChatTest 5 用例；前端 api/client.ts（+scriptPrivateSay/scriptPrivateHistory）、components/ChatPage/ChatPage.tsx（💬 私聊按钮+抽屉：目标 chips/历史加载/气泡渲染）；static 同步新产物 index-CqdAlTZ2.js + index-k6prgkhJ.css（SHA256 dist↔static 一致 97D544C9 + 删旧产物 index-BKIx7IAX.js）；docs：修改记录.md（#116）、TEST_STATUS.md（Round 47/v47）、并行作业登记.md（本行）、DECISION_LOG.md（D-041）。禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；不打包不重启 8000 不 git commit | ✅ 完成 2026-08-05 14:3x（全量 mvn **452/0 BUILD SUCCESS**（65 类 surefire 汇总，447 基线零破坏 + ScriptGamePrivateChatTest 5；LONG-01 PASS）；npm run build 通过（tsc 0 错误 + vite 74 modules，index-CqdAlTZ2.js 1,865.81 kB）+ static 同步 SHA256 dist↔static 一致（97D544C9...）+ index.html 更新 + 删旧产物 index-BKIx7IAX.js；台账 docs/修改记录.md #116 + TEST_STATUS Round 47/v47 + DECISION_LOG D-041；待主会话核查） |
| P-0805-C（本批） | coder（主人需求「剩下的全部做完」批次三：①私聊 SSE 实时推送（script_private 事件，会话定向 broadcastToSession）②阶段倒计时强制推进（roleplay.game.script.phase-timeout-ms 可配，INVESTIGATION/DISCUSSION/VOTE 超时自动推进/弃票）③生图真实 provider 适配（ImageSpecService 后端异步生成任务 + 可配 provider 端点调用 + 结果落 assets 登记 + 前端消费）。禁动文件 RouterService/ArbiterService/审批/狼人杀/SSE 主链路零改动） | 15:0x-17:3x | 后端 controller/SSEController.java（+broadcastScriptPrivate）、service/ScriptGameService.java（私聊 SSE 推送；+phaseStartedAt/phaseTimeoutMs+快照+toMap phase_*+maybeAdvanceOnTimeout 惰性推进+touchGame 挂载）、service/ImageSpecService.java（+generateImage 三层 provider/离线 SVG+@Value image.provider-*）、controller/ImageController.java（重写 +POST /api/image/generate：生成→写 static/assets/IMAGES→saveAsset 登记）、controller/AssetController.java（ASSET_TYPES +3 类型）、application.yml + test（+phase-timeout-ms:0 +image.provider-*）；测试新增 service/ImageGenerationTest 3 + ScriptGamePrivateChatTest +3；前端 api/useSSE.ts（+script_private）、api/client.ts（+imageSpec/imageGenerate）、components/ChatPage/ChatPage.tsx（私聊抽屉 3s 轮询实时刷新；阶段 banner 倒计时；🎨 美术抽屉 image_spec→生成→展示）；static 同步 index-C03zxnb0.js + index-laRQDEWy.css（SHA256 一致 88825345 + 删旧产物）；docs：修改记录.md（#117）、TEST_STATUS.md（Round 48/v48）、并行作业登记.md（本行）、DECISION_LOG.md（D-042）。禁动文件零改动；不打包不重启 8000 不 git commit | ✅ 完成 2026-08-05 17:3x（全量 mvn **458/0 BUILD SUCCESS**（65 类 surefire 汇总，452 基线零破坏 + 新增 6 用例；LONG-01 单跑 7.75s PASS——首轮堆 31.1%>30% 为既有脆性 #37/#40/#41 同款）；npm run build 通过（tsc 0 错误 + vite 74 modules，index-C03zxnb0.js 1,869.63 kB）+ static 同步 SHA256 dist↔static 一致（88825345...）+ index.html 更新 + 删旧产物 index-CqdAlTZ2.js/index-k6prgkhJ.css；台账 docs/修改记录.md #117 + TEST_STATUS Round 48/v48 + DECISION_LOG D-042；待主会话核查）。**收尾修复（2026-08-05 13:5x，台账 #118）**：打包重启 8000 真机验证发现生图图片 classpath 不可达（`GET /assets/IMAGES/x.svg` 被 SPA 兜底返回 text/html）→ ImageController 改写入运行时目录 `./data/generated/images/` + 新增 `GET /api/image/file/{name}` 专用端点（显式 content-type + 路径穿越防护）；真机验证 generate→file GET **image/svg+xml**→assets 登记成功；重新打包重启 8000（PID 10608） |

## 当日作业（2026-08-05）

> 按规则：2026-08-04 及更早登记跨天作废（历史见 `docs/修改记录.md`），新批次用当日标记。

| 标记 | agent/任务 | 时间窗 | 涉及文件 | 状态 |
|---|---|---|---|---|
| P-0805-A（本批） | coder（主人需求「全部做完」：对现有架构有影响项先出独立 demo 验证再整合，纯增量项全做。阶段一=生图管线独立 demo：LLM 描述→image_spec 契约→生图 provider（真实 API 可配 + 离线占位降级）→渲染展示，独立静态页 static/simulation/imagegen/imagegen_demo.html，零后端零禁动；阶段二=剧本杀双通道合并 demo；整合 B1/B2/B4/B5 + 记忆检索 + 生图后端接入） | 09:0x-12:5x | 新增 `src/main/resources/static/simulation/imagegen/`（index.html + js/ + README）+ `static/simulation/discussion_demo/`（index.html + js/ + README）；后端 service/ScriptGameService.java（B2 roleNamesById+killerId 结构化判定 / B4 revoteCount 重投上限 / B5 lastAccess TTL 惰性清扫 / buildDiscussionPersona 持有线索注入）、service/ImageSpecService.java + controller/ImageController.java（新增，POST /api/image/spec）、application.yml + application-test.yml（+max-revotes +game-ttl-ms）；测试新增 service/ScriptGameKillerIdRevoteTest 6 + ScriptGameDiscussionTest +1 + ImageSpecServiceTest 5；前端 api/client.ts（+scriptDiscussionSay）、components/ChatPage/ChatPage.tsx（composer 双通道合并路由）、store/appStore.ts（+scriptRoleKey）；static 同步新产物 index-BKIx7IAX.js + index-k6prgkhJ.css（SHA256 dist↔static 一致 + 删 5 旧产物）；docs：修改记录.md（#115）、TEST_STATUS.md（Round 46/v46）、并行作业登记.md（本行）、DECISION_LOG.md（D-040）。禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；不打包不重启 8000 不 git commit | ✅ 完成 2026-08-05 12:5x（全量 mvn **447/0 BUILD SUCCESS**（65 类 surefire 汇总，434 基线零破坏 + 新增 3 测试类 12 用例；LONG-01 PASS）；npm run build 通过（tsc 0 错误 + vite 71 modules，index-BKIx7IAX.js 1,851.72 kB）+ static 同步 SHA256 dist↔static 一致（7005BDC6...）+ index.html 更新 + 删 5 旧产物；两 demo 自测 node 全 PASS（imagegen 15/15 + discussion_demo 6/6）；台账 docs/修改记录.md #115 + TEST_STATUS Round 46/v46 + DECISION_LOG D-040；待主会话核查） |

## 当日作业（2026-08-04）

> 按规则：2026-08-03 及更早登记跨天作废（历史见 `docs/修改记录.md`），新批次用当日标记。

| 标记 | agent/任务 | 时间窗 | 涉及文件 | 状态 |
|---|---|---|---|---|
| P-0804-C（本批） | coder（角色与场景素材库：AssetEntity/AssetRepository/DatabaseService saveAsset 族 + AssetController（POST /api/assets/import + GET /api/assets 过滤 + GET/{id} + DELETE/{id}）+ AssetLibraryTest 8 用例；前端 client.ts +assetList/assetImport/assetGet/assetDelete + MaterialPage 🧰 素材 Tab（类型 chips 过滤/导入表单/删除）+ Phaser 消费：SimulationScene Aseprite 角色动画（agentAnims 可选参数，无素材零破坏）+ ScriptMapScene tilesetUrl 可选外部图集（无素材运行时色块零破坏）+ PhaserScriptMapView/PhaserSimulationView 拉取素材自动接入 + 示例素材文件复制到 static/assets/（CHARACTER_ANIMATION/demo_player/player.png+player.json、SCENE_TILESET/demo_tiles/tiles.png）） | 15:58- | 后端新增 db/entity/AssetEntity.java、db/repository/AssetRepository.java、controller/AssetController.java；改 db/service/DatabaseService.java（+7 参 @Autowired 构造保留 6 参委托 + saveAsset/findAssets/findAssetById/deleteAsset/entityToMap(AssetEntity)）；测试新增 controller/AssetLibraryTest.java；前端 api/client.ts（+assetList/assetImport/assetGet/assetDelete/assetFileUrl/asepriteJsonUrl）、components/MaterialPage/MaterialPage.tsx（+assets Tab）、phaser/SimulationScene.ts（+agentAnims 可选 + preload Aseprite + sprite 渲染回退）、phaser/PhaserSimulationView.tsx（拉取角色动画素材接入）、phaser/ScriptMapScene.ts（+tilesetUrl 可选）、phaser/PhaserScriptMapView.tsx（拉取瓦片素材接入）；static/assets/ 新增示例素材（可写子目录）；docs：修改记录.md、TEST_STATUS.md、并行作业登记.md（本行）、DECISION_LOG.md（D-038）。禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路/既有 static 产物）零改动；不打包不重启 8000 不 git commit | ✅ 完成 2026-08-04 16:15（全量 mvn **434/0 BUILD SUCCESS**（63 类，426 基线零破坏 + AssetLibraryTest 8 用例；LONG-01 前 2 轮堆 47.3%/30.5% 为既有脆性，单跑 PASS 后复跑全绿）；npm run build 通过（tsc 0 错误 + vite 67 modules，index--3UfVeic.js 1,891,327B）+ static 同步 SHA256 dist↔static 一致 + index.html 更新 + 删旧产物 index-4xjAceRg.js/index-B4VS6nUI.js + bundle grep 命中（素材库（角色动画/角色动画/场景瓦片/load.aseprite/createFromAseprite/map-tileset-asset 等）；禁动文件零改动；台账 docs/修改记录.md #103 + TEST_STATUS Round 43/v43 + DECISION_LOG D-038；待主会话核查） |【整改 2026-08-04 16:2x，未衡终审 P-0804-C 整改项：回退 MaterialPage assets Tab 死代码——MaterialPage 为孤儿组件（App.tsx 未注册路由/未 import），assets Tab 是死代码；git checkout 回退 MaterialPage.tsx 至批次前状态（diff +135/-2 全为 assets Tab 改动，回退干净无残留）；素材库唯一入口保留 AssetLibraryPanel（ScenePage 已集成）；npm build 通过（tsc 0 错误，产物 hash 未变 index--3UfVeic.js——孤儿组件代码从未入包，static SHA256 5 项一致，index.html 无需更新）；bundle grep：素材库串 FOUND + MaterialPage 独有串 absent；后端零改动（测试 434/0 基线维持），台账 docs/修改记录.md #104，TEST_STATUS v43 整改补充；待主会话核查】 |
| P-0804-D（本批） | coder（修复 Phaser 外部瓦片图集渲染 tile_size 不匹配，subagent 812409b3）：真机问题——导入的 OpenGameArt CC0 Top Down Tileset（oga_topdown，16px 瓦片 256×400）配地图契约 tile_size=32 → Phaser 警告 "Image tile area not tile size multiple" 且按 32 切片渲染错乱。修复（素材 tile_size 主导）：①PhaserScriptMapView.tsx 拉取 SCENE_TILESET 列表时解析 list[0].meta_json 的 tile_size（新增 parseMetaTileSize：兼容 Java Map.toString "{tile_size=16,...}" 正则提取 + 标准 JSON 两种格式，解析失败回退 undefined），把 tilesetTileSize 传 ScriptMapScene 新第五参（旧调用零破坏）②ScriptMapScene.ts 新增 tilesetTileSize 字段，create 里 ts = (tilesetUrl && tilesetTileSize) ? tilesetTileSize : (m.tile_size || 32)——有外部素材时地图格子/切片/相机边界（mapPxW=width×ts）全按素材 tile_size，无素材回退契约默认 32（逐字节零破坏）；addTilesetImage 不变。兼容性：无 SCENE_TILESET 素材时行为与 P-0804-C 前完全一致（运行时 5 色块 ts=32）。后端零改动。 | 16:52-17:1x | 前端 roleplay-v4/frontend/src/phaser/PhaserScriptMapView.tsx（+parseMetaTileSize +tilesetTileSize 透传）、phaser/ScriptMapScene.ts（+tilesetTileSize 字段 +ts 素材主导）；static 同步新构建产物（index-CsjAvNOf.js 1,891,788B，SHA256 dist↔static 一致，index.html 更新，删旧产物 index--3UfVeic.js）；docs：修改记录.md（#105）、TEST_STATUS.md（Round 44/v44）、并行作业登记.md（本行）。禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路）零改动；重新 mvn package + 重启 8000 真机验证 | ✅ 完成 2026-08-04 17:2x（全量 mvn **434/0 BUILD SUCCESS**（63 类，426 基线零破坏，纯前端无新增 Java 测试；LONG-01 PASS）；npm run build 通过（tsc 0 错误 + vite 67 modules，index-Oe53TptJ.js 1,891,833B）+ static 同步 SHA256 dist↔static 一致（D40CEE90...）+ index.html 更新 + 删旧产物 index-CsjAvNOf.js；bundle grep 命中（tile_size 6/tilesetTileSize 4/map-tileset-asset 3/flexShrink 5）；mvn clean package -DskipTests 重新打包（jar 内仅含新产物）+ 重启 8000（PID 13456）；真机验证（Edge headless + CDP 实机 1440×900）：点开「默认场景」卡地图预览 —— Phaser 主 canvas 800×320 挂载、**console 0 tile 警告**（“Image tile area not tile size multiple” 未出现，证明 ts=16 整除 256×400；若仍用契约 32 则 400%32≠0 必告警）、像素直读缓冲主色 (7,8,12)=31.3%/(17,19,22)=21% 与离线 ts=16 期望渲染直方图 (33.7%/25.3%) 高度吻合=素材真实纹理（非 5 色块）、React 无白屏无异常；**附带修复既有布局缺陷**：scriptSetup 视图 panel-body flex 列在角色卡网格高时将地图预览压至 71px、Phaser canvas 被 overflow:hidden 裁剪不可见（真机验证发现，非本批引入——本批零改 ScenePage 布局前）；修复=ScenePage.tsx 地图预览区包 flexShrink:0 包装 div，修复后 view 恢复 433px、滚动面板后 canvas 完全可见（elementFromPoint 三点击中 CANVAS）；遗留：地图预览位于可滚动 panel-body 底部需滚动查看（角色卡网格高时）；台帐 docs/修改记录.md #105 + TEST_STATUS Round 44/v44；禁动文件零改动；未 git commit（统一 gate 待授权）；待主会话核查） |
| P-0804-E（本批） | coder（删除前端素材库管理页，subagent c16c0fda）：主人需求「素材库不用放前端，2D 游戏内能调用即可」——删除 AssetLibraryPanel.tsx 组件 + ScenePage 素材库 Tab 按钮/面板渲染/import + client.ts 删除 assetImport/assetGet/assetDelete（保留 assetList/assetFileUrl/asepriteJsonUrl 供 Phaser 2D 消费链）；types 无 asset 类型定义无需清理；MaterialPage 已核实无残留（P-0804-C 已回退）不动；static 同步新构建产物 + 删旧产物 index-Oe53TptJ.js；重新 mvn package + 重启 8000 真机验证；后端零改动 | 17:2x- | 前端 roleplay-v4/frontend/src/components/AssetLibraryPanel.tsx（删除）、components/ScenePage/ScenePage.tsx（import L6 + 素材库按钮 L831 + 面板渲染 L1618-19，保留 P-0804-D flexShrink:0 修复）、api/client.ts（-assetImport/-assetGet/-assetDelete）；static 新产物 + index.html；docs：修改记录.md（#106）、TEST_STATUS.md、本表 | ✅ 完成 2026-08-04 18:3x（全量 mvn **434/0 BUILD SUCCESS**（63 类，LONG-01 PASS，后端零改动基线不降）；npm run build 通过（tsc 0 错误 + vite 66 modules，index-rtqdhfbk.js 1,870,842B）+ static 同步 SHA256 dist↔static 一致（C6FEABC9...）+ index.html 更新 + 删旧产物 index-Oe53TptJ.js；bundle grep：管理页串 0（素材库/assetImport/assetDelete/assetGet/AssetLibraryPanel 全 absent）+ 2D 消费链命中（assetList 3/map-tileset-asset 3/SCENE_TILESET 1/CHARACTER_ANIMATION 1）；mvn clean package -DskipTests 重新打包（jar 仅含新产物）+ 重启 8000（PID 31324，17:28，日志 tmp/logs/roleplay-8000.log）；真机验证（Edge headless + CDP 1440×900，tools/cdp_driver_E.mjs）：素材库 Tab 已消失（只剩 一般模式/剧本杀模式 chips）✅ + 默认场景卡地图预览真实瓦片纹理渲染（像素 285 色/主色纯黑 92%≈图集 87-96%，console 0 tile 警告 0 异常）✅ + 素材文件 GET 200 ✅；后端零改动（AssetController/AssetEntity/AssetRepository 全保留）；台账 docs/修改记录.md #106 + TEST_STATUS Round 45/v45；待主会话核查；未 git commit） |
| P-0804-A（本批） | 主会话未为（授权批次：GroupAnchor 完整实现 + 零冲突项 + 编码损坏事故修复重建） | 13:1x-13:5x | 后端：`simulation/movement/MovementConstraint.java`（GroupAnchor leader+follow slot 实现）、`service/ScriptMapService.java`（bsp-seed @Value 注入）、`application.yml`/`application-test.yml`（+bsp-seed 键）、`src/test/.../MovementConstraintTest.java`（GroupAnchor 用例改写）、`stability/LongSimulationConcurrentTest.java`（新增 LONG-04）；前端：`api/useSSE.ts`（断线补发 announcementRecent）、`api/client.ts`（scriptInit +room_code、scriptMap +width/height）、`components/ScenePage/ScenePage.tsx`（联机房 room_code + 地图尺寸/seed UI）、`utils/silenceMarker.tsx`（新增）、`styles/global.css`（静默样式）、`components/ChatPage/ChatPage.tsx`+`phaser/PhaserSimulationView.tsx`（⚠️ 事故：本批用 PowerShell Set-Content 处理时编码损坏，已从 static bundle 逐处重建恢复 + tsc 全绿）、删除 `ChatPage.tsx.bak`；static 同步 index-Bdi8t8bk.js；docs：修改记录 #101 / TEST_STATUS v41 / 本行 / DECISION_LOG D-036 | ✅ 完成 2026-08-04 13:5x（全量 mvn **412/0 BUILD SUCCESS**（58 类 + LONG-04 3 用例 + GroupAnchor 新增 2 用例；首轮 LONG-01 堆 32.3%>30% 为既有脆性 #37/#40/#41 同款，单跑 PASS 后复跑全绿）；npm run build 通过（66 modules，index-Bdi8t8bk.js）+ static 同步 SHA256 一致；禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路）零改动；未 git commit（统一 gate 待授权）） |

## 当日作业（2026-08-04）

| 标记 | agent/任务 | 时间窗 | 涉及文件 | 状态 |
|---|---|---|---|---|
| P-0804-B（本批） | coder（剧本杀 P1 可玩性修复清单：①投票超时+quorum ②玩家退出托管+ENDED 后重开 ③LLM 降级提示，subagent 9d5a3d47） | 13:38-16:xx | 后端 service/ScriptService.java（+generateScriptChecked 兜底标记）、service/ScriptGameService.java（+voteStartedAt/quorumFailCount/lowParticipation/trustees/llmDegraded/theme 字段 +resolveVote 超时弃票+quorum+低参与度 +leaveGame/restartGame +enterVotePhase +快照/恢复新键 +@Value roleplay.game.script.*）、controller/ScriptController.java（+POST /api/script/leave +POST /api/script/restart）、application.yml + application-test.yml（+roleplay.game.script.{vote-timeout-ms,vote-timeout-enabled,quorum-enabled}）；测试新增 service/ScriptGameVoteTimeoutQuorumTest.java + service/ScriptGameLeaveRestartTest.java；前端 api/client.ts（+scriptRestart/scriptLeave）、components/ChatPage/ChatPage.tsx（ScriptStatePanel：llm_degraded 提示条/托管标记/再来一局+回到剧本选择+退出托管按钮）；static 同步新构建产物；docs：修改记录.md（#101）、TEST_STATUS.md（Round 41/v41）、并行作业登记.md（本行）、DECISION_LOG.md（D-036）。禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/MovementConstraint/ScriptMapService/一般模式 2D 模拟）零改动；不打包不重启 8000 不 git commit | ✅ 完成 2026-08-04 16:xx（全量 mvn **426/0 BUILD SUCCESS**（58 类，407 基线零破坏 + 新增 ScriptGameVoteTimeoutQuorumTest 7 + ScriptGameLeaveRestartTest 7 + 并行未登记批次 MovementConstraintTest/LongSimulationConcurrentTest 5 共存；Script 专项 117/0；LONG-01 PASS）；npm run build 通过（tsc 0 错误 + vite 66 modules，index-4xjAceRg.js 1,868.01 kB）+ static 同步 SHA256 dist↔static 一致 + index.html 更新 + 删旧产物 index-Bdi8t8bk.js + bundle grep 8 项命中；台账 docs/修改记录.md #102 + TEST_STATUS Round 42/v42 + DECISION_LOG D-037；待主会话核查） |

## 当日作业（2026-08-03）

> 按规则：2026-08-02 及更早登记跨天作废（历史见 `docs/修改记录.md`），新批次用当日标记。

| 标记 | agent/任务 | 时间窗 | 涉及文件 | 状态 |
|---|---|---|---|---|
| P-0803-O（本批） | coder（两条地图链路 LLM 全量生成，subagent 3e144626）：①后端 SceneController.generateDefaultMap（POST /api/scenes/map）双模式——body 可选 theme：非空走 ScriptMapService LLM 全量生成统一路径（契约 v1 校验 + 失败/超预算 BSP 兜底，响应附加 mode/generator/validation/fallback 溯源键），空/缺省保持 BSP 确定性零回归（P-0803-H 既有行为）；注入可行性已核实：ScriptMapService 为 @Service 仅依赖 LLMClient，SceneController→ScriptMapService→LLMClient 无环，5 参 @Autowired 构造 + 4 参旧构造委托 null（防御回落 BSP）②前端剧本编辑弹窗「生成方式」选择（✨ LLM 全量生成需主题 / BSP 默认无主题），生成后绑定 default_map 逻辑不变，既有 BSP「生成默认地图」按钮保留为 BSP 模式入口 ③前端剧本杀设置页地图区（gameSetup + rules script tab 两处）theme 暴露为可编辑输入（默认剧本名可改，genScriptMap/genScript chat 自动地图均消费），seed/尺寸参数后端已支持 UI 保持现状 ④新增后端测试 SceneMapLlmModeTest（LLM 成功/无 theme BSP 零回归/LLM 失败 BSP 兜底/4 参构造防御）。禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路）零改动；不打包不重启 8000 不 git commit | 23:5x-25:xx | 后端 controller/SceneController.java（+ScriptMapService 注入 +generateDefaultMap 双模式）；测试新增 controller/SceneMapLlmModeTest.java；前端 roleplay-v4/frontend/src/api/client.ts（sceneMap +theme 参数）、components/ScenePage/ScenePage.tsx（编辑弹窗生成方式选择+主题输入、剧本杀设置页地图区 theme 输入、mapTheme 状态）；static 同步新构建产物；docs：修改记录.md（#100）、TEST_STATUS.md（Round 40/v40）、并行作业登记.md（本行） | ? 完成 2026-08-03 25:0x（单跑 SceneMapLlmModeTest 4/0 + SceneBindingTest 5/0 零回归；全量 mvn **407/0 BUILD SUCCESS**（58 类，403 基线 + 新 4 用例，LONG-01 PASS 2.529s）；npm run build 通过（tsc 0 错误 + vite 65 modules，index-StVXWN_F.js 1,863.64 kB）+ static 同步 SHA256 dist?static 一致（JS/CSS 双哈希 + index.html 引用更新 + 删旧产物 index-Cx-YssBi.js）+ bundle grep 命中（生成方式：/LLM 全量生成/BSP 默认/民国宅邸凶案/LLM 全量输出（契约 v1 校验）/失败自动 BSP 兜底/BSP 确定性生成（契约 v1）/默认剧本名可改/sceneMap body 签名）；零改动禁动文件；不打包不重启 8000 不 git commit；台账 docs/修改记录.md #100 + TEST_STATUS Round 40/v40 + DECISION_LOG D-035；待主会话核查） |
| P-0803-N（本批） | coder（点开剧本卡后角色区只剩 me 的修复，subagent fb715635）：根因=selectScript 点开剧本卡后 setCharTab(scene.scene_id) 切到该剧本 default_roles 分组，而 DB 9 个剧本 default_roles 全空 → tabChars 返回空列表 → renderCharGrid 只剩固定渲染的 me-char-card。修复：①selectScript 内 setCharTab 按需回退——剧本 default_roles 非空才切到该剧本分组（分类跟着剧本走语义不变），为空保持/回退 'all'（默认展示全部角色卡可自选）；②tabChars 防御性回退——剧本 default_roles 为空时返回全部角色卡（覆盖手动点击空分组 chips 的同类空网格路径）；③自动预选逻辑（default_roles 入 selected 排除 myCharName）零改动；④gameSetup 视图复核：openGameSetup 已 setCharTab('all') 无同类问题；rules 模式角色库独立 chips 不受影响。前端单文件最小改动，不重构 renderCharGrid。禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路/后端）零改动；npm build + static 同步（SHA256 dist↔static 一致 + index.html 更新 + 删被取代旧产物）+ bundle grep + 全量 mvn test 基线 403/0 不得下降；不打包不重启 8000 不 git commit | 23:30-24:xx | 前端 roleplay-v4/frontend/src/components/ScenePage/ScenePage.tsx（selectScript 约 208-225 行 setCharTab 回退 + tabChars 约 150-160 行空 default_roles 回退）；static 同步新构建产物；docs：修改记录.md（#99）、TEST_STATUS.md（Round 38/v38）、并行作业登记.md（本行） | ✅ 完成 2026-08-03 23:5x（根因复核与主会话定位一致：9 剧本 default_roles 全空 → charTab 切空分组 → 只剩 me 卡；修复：selectScript setCharTab 按需回退（roles 非空才切组，空保持 'all'）+ tabChars 空 default_roles 回退全部角色卡，自动预选逻辑零改动；npm run build 通过（tsc 0 错误 + vite 65 modules，index-Cx-YssBi.js 1,862.23 kB）+ static 同步 SHA256 dist↔static 一致 + index.html 更新 + 删被取代旧产物 index-ChF7_wXr.js + bundle grep 验证（修复逻辑 minified 形态 + 关键串命中 + 自动预选排除逻辑与旧产物一致）；全量 mvn **403/0 BUILD SUCCESS**（58 类，首轮 LONG-01 堆测量 31.1%>30% 为既有脆性 #37/#40/#41 同款，单跑 PASS 后复跑全绿 + MAVEN_EXIT_CODE=0）；零改动禁动文件/后端；不打包不重启 8000 不 git commit；台账 docs/修改记录.md #99 + TEST_STATUS Round 39/v39；待主会话核查）⚠️ 与 P-0803-M（chat 地图开关）同文件 ScenePage.tsx 不同区域共存，构建基于含双方改动的完整源码 |
| P-0803-K（本批） | coder（剧本杀模式多地图切换，subagent 1c283f43）：①对局层多图注册表——ScriptGame +maps（mapId→契约 v1 数据）+mapFallbacks（每图溯源）+searchedByMap（每图足迹）+currentMapId；generateMap 生成即注册并设为当前（map_id 显式/自动 map_<n>，LLM/BSP 输出 map_id 统一归一为注册表键）②door zone 触发切换——POST /api/script/map/switch（door_zone_id 必须命中当前图 type=door zone + 可选 x/y 靠近校验（radius+容差）+ 目标解析 body target_map_id→zone target/to/target_map_id→未注册自动生成 BSP 兜底；非法 door 目标全容错：不存在/非 door 型/无目标/目标=当前图/阶段不符/非本局玩家）③切换后全员同步——复用现有 script_status SSE（全量状态含新 map）+ announcement SYSTEM 横幅，SSE 主链路零改动④尺寸联动——每图独立尺寸，切换后 mapWidth/mapHeight 随目标图更新，自动生成目标图取 door 可选 width/height⑤状态迁移——角色/线索/AP/秘密/票型对局级天然保留，足迹按图隔离迁移（切走暂存切回恢复）；注册表/当前图/每图足迹随快照持久化；toMap 暴露 current_map_id/map_ids；POST /api/script/map/door 布门端点 + LLM prompt 预留 door 可选输出。禁动文件（SceneController/SceneEntity/static/前端/SSE 主链路/RouterService/ArbiterService/审批/狼人杀）零改动；不打包不重启 8000 不 git commit | 22:5x-23:5x | 后端 service/ScriptGameService.java（+多图注册表 +switchMap +addDoorZone +快照 maps/current_map_id/searched_by_map +toMap 新键）、controller/ScriptController.java（+POST /api/script/map/switch +POST /api/script/map/door +map 端点透传 map_id）、service/ScriptMapService.java（buildPrompt 预留 door zone 可选输出）；测试新增 service/ScriptMapSwitchTest.java；docs：修改记录.md（#97）、TEST_STATUS.md（Round 36/v36）、并行作业登记.md（本行）、DECISION_LOG.md（D-032） | ✅ 完成 2026-08-03 23:4x（定向 8/0 + 全量 **403/0 BUILD SUCCESS**（58 类，390 基线零破坏 + ScriptMapSwitchTest 7（K1-K7）+ ScriptMapSwitchPersistenceTest 1（K8）+ 并行 ScriptChatModeTest 5 共存；LONG-01 首轮堆测量 32.5%>30% 为既有脆性，单跑 PASS 后全量复跑全绿）；修复 4 失败（2 实现 bug：generateMap 补足迹迁移（K5/K8 连锁根因）+ 2 测试 bug：K7 缺 session_id 改显式空串 / K4 h)双缺移至搜证阶段 / K8 Bob 用本人 roleKey）；台账 docs/修改记录.md #97 + TEST_STATUS Round 36/v36 + DECISION_LOG D-032；UTF-8 无 BOM hex 校验通过；零改动禁动文件；不打包不重启 8000 不 git commit；待主会话核查） |
| P-0803-J（本批） | coder（剧本杀地图容量扩展，subagent a45f6830）：①尺寸参数贯穿地图生成 API 链路（POST /api/script/map body 可选 width/height → ScriptGameService.generateMap 重载 → ScriptMapService.generateMap 重载 → BspMapGenerator，默认 24×16 零破坏）②尺寸进配置（roleplay.game.map.default-width/default-height 双份 yml）③热点数按面积缩放（BspMapGenerator.scaledZonesCount，sqrt 缩放下限 3，Options.of zonesCount<0 语义=按面积自动缩放，默认尺寸下与旧值 3 一致）④大图超 LLM token 预算（40×24）跳过 LLM 直接 BSP（fallback 原因注明）⑤新增参数化测试（64×64/48×48/100×60/128×64 大图生成+校验+热点缩放）+ 快照持久化 map_width/map_height。禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE/static/前端）零改动；不打包不重启 8000 不 git commit | 22:3x-23:5x | 后端 `simulation/map/BspMapGenerator.java`（+scaledZonesCount + Options.of 自动缩放）、`service/ScriptMapService.java`（+LLM_MAX_WIDTH/HEIGHT 预算闸 + generateMap/buildPrompt 尺寸重载）、`service/ScriptGameService.java`（+@Value map 默认尺寸 + ScriptGame.mapWidth/Height + generateMap 重载 + 快照 map_width/map_height）、`controller/ScriptController.java`（map 端点透传 width/height）、`application.yml` + `application-test.yml`（+roleplay.game.map.default-width/height）；测试新增 `service/ScriptMapSizeExpansionTest.java`（参数化大图 + 预算闸 + 尺寸透传）+ `service/ScriptMapPersistenceTest.java` 增 M9（大图尺寸快照恢复）；docs：修改记录.md（#96）、TEST_STATUS.md（Round 35/v35）、并行作业登记.md（本行）、DECISION_LOG.md（D-031） | ✅ 完成 2026-08-03 22:5x（全量 mvn **390/0 BUILD SUCCESS**（55 类，378 基线零破坏 + ScriptMapSizeExpansionTest 11 参数化用例 + ScriptMapPersistenceTest M9 1 用例，LONG-01 PASS 2.337s）；UTF-8 无 BOM hex 校验 6 文件；零改动禁动文件；不打包不重启 8000 不 git commit；待主会话核查） | coder（剧本选择与角色卡功能改造，subagent 3ec9d490）：①前端「场景选择」整体改为「剧本选择」（场景→剧本文案全量替换）②剧本卡绑定默认角色组+默认地图（后端 SceneEntity/SceneController 增 category/default_roles/default_map 三字段持久化 + 新端点 POST /api/scenes/map BSP 确定性生成默认地图，前端编辑弹窗可绑定；选择剧本自动带上默认角色+地图预览）③场景分类：一般模式/狼人杀模式（category 字段+前端分类 chips）④点开剧本卡显示角色卡栏+地图预览 ⑤角色卡按所属场景（剧本 default_roles）分组展示+自由角色卡页 ⑥用户角色卡每栏置顶+不默认勾选 ⑦2D 规则：狼人杀默认 2D（checkbox 默认勾选），一般模式给「是否 2D」选择 ⑧角色卡添加/删除（删除按钮+client deleteCharacter 接线）。禁动文件（RouterService/ArbiterService/审批/狼人杀/剧本杀 Service/SSE 主链路）零改动；不打包不重启 8000 不 git commit | 18:0x-19:xx | 后端 src/main/java/com/roleplay/engine/db/entity/SceneEntity.java（+category/defaultRoles/defaultMap 列）、db/service/DatabaseService.java（saveScene 8 参重载+entityToMap 新键）、controller/SceneController.java（create/update/persist 新字段+POST /api/scenes/map）；前端 roleplay-v4/frontend/src/types/index.ts（Scene+3 字段）、api/client.ts（+sceneMap）、components/ScenePage/ScenePage.tsx（剧本选择/角色卡分组/删除/置顶/2D 规则改造）、styles/global.css（剧本卡/分类 chips 样式）；docs：修改记录.md、TEST_STATUS.md、并行作业登记.md（本行） | ✅ 完成 2026-08-03 18:2x（全量 mvn **378/0 BUILD SUCCESS**（54 类，SceneBindingTest 5 用例）+ npm run build 通过（tsc 0 错误 + vite 65 modules，index-BvHFWNFM.js）+ static 同步 SHA256 dist↔static 一致 + index.html 更新 + bundle grep 命中（剧本选择/自由角色卡/生成默认地图/默认 2D/是否 2D/sceneMap）；零改动禁动文件；不打包不重启不 git commit；待主会话核查。⚠️ 本批标记与 2026-08-03 18:12 并行「scripts.name 修复批次」撞标，已提请主会话协调） |
| P-0803-L（本批；原顺延 K 与多地图批次撞标，主会话裁决改签 L） | coder（剧本选择净化与剧本杀双版本改造，subagent 5e96fc0e；⚠️ P-0803-J 标签已被地图容量扩展批次 a45f6830 占用，本批顺延 K，已提请主会话知悉）：
①剧本选择页净化：去掉内嵌角色库栏，页面只分两类页签「一般模式 / 剧本杀模式」；
②选中剧本 → 新增独立设置页（非弹窗）：角色选择（默认角色组跟剧本 default_roles 走 + 用户角色卡置顶不默认勾选 + 角色卡增删/编辑/新建保留）+ 2D 设置 + 启动按钮；
③剧本杀模式 = 双版本（主人选 B）：真剧本杀（scriptInit full + 地图搜证，现有 ScriptGameService/scriptMap 流程）/ 简单对话版（scriptInit mode=chat：无取证无地图，init 直达 DISCUSSION 并自动启动讨论引擎；设计出处=蓝图 v3 Step3v 降级路径「轮次发言」+ raw 报告 N-1 聊天页对话通道，详见最终报告）；
④启动分流：一般模式→start/openPhaserSim（2D 自选）；真剧本杀→genScript(full)；简单对话版→genScript(chat)；
⑤ChatPage 简单版隐藏搜证区/2D 讨论区（scriptState.mode 驱动）；
⑥保留 P-0803-H 全部能力（角色卡增删/置顶/不默认勾选、剧本绑定默认角色/地图、狼人杀默认 2D 逻辑零改动）。禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路）零改动；不打包不重启 8000 不 git commit | 22:45-24:xx | 后端 service/ScriptGameService.java（initGame mode 重载 + ScriptGame.mode + toMap.mode + 快照落库/恢复 mode）、controller/ScriptController.java（init 读 mode）；前端 api/client.ts（scriptInit +mode）、components/ScenePage/ScenePage.tsx（净化/设置页/双版本/启动分流）、components/ChatPage/ChatPage.tsx（简单版隐藏搜证/2D 讨论区）；测试新增 service/ScriptChatModeTest.java；docs：修改记录.md（#98）、TEST_STATUS.md（Round 37/v37）、并行作业登记.md（本行） | ✅ 完成 2026-08-03 24:0x（后端 mode 能力已就位：ScriptChatModeTest 5/0 通过；前端 npm run build 通过（tsc 0 错误 + vite 65 modules，index-NSMdVek6.js）+ static 同步 SHA256 dist↔static 一致 + index.html 更新 + 删旧产物 3 个 + bundle grep 命中（简单对话版 9/真剧本杀 4/剧本杀模式 2/游戏模式 1/script-version-card 2）；全量 mvn **403/0 BUILD SUCCESS**（58 类，378 基线零破坏 + 并行批次用例共存）；零改动禁动文件；不打包不重启 8000 不 git commit；待主会话核查。⚠️ 原顺延 P-0803-K 与多地图批次 1c283f43 撞标，主会话裁决本批改签 P-0803-L） |
| P-0803-M（本批） | coder（简单对话版可选配置地图，subagent 3b067206）：①ScenePage 剧本杀设置页 chat 版新增「🗺️ 配置地图」开关（默认关=保持 P-0803-L chat 无地图基线；开启后显示地图生成/预览区，复用 genScriptMap/PhaserScriptMapView，full 版现有逻辑零改动）②启动链路：genScript('chat') 开启地图时 init 成功后自动调 POST /api/script/map 生成并携带地图（已核实该端点无 phase/mode 守卫，chat 模式可用 → 后端零改动）；关闭则不生成不显示③ChatPage：chat 模式有地图时 ScriptStatePanel 增「🗺️ 对局地图（氛围展示）」查看入口 + chat-main 面板放行（复用 PhaserScriptMapView 新增 readOnly 只读模式：禁搜证交互/提示文案区分，WASD/缩放/全屏保留）；无地图维持纯对话④PhaserScriptMapView/ScriptMapScene +readOnly prop。禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE 主链路/后端）零改动；不打包不重启 8000 不 git commit | 23:2x-24:xx | 前端 phaser/ScriptMapScene.ts（+readOnly 禁搜证）、phaser/PhaserScriptMapView.tsx（+readOnly prop）、components/ScenePage/ScenePage.tsx（chat 地图开关+地图区+启动自动生成）、components/ChatPage/ChatPage.tsx（chat 地图查看）；static 同步新构建产物；docs：修改记录.md（#99）、TEST_STATUS.md（Round 38/v38）、并行作业登记.md（本行）、DECISION_LOG.md（D-034） | ✅ 完成 2026-08-03 24:1x（全量 mvn **403/0 BUILD SUCCESS**（58 类 surefire 汇总，基线零破坏，LONG-01 PASS 2.520s）；npm run build 通过（tsc 0 错误 + vite 65 modules，首构建 index-ChF7_wXr.js 1,862.20 kB）+ static 同步 SHA256 dist↔static 一致 + 删旧产物 index-NSMdVek6.js；⚠️ 与并行批次 **P-0803-N**（23:31:31 重构建）共存：其最终构建 index-Cx-YssBi.js 基于含本批改动的完整源码（同改 ScenePage.tsx 不同区域），SHA256 dist↔static 一致 + index.html 指向新产物 + bundle grep 复核命中（配置地图 5/氛围展示 10/只读浏览 1/对局地图（氛围展示 2/readOnly 10/简单对话版 12/script-version-card 2/无取证 4/无地图 2/生成地图 14）；后端零改动（POST /api/script/map 无 phase/mode 守卫 chat 模式可用已核实）；零改动禁动文件；不打包不重启 8000 不 git commit；待主会话核查） |
| P-0803-H2（本批） | 主会话未为（对局找回 + 超时 600s：前端恢复对局入口 + 后端 currentSessionId） | 19:20-20:1x | client.ts（600s）、ScenePage.tsx（恢复对局入口）、ScriptController.java（resume 设 currentSessionId）、static index-DYK27jhu.js；8000 重启 PID 28992；docs：修改记录 #94 / TEST_STATUS / 本行 | OK 完成（API 闭环 + 浏览器实测进入搜证阶段；mvn compile 0 错；待主会话核查） |
| P-0803-H3（本批） | 主会话未为（2D 模拟显示对局地图：ChatPage script 模式渲染 PhaserScriptMapView） | 20:1x-20:3x | ChatPage.tsx（渲染分支 + import）、static index-Ctpf4Fmo.js；8000 重启 PID 26840；docs：修改记录 #95 / TEST_STATUS / 本行 | OK 完成（浏览器截图验证对局地图 24×16 BSP + 线索点；待主会话核查） |
| P-0803-G（本批） | coder（轨道系统用户加入前端 2D 加入入口，subagent 27742020，方案A 前端部分，纯前端+static 同步，零后端改动）：①api/client.ts +joinConversation/leaveConversation（POST /api/simulation/group/{groupId}/join·leave，body player_name，对齐既有 request 错误处理）②SimulationScene 群组框叠加「💬 加入对话 / 🚪 离开对话」悬浮按钮（applyGroups 增 opts{playerName,playerInWorld}；可加入判定：玩家在场+未在组内+非 DYAD（DYAD 上限 2 必满）+组有 id；玩家在组→离开入口；点击经 SceneCallbacks.onGroupAction 上抛；hitTestPointer 防误触移动目标）③PhaserSimulationView 交互闭环（handleGroupAction 调 join/leave → 成功/失败可见提示（聊天面板系统消息 + 地图角标 toast 4.5s 自消）+ 手动触发一次 conversation-status 刷新，4s 轮询兜底）④simulationData.ts SimGroup +id/idleMs ⑤npm run build + static 同步（SHA256 dist↔static 一致 + index.html 引用更新 + 删被取代旧产物）+ bundle grep 命中（joinConversation/加入对话/离开对话）⑥docs：修改记录（#91，表首）、TEST_STATUS（Round 33/v33，后端基线 372/53 不动，注明纯前端）、并行作业登记（本行）。零后端改动（不动 RouterService/ArbiterService/狼人杀/剧本杀/SSE/P-0803-E 地图增强功能本身）；不打包 jar、不重启 8000、不 git commit | 13:2x-14:1x | 前端 `roleplay-v4/frontend/src/api/client.ts`（+joinConversation/leaveConversation）、`phaser/SimulationScene.ts`（群组按钮 + SceneCallbacks.onGroupAction + applyGroups opts）、`phaser/PhaserSimulationView.tsx`（handleGroupAction/即时刷新/可见提示）、`phaser/simulationData.ts`（SimGroup +id/idleMs）；`src/main/resources/static/` 同步新构建产物（SHA256 dist↔static 一致 + index.html 引用更新 + 删除被取代旧产物）；docs：`修改记录.md`（#91）、`TEST_STATUS.md`（Round 33/v33 前端注记）、`并行作业登记.md`（本行） | ✅ 完成 2026-08-03 14:1x（npm run build 通过（tsc 0 错误 + vite 65 modules，index-CD_FWSaH.js）+ static 同步 SHA256 dist↔static 一致 + index.html 引用更新 + 旧 index-lbgMwQKU.js 已删 + bundle grep 命中（joinConversation/leaveConversation 各 2、加入对话/离开对话 各 5）；零后端改动（后端基线 372/53 不动）；待主会话核查） |
| P-0803-I（本批；原 P-0803-H 与 coder 剧本批次撞标，主会话裁决改签 P-0803-I） | 主会话未为（HTTP 500 修复补全：scripts.name 溢出，调研 P1+P1' 实施） | 18:10-18:20 | ScriptEntity.java（name 2000）、DatabaseService.java（saveScript 截断 500 + saveCharacter 截断 200）、ScriptSchemaV1.java（title 源头规约 100）；H2 直查 SCRIPTS.NAME=2000；8000 重启 PID 9344；docs：修改记录 #93 / TEST_STATUS / 本行 | OK 完成 2026-08-03 18:20（排除 LONG-01 全量 372/0；H2 验证列宽 2000；待主会话核查） |
| P-0803-F（本批，⚠️ 批次标记两次顺延：任务书标 P-0803-B 已被 analyst 调研批次占用（已完成）→ 初拟 P-0803-E 又被主会话地图增强前端批次（13:03 登记）占用 → 最终登记 F） | coder（轨道系统用户加入后端原语，subagent 4b06e1b3，方案A 后端部分，纯后端+测试零前端零 static）：①ConversationGroup +addParticipant/removeParticipant（重复加入防护 + 上限 maxParticipants 满员校验 + 成员访问器同步化防 join/executeRound 并发 CME）②ConversationManager +joinGroup/leaveGroup 原语（组存在/角色存在在场/未在组/未满 校验链 → 加入 → 在场状态对齐 startGroup + 组内轨道重算；leave 无人自动解散）+ tick DYAD 双路径 isBusy 防护（玩家在组内不再自动建 DYAD，消息由所在组玩家分支消费）+ startGroup DYAD 上限 2 接线③SimulationController +POST /api/simulation/group/{groupId}/join·leave（player_name，21→23 端点，conversation-status participants 自动反映）④SimulationService 委托 ⑤新增测试 ConversationJoinTest 13 用例 + 全量 mvn 372/0 BUILD SUCCESS（359 基线零破坏，53 类）。零改动 RouterService/ArbiterService/审批/狼人杀/SSE/static/前端；不打包不重启 8000 不 git commit | 12:55-13:2x | 后端 src/main/java/com/roleplay/engine/simulation/conversation/ConversationGroup.java、simulation/conversation/ConversationManager.java、simulation/SimulationController.java、simulation/SimulationService.java；测试 src/test/java/com/roleplay/engine/simulation/conversation/ConversationJoinTest.java（新增）；docs：`修改记录.md`（#90）、`TEST_STATUS.md`（Round 32/v32）、`并行作业登记.md`（本行） | ✅ 完成 2026-08-03 13:2x（全量 372/0 BUILD SUCCESS，53 类，LONG-01 PASS；单跑 13/0；待主会话核查 + 8000 重启生效） |
| P-0803-G（本批） | 主会话未为（HTTP 500 修复：LLM 剧本 title 超 255 字符 → scenes.name 溢出） | 16:43-16:56 | SceneEntity.java（name 列 2000）、DatabaseService.java（truncateName 截断 500）；H2 直查验证 709→500；8000 重启 PID 3756；docs：修改记录 #92 / TEST_STATUS / 本行 | OK 完成 2026-08-03 16:56（排除 LONG-01 全量 372/0；H2 验证 DB 500 + 列宽 2000；测试数据已清理；待主会话核查） |
| P-0803-A（本批） | researcher（轨道系统用户加入调研补齐 + 输入质量记录，subagent 13e9c0ca）：①复核并补齐 `docs/轨道系统用户加入调研-20260803.md`（关键行号证据复核通过；修正端点计数 26→21；新增 §2.5 一般模式对照 / §4.2 边界清单 / §9 数据模型权限 / §10 输入质量附录）②新增 `docs/输入质量记录-20260803.md` ③登记 INDEX/修改记录。**零改动 src/main/static/前端（纯文档）** | 12:16-12:5x | docs/轨道系统用户加入调研-20260803.md、docs/输入质量记录-20260803.md、docs/并行作业登记.md（本行）、docs/INDEX.md、docs/修改记录.md（#86） | ✅ 完成（纯文档零代码，待主会话核查） |
| P-0803-B（本批） | analyst（地图功能可行性调研复核补齐 + 输入噪声附录，subagent 930b3282）：①复核既有 `docs/地图功能可行性调研-20260803.md` 全部关键断言（后端行号/前端行号/校验项/生成参数，**全部通过**；唯一漂移 PhaserScriptMapView height 默认 560 vs 旧记 480，系并行 P-0802-P4 前端改动）②补充「需求背景与需求来源」章节（需求文档-完整需求.md 缺失来源缺口 + D-020 决策链 + 前端问题调研 UX 缺口；需求四维拆解）③新增附录二逐文件输入噪声统计（24 项：总摄入 ≈286KB、噪声 ≈120.9KB、≈42.3%；剔除门禁必读大文件后 ≈25.2%；直接证据面 ≈19.8%）④登记 INDEX/修改记录。**零改动 src/main/static/前端（纯文档）** | 12:3x-12:4x | docs/地图功能可行性调研-20260803.md、docs/并行作业登记.md（本行）、docs/INDEX.md、docs/修改记录.md（#87） | ✅ 完成（纯文档零代码，待主会话核查） |
| P-0803-D（本批） | 主会话未为（地图增强实施批次）：①ScriptMapService maxTokens 800→4000（G1-3）②生成后覆盖 pass（4-A：补 zone 覆盖全部线索地点）③ScriptGameService.initGame 自动 generateMap（1-A 自动串联）④新增测试 + mvn 全量验证 ⑤登记修改记录/TEST_STATUS。**零改动禁动文件（RouterService/ArbiterService/审批/狼人杀/SSE/static）** | 12:47-13:3x | src/main/java/com/roleplay/engine/service/ScriptMapService.java、service/ScriptGameService.java、测试类、docs/修改记录.md、docs/TEST_STATUS.md、docs/并行作业登记.md（本行）、docs/输入质量记录-地图实施-20260803.md | ✅ 完成 2026-08-03 13:0x（全量 359/0 BUILD SUCCESS，待主会话核查；零改动禁动文件） |
| P-0803-E（本批） | 主会话未为（地图增强批次：①前端相机/小地图/全屏 ②方案 B 搜证足迹持久化 ③static 清理） | 13:03-13:2x | ①phaser/ScriptMapScene.ts+MiniMap.tsx+PhaserScriptMapView.tsx+global.css（相机跟随/滚轮缩放/小地图/全屏）②ScriptGameService searchedLocations 快照持久化 + 前端 restoreSearched + ScenePage init 自动地图消费 ③static 清理 3 旧产物；8000 重启 PID 21140；docs：修改记录 #89/#90 / TEST_STATUS / 本行 | OK 完成 2026-08-03 13:2x（mvn 373/0 + 冒烟 23/23 ALL PASS + npm build 65 modules + SHA256 一致 + HTTP 200；零改动禁动文件；待主会话核查） |
| P-0803-F（本批） | 主会话未为（剧本生成超时 abort 修复：signal is aborted without reason） | 16:14-16:2x | LLMClient.java（超时重载）、ScriptMapService.java（地图 45s）、client.ts（300s + AbortError 友好化）、测试 mock 3 参 stub ×8、static 同步 index-Cfl3kfPS.js；8000 重启 PID 26236；docs：修改记录 #91 / TEST_STATUS / 本行 | OK 完成 2026-08-03 16:2x（相关测试 BUILD SUCCESS + 排除 LONG-01 全量通过 + npm build + HTTP 200；LONG-01 单独 PASS 系既有脆性；待主会话核查） |
| P-0803-C（本批） | analyst（工程文件输入噪声统计，subagent 3edfabe5）：全量扫描 388 个工程文本文件（排除 .git/target/node_modules/dist/static bundle/maven/logs/data/backend-data/docs-archive/*.log 等生成物与运行时产物），逐文件统计 总行数/总字节/空行/纯注释行/未完成占位标记行（TODO/FIXME/XXX/占位符/示例填充 严格口径）/重复冗余行（重复块+注释分隔线），汇总噪声占比 17.13%（690,722B/4,031,813B 行内容字节口径；磁盘口径 16.74%）/有效内容 82.87%；新增 `docs/工程文件噪声统计-20260803.md`（UTF-8 无 BOM，含方法说明与排除清单）+ 登记 INDEX。**零改动代码/static/既有文档（纯新增报告）** | 12:32-12:5x | docs/工程文件噪声统计-20260803.md（新增）、docs/并行作业登记.md（本行）、docs/INDEX.md（登记）；脚本产物留档 analyst 工作区 noise_scan.py/noise_files.csv | ? 完成（纯文档新增，待主会话核查） |
## 当日作业（2026-08-02）

| 标记 | agent/任务 | 时间窗 | 涉及文件 | 状态 |
|---|---|---|---|---|
| P-0802-P4（本批） | coder（改造方案 Phase 4 前端收尾，subagent a5bd22f1）：①client.ts +playerRename（POST /api/player/rename，body player_id+old_name+new_name，复用 getPlayerId）②ScenePage/ChatPage 角色库改名弹窗：玩家本人角色（绑定 player_id）改名改调 playerRename，成功后同步本地 playerName/store 角色名；非绑定角色仍走 PUT /api/characters/{name} ③消除「一玩家一角色」409 副作用：仅第一个创建角色（当前无绑定角色时）携带 player_id 自动绑定，之后创建的不携带；编辑已绑定角色仍携带、未绑定不携带（store 记录 boundCharacterName 用 localStorage 持久化，对齐 getPlayerId 先例）④npm build + static 同步（含 P-0802-M 已同步产物之上的最终构建）+ bundle grep 验证。⚠️ 与已完成批次共存：P-0802-M 前端改动（streaming/流式/G2）已落盘且其构建产物 index-Cc-70P4p.js 已同步 static（23:15），本批在其上增量叠加；后端 Java 零改动；SSE 零改动；不 git commit | 00:0x- | 前端 `roleplay-v4/frontend/src/api/client.ts`（+playerRename）、`components/ScenePage/ScenePage.tsx`（改名弹窗分支 + 创建角色 player_id 规则）、`components/ChatPage/ChatPage.tsx`（改名弹窗分支）、`store/appStore.ts`（+boundCharacterName 持久化镜像/改名同步）、`types/index.ts`（如有类型需补）；`src/main/resources/static/` 同步新构建产物（删除被取代的 index-Cc-70P4p.js）；docs：`修改记录.md`（顺延编号）/ `TEST_STATUS.md`（Round 30/v30 前端注记）/ `并行作业登记.md`（本行）。零改动后端 Java/SSE 主链路/RouterService | ⏳ 进行中 |
| P-0802-P3（本批） | coder（改造方案 Phase 3 局中改名端点·同步式，subagent d6b0e8be）：新 PlayerController（POST /api/player/rename）+ PlayerIdentityService.renamePlayerCharacter 编排（撞名校验②→角色库改名→四服务同步→失败回滚→synced_sessions）+ RouterService.renameAgent（⚠️ 已获主人授权 2026-08-02 23:16 主会话「继续」确认，沿用 P-0802 授权链；仅新增方法不改既有签名）+ SimulationWorld.renameAgent + AgentState.rename（:8 去 final）+ SimulationService.renamePlayerCharacter + WerewolfService.renamePlayer + ScriptGameService.renamePlayer（+saveSnapshot 增补 player_id_bindings + restore/resume 按绑定重映射）+ 两 controller playerSessions 键同步；新增 PlayerRenameE2ETest + ScriptRenameResumeTest；⚠️ 与进行中 P-0802-M 共存：RouterService 仅新增 renameAgent 方法（串行区之外的独立区，不覆盖其 executeRoundSerial 流式改动），先读后改；不改既有 PUT /api/characters/{name} 契约；不 git commit | 23:16-23:5x | 后端 `controller/PlayerController.java`（新）、`service/PlayerIdentityService.java`（+renamePlayerCharacter 编排+回滚）、`service/RouterService.java`（授权：+renameAgent）、`simulation/SimulationWorld.java`（+renameAgent）、`simulation/AgentState.java`（:8 去 final +rename）、`simulation/SimulationService.java`（+renamePlayerCharacter）、`service/WerewolfService.java`（+renamePlayer）、`controller/WerewolfController.java`（playerSessions 键同步）、`service/ScriptGameService.java`（+renamePlayer +快照 player_id_bindings）、`controller/ScriptController.java`（playerSessions 键同步）；测试：新增 `controller/PlayerRenameE2ETest.java` + `service/ScriptRenameResumeTest.java`；docs：`修改记录.md` #83 / `TEST_STATUS.md` Round 29 / `并行作业登记.md`（本行）；零改动 ArbiterService/审批/SSE 主链路/static/前端（npm build 跳过） | ✅ 完成 2026-08-02 23:5x（mvn 全量 **352/0 BUILD SUCCESS**，51 类，LONG-01 2.758s PASS；PlayerRenameE2ETest 6/0 + ScriptRenameResumeTest 4/0；含回滚路径证据：剧本杀同步抛异常→500+rolled_back=true+角色库回滚旧名+router/2d 逆操作 verify；含并行 P-0802-M LLMClientStreamTest 7 用例共存通过；无 player_id 请求行为零变化；待主会话核查） |
| P-0802-M（本批） | coder（后端 token 级流式输出批次：LLMClient 加 callStream（stream=true SSE 增量解析+非流式兜底）/ Agent.generateWithContextStream（失败降级非流式）/ SSEController 新增 agent_token 事件 / RouterService.executeRoundSerial 串行主链路生成改流式（增量 sse.broadcastAgentToken 推送，完整内容仍 agent_output 结算）/ 前端 useSSE 注册 agent_token + appStore 增量累积渲染（streaming 草稿消息+streamBuffers+暂停恢复）+ ChatPage 流式光标与暂停按钮 / G2 顺带修：client.ts scriptStartDiscussion/StartVoting/Resolve/Finish 硬编码 session_id:'' → localStorage 镜像读取（避免 api↔store 循环依赖）），subagent a08df2bb。⚠️ 与已完成批次共存：P-0802-P2 已 ✅ 完成（RouterService 判定区/构造 14 参改动为工作区基础，本批在其上仅改 executeRoundSerial 生成调用 1 行——串行区，不覆盖其判定区改动）；SSE 主链路/LLM 调用链任务书明确授权可动；零改动 ArbiterService/审批/狼人杀/剧本杀 Service/static 既有产物 | 23:0x- | 后端 `llm/LLMClient.java`（+callStream/parseStreamResponse/buildChatRequest stream 重载）、`agent/Agent.java`（+generateWithContextStream）、`controller/SSEController.java`（+broadcastAgentToken typed helper）、`service/RouterService.java`（executeRoundSerial :635 生成调用改流式 1 行+注释，P-0802-P2 判定区改动保留）；前端 `api/useSSE.ts`（+agent_token）、`api/client.ts`（G2：script 四端点 session_id 读 localStorage 镜像）、`store/appStore.ts`（+streamingByAgent/streamPaused/appendAgentToken/settleAllStreaming/setStreamPaused/resumeStreaming + addAgentMsg 结算草稿 + setScriptSessionId 镜像 localStorage）、`types/index.ts`（AppMessage +streaming?）、`components/ChatPage/ChatPage.tsx`（MessageView 流式光标 + 暂停/继续按钮 + 自动滚动依赖内容）；测试：新增 `llm/LLMClientStreamTest.java`（本地 HttpServer 喂 SSE 增量/非 SSE JSON 兜底/Agent 流式降级）；docs：`修改记录.md`（顺延编号）/ `TEST_STATUS.md` / `并行作业登记.md`（本行）。⚠️ P-0802-P2 已登记 RouterService 占用但状态 ✅ 完成——本批改动区域（串行区）与其（判定区）不相交，先读后改不覆盖 | ⏳ 进行中（mvn 全量 335/0 基线，新增流式测试后复跑；npm build + static 同步待做；待主会话核查 + 8000 重启生效） |
| P-0802-P2（本批） | coder（改造方案 Phase 2 判定链路切换·解析式，subagent 8090830f）：RouterService.runRound :233 判定加 playerId 解析式豁免 + 四参重载（旧三参委托）+ :340 排除同步改解析名（防双声）；SessionController.send 收 player_id 传新重载；SimulationService.initWithPersonas 四参重载（:200-202 判定加 resolve(playerId)，三参委托）；SimulationController.loadCharacters :74 读 player_id → :86 传新重载；WerewolfController.init 收 player_id 有则 humans=Set.of(resolve(playerId)) 无则现状、playerSessions 键不变；ScriptController.init 收可选 player_id 按解析名登记 playerIdBindings；新增四组判定测试（RouterRenameTest 5/Test2dPlayerRenameTest 4/WerewolfRenameTest 4/ScriptRenameTest 3）；⚠️ RouterService 为禁动文件，已获主人明确授权（2026-08-02 22:44 主会话确认「直接 b」，修改记录 #82 已注明） | 22:45-23:0x | 后端 `service/RouterService.java`（授权：判定点+四参重载+排除解析名+构造 14 参）、`service/SessionRegistry.java`（构造链透传）、`controller/SessionController.java`（send 读 player_id）、`simulation/SimulationService.java`（四参重载+构造 8 参）、`simulation/SimulationController.java`（读 player_id 传新重载）、`controller/WerewolfController.java`（init 收 player_id 解析登记+三参构造）、`controller/ScriptController.java`（init 收可选 player_id 登记 playerIdBindings+四参构造）；测试新增 4 类 16 用例 + 适配 RouterServiceSerialRoundTest（14 参）/MergedSpeechModeTest（8 参）/WerewolfGameSmokeTest·FixTest·RoleKeyTest·Stage1Test（init 补 "" 参）；docs：`修改记录.md` #82 / `TEST_STATUS.md`（Round 28/v28，基线 319→**335/48 类**）/ `并行作业登记.md`（本行）。与 P-0802-P1-demo 增量共存（PlayerIdentityService 已存在直接用）；零改动 ArbiterService/审批/狼人杀/剧本杀 Service 内部判定/SSE/static；前端零改动（npm build 跳过） | ✅ 完成 2026-08-02 23:0x（mvn 全量 **335/0 BUILD SUCCESS**（48 类，LONG-01 2.503s）+ 四组新测试单跑 16/0 + 无 player_id 行为零变化四组断言全过；待主会话核查） |
| P-0802-P1-demo（本批） | coder（改造方案 Phase 1 demo：身份字段落地——CharacterEntity+playerId 列 / findByPlayerId / saveCharacter 重载+entityToMap / CharacterController 透传 player_id+撞名校验① / 新建 PlayerIdentityService（resolve 双方法）/ 前端 client.ts create·update 带 player_id + appStore playerId 生成·localStorage 持久化），subagent a5d39dde。⚠️ 与已完成批次共存：P-0802-J 已完成的 client.ts/appStore.ts 改动为本批改动基础（增量共存不覆盖）；RouterService/ArbiterService/审批/狼人杀/剧本杀主链路/SSE/static 零改动 | 22:28-23:0x | 后端 `db/entity/CharacterEntity.java`（+playerId 列 nullable+unique + 构造重载）、`db/repository/CharacterRepository.java`（+findByPlayerId）、`db/service/DatabaseService.java`（saveCharacter 5 参重载 4 参委托 + saveAllCharacters 透传 + entityToMap 输出 player_id）、`controller/CharacterController.java`（create/update/batch 透传 player_id + 撞名校验①：同名 409 update 排除自身 + DataIntegrityViolation 兜底 409）、`service/PlayerIdentityService.java`（新建，resolveCharacterName/resolvePlayerId 双方法）；前端 `api/client.ts`（+getPlayerId 导出 + createCharacter/updateCharacter 带 player_id）、`store/appStore.ts`（+playerId 字段 getPlayerId 初始化持久化）、`types/index.ts`（Character +player_id?）；测试：新增 `controller/CharacterRenameValidationTest.java`（create/update/batch 撞名 409 + 原数据未覆盖 + playerId 绑定落库/反查 + 撞名后内存与 DB 一致）；docs：`修改记录.md` #80 / `TEST_STATUS.md`（Round 26/v26，基线 309→319/44 类）/ `并行作业登记.md`（本行） | ✅ 完成 2026-08-02 23:0x（mvn compile 0 错误 + 单跑 CharacterRenameValidationTest 10/0 + 全量 **319/0 BUILD SUCCESS**（44 类，LONG-01 2.879s PASS）+ npm build 通过（64 modules，index-DXQ1IdZ-.js 1,831.32 kB）+ bundle grep 命中 player_id/randomUUID/pid-；无 player_id 请求行为零变化；RouterService/ArbiterService/审批/狼人杀/剧本杀/SSE 主链路/static 零改动（static 未同步，8000 重启由主会话负责）；待主会话核查） |
| P-0802-L（本批） | coder（2D 视图 UI 遮挡修复批次：①剧本杀 script 模式 ChatPage 工作区 grid 修复（workspace 加 script-mode 类 + .workspace.script-mode 三列 grid，chat-main 不再被挤成 140px 窄条）②ScenePage 2D 全屏 46px 底部溢出修复（height 640 → max(480px, calc(100vh-210px))）③start_discussion 空 session_id Bug 只记录不改④顺带 scriptInit 超时修复 60s→120s（验证阻塞点，真实 LLM 生成 72.4s）），subagent 7777c377，任务书指定本批登记 P-0802-L。⚠️ 与并行批次共存：纯前端零后端 Java；P-0802-P1-demo（改造方案 Phase 1）同窗作业——其 npm build 覆盖 dist（index-DXQ1IdZ-.js 含双方改动），本批最终以含 scriptInit 修复的重构建 index-BFSCKpeC.js 重新同步 static | 22:26-23:0x | 前端 `roleplay-v4/frontend/src/components/ChatPage/ChatPage.tsx`（workspace +script-mode 类）、`styles/global.css`（+.workspace.script-mode 三列 grid + ≤1080/≤900 响应式）、`components/ScenePage/ScenePage.tsx`（2D 全屏高度 `max(480px, calc(100vh - 210px))`）、`api/client.ts`（scriptInit +timeout 120000）；`src/main/resources/static/` 同步（最终 index-BFSCKpeC.js + index-ByvBQn5e.css，index.html 引用已更新，SHA256 dist↔static 一致）；`roleplay-v4/frontend/tools/` 新增实测工具（dev_proxy.mjs / cdp_driver_L.mjs / probe_scene.mjs，纯测试不入生产）；docs：`修改记录.md` #81 / `TEST_STATUS.md`（Round 27 / v27，纯前端基线 319/0 不动）/ `并行作业登记.md`（本行）。⚠️ 与 P-0802-P1-demo 共用 dist/static——其 npm build 覆盖本批原构建（index-Ct8E7agO.js → index-DXQ1IdZ-.js），本批已核实 DXQ1IdZ 含双方全部改动并以之同步 static | ✅ 完成 2026-08-02 23:0x（npm build 通过 + static SHA256 dist↔static 一致 + bundle grep 全命中（script-mode/max(480px/退出 2D/恢复对局/player_id/timeout 12e4）；Edge headless + CDP 浏览器实测（代理 8099→8000 零触碰后端）：scene2d 三档视口 900/792/700 overflowY 全 0（修复前 792 视口 14px），chat2d 剧本杀完整链路 script 模式三列几何正确（grid 140px 260px 1040px，chat-main 140px 窄条 → 1040px）、内嵌 2D 面板 1040×503 完整可见（auditOverlay 唯一提示=2D 内公告栏 P1-8 设计内组件）；验证层注记：start_discussion 空 session_id Bug 实测阻断讨论推进（per-game 隔离后），按任务只记录不改，验证经 CDP 注入真实 session_id 推进；后端 Java 零改动；待主会话核查 + 8000 重启生效） |
| P-0802-J（本批） | coder（狼人杀范围外遗留项批次：①ScriptGameService 讨论引擎 per-game 隔离（剧本杀侧，对齐 P-0802-I 狼人杀三 Map）②script_* SSE 全局广播→会话定向 ③狼人杀 resume 增加 roleKey 防冒充（对齐剧本杀 C3 roleKey 体系）），subagent c40c6318。⚠️ 与并行批次共存：C-2/C-2-A（RouterService 串行区）零触碰；P-0802-I 既有改动（WerewolfService 三 Map/SseBroadcaster.broadcastToSession/SSEController 定向）为本批对齐基础，增量共存不覆盖 | 22:0x-23:0x | 后端 `service/ScriptGameService.java`（讨论引擎三 Map per-game 隔离 + getDiscussionConversation/World 测试钩子）、`controller/SSEController.java`（script_phase/script_status/script_reveal 三 helper 改走 broadcastToSession 定向）、`service/WerewolfService.java`（+playerKeys roleKey 发放/toMap role_key 暴露/resumeGame 三参 roleKey 校验/findPlayerByKey/findSessionByPlayerKey/isPlayerKeyValid/getRoleKey/getPlayerKeys/快照落 player_keys+恢复）、`controller/WerewolfController.java`（resume 收 player_key 校验 + GET /api/werewolf/keys 分发）；前端 `api/client.ts`（werewolfResume +player_key、+werewolfKeys）、`api/useSSE.ts`（不动，P-0802-I 已支持）、`store/appStore.ts`（+werewolfRoleKey/scriptSessionId）、`App.tsx`（SSE 连接按 mode 选会话：script→scriptSessionId/werewolf→werewolfSessionId）、`components/ScenePage/ScenePage.tsx`（init 存 role_key/session_id）、`components/ChatPage/ChatPage.tsx`（狼人杀恢复入口 +roleKey 输入与传递、script 轮询/resume 存 scriptSessionId）；`static/` 同步新构建产物（index-Bp4ixzCe.js，index.html 引用已更新，SHA256 dist↔static 一致，删除被取代的 index-DEFyKJ5G.js）；测试：新增 `service/ScriptGamePerGameIsolationTest.java`（2 用例）+ `service/WerewolfRoleKeyTest.java`（4 用例）+ `controller/SSEControllerSessionTest.java` 增 script_* 定向用例（2→3），改 `service/WerewolfStage1Test.java`（resume 三参适配）；docs：`修改记录.md` #78 / `TEST_STATUS.md`（Round 25/v25）/ `并行作业登记.md`（本行）/ `DECISION_LOG.md` D-029 | ✅ 完成 2026-08-02 23:0x（mvn 全量 **309/0 BUILD SUCCESS**（43 类）+ 8 类定向 33/0 + npm build 通过（64 modules）+ static SHA256 dist↔static 一致 + bundle grep 全命中；RouterService/ArbiterService/审批/狼人杀主状态机零改动，SSEController 仅 helper 内部改定向（全局 broadcast 保留）；与 P-0802-G/I 前端改动共存（本批构建基于含其改动的完整源码）；待主会话核查 + 8000 重启生效） |
| P-0802-G（本批） | coder（主人反馈三件事批次：①串行调度开启 serial=true ②2D 视图被场景设置 UI 遮盖修复 ③用户在场判定自查），subagent ca028cac，任务书指定本批登记 P-0802-G | 21:26-22:0x | 后端 `src/main/resources/application.yml`（`roleplay.round.serial: false → true`，保留注释可切回；application-test.yml 不动）；前端 `roleplay-v4/frontend/src/components/ScenePage/ScenePage.tsx`（showPhaserSim=true 整页折叠场景设置区，2D 视图 height=640 占主体 + 「✕ 退出 2D」恢复）；`src/main/resources/static/` 同步（index-4rQ391AJ.js + index-aWRy_sYe.css，删除被取代的 index-Bu8YksJU.js，index.html 引用已更新，SHA256 dist↔static 一致）；docs：`修改记录.md` #74 / `TEST_STATUS.md` Round 23 + v23 / `并行作业登记.md`（本行）/ `DECISION_LOG.md` D-027。⚠️ 与 P-0802-I（进行中）共存：本批构建基于含其改动的完整源码，产物 index-4rQ391AJ.js 已含双方前端改动；RouterService 仅自查零改动（serial 分支为 P-0802-F 既有授权实现） | ✅ 完成 2026-08-02 22:0x（mvn 全量 **302/0 BUILD SUCCESS**（serial=true 下 RouterServiceSerialRoundTest 4/4 仍全绿，测试走显式配置不受主 yml 影响）+ npm build 通过（64 modules）+ static SHA256 一致 + bundle grep 命中「退出 2D/返回场景设置」；待主会话核查 + 8000 重启生效） |
| P-0802-未知（21:35 static） | ⚠️ 溯源占位（按规则 5 登记）：`static/assets/index-4rQ391AJ.js`（1,835,577B）来源——已溯源为 **P-0802-G 本批构建产物**（subagent ca028cac，21:35 同步），非未知改动；本批已保留该产物并更新 index.html 引用 | 21:35- | `src/main/resources/static/assets/index-4rQ391AJ.js`（P-0802-G 构建产物，含 P-0802-I 前端改动）、`static/index.html`（引用指向本产物） | ✅ 已溯源（P-0802-G 产物，非无主改动） |
| P-0802-I | coder（狼人杀阶段1遗留项批次：G1-2 女巫获知被刀者机制 / 讨论引擎 per-game 隔离 / werewolf_* SSE 按 session 定向 / 落库+重连+联机房绑定），subagent 114daec4。⚠️ 与活动批次 C-2/C-2-A（RouterService 串行调度区）错开：本批**零改动 RouterService/ArbiterService/审批**；SSEController 仅新增定向推送方法（现有全局 broadcast 保留）；static 按任务要求同步新构建产物 | 21:2x-22:1x | 后端 `service/WerewolfService.java`（女巫获知机制 witchInformed/救不救毒 + 讨论引擎 per-session 隔离 Map + 快照落库 saveSnapshot/resumeGame/restoreFromSnapshot + werewolf_* 改走 broadcastToSession 定向推送 + 4 参构造注入 DatabaseService）、`service/WerewolfAiPlanner.java`（女巫获知狼刀目标后按 saveProbability 决策救/不救，新增 nosave 决策）、`controller/WerewolfController.java`（+POST /api/werewolf/resume + init 可选 room_code 绑定 roomGames + status 支持显式 session_id）、`broadcast/SseBroadcaster.java`（+broadcastToSession 默认回退全局）、`controller/SSEController.java`（stream 支持 ?session_id 过滤 + broadcastToSession 定向送达，全局 broadcast 不变）、`application.yml`/`application-test.yml`（+roleplay.game.werewolf.witch-save-probability）；前端 `api/client.ts`（+werewolfResume + werewolfStatus 支持 session_id）、`api/useSSE.ts`（连接带 ?session_id=）、`store/appStore.ts`（+werewolfWitchVictim）、`App.tsx`（werewolf_witch_info 消费 victim + useSSE 传 session_id + 新夜清空 victim）、`components/ChatPage/ChatPage.tsx`（女巫获知面板：被刀者展示+救/不救/毒 + 狼人杀恢复对局入口 + 轮询带 session_id）；`static/` 同步新构建产物（index-DEFyKJ5G.js，index.html 引用已更新，SHA256 dist↔static 一致）；新增测试 `service/WerewolfStage1Test.java`（7 用例：女巫获知/讨论隔离/定向推送/快照重连/房间绑定）+ `controller/SSEControllerSessionTest.java`（2 用例）；docs：`修改记录.md` #75（任务书 #74 被 P-0802-G 占用顺延）/ `TEST_STATUS.md`（Round 24/v24）/ `并行作业登记.md`（本行）/ `DECISION_LOG.md` D-028（D-027 被 P-0802-G 占用顺延） | ✅ 完成 2026-08-02 22:1x（mvn 302/0 全绿 + npm build 通过 + static SHA256 一致；零改动 RouterService 串行调度区；与 P-0802-H/P-0802-G 既有改动共存不覆盖；待主会话核查 + 8000 重启生效） |
| P-0802-H | coder（狼人杀后端授权批次：init session_id+router 注册 / customRoles 宽容解析 / toMap visible 修复 / 猎人开枪修复 / AI 夜间行动器 / werewolf_* SSE / 白天讨论接对话引擎 / 前端夜间·投票·讨论·开枪·审批面板），subagent 53b74666。⚠️ 主会话派单指定 P-0802-E（已被 C-2 占用），顺延 F 时与 C-2-A（串行调度）撞标，最终改登记 **P-0802-H** | 20:13-21:1x | 后端：`service/WerewolfService.java`（autoPlay 自动推进闭环 + 讨论引擎 + SSE 推送）、新增 `service/WerewolfAiPlanner.java`（纯规则 AI 行动器）、`controller/WerewolfController.java`（session_id/router 注册/humans/discussion_say）、`service/RouterService.java`（setWerewolfGame + 狼人杀角色卡，与并行 C-2-A 串行调度改动共存）、`application.yml`/`application-test.yml`（+roleplay.game.werewolf.*）；前端 `api/client.ts`（+werewolfDiscussionSay）、`api/useSSE.ts`（+4 事件）、`store/appStore.ts`（+9 状态）、`App.tsx`（+4 SSE 分支）、`components/ChatPage/ChatPage.tsx`（+WerewolfActionPanel + 3s 轮询 + autoPlay 改后端驱动）、`components/ScenePage/ScenePage.tsx`（存 session_id + 注释更新）、`styles/global.css`（+行动面板样式）；static 同步（index-Bu8YksJU.js + index-aWRy_sYe.css，SHA256 一致，index.html 已更新）；测试：新增 `WerewolfAiPlannerTest`（9 用例）+ `WerewolfGameFixTest`（8 用例），改 `WerewolfGameSmokeTest`（W-5/W-10 断言旧 bug → 修复后行为）；docs：`修改记录.md` #71 / `TEST_STATUS.md` Round 22 / v22 / `并行作业登记.md`（本行）/ `DECISION_LOG.md` D-024 | ⏳ 待核查（mvn 293/0 + npm build 通过 + static SHA256 一致 + bundle grep 命中；待主会话核查 + 8000 重启生效） |
| P-0802-G | ⚠️ 溯源占位（原登记「发现未登记并行批次 C-2 A 部分」）：C-2-A（subagent b938dfb7）一般模式串行调度 roleplay.round.serial 已自登记为 **P-0802-F**（与我方 P-0802-F 撞标，我方顺延 P-0802-H）；涉及 RouterService.java（+executeRoundSerial ~300 行）、AppConfig.java、yml roleplay.round.serial 键、RouterServiceSerialRoundTest（4 用例）；与本批 P-0802-H 同改 RouterService.java，双方改动已共存编译通过（293/0 含其 4 用例）；占用已自登记，待主会话复核 | ✅ 完成 2026-08-02 22:0x（本批 coder subagent ca028cac 承接：开启 serial=true + 2D 视图遮盖修复 + 在场判定自查；mvn 302/0 + npm build + static SHA256 一致；待主会话核查 + 8000 重启生效） |

| P-0802-F | coder（C-2 批次 A 部分实施：一般模式串行调度，subagent b938dfb7）——按 docs/串行调度方案-20260802.md 实施（主人已授权动 RouterService.runRound）：新增配置开关 roleplay.round.serial（默认 false=并行零破坏；true=串行+同轮即时入史）；RouterService.runRound Step3-4 改串行循环（轨道顺序×轨道内 agent 顺序，PLAYER>DM>NPC 优先级，每 agent 输出完成立即 memory.addMessage + SSE 推送，后发言者 buildAgentContext 自然读到前面角色本轮发言）；AgentExecutor 非禁动零改动；新增测试 RouterServiceSerialRoundTest（serial=true 同轮上下文共享：后发言者上下文含前者发言 / serial=false 默认并行行为不变 / 配置默认 false） | 20:1x-20:5x | 后端 `src/main/java/com/roleplay/engine/service/RouterService.java`（runRound 串行分支 + executeRoundSerial 辅助 + serialRound @Value + setter）；`config/AppConfig.java`（RoundConfig 补 serial 字段+getter/setter）；`application.yml` + `application-test.yml`（roleplay.round.serial: false 双份）；新增测试 `src/test/java/com/roleplay/engine/service/RouterServiceSerialRoundTest.java`；docs：`修改记录.md` / `TEST_STATUS.md` / `并行作业登记.md`（本行）——禁动文件（ArbiterService/审批/狼人杀后端/SSE 主链路/static）零改动 | ✅ 完成 2026-08-02 20:5x（本批 4/4 全绿 + 全量 284/0（排除并发未登记批次失败用例）；⚠️ 并发撞标：另一未登记批次（狼人杀 AI 行动器 G0-2）代码注释也标 P-0802-F 且正在改 RouterService/WerewolfService/WerewolfController/AiPlanner（20:22-20:31），其 WerewolfAiPlannerTest.humanWolfWaits 单跑失败与本批无关；双方改动在 RouterService 不同区域共存无冲突，本批测试已在其合并后重跑验证；待主会话核查） |
| P-0802-未知 | ⚠️ 未登记批次（发现未登记改动，按规则 5 登记占位，通知主 agent 溯源）：狼人杀 AI 行动器（G0-2）——代码注释用 P-0802-F 标记与本批撞标，未在登记表登记 | 20:22-（进行中） | `src/main/java/com/roleplay/engine/service/WerewolfAiPlanner.java`（新增）、`service/WerewolfService.java`、`controller/WerewolfController.java`、`service/RouterService.java`（werewolfGame 字段 + buildAgentContext 狼人杀角色卡 + setWerewolfGame，与 P-0802-F 串行调度改动共存不同区域）、新增测试 `service/WerewolfAiPlannerTest.java`（9 用例，其中 humanWolfWaits 当前失败）/`WerewolfGameFixTest.java`、static 多个新产物 | ⏳ 进行中（本批零介入：狼人杀后端=禁动；其失败用例与本批无关，单跑复现，待其自查或主会话协调） |
| P-0802-E | coder（C-2 批次：输出机制重构——前端打字机流式播放队列 + 一般模式串行调度调研实施，subagent 8aecb2bc）——调研报告 docs/前端问题调研-20260802.md 背景；C-1 已交付 PhaserSimulationView SimChatMsg 消息结构 + status CSS，本批直接消费 status 不动消息结构 | 19:08-19:5x | 前端 `roleplay-v4/frontend/src/`（新增 `phaser/simChatConfig.ts`；改 `phaser/PhaserSimulationView.tsx` 打字机队列 + 暂停/恢复/超时 + 在场判定 + 气泡单例；改 `phaser/SimulationScene.ts` setBubbleFilter+避让层；改 `styles/global.css` +typewriter-caret）；后端非禁动（`simulation/conversation/TrackStrategy.java` +MAX_SENTENCE_CHARS=60 prompt 轻提示、`GroupStrategy.java` 同）；static 同步（index-4KH0B1ke.js + index-D1bQ2MoB.css，SHA256 一致，index.html 已更新）；docs：`修改记录.md` #69 / `TEST_STATUS.md` Round 20+v20 / `并行作业登记.md`（本行）/ 新增 `docs/串行调度方案-20260802.md`（A 部分方案报告）；禁动文件零改动（A 部分最小改动点在 RouterService=禁动 → 按纪律停手报方案，见方案报告） | ✅ 完成 2026-08-02 19:5x（mvn 272/0 + npm build 通过 + static SHA256 一致 + bundle grep 10 项命中；待主会话核查 + 8000 重启生效；A 部分待主会话决策是否授权动 RouterService） |
|---|---|---|---|---|
| P-0802-D | coder（狼人杀重构：前端 init 根因修复 + 游戏 API 封装 + 后端测试骨架，subagent 15a56863）——调研报告 docs/狼人杀重构调研-20260802.md 结论：后端/SSE 禁动待主人授权，本批仅做非禁动部分。⚠️ 原登记 P-0802-C 与并行批次 C-1（subagent 6e7d626f，已完成）撞标，顺延改为 P-0802-D | 18:53- | 前端 `roleplay-v4/frontend/src/api/client.ts`（werewolfInit 改 body + 6 个游戏 API 封装）、`components/ScenePage/ScenePage.tsx`（startWWGame 传全量 players+roles、res.ok 检查、start() werewolf 分支同步；与 C-1 改动共存于同一文件，C-1 只改 use2D 区域）；新增测试 `src/test/java/com/roleplay/engine/service/WerewolfGameSmokeTest.java`（11 用例）；docs：`修改记录.md` / `TEST_STATUS.md` / `并行作业登记.md`（本行）——禁动文件（WerewolfService/WerewolfController/SSEController/RouterService/ArbiterService/static）零改动；static 已由 C-1 批次（subagent 6e7d626f）最终重构建同步：index.html → index-BX3fEiRB.js（基于含本批 client.ts/ScenePage 改动的完整源码构建，同时含 C-1 与 P-0802-D 双方前端改动，SHA256 dist↔static 一致） | ✅ 完成 2026-08-02 19:11（mvn 272/0 + npm build 通过；禁动文件零改动；主会话已核查 #68：static 实为已同步 SHA256 一致，无需重同步；8000 已重启生效 pid 416） |
|---|---|---|---|---|
| P-0802-C | coder（C-1 批次纯前端：P3-10 演讲广播 demo 迁入 2D 视图 / P3-11 场景设置分类排序+max-width none / 2D UI 重构左地图右聊天可折叠+消息 status 字段 / 2D 入口合并单一）——归属：本批次（subagent 6e7d626f，主会话派单） | 18:5x-19:4x | 前端 `roleplay-v4/frontend/src/`（PhaserSimulationView.tsx / ScenePage.tsx / ChatPage.tsx / styles/global.css）+ static 构建产物同步；docs：`修改记录.md` #66 / `TEST_STATUS.md` Round 18 + v18 / `并行作业登记.md`（本行）；**禁动后端 Java（零改动）** | ✅ 完成 2026-08-02 19:4x（npm run build 通过 + 产物 index-BX3fEiRB.js/index-BANIh__s.css 已同步 static（SHA256 dist↔static 一致）+ index.html 引用已更新 + bundle grep 验证；**最终产物为基于含 P-0802-D 前端改动（client.ts/ScenePage 狼人杀区）的完整源码重构建，部署包同时含 C-1 与 P-0802-D 双方前端改动**；待主会话核查 + 8000 重启） |
|---|---|---|---|---|
| P-0802-B | coder（P1 批次修复：P1-6 删除报错 / P1-8 公告栏改 2D 触发 + 设置聊天模式 / P1-9 狼人杀文案门控）——归属：本批次（subagent b8c50ca0，主会话派单） | 13:09-13:55 | 前端 `roleplay-v4/frontend/src/`（client.ts / App.tsx / ChatPage.tsx / ScenePage.tsx / 相关组件）+ static 构建产物同步 + 后端可选（SceneController/CharacterController 非禁动）；docs：`修改记录.md` / `TEST_STATUS.md` / `并行作业登记.md`（本行） | ✅ 完成 2026-08-02 13:5x（重派 subagent 70f8a090：P1-6 删除空 body 兜底 / P1-8 公告栏改 2D 内触发 + 设置聊天模式 / P1-9 文案门控；npm run build 通过 + 产物 index-DYdLFONY.js 已同步 static；后端零改动；待主会话核查 + 8000 重启） |
| P-0802-A | coder（P0 批次修复：P0-1 me 身份 / P0-2 No active session / P0-3 2D 卡住+双开+无聊天框 / P0-5 剧本杀两步式）——归属：本批次（subagent 035a7279，主会话派单） | 12:50-13:45 | 前端 `roleplay-v4/frontend/src/`（appStore.ts / ScenePage.tsx / ChatPage.tsx / PhaserSimulationView.tsx）+ `static/simulation.html`（auto start + player_name）+ static 构建产物同步（index-CGo4Hc0R.js）+ 后端（RouterService/SessionController/SimulationService/SimulationController，主人已授权）；docs：`修改记录.md` #64 / `TEST_STATUS.md` Round 16 / `并行作业登记.md`（本行） | ✅ 完成 2026-08-02 13:45（mvn test 261/0 全绿 + npm build 通过 + 产物已同步 static；待主会话核查 + 8000 重启） |

## 当日作业（2026-08-01）

| 标记 | agent/任务 | 时间窗 | 涉及文件 | 状态 |
|---|---|---|---|---|
| P-0801-A | **批次 D（P0-1 发言门控 SpeechGate）**——归属：主会话「剧本杀实现架构差距分析」（session ed49ef10）| 13:10-13:54 | `SpeechGate.java`（新增）+ `ScriptSchemaV1`/`ScriptGameService`/`ScriptController`/`ConversationManager`/`WorldDirectorService`/`application.yml`/`application-test.yml` + `SpeechGateTest.java`（新增） | ✅ 完成 2026-08-01 13:54（commit `1181393`，已入库；含测试）|
| P-0801-B | **Phaser 批次（地图 JSON 契约 draft→v1 升级 + phaser_validate 微调）**——归属：待溯源（另一并行批次，git 工作区发现）| 13:5x-?（进行中） | `docs/Phaser迁移计划.md`(M)、`docs/地图JSON契约-draft.md`(D 删除)、`docs/地图JSON契约-v1.md`(新增)、`static/index.html`(M)、`phaser_validate/{README,index.html,bsp.js,map_contract.js}`(M)、`static/assets/index-DIix5PE0.js`(新增) | ⏳ 进行中（占用中勿动；待溯源登记） |
| P-0801-B | coder（Phaser 阶段1 ScenePage 渲染层迁移）——归属：本批次（subagent 14a165e1） | 13:20-14:00（已完成） | 新增 `roleplay-v4/frontend/src/phaser/`（SimulationScene.ts / PhaserSimulationView.tsx / simulationData.ts）、`frontend/src/components/ScenePage/ScenePage.tsx`（内嵌 Phaser 视图 + 引擎切换）、`frontend/tools/`（phaser_smoke.html + phaser_integration_smoke.html + self_test_stage1.py）、`frontend/dist/` 与 `src/main/resources/static/`（构建同步 index-B2ueyU_u.js）、docs：`修改记录.md` #55、`TEST_STATUS.md` 阶段1 条目；**不碰** Java 后端/App.tsx/appStore.ts/useSSE.ts/ChatPage | ✅ 2026-08-01 14:00（自测全绿：build 通过 + 纯渲染冒烟 10/10 + 真实后端集成冒烟 9/9，详见台账 #55） |
| P-0801-C | coder（Phaser 阶段2 LLM 地图生成接入 + 搜证线索绑定热点）——归属：本批次（subagent f243bfcf）+ 收尾 worker（59b28d0e） | 18:36-19:20 | 新增 Java \simulation/map/\（MapContract.java / MapValidator.java / BspMapGenerator.java）+ \service/ScriptMapService.java\；改 \service/ScriptGameService.java\（mapData 字段 + generateMap + 快照持久化）、\controller/ScriptController.java\（+POST /api/script/map）；新增测试 \MapValidatorTest\/\BspMapGeneratorTest\/\ScriptMapServiceTest\/\ScriptMapPersistenceTest\（4 类 37 用例）；前端新增 \src/phaser/\（mapData.ts / ScriptMapScene.ts / PhaserScriptMapView.tsx）、改 \ScenePage.tsx\（仅状态声明，**接线未完成**）、\pi/client.ts\（+scriptMap）；\
rontend/tools/\（phaser_map_smoke.html + self_test_stage2.py **磁盘缺失**）；\static/\ 未同步（build 失败 23 处 TS 错误）；docs：\修改记录.md\ #56（已登记）、\TEST_STATUS.md\ 阶段2 条目（已登记，Round 14 / v14） | ✅ 2026-08-01 19:20（收尾核查：后端 254/0 全绿；前端 ScenePage 接线未完成 + build 失败 23 处 TS 错误、static 未同步——待 coder 修复，详见台账 #56；占用解除） |
| P-0801-D | coder（P1 缺陷：LLM 剧本生成 JSON 截断 maxTokens 修复）——归属：本批次（subagent 49286de6，主会话派单） | 18:38-18:50 | 改 `service/ScriptService.java`（600→4000）、`service/ArbiterService.java`（400→600 / 800→1000）、`service/TrackRequestService.java`（300→600 / 200→400）、`service/Compressor.java`（150→300）、`simulation/SimulationService.java`（600→1000）；新增测试 `service/ScriptServiceMaxTokensTest.java`（3 用例）；docs：`修改记录.md` #57、`TEST_STATUS.md` v13/Round 13、`DECISION_LOG.md` D-023 | ✅ 2026-08-01 18:50（全量 217/0 + 真机 4/4 完整生成 PASS；打包期间并行批次 P-0801-C 未完成代码编译错误，经 -Dmaven.test.skip=true 规避其未完成测试编译，已在其登记行注明） |
| P-0801-G | coder（邀请码功能显式开关，默认关闭可配置重启启用）——归属：本批次（subagent 981616ef，主会话派单） | 22:42-22:47 | 改 `src/main/java/com/roleplay/engine/controller/AuthController.java`（+@Value invite-enabled/invite-code、verify 关闭时 403）、`src/main/resources/application.yml`（+roleplay.auth.invite-enabled: false + invite-code: B3283A78）、`src/test/resources/application-test.yml`（+同配置 invite-enabled: true）；新增测试 `src/test/java/com/roleplay/engine/controller/AuthControllerInviteSwitchTest.java`（7 用例）；docs：`修改记录.md` #63、`TEST_STATUS.md` Round 15 / v16 | ✅ 2026-08-01 22:47（完成：全量 261/0 BUILD SUCCESS，待核查；8000 重启由主会话负责） |

## 历史

> 按规则：每日清空昨日区块（标记作废）。历史批次永久记录于 `docs/修改记录.md`（台账编号持续递增），git 提交记录于 commit message。
| P-0801-E | coder（Phaser 阶段2 前端补接线+构建+冒烟，P-0801-C 遗留修复）——归属：本批次（subagent 00ea4518） | 18:58-19:40 | 改 `roleplay-v4/frontend/src/phaser/ScriptMapScene.ts`（20 处 TS 错误修复）、`components/ScenePage/ScenePage.tsx`（阶段2 接线：生成地图按钮+PhaserScriptMapView 渲染+搜证回调）、新增 `frontend/tools/phaser_map_smoke.html` + `self_test_stage2.py`（P-0801-C 磁盘缺失补上）、`static/`（index.html → index-Ccc-CMzG.js；行初记 BmMkAeV0 为中间产物 hash 笔误，P-0801-F 复核时已修正）；docs：`修改记录.md` #58、`TEST_STATUS.md` 阶段2 前端条目 | ✅ 2026-08-01 19:40（build 全绿 + 冒烟 16/16 + 阶段1 回归 10/10，见台账 #58；P-0801-F 独立复核通过 2026-08-01 19:30） |
| P-0801-F | coder（Phaser 阶段2 前端构建独立复核——P-0801-E 验证批次）——归属：本批次（subagent 7bd0dd81） | 19:12-19:30（已完成） | 无源码改动（复核验证）：重跑 `npm run build`（tsc -b 0 错误 + vite 63 modules 全绿）+ dist↔static SHA256 字节比对（JS/CSS/index.html 三方一致）+ bundle grep 阶段2 字符串（scriptMap/生成地图/clue_location/spawn_points/map_version/BSP/markZoneSearched/已搜证）+ 冒烟双跑（stage2 16/16、stage1 10/10 ALL PASS）；docs：`修改记录.md` #59、`并行作业登记.md`（P-0801-E hash 修正 + 本行）、`TEST_STATUS.md` 阶段2 复核注记 | ✅ 2026-08-01 19:30（P-0801-E 声称全部属实，最终产物 hash index-Ccc-CMzG.js 与 static 一致；后端 Java 零改动） |
| P-0804-G（本批次） | coder（前端重写为可玩 demo，不接后端 API，subagent 463b2382）：新增 src/demo/ 整套 mock 层（mockData/mockEngine/demoStore/demo.css + 16 个页面组件），重写 App.tsx 为 demo shell（登录绕过直进首页，旧 App 逻辑保留至 AppLegacy.tsx 不参与构建），PhaserScriptMapView 增 searchProvider/fetchAssets 可选 prop（demo 搜证走 mock，既有行为零破坏）；不动后端/禁动文件/不动旧页面（保留编译） | 19:00- | 前端 src/App.tsx（重写）、src/AppLegacy.tsx（新增保留旧逻辑）、src/phaser/PhaserScriptMapView.tsx（+searchProvider/+fetchAssets）、src/demo/**（全新增）；docs：修改记录.md、并行作业登记.md（本行） | ✅ 完成 2026-08-04 21:4x（npm run build tsc 0 错误 + Edge headless CDP 真机走查 12 步全 PASS，console 0 错误 0 异常 0 网络请求，台账 #107） |
