# 输入质量记录 — 轨道系统用户加入调研（2026-08-03）

> 任务：`docs/轨道系统用户加入调研-20260803.md` 复核补齐全程的工具调用输入/输出质量台账。
> 度量口径：每次工具调用一行；字符数粗略估算（整十/整百），宁多勿漏；分类按该调用**输出的主要成分**计，工具入参侧（路径/查询串/写入内容）全部计入有效。
> 度量口径补充（未衡审查意见②）：输入侧不拆细分类——查询/路径串全计有效；大型文档读取按输出主要成分计有效，未细拆其中格式/模板噪音。
> 统计截至：2026-08-03 12:5x（含本文档写入调用自身）。

## 分类定义

| 分类 | 定义 |
|---|---|
| 有效 | 直接服务于任务（项目上下文、相关代码/文档、相关检索结果、交付写入） |
| 噪声 | 与任务无关（离题结果、无关内容、干扰信息） |
| 冗余 | 重复信息（同一内容重复读取/重复返回） |
| 无效 | 失败/不可用输出（报错、空结果、乱码、截断） |
| 填充垃圾 | 样板填充（模板样板、格式噪音、无信息量内容） |

## 逐条记录

| 序号 | 工具 | 输入字符数(估) | 输出字符数(估) | 分类 | 说明 |
|---|---|---|---|---|---|
| 1 | read PROJECT_CONTEXT.md | 60 | 17000 | 有效 | 开工必读①：项目速览（阶段/架构/硬性约束） |
| 2 | read DECISION_LOG.md | 60 | 30000 | 有效 | 开工必读②：决策史 D-001~D-029（为什么这么设计） |
| 3 | read AGENTS.md | 60 | 7000 | 有效 | 协作规则（禁 spring-boot:run / 禁 git commit / 登记台账） |
| 4 | read docs/并行作业登记.md | 60 | 11000 | 有效 | 并行占用检查：目标文档未被占用，可开工 |
| 5 | read docs/INDEX.md | 60 | 6000 | 有效 | 文档地图定位（场景→文档） |
| 6 | exec `dir /b` 列目录 | 100 | 400 | 无效 | PowerShell 语法错误：`/b` 被解析为路径 `C:\b`，两次报错；改原生语法重试 |
| 7 | exec Get-ChildItem docs 清单 | 120 | 700 | 有效 | 确认目标文档已存在；发现并行产物（图集 html/地图调研） |
| 8 | read docs/轨道系统用户加入调研-20260803.md | 60 | 18000 | 有效 | 既有调研文档全文（任务：存在→读取并补齐完善） |
| 9 | read docs/现状分析-2D对话与铁轨系统.md | 60 | 5000 | 有效 | Track 现状取证文档（对照核实） |
| 10 | read docs/融合架构判断-完整版.md | 60 | 9000 | 有效 | 架构判断与 Phase 1-4 依据 |
| 11 | exec 后端 join 能力 grep | 200 | 2600 | 有效 | 核验 joinGroup/addParticipant 零命中；仅 RoomController join；前端仅房间加入 |
| 12 | exec 需求文档检索 | 150 | 600 | 无效 | `需求文档-完整需求.md` 不在工作区（来源缺口，如实记录不臆造） |
| 13 | exec 根目录清单 | 100 | 400 | 有效 | 确认需求文档缺失事实 + 根目录活文件清单 |
| 14 | read ConversationManager.java L130-219 | 80 | 6000 | 有效 | 核验策略注册（TrackStrategy 仅 GROUP_DISCUSSION/DEBATE）/ 排除点 / DYAD 入口 |
| 15 | read ConversationManager.java L340-364 | 80 | 1700 | 有效 | 核验 executeRound 玩家跳过 LLM、占位 "..." |
| 16 | read ConversationGroup.java L45-114 | 80 | 3500 | 有效 | 核验成员表只读访问器、无 addParticipant（加入能力缺失证据） |
| 17 | read SimulationOrchestrator.java 全文 | 80 | 7500 | 有效 | 核验每 tick 双导演编排、applyToGroup 回写活动群组 |
| 18 | exec SimulationController 端点 + RouterService grep | 200 | 3200 | 有效 | 端点实测 **21** 个（修正文档 26 旧口径）；一般模式轨道由 Arbiter.configureTracks 配置 |
| 19 | exec 前端 SimGroup/SimulationScene grep | 180 | 1400 | 有效 | 核验 SimGroup 无 id 字段、群组框「仅可视化」注释 |
| 20 | exec SessionController/SimulationService/MovementConstraint grep | 220 | 3000 | 有效 | 核验 /api/send 用户入口、playerControlled 标记点、移动豁免 |
| 21 | exec html 头部 + 修改记录 tail 30 行 | 160 | 16000 | 有效 3000 + 噪声 13000 | 图集关联确认（有效）；修改记录 tail 中约 13,000 为与本次无关的历史批次描述，仅表格格式 ~3,000 有效——格式学习所需但内容超量（教训：应只读 2-3 行样例） |
| 22 | exec TrackAssignment/TrackType/常量 grep | 160 | 2800 | 有效 | 核验 TrackAssignment record 形状、复用 core/Track.Mode、组常量实测行号 |
| 23 | edit 调研文档 5 处补齐（头部注记 / §3.3 修正 / §2.5 / §4.2 / §7 证据补行） | 7300 | 40 | 有效 | 交付写入：补齐完善调研文档 |
| 24 | edit docs/并行作业登记.md（P-0803-A 登记） | 730 | 30 | 有效 | 协作规则：批次登记 |
| 25 | edit docs/修改记录.md（#82 登记） | 1595 | 30 | 有效 | 协作规则：修改登记 |
| 26 | edit docs/INDEX.md（场景索引/清单/计数 3 处） | 920 | 30 | 有效 | 文档地图维护（新增文档登记） |
| 27 | edit 调研文档 §9+§10 追加（数据模型/权限补全 + 输入质量附录） | 4530 | 30 | 有效 | 交付写入 |
| 28 | write docs/输入质量记录-20260803.md（本文件） | 5600 | 50 | 有效 | 交付写入（本行） |

## 汇总统计

- **总输入字符**（全部工具入参）：**23,065**（查询/路径串 2,390 + 文档写入/编辑内容 20,675——后者即交付物本身，全部有效）
- **总输出字符**：**153,010**
- **总处理量**（输入+输出）：**176,075**

| 分类 | 字符数（估） | 占总输出（153,010） | 占总处理量（176,075） |
|---|---|---|---|
| 有效 | 139,010 | 90.9% | 79.0% |
| 噪声 | 13,000 | 8.5% | 7.4% |
| 无效 | 1,000 | 0.7% | 0.6% |
| 冗余 | 0 | 0% | 0% |
| 填充垃圾 | 0 | 0% | 0% |
| **非有效合计（噪声+冗余+无效+填充垃圾）** | **14,000** | **9.1%** | **8.0%** |

## 结论

1. **总体输入质量良好**：核心结论 100% 来自源码定点读取与 grep 实测（有效占比 90.9%），无编造、无凭空引用、无重复读取（冗余 0）。
2. **噪声主源（13,000/14,000）**：一次读取 `docs/修改记录.md` 尾部 30 行——16,000 输出中约 13,000 为与本次任务无关的历史批次描述，仅表格格式 ~3,000 有效（格式学习所需）。教训：学习台账格式只读 2-3 行样例即可，勿整段拉取。
3. **无效（1,000）全部来自 2 次失败命令**：PowerShell 语法错误（`dir /b` 被解析为路径 `C:\b`，400）+ 需求文档缺失检索（600）。均为探路性调用，失败即修正，未污染结论；已如实计入。
4. **非有效合计占总处理量 ≈8.0%（占总输出 9.1%）**，主要由上述单次超量读取造成；若剔除该次读取的冗余部分，非有效占比 <1%。

*本日志与调研文档 §10 附录（摘要版）同源，统计口径一致（两者均含本文档写入调用）；附录为摘要表，本文件为逐条全量。*
