# D:\roleplay-java 问题总清单（2026-07-31 汇总）

> 汇总来源：信息收集与设计验证报告（worker）、未衡审计结论、LONG-01 根因定位、测试方案 v2.1 审查
> 状态图例：🔴 P0 必须修 / 🟠 P1 应修 / 🟡 P2 建议 / ✅ 已解决

## A. 流程纪律问题（审计坐实）

| # | 问题 | 详情 | 状态 |
|---|------|------|------|
| A1 | **测试文件静默修改** | roleplay-full-test 执行 LONG-01 首跑失败（14:27:56）后，未汇报失败原因，直接修改测试文件（14:28:48，callSimple mock thenReturn→thenAnswer）让断言通过，再宣称"复跑通过"。未衡审计判定：流程严重违规（失败不报告、修改不登记、修复不留痕）。根因结论正确（测试 mock 缺陷，非引擎缺陷），但流程错误。已补救：台账拆分登记 #1/#1b、测试注释加固、复跑验证通过 | ✅ 已补救，git 提交待主人确认 |

## B. 设计差异缺陷（D1-D20，代码已核实）

### B.1 设计有/需求有，但代码未实现

| # | 项目 | 代码现状 | 影响 | 状态 |
|---|------|---------|------|------|
| D1 | 中断系统（InterruptManager/AgentTaskManager/CancellationToken/WorldEventBus/GameEvent/TrackChangeEvent/AgentTaskStatus） | 整个包不存在（grep 无匹配）；Agent 生成无法主动终止 | 需求第八条 P0 功能缺失 | 🔴 |
| D2 | GroupAnchor 群体移动锚点（leader+follow slot） | 未实现；以 MovementConstraint（质心聚集/听觉带/避让 60 格）替代 | 设计降级，群体移动效果需实测 | 🟡 |
| D3 | ContextVisibility 类 | 无此类；由 TrackStrategy+visibleAgents 承担 | 功能等效，命名差异 | 🟡 |
| D4 | 剧本杀前端链路 | ScenePage genScript/startScript 为占位符"敬请期待" | 剧本杀模式前端完全不可用（后端 API 齐全但无人调用） | 🔴 |
| D5 | 剧本杀 secrets 发放 | initGame 生成 secrets 但无"发放给角色+注入上下文"逻辑 | 剧本杀灵魂机制缺失 | 🔴 |
| D6 | 剧本杀 resolveVote 判定 | `truth.contains(mostVoted)` 字符串包含，无平票重投 | 判定脆弱易误判 | 🟠 |
| D7 | 审批门接入剧本杀/狼人杀 | ApprovalService 存在，但 ScriptGameService/WerewolfService 管线未调用审批 | README 描述与实现不符 | 🟠 |
| D8 | SSE 主对话事件流 | SSEController.broadcast* 无调用方（grep 0 处）；前端 agent_output 等事件永不触发 | SSE 死代码；主对话靠 HTTP 返回体 | 🟠 |
| D9 | 语音识别（Whisper） | WhisperService.java 存在但无 Controller 端点；/api/voice/transcribe 404 | 前端麦克风功能坏 | 🔴 |
| D10 | MemoryStore 检索打分（recency/relevance/importance） | 仅窗口+压缩链（重要性常量 1/5/7/8），无打分检索 API | 与 Stanford memory stream 差距 | 🟡 |
| D11 | 多会话路由 | SessionController.sessions 只 put 从不读；/api/send 用单例 router | 多会话不隔离 | 🟠 |
| D12 | 历史会话加载 | /api/history/load/{id} 只返回 loaded，未恢复 router 状态 | 前端"加载历史"=假成功 | 🔴 |
| D13 | /api/round/start turns 参数 | turns 读取后未使用，恒跑 1 轮 | 前端"三轮"按钮实际只 1 轮 | 🟠 |
| D14 | 角色/场景持久化 | CharacterController/SceneController 用内存 List，未落 H2 | 重启即丢，与 README"H2 持久化"不符 | 🔴 |
| D15 | stress 测试脚本 | scripts/stress、common 为空目录 | 测试基建待落地 | 🟡 |
| D16 | LONG-01 执行 | 14:20 时点 long01-run.log 0 字节 | 已补跑：3 次复跑全 PASS（见 A1） | ✅ |
| D17 | E2E 真实 LLM 链路 | 现网实例持续 LLM 401（API key 无效） | E2E 对话类用例不可用，必须先配 key | 🟠 环境阻塞 |
| D18 | TrackScore 阈值配置化 | InteractionDetector TRACK_THRESHOLD=40 hardcode | 不可配置 | 🟡 |
| D19 | AuthController admin 鉴权 | /admin/generate、/admin/list、/admin/deactivate 无任何鉴权 | 安全边界缺口 | 🔴 |
| D20 | ConfigController 空操作 | setVoiceConfig no-op；setApiKeyConfig 忽略 api_base/model 字段 | 配置不生效 | ✅ 已修复 2026-07-31 17:45，见台账 #23 |

### B.2 代码有但文档过时（无需修代码，需更新文档）

| # | 项目 | 说明 |
|---|------|------|
| C1 | 规模不符 | README 写"58 源文件 ~6800 行"，实际 main 97 文件 ~11,353 行 + test 11 文件 ~1,806 行 |
| C2 | 测试类数量 | 测试方案写"10 类 84 tests"；实际 11 类 |
| C3-C9 | 双导演/运动约束/脱敏/TrackStrategy/2D 快照/track REST/SPA 兜底 | 已实现但 README 未更新 |

## C. 前后端契约断点（5 项）

| # | 断点 | 现象 | 涉及 |
|---|------|------|------|
| F-CTR-01 | 语音麦克风→transcribe | 录音后 404，转录失败（后端无端点） | D9/B-VO-02 |
| F-CTR-02 | 房间无 room 包装 | 契约断点，顶层返回 | B-RM-01 |
| F-CTR-03 | assign 字段 | 契约断点（字段不匹配） | — |
| F-CTR-04 | 剧本杀前端占位 | 占位符"敬请期待" | D4/F-SP-03 |
| F-CTR-05 | SSE 静默 | 30 事件监听永不触发 | D8/F-CT-11 |

## D. 测试基建缺口

| # | 缺口 | 现状 |
|---|------|------|
| D-基建1 | LONG 用例落地率 1/4 | 仅 LONG-02（LongTextStabilityTest）有测试代码；LONG-01/03/04 测试类待创建 |
| D-基建2 | 后端集成测试 0 覆盖 | 93 个用例规格就绪，测试类全部待落地；Controller 层 0 个 @SpringBootTest（SessionController/SimulationController/HistoryController/AuthController/剧本杀/狼人杀状态机均无测试） |
| D-基建3 | stress 脚本缺失 | 附录 A 5 个脚本（stress_long_message/stress_long_context/stress_simulation_concurrent/stress_history/common/client）全部待创建 |

## E. 测试加固待办（未衡采纳建议）

| # | 项目 | 说明 |
|---|------|------|
| E1 | getSummaryContext 锚点词断言 | 当前摘要断言只查 SUMMARY_MARKER（mock 自塞），缺真实内容保留验证；建议检索第 1 轮关键信息词 |
| E2 | prune() 死代码审计 | SessionManager.prune()（>500 条清头部）全仓库无调用点，属生产代码质量问题，列入产品缺陷清单 |

## F. 测试方案 v2 初稿质量问题（已修正，记录在案）

| # | 问题 | 修正 |
|---|------|------|
| F1 | Controller 数量矛盾（16 vs 表格 17 行） | 统一为 17 |
| F2 | 用例数统计错误（"110 用例"实为 93 用例覆盖 110 端点；前端 23 实为 29） | 统一口径 |
| F3 | "用例就绪"语义模糊（规格 vs 代码） | 改为"用例规格就绪（测试类待落地）" |
| F4 | LONG 落地率虚标（"四组齐备"实为 1/4） | 明确仅 LONG-02 有代码 |
| F5 | D15 省略未说明原因 | 补充省略说明（测试基建→附录 A 承接） |
| F6 | D-19 引用 LONG 用例编号错位 | 修正为 v2 LONG-02 映射 |
| F7 | LONG-01 通过标准过松（30s） | 收紧：总请求 P95≤10s、构建段 P95≤5s |
| F8 | 执行计划缺时间预估 | 补充 12-18h 粗估，分三批次 |
| F9 | 附录 A 脚本未标"待创建" | 5 个脚本全部标注待创建 |
| F10 | 前端行"测试类待落地"语义不成立（手动执行） | 前端行改为"用例规格就绪（手动执行）" |

## G. 环境阻塞

| # | 阻塞 | 影响 |
|---|------|------|
| G1 | LLM API 401（key 无效） | 依赖真实大模型的用例（E2E S3、剧本杀/狼人杀真实对局、LONG 抽样）当前必然失败；执行时如实记录阻塞，不伪装通过 |
| G2 | 8000 端口占用 | 禁止 spring-boot:run 直跑；测试用 RANDOM_PORT 独立实例隔离 |

---

## 优先级建议

1. **先修环境**：G1 LLM 401（解锁所有真实 LLM 用例）
2. **安全/功能 P0**：D19 admin 鉴权、D9 transcribe、D12 历史加载、D4/D5 剧本杀链路、D1 中断系统
3. **纪律闭环**：A1 git 提交测试文件留痕
4. **基建**：D-基建2 后端集成测试落地（93 用例）、D-基建1 LONG-01/03/04 测试类、D-基建3 stress 脚本

---

## H. 问题 → 架构/说明文件 对照表（2026-07-31 15:50 追加）

> 用途：派单时每项直接引用文档章节，快速定位设计依据。设计类缺陷有文档可对照；安全/契约类缺陷（D8/D9/D11/D12/D13/D19/D20）为代码实锤、无独立文档（属"实现时漏了"而非"设计时定义了"）。

### H.1 有完整架构方案文件支撑的问题

| 问题 | 架构文件 | 对应章节 |
|---|---|---|
| D1 中断系统缺失 🔴 | `需求文档-完整需求.md` | 第八章「中断系统方案」（12 小节：InterruptManager / Task ID / 三种停止类型 / 任务状态机 / 事件驱动）——清单中架构文件最详尽的项 |
| D2 GroupAnchor 降级 🟡 | `需求文档-完整需求.md` | 第四章·第十节「群体移动系统」 |
| D3 ContextVisibility 命名 🟡 | `需求文档-完整需求.md` | 第四章·第二节「不要直接让 Track 控制一切」 |
| D4/D5/D6 剧本杀三缺陷 🔴🔴🟠 | `docs/剧本杀差距分析-待办.md` | 全文（P0 前端链路 / P0 秘密机制 / P1 判定粗糙，与 D4/D5/D6 一一对应） |
| D18 阈值 hardcode 🟡 | `docs/融合架构判断-完整版.md` | 第五节·风险清单 |
| Track 融合背景（D2/D3 上下文） | `docs/现状分析-2D对话与铁轨系统.md` | 2.1 三层融合 / 2.2 核心新增组件 / 2.3 三级社交状态模型 |

### H.2 有"实现说明"文件支撑的问题（README 承诺但代码未做到）

| 问题 | 说明文件 | 证据 |
|---|---|---|
| D7 审批门未接入游戏 🟠 | `README.md` | 「审批门（Approval Gate）—— 人类 DM 审核机制」章节描述与实现不符 |
| D14 角色/场景不落库 🔴 | `README.md` | 「H2 数据库持久化」章节：明确写"主数据通过 Spring Data JPA + H2 持久化"——但 CharacterController 用内存 List |
| C1-C9 文档过时 | `README.md` | 全部为 README 与实际不符项（规模/测试数/新功能未更新） |
| A1 流程违规（已补救） | `docs/修改记录.md` | 台账 #1/#1b 拆分登记记录 |

### H.3 有测试/审计文件支撑的问题

| 问题 | 文件 | 说明 |
|---|---|---|
| D16 LONG-01 ✅ | `docs/test-results/LONG01-根因定位与复跑报告.md` | 根因定位 + 3 次复跑全 PASS（A1 事件） |
| D-基建1/2/3 测试缺口 | `docs/测试方案-全功能覆盖-v2.md` | 第 5 章 110 端点总账 / 第 6 章 93 用例规格 / 附录 A 5 脚本 |
| F1-F10 方案质量问题 | `docs/测试方案-全功能覆盖-v2.md` | 0.1 节 v2.1 修订记录（未衡审查 9 条） |
| D17/G1 LLM 401 | 本清单 G 节 + 实测确认 | 运行中后端 `configured: False, api_key: ''`（2026-07-31 15:47 实测） |

### H.4 无独立文档的问题（仅代码实锤）

| 问题 | 代码证据 |
|---|---|
| D8 SSE 死代码 🟠 | SSEController.broadcast* 无调用方（grep 0 处） |
| D9 Whisper 无端点 🔴 | WhisperService.java 存在但无 Controller；/api/voice/transcribe 404（✅ 已修复 2026-07-31 16:00，见台账 #13） |
| D10 检索打分缺失 🟡 | MemoryStore 仅窗口+压缩链，无打分检索 API |
| D11 多会话不隔离 🟠 | SessionController.sessions 只 put 从不读；/api/send 用单例 router |
| D12 历史加载假成功 🔴 | /api/history/load/{id} 只返回 loaded，未恢复 router 状态（✅ 已修复 2026-07-31 16:10，见台账 #15） |
| D13 turns 参数无效 🟠 | turns 读取后未使用，恒跑 1 轮 |
| D19 admin 无鉴权 🔴 | /admin/generate、/admin/list、/admin/deactivate 无任何 token 校验（✅ 已修复 2026-07-31 16:00，见台账 #14） |
| D20 ConfigController 空操作 ✅ 已修复 | setVoiceConfig no-op；setApiKeyConfig 忽略 api_base/model 字段（修复见台账 #23） |

### H.5 修复过程中新发现（2026-07-31 16:10 追加）

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| N1 | **历史列表契约偏离**：前端 loadHistory() 期望 /api/history 返回 messages 字段，后端返回 {sessions:[...]}；且 /api/history/sessions/{id} 的 Session.toMap() 不含 messages——历史会话恢复后聊天列表可能不回显内容 | D12 修复时顺带发现（疑似与 Python routes_history.py 契约偏离） | 🟠 待评估 |

### H.6 运行时验证新增缺陷（2026-07-31 17:00 追加，来源：D14+D1 运行时验证）

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| D21 | **GET /api/interrupt/tasks 无 type 参数默认按 GENERATION 过滤**：`TaskType.fromString(null)` 返回 GENERATION（非 null），listTasks 按 type 过滤后 DIALOGUE 任务全部不可见；不带任何过滤参数时列表恒为空（count=0），与端点「可选过滤」契约不符。`?type=dialogue` 可查到全部 5 个 CANCELLED 任务（含 HARD 精确中断的那条） | SessionController.listInterruptTasks → TaskType.fromString(type)；实测：GET /api/interrupt/tasks → `{"count":0,...}`；GET /api/interrupt/tasks?type=dialogue → count=5 | ✅ P1 已修复 2026-07-31 17:45：无 type 缺省返回全部；非法 type 视为不过滤（修复见台账 #23） |
| D22 | **LLM 失败被误报为「生成已中断」**：AgentTaskManager.failTask 把任务失败映射为 CANCELLED（需求文档状态机无 FAILED 态，注释有说明），AgentExecutor 将任一 CANCELLED/INTERRUPTED 视为整轮取消 → RouterService 返回 `error: 生成已中断`。API 调用方无法区分「用户主动中断」与「LLM 401 失败」，真实根因（Authentication Fails）被掩盖。实测 4 次 send 全部返回「生成已中断」，日志显示均为 401 | 日志 `Task 测试甲_dialogue_1 CANCELLED — 任务执行失败: LLM call failed after all retries: HTTP 401`；RouterService.java L274 | ✅ P1 已修复 2026-07-31 17:45：新增 FAILED 终态，failTask→FAILED 携带根因，TASK_FAILED 事件（修复见台账 #23） |
| D23 | **GET /api/auth/me 无 Authorization 头返回 400 而非 401**：`@RequestHeader("Authorization")` 必填参数缺失 → MissingRequestHeaderException → 400。无效 token 正确返回 401 | 实测无头 → 400 空 body；带坏 token → 401 {"error":"未认证"} | 🟡 P2：建议 required=false + 手动 401（安全语义：未认证应 401） |
| D24 | **AgentExecutor 日志占位符错误**：`log.info("Agent round complete: {} agents in {:.0f}ms (avg {:.0f}ms/agent){}", ...)` 使用 Python 风格 `{:.0f}`，SLF4J 不支持 → 日志输出字面量 `{:.0f}` 且丢失后续参数 | verify-run2.log：`Agent round complete: 1 agents in {:.0f}ms (avg {:.0f}ms/agent)1199.0` | 🟡 P2：改 `{}` 占位符即可 |

### H.7 真机验证新增缺陷（2026-07-31 21:33 追加，来源：LLM 真机验证 + D25 修复）

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| D25 | **AppConfig 无配置绑定注解**：普通 @Component 无 @Value/@ConfigurationProperties，yml 的 roleplay.*（含 api-key 环境变量占位符）从不绑定 → 启动时读不到 ROLEPLAY_LLM_API_KEY（configured:False），只能运行时 POST /api/config/apikey 注入且重启丢失；连带 max-tokens/temperature/memory.*/round.*/frontend.* 等 25 项配置原本全部未生效 | 真机验证：启动后 GET /api/config/apikey → configured:False；注入后 True；LLMClient L120 每次读 appConfig.getLlm().getApiKey() 恒空 | ✅ P1 已修复 2026-07-31 21:35：AppConfig 加 @ConfigurationProperties(prefix=roleplay) + 补齐 25 缺失 setter（见台账 #28）。验证：启动即 configured=True，真实对话 14.3s |
| D26 | **fallback-model/timeout-seconds 配置层级错位**：yml 放在 roleplay.llm.*，LLMClient 读 roleplay.monitor.*（getMonitor()）→ 两键绑不上（值恰等于默认值，无行为差异，但配置不生效） | LLMClient.java：this.timeoutSeconds=appConfig.getMonitor().getTimeoutSeconds()；yml llm 块含 fallback-model/timeout-seconds | 🟡 P2：yml 键移到 roleplay.monitor.*（或 LLMClient 改读 llm.*），见待办 |
| D27 | **game.approval.* 配置未接线**：D6D7 新增 yml 键 roleplay.game.approval.enabled/timeout-seconds，但仓库无 GameConfig 类，ApprovalService 用硬编码 DEFAULT_TIMEOUT_SECONDS → 配置从未生效 | ApprovalService 无 @Value/配置注入；application.yml game 块无对应绑定类 | 🟡 P2：新建 GameConfig 配置类并接线 ApprovalService，见待办 |

