package com.roleplay.engine.simulation.agentruntime;

/** Long-lived memory lanes with different retrieval semantics. */
public enum MemoryKind {
    EPISODIC,
    SOCIAL,
    SPATIAL,
    SEMANTIC
}
